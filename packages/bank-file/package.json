{
  "name": "@juneflow/bank-file",
  "version": "0.1.0",
  "private": true,
  "description": "BankFileFormatter compliance interface + kbank-direct implementation (skeleton + fake, mock-first). Credentials via env only (P0-INT-04) - never committed.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./kbank-direct": "./src/kbank-direct/index.ts"
  },
  "scripts": {
    "build": "echo 'TODO(P0-BE-01): build wired by monorepo tooling (pnpm workspaces + Turborepo)'",
    "lint": "echo 'TODO(P0-BE-01): lint wired by monorepo tooling'",
    "typecheck": "echo 'TODO(P0-BE-01): typecheck wired by monorepo tooling'",
    "test": "echo 'TODO(P0-INT-02): unit tests for BankFileFormatter interface + fake adapter (gate G3)'"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
