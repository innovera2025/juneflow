{
  "name": "@juneflow/contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Juneflow API contract - openapi.yaml (SACRED after merge) + TS/Dart client codegen. FE/Mobile generate clients from here; hand-written models are forbidden (PLAN.md section 5).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./openapi.yaml": "./openapi.yaml",
    "./types": "./src/generated/types.ts"
  },
  "files": [
    "openapi.yaml",
    "src"
  ],
  "scripts": {
    "generate": "mkdir -p src/generated && openapi-typescript openapi.yaml -o src/generated/types.ts",
    "gen:ts": "pnpm run generate",
    "gen:dart": "echo 'TODO(P0-MOB-03): Dart client codegen from openapi.yaml is owned by the mobile zone (see TASKS.md P0-MOB-03)' && exit 0",
    "typecheck": "pnpm run generate && tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "openapi-typescript": "^7.5.2",
    "typescript": "^5.7.3"
  }
}
