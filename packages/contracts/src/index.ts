// Stable entry for @juneflow/contracts.
// The generated OpenAPI types are the ONLY client models FE/Mobile may use —
// hand-written models are forbidden (PLAN.md section 5). Regenerate via
// `pnpm --filter @juneflow/contracts run generate` after any openapi.yaml change
// (openapi.yaml is SACRED once merged — change through Wei only).
export type { paths, components, operations, webhooks } from "./generated/types.js";
