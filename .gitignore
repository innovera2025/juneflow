# Dependencies
node_modules/

# Build outputs
dist/
build/
.turbo/

# Test artifacts (generated at run time)
coverage/
playwright-report/
test-results/

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# OS / logs
.DS_Store
*.log

# Read-only reference sources — NOT tracked (Wei delta 11 ก.ค. 2569, B-004):
# the repo-internal sources are docs/handoff + docs/extract only
pototype/
design_handoff_juneflow/
juneflow-extract/

# NOTE (intentional — do NOT add ignore rules for these):
# - tests/visual/reference/  -> visual gate reference images MUST be tracked
# - docs/                    -> copied extract/handoff docs MUST be tracked

# Agent worktrees (one zone = one worktree - PLAN.md §8)
.worktrees/

# Investor decks + deck assets + stray dup (B-006 exclusions — never track; found in dev via P0-MOB-01 merge contamination 12 ก.ค.)
*.pptx
*.pdf
/clear_layout_assets/
/deck_assets_glam/
BLOCKERS 2.md

# Orchestrator coordination channel — filesystem-shared, never track (2026-07-14)
/agents/channel.md
.playwright-mcp/
