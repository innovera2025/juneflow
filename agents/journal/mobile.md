# Journal — Mobile (เขต: `apps/mobile` · Flutter)

> บันทึกต่อรอบ loop ตาม PLAN.md §10 — หนึ่ง entry ต่อหนึ่งรอบ · entry ใหม่อยู่บนสุด
> รูปแบบ entry ต่อรอบ:
>
> ```
> ## [YYYY-MM-DD] · รอบที่ N · task: <task id>
> - ทำอะไร: (งานที่ลงมือทำจริงในรอบนี้ + ผล gates)
> - ตัดสินใจอะไร: (การตัดสินใจในเขตตัวเอง — ความขัดแย้ง design/spec ห้ามตัดสินเอง → BLOCKERS.md)
> - เจออะไร: (สิ่งที่พบ/ติดขัด/blocker ที่เปิด/สิ่งที่ agent รอบถัดไปควรรู้)
> ```

## 2026-07-14 · รอบที่ 5 · task: P1-MOB-01

- ทำอะไร: หยิบ P1-MOB-01 (ready→doing→review · dep ว่าง · sync worktree = dev HEAD `9a7c27b`). งาน = regen Dart client หลัง §4 merge (`2dae372` — 7 endpoint ใหม่ `/models`·`/users`·`/roles` + B-014 envelope). สภาพเริ่มรอบ: มี uncommitted regen ค้างจากรอบก่อน — client/model `.dart` surface ถูกเขียนแล้ว (`master_api.dart`+`auth_api.dart`+`export.dart` modified · 3 model `.dart` ใหม่) แต่ **build_runner ไม่จบ** → codegen partner ขาด. อ่าน spec ครบ: TASKS แถว MOB-01 + `tool/gen_api_client.sh` (pipeline P0-MOB-03) + test 2 ไฟล์ที่พัง + generated diff. **root cause:** (1) model `.g.dart` 3 ตัว (`get_models/get_roles/get_users_response.g.dart`) **ขาดทั้งไฟล์** → `_$GetXResponseFromJson/ToJson` undefined · (2) retrofit client `.g.dart` (`auth_api.g.dart`/`master_api.g.dart`) **เก่า** → `_AuthApi`/`_MasterApi` ขาด impl ของ member ใหม่ (listUsers/createUser/listRoles/createRole/updateRole · listModels/createModel). `flutter analyze` = No issues (analysis server ทนต่อ generated member ที่ขาด) แต่ `flutter test` พังโหลด `api_client_test.dart`+`widget_test.dart` (CFE compile ทั้ง graph ล้ม แล้ว poison resident compiler ลามถึง widget_test แม้ไม่ import generated). แก้: รัน **scoped `build_runner build --build-filter`** เติม `.g.dart` ที่ขาด/เก่า — model ก่อน (`source_gen combining_builder` 2s · wrote 14) แล้ว client ตาม (retrofit 1s · wrote 25). **gates ผ่านครบ:** `flutter analyze` = No issues · `flutter test` = **16/16** (api_client_test 3 + widget_test 1 กลับเขียว + offline 12). **ไม่แตะ test** (assertion เดิม compile+smoke ครอบ `AuthLoginInput`/`Entity` round-trip ยังตรง envelope-agnostic → เขียวเองหลัง regen · ไม่ลบ/ไม่ stub assertion). commit `6bd8652` (regen client + 3 model + 3 model.g + 2 client.g) บน feature/mobile → TASKS `review` + REVIEW-QUEUE row. **หยุดก่อน merge** — orch-B รันด่าน 4.5 + merge เอง (ไม่ push · ไม่รัน diff-reviewer เอง).
- ตัดสินใจอะไร: ไม่มีการตัดสิน design/spec เอง — envelope shape/operation ทั้งหมดมาจาก §4 contract ที่ P1-BE-09 apply แล้ว (verbatim ผ่าน swagger_parser/json_serializable) · **ไม่แตะ `openapi.yaml` (sacred read-only)** · zone `apps/mobile` ล้วน. engineering ในเขต: การ "regen if truly needed" คือทางแก้ถูก (ไม่ใช่แก้ test) เพราะรากคือ generated `.g.dart` ขาด/เก่า ไม่ใช่ assertion ผิด — hand-edit generated ต้องห้าม จึงใช้ build_runner เติม แล้วยืนยัน output ตรง sibling `get_customers_response.g.dart` pattern เป๊ะ. เลือก scoped `--build-filter` แทน full build เพราะ **full `build_runner build` ค้าง 0% CPU ที่ retrofit phase (170 inputs) ซ้ำ 2 ครั้ง** (env hang) — scoped จบใน 1-2s.
- เจออะไร: (1) **full-graph `build_runner build` แขวนที่ retrofit_generator phase (0% CPU, ไม่จบ)** บน host นี้ — พบซ้ำได้ · มี stale build_runner ค้าง 34 นาทีจากรอบ regen ก่อน + lock `.requested` deadlock · **ทางแก้: kill build_runner ค้าง + ลบ `.dart_tool/build/lock/*` + ใช้ `--build-filter` scope เฉพาะไฟล์ที่ต้อง gen** (เร็ว + ไม่แขวน). agent mobile รอบหน้าที่ regen ควรใช้ scoped filter. (2) **`--build-filter` รอบนี้ไม่ clobber** `.g.dart` นอก filter (ต่างจาก P0-MOB-05 finding #1 ที่ `--build-filter=lib/offline/*` ลบ api `.g.dart`) — ยืนยัน `.g.dart` 102 ไฟล์ครบ · git status ไม่มี `D` · working tree สะอาด. แต่ยังควรเช็ค count `.g.dart` หลัง gen ทุกครั้ง. (3) **`flutter analyze` เขียวไม่พอเป็นหลักฐาน generated ครบ** — analysis server ผ่านแม้ `part` `.g.dart` ขาด/`_$X` undefined · **ต้องรัน `flutter test` (CFE) เสมอ** เพื่อจับ generated graph ที่ compile ไม่ได้จริง. (4) คิว ready เขต mobile หลังรอบนี้ = **0** · Phase 4 screens (26 จอ ตาม screen-map.md) รอ Wei เติมคิว/ตอบ B-015 · **ห้าม agent สร้าง task ผูก MVP เอง [TBD-MVP]**.

## 2026-07-13 · รอบที่ 4 · task: P0-MOB-04

- ทำอะไร: หยิบ P0-MOB-04 (ready→doing→review · ปลด block แล้วโดย B-015 ตอบ (ก) — inventory = 26 จอ distinct + host · dep ว่าง). sync worktree ก่อนเริ่ม: `git merge dev` (fast-forward → cb70d85 ไม่มี conflict). อ่าน spec ครบในรอบนี้: TASKS.md แถว MOB-04 (re-scoped) + B-015 ทั้ง entry + PLAN.md §0 + `.jsx` ต้นทาง**ทั้ง 5 ไฟล์เต็ม** (1,947 บรรทัด — ไม่ทำจากความจำ) + INVENTORY.md/NAV-ROUTES.md/reference-index.md + port-map.md (แบบอย่าง D-section). เขียน `apps/mobile/docs/screen-map.md`: ตาราง 26 จอ (7 กลุ่มตาม MOBILE_GROUPS) — ต่อจอมี component@file:line + โครงจอจาก source + โครง Flutter structural + ช่องภาพอ้างอิง · §host · §primitives (MobileHeader/MTabBar/MSection ฯลฯ + IOSDevice ไม่ port) · §dual context approval · §ภาพอ้างอิง · §mock mechanics · §D1–D4 · §สรุปความครบ. **gates ผ่าน:** cross-check เชิงโปรแกรม screen ids (parse `screens:[...]` ใน mobile-preview.jsx) 26 ตัว diff ว่างกับ 26 แถวตาราง (0 missing/0 extra) + router 26 branches + exports 28−2 non-screen=26 · `flutter analyze` = No issues (docs-only). commit `e6dc6f3` (doc) บน feature/mobile → TASKS `review` + REVIEW-QUEUE row. **หยุดก่อน merge ตามคำสั่งรอบ** — orchestrator รันด่าน 4.5 + merge เอง (ไม่ push).
- ตัดสินใจอะไร: ไม่มีการตัดสิน design/spec เอง — การนับยึด B-015 (ก) ที่ Wei ตอบแล้ว · จอ approval 5 จอบันทึก dual context (INVENTORY "iOS frame" + MOBILE_GROUPS) แต่**นับครั้งเดียว**ตามคำตัดสิน — grep ยืนยัน mount จริงจุดเดียวที่ MobileScreenRouter · ความไม่ตรงของแหล่งบันทึกเป็น D1–D4 ตามแบบ port-map §5 แทนการ adjudicate: D1 เลข "31" ใน zone CLAUDE.md (sacred — Wei แก้เอง) · D2 ไม่มีภาพอ้างอิงต่อจอ (ทางออกอยู่ใน §0 แล้ว) · D3 tab bar 4 แท็บ (inbox inline) vs 5 แท็บ (MTabBar) + แท็บ เอกสาร/โปรไฟล์ ไม่มีจอปลายทาง — ถ้า Phase 4 บังคับเลือก = เปิด BLOCKERS ก่อน · D4 comment หัว mobile-pm.jsx ชี้ MobileHeader ผิดไฟล์ (อยู่ mobile.jsx:7) · widget mapping เขียนแบบ structural ล้วน (Scaffold/ListView/BottomNavigationBar ฯลฯ ตามโครงที่เห็นใน source) ไม่ invent behavior.
- เจออะไร: (1) ภาพอ้างอิง mobile ใน `tests/visual/reference/` มีใบเดียวจริง = `g2/45-s.jpg` (host ทั้งใบฝั่ง web) — ตรวจครบ 128 แถว index · **ทุกจอ Phase 4 ต้องแคป reference จาก Fiori.html ก่อนสร้าง** (วางเวลาเพิ่มต่อจอ). (2) dependency ข้ามไฟล์ที่ agent Phase 4 ต้องรู้: `APPROVERS`@pr-form.jsx:79 (detail) · `window.ACCEPT_ITEMS`@company-accept.jsx:108 (fm-accept) · `__juneflowCtx.notify` — ทั้งหมด = mock ห้ามลอก (§0 ข้อ 3). (3) `MTabBar` ถูก window-export ปนกับจอ — สคริปต์นับจอห้ามนับ exports ดิบ (28 ≠ 26) ต้องหัก primitive/host. (4) คิว ready เขต mobile หลังรอบนี้ = **0** (MOB-01/02/03/05 done · MOB-04 → review) — ต่ำกว่าเกณฑ์ ≥5 มาก · **เตือน Wei เติมคิว** (งานเตรียม Phase 4 เช่น แคป reference ต่อจอ / โครง routing 26 route ตาม screen-map — ห้าม agent สร้าง task ผูก MVP เอง [TBD-MVP]).

## 2026-07-12 · รอบที่ 3 · task: P0-MOB-02

- ทำอะไร: หยิบ P0-MOB-02 (ready→doing→review · dep P0-MOB-01 done + P0-BE-04 done). eligible mobile รอบนี้ = MOB-02 / MOB-03 / MOB-05 (MOB-04 blocked B-015) → เลือก MOB-02 (เสี่ยงต่ำสุด · pipeline พร้อมแล้ว). รัน generator ที่ P0-BE-04 สร้างไว้: `pnpm --filter @juneflow/tokens gen:flutter -- --out ../../apps/mobile/lib/theme` → ได้ `lib/theme/juneflow_theme.dart` (export `juneflowFioriTheme()` + `JuneflowTokens`) · แก้ `lib/main.dart`: `theme: ThemeData(useMaterial3: true)` → `juneflowFioriTheme()` + import · อัปเดต `lib/theme/README.md` (เอา TODO(P0-MOB-02) ออก + ใส่ regen cmd). **gates ผ่านครบ:** `flutter analyze` = No issues · `flutter test` = 1/1 · `flutter build web` = ✓ Built build/web. **ด่าน 4.5 diff-reviewer = PASS** (traced ทุก literal ตรง tokens.json fiori · no hardcode). commit `ea8c80e` บน feature/mobile → TASKS `review` + REVIEW-QUEUE row.
- ตัดสินใจอะไร: **generator ทำงาน read-only ต่อ packages/tokens** — รัน pipeline ที่มีอยู่แล้ว (P0-BE-04) ไม่แก้ source token/generator ใดๆ (out of zone) · output เขียนลง `apps/mobile/lib/theme` (in-zone) เท่านั้น. ไม่ hardcode ค่า design ใน main.dart — อ้าง `juneflowFioriTheme()` ล้วน (PLAN.md §0 กฎ 2). ไม่มีการตัดสิน design/spec — theme มาจาก token ต้นทาง verbatim. build พิสูจน์ผ่าน web target เหมือน MOB-01 (Android/iOS SDK ยังไม่มีในเครื่อง — env constraint).
- เจออะไร: (1) generated `juneflow_theme.dart` เป็น output → commit เข้า repo (ไม่ gitignore ที่ apps/mobile/lib/theme · ต่างจาก default `packages/tokens/gen/` ที่ gitignored) — regen ได้ตลอดด้วย cmd เดียว · **ห้ามแก้มือ** (header GENERATED). (2) `build/web` เป็น artifact → gitignored ✓ (ไม่หลุดเข้า diff). (3) คิว ready เขต mobile หลังรอบนี้เหลือ MOB-03 (Dart client · dep done พร้อม) + MOB-05 (offline queue · ทำเฉพาะส่วนไม่ขึ้น Open Q #5) = **2 ตัว** (MOB-04 blocked B-015) — ต่ำกว่าเกณฑ์ ≥5 มาก · **เตือน Wei:** ตอบ B-015 (ปลด MOB-04) + เติมคิว Phase 0/เตรียม Phase 4 (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]). (4) หาก tokens.json เปลี่ยนในอนาคต ต้อง regen theme ใหม่ (ผูก dep P0-BE-04) — agent รอบถัดไปที่แตะ theme ควรรู้.

## 2026-07-12 · รอบที่ 2 · task: P0-MOB-01

- ทำอะไร: หยิบ P0-MOB-01 (ready→doing→review · dep P0-BE-01 done). eligible mobile รอบนี้เหลือ **ตัวเดียว** = MOB-01 (MOB-02/03/05 ยังติด dep MOB-01 ไม่ done · MOB-04 blocked B-015). skeleton จาก foundation commit มีแล้ว (lib/main.dart · pubspec.yaml · analysis_options.yaml · lib/theme/README.md) แต่ยังไม่มี platform dir → `flutter build` ไม่ได้. เติม platform scaffold: `flutter create --platforms=web` (เพิ่ม `web/` + `.metadata` + `pubspec.lock`) · แก้ `test/widget_test.dart` จาก template counter (อ้าง `MyApp` ที่ไม่มี → analyze error/test fail) เป็น smoke test จริงของ `JuneflowApp` · แทน README generic. **gates ผ่านครบ:** `flutter analyze` = No issues · `flutter test` = 1/1 · `flutter build web` = ✓ Built build/web. commit บน feature/mobile → TASKS `review` + REVIEW-QUEUE row.
- ตัดสินใจอะไร: **web เป็น build target ที่ใช้พิสูจน์ gate** — `flutter doctor` ยืนยัน Android toolchain ✗ + Xcode ✗ (ไม่มี SDK ในเครื่อง) · Chrome/web ✓. gate เขียนว่า "build ผ่าน" ไม่ระบุ platform → เลือก web (compile Dart→app สำเร็จ พิสูจน์โครง build ได้) แทน apk/ipa ที่ต้องมี SDK แต่ละ platform. ไม่ใช่การตัดสิน design/spec — เป็น env constraint · target มือถือจริง (android/ios scaffold + SDK) ทำตอนมี toolchain/Phase 4. ไม่แตะ lib/theme (theme ยัง gen-only P0-MOB-02) · ไม่เขียน API model มือ (P0-MOB-03).
- เจออะไร: (1) CI (`.github/workflows/ci.yml`) **ไม่มี Flutter stage เลย** — pnpm/node ล้วน → gate MOB-01 เป็น local เท่านั้น (agent รัน `flutter analyze`+build เอง). (2) `flutter create` สร้าง `.idea/` (gitignored ✓) + `test/widget_test.dart` เป็น counter template ที่พังกับ skeleton — ต้องแก้ทุกครั้งที่ scaffold. (3) MOB-01 → review จะปลดล็อก MOB-02 (theme · +dep P0-BE-04 ต้อง done ก่อน — ตอนนี้ ready) + MOB-03 (Dart client · dep P0-BE-12 done แล้ว → พร้อมเมื่อ MOB-01 done) + MOB-05 (offline queue). (4) คิว ready เขต mobile: หลังรอบนี้เหลือ MOB-02/03/05 = 3 ตัว (MOB-04 blocked) — ต่ำกว่าเกณฑ์ ≥5 · เตือน Wei เติมคิว/ตอบ B-015 (ห้ามสร้าง task ผูก MVP เอง). (5) offline (ก)/(ข) ยังค้าง Open Q #5 — MOB-05 ทำเฉพาะส่วนไม่ขึ้นระดับ.

## 2026-07-12 · รอบที่ 1 · task: P0-MOB-04

- ทำอะไร: หยิบ P0-MOB-04 (ready→doing · dep ว่าง). eligible mobile tasks รอบนี้ = P0-MOB-01 (dep BE-01 done) และ P0-MOB-04 (no dep) เท่านั้น (MOB-02/03/05 ติด dep MOB-01 ยังไม่ done). เลี่ยง MOB-01 เพราะ gate ต้อง `flutter build` แต่ยังไม่มี platform dirs (android/ios/…) + ไม่มี Android SDK/Xcode แน่นอน → RED-prone. ลง MOB-04 (inventory ล้วน · ในเขต apps/mobile/docs · ไม่มี visual gate). อ่าน spec ครบ: mobile-preview.jsx (MOBILE_GROUPS + router) · mobile.jsx · mobile-screens.jsx · mobile-pm.jsx · mobile-field.jsx · docs/extract/INVENTORY.md · NAV-ROUTES.md · tests/visual/reference-index.md. ผล: **ตัน — เปิด B-015**, ยังไม่เขียน screen-map.md.
- ตัดสินใจอะไร: ไม่ตัดสิน design/spec เอง. count "31 จอ" ใน zone CLAUDE.md (sacred) ขัดกับ enumerate source = 26 จอ distinct (+1 host) — ไม่มีเขตใด/ตาราง C ให้คำตัดสิน → escalate B-015 แทนการแต่งจอ (ผิด fidelity) หรือ redefine count เอง (ผิด PLAN.md §0 กฎ 4). ตั้ง P0-MOB-04 = blocked แล้วหยุดรอบ.
- เจออะไร: (1) source mobile*.jsx มี 26 จอ distinct จริง (window exports ครบ ไม่มี orphan) — MOBILE_GROUPS ก็ list 26 ตรงกัน · "31" น่าจะมาจากนับ approval 5 จอซ้ำ (mobile.jsx standalone iOS-frame + preview) → ราย B-015. (2) gallery มี mobile ref แค่ 1 ภาพ (g2/45-s.jpg = Mobile Approval/preview host ตาม reference-index.md) — จอ mobile ที่เหลือ **ไม่มีภาพอ้างอิง gallery** ต้องแคปจาก Juneflow Fiori.html ตอน build Phase 4 (PLAN.md §0). agent รอบถัดไปควรทราบก่อนวางคอลัมน์ "ภาพอ้างอิง". (3) คิว ready เขต mobile เหลือ 4 (MOB-01/02/03/05) ต่ำกว่าเกณฑ์ ≥5 — เตือน Wei เติมคิว (แต่ห้ามสร้าง task ผูก MVP เอง). (4) MOB-02/03/05 ปลดล็อกเมื่อ MOB-01 done — MOB-01 build gate ต้องมี Flutter toolchain ครบ (platform scaffold + SDK) รอบถัดไปเตรียมประเด็นนี้.

## 2026-07-06 · bootstrap · task: —

- ทำอะไร: bootstrap ไฟล์สถานะ loop ตาม Manifest v2 กลุ่ม 3 · เขต mobile มี 5 task ใน `TASKS.md` (P0-MOB-01 ถึง P0-MOB-05) สถานะ `ready` — P0-MOB-04 (mobile screen inventory 31 จอ) เริ่มได้ทันทีโดยไม่รอเขตอื่น
- ตัดสินใจอะไร: ไม่มีการตัดสินใจเชิง design/spec — ความขัดแย้งถูกยกเข้า `BLOCKERS.md` (B-001, B-002)
- เจออะไร: จอ mobile เริ่มจริง Phase 4 (PLAN.md §7) — Phase 0 เป็นโครง skeleton + pipeline เท่านั้น · theme ต้องมาจาก ThemeData ที่ gen จาก tokens.json ห้ามแก้มือ · ระดับ offline-first (ก)/(ข) ยังเป็น Open Q #5 — P0-MOB-05 ทำเฉพาะส่วนที่ไม่ขึ้นกับระดับ ถ้าชนทางเลือกต้องเข้า BLOCKERS
- 2026-07-12T01:09:49Z loop round ended (agent: mobile)

## 2026-07-12 08:09 · loop-runner · รอบที่ 1/10 · task: P0-MOB-04
- ทำอะไร: รัน claude headless 1 รอบ · task P0-MOB-04 → สถานะ blocked · ค่าใช้จ่ายรอบนี้ $2.396243 (สะสม $2.3962/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T01:21:30Z loop round ended (agent: mobile)

## 2026-07-12 08:21 · loop-runner · รอบที่ 2/10 · task: P0-MOB-01
- ทำอะไร: รัน claude headless 1 รอบ · task P0-MOB-01 → สถานะ review · ค่าใช้จ่ายรอบนี้ $3.1500837499999994 (สะสม $5.5463/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T01:22:07Z loop round ended (agent: mobile)

## 2026-07-12 08:22 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 3/10: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต mobile — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $6.0468/$20 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)

## 2026-07-12 08:54 · loop-runner · fail-safe
- ทำอะไร: หยุดการทำงานจาก error: tracked worktree is dirty at loop start — commit or stash changes first (clean-state requirement) (รอบที่ 1/10 · task ล่าสุด: -)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: ตรวจสถานะ repo และ log ก่อนสั่งรันใหม่ · งบสะสมรอบนี้: $0
- 2026-07-12T02:02:09Z loop round ended (agent: mobile)

## 2026-07-12 09:02 · loop-runner · รอบที่ 1/10 · task: P0-MOB-02
- ทำอะไร: รัน claude headless 1 รอบ · task P0-MOB-02 → สถานะ review · ค่าใช้จ่ายรอบนี้ $2.82959825 (สะสม $2.8296/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes

## 2026-07-12 · รอบที่ 4 · task: P0-MOB-03
- ทำอะไร: หยิบ P0-MOB-03 (ready→doing→review · deps P0-MOB-01+P0-BE-12 done). eligible mobile รอบนี้ = MOB-03 / MOB-05 (MOB-02 อยู่ review · MOB-04 blocked B-015) → เลือก MOB-03 (concrete กว่า เสี่ยง spec-conflict ต่ำ). **generate Dart client จาก `packages/contracts/openapi.yaml`** → `apps/mobile/lib/api/generated/**`: retrofit-on-`dio` (12 tag `*Api` + root `JuneflowApiClient`) + json_serializable models (49). สร้าง pipeline `tool/gen_api_client.sh` (normalize→swagger_parser→build_runner) + `tool/normalize_openapi.py` + `swagger_parser.yaml` + `test/api_client_test.dart` (compile+smoke จริงของ generated client). pubspec: +retrofit/json_annotation (+dev retrofit_generator/json_serializable/build_runner) · sdk floor 3.4→3.8. **gates ผ่านครบ:** codegen (swagger_parser Success 12/145/49 + build_runner Built 74 outputs) · `flutter analyze` = No issues · `flutter test` = 4/4 · `flutter build web` = ✓. commit บน feature/mobile → TASKS `review` + REVIEW-QUEUE row → (ต่อ) diff-reviewer.
- ตัดสินใจอะไร: **generator choice = engineering ในเขต ไม่ใช่ design/spec** — openapi-generator (Java `dart-dio`) ใช้ไม่ได้เพราะ host ไม่มี JRE (`/usr/bin/java` เป็น stub) → เลือก `swagger_parser` (pure Dart, OpenAPI 3.1, retrofit-on-dio ตรงกับ dio ที่ pubspec commit ไว้แล้ว). เจอ generator 2 จุดพังกับ contract นี้ → แก้ด้วย **normalize สำเนา derived (build/, gitignored) ไม่แตะ sacred openapi.yaml เลย** ด้วย transform semantics-preserving 2 อย่าง: (1) inline path-level `parameters` 44 paths (swagger_parser 1.44 crash เพราะ cast path-item เป็น Map ก่อน guard) (2) free-form object 8 ตัว (Entity ฯลฯ = `type object`/no props/additionalProperties free) → empty `@JsonSerializable` class แทน `typedef=dynamic` เพราะ retrofit_generator 10.2.7 (ก) crash คำนวณ inner type ของ `Future<List<dynamic>>` เป็น null (ข) emit `.fromJson` บน typedef ที่ compile ไม่ได้ — empty class faithful กับ contract ที่ "ยังไม่ประกาศ field ให้ opaque resource" (field ไป schema task; regen เติมเองเมื่อ contract เพิ่ม). **ไม่เขียน model มือ · ไม่แตะ sacred/i18n/token/C1–C10 · zone = apps/mobile ล้วน**.
- เจออะไร: (1) `/usr/bin/java` = macOS stub (no JRE) → **Java-based generator ใดๆ ใช้ไม่ได้บน host นี้** — agent อื่นที่จะ gen จาก openapi (เช่น P0-WEB-06 ใช้ openapi-typescript = node ok) ควรทราบ; ฝั่ง Dart ต้อง pure-Dart (swagger_parser) เท่านั้น. (2) generated client ต้อง **build_runner** (retrofit `_$XApi` + json `*.g.dart`) — เป็น step บังคับหลัง swagger_parser; `tool/gen_api_client.sh` รวมให้แล้ว. (3) prereq host เพิ่ม: `python3`+pyyaml (normalize), `dart pub global activate swagger_parser` — CI ไม่มี Flutter/py stage (gate MOB เป็น local เหมือน MOB-01/02) จึง commit generated output เข้า repo. (4) `lib/api/generated/**` ถูก exclude จาก analyze อยู่แล้วใน analysis_options (P0-MOB-01 ผู้วางโครงจองไว้) — วาง output ตรงนั้นพอดี. (5) คิว ready เขต mobile หลังรอบนี้เหลือ **MOB-05 ตัวเดียว** (offline queue · ทำเฉพาะส่วนไม่ขึ้น Open Q #5) — ต่ำกว่าเกณฑ์ ≥5 มาก · **เตือน Wei:** ตอบ B-015 (ปลด MOB-04) + เติมคิว Phase 4-prep (ห้าม agent สร้าง task ผูก MVP เอง [TBD-MVP]). (6) Phase 4 screens จะ import `package:juneflow_mobile/api/generated/export.dart` แล้ว `JuneflowApiClient(dio, baseUrl: '.../api/v1')` — ดู `lib/api/README.md`.
- 2026-07-12T02:25:39Z loop round ended (agent: mobile)

## 2026-07-12 09:25 · loop-runner · รอบที่ 2/10 · task: P0-MOB-03
- ทำอะไร: รัน claude headless 1 รอบ · task P0-MOB-03 → สถานะ review · ค่าใช้จ่ายรอบนี้ $8.286724250000002 (สะสม $11.1163/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes

## 2026-07-12 · รอบที่ 5 · task: P0-MOB-05
- ทำอะไร: หยิบ P0-MOB-05 (ready→doing→review · dep P0-MOB-01 done). eligible mobile รอบนี้ = **MOB-05 ตัวเดียว** (MOB-02/03 อยู่ review · MOB-04 blocked B-015). อ่าน spec ครบ: PLAN.md §0/§10/§11(Open Q #5)/ภาคผนวก A(Mobile row) + zone CLAUDE.md. implement **โครง offline queue เฉพาะส่วน level-agnostic** ที่ `apps/mobile/lib/offline/`: `sync_operation.dart` (model งานเขียนคิว · createdAt→UTC · toRow/fromRow) · `sync_queue.dart` (contract) · `in_memory_sync_queue.dart` (reference impl test ได้ไม่ต้องมี native SQLite) · `local_db.dart` (drift `DriftSyncQueue`+`SyncQueueItems` · createdAt เก็บ ISO-8601 UTC string) + `local_db.g.dart` (drift_dev gen) · `sync_processor.dart` (**seam ปล่อยว่างจงใจ**) · `offline.dart` barrel + README. pubspec +`drift_dev` (dev). test `test/offline/sync_queue_test.dart` (13 contract tests). **gates ผ่านครบ:** `flutter analyze` = No issues · `flutter test` = 16/16. **ด่าน 4.5 diff-reviewer = PASS** (sacred ✓ · zone apps/mobile ล้วน · ยืนยันไม่ตัดสิน Open Q #5 · lib/api/generated ไม่ถูกแตะ). commit `682d47c` (feat) + chore(loop) นี้ → TASKS `review` + REVIEW-QUEUE row.
- ตัดสินใจอะไร: **ไม่ตัดสิน Open Q #5 (offline level (ก)/(ข)) เด็ดขาด** — แยกชั้นให้ persistence/queue-contract เป็น level-agnostic (เก็บ/เรียง/เปลี่ยน state) ส่วน *policy* (เมื่อไร drain · conflict resolution · read-cache · retry/back-off) ที่ขึ้นกับระดับ ยกไปไว้หลัง seam `SyncProcessor` แบบ **abstract ไม่มี body** → task ระบุ "เฉพาะส่วนไม่ขึ้นระดับ (ก)/(ข)" พอดี ไม่ต้องเปิด blocker เพราะ scope ถูกออกแบบมาให้ทำได้โดยไม่แตะ Open Q. engineering ในเขต: createdAt เก็บ UTC ทุกจุด (root CLAUDE.md) · drift createdAt เป็น TEXT ISO-8601 เลี่ยง timezone semantics · test contract ผ่าน InMemorySyncQueue เลี่ยง dependency native sqlite (device/Phase 4 ค่อยรัน DriftSyncQueue จริง) เหมือน pattern api_client_test (P0-MOB-03). ไม่มีการตัดสิน design/spec.
- เจออะไร: (1) **build_runner clobber**: `dart run build_runner build --build-filter=lib/offline/*` **ลบ committed `.g.dart` ของ API client (53 ไฟล์) ทิ้ง** — build_runner cleanup ลบ generated output นอก filter · แก้ด้วย `git checkout -- apps/mobile/lib/api/generated/` (restore จาก commit P0-MOB-03) แล้ว re-run analyze/test เขียว · **agent รอบถัดไปที่รัน build_runner ต้องระวัง**: drift codegen (P0-MOB-05) กับ API codegen (`tool/gen_api_client.sh`) เป็นคนละ flow — รัน build_runner เต็มจะชนกัน · แนะนำ restore api generated ด้วย git ทุกครั้งหลัง gen drift หรือ regen ทั้งคู่. (`--delete-conflicting-outputs` flag ถูก build_runner รุ่นนี้ ignore แล้ว). (2) drift_dev ^2.18 resolve เข้ากับ drift ^2.18 ที่ pubspec จองไว้แต่ P0-MOB-03 · gen `local_db.g.dart` (~30KB) commit เข้า repo (CI ไม่มี Flutter stage — gate MOB เป็น local เหมือน MOB-01/02/03). (3) `local_db.dart` **ไม่ถูก re-export** ใน barrel `offline.dart` จงใจ (กัน native-sqlite dep รั่วเข้า consumer ทุกตัว — import ตรงตอน wire device DB ใน Phase 4). (4) **คิว ready เขต mobile หลังรอบนี้ = 0** (MOB-01/02/03/05 อยู่ review/done · MOB-04 blocked B-015) — ต่ำกว่าเกณฑ์ ≥5 มาก · **เตือน Wei อย่างแรง:** (ก) ตอบ B-015 ปลด MOB-04 · (ข) ตอบ Open Q #5 เพื่อปลด offline-level work (SyncProcessor impl + sync/drain/conflict + drift device DB wiring) · (ค) เติมคิว Phase 4-prep — **ห้าม agent สร้าง task ผูก MVP เอง [TBD-MVP]** · รอบลูป mobile ถัดไปจะ no-task จนกว่าคิวถูกเติม.
- 2026-07-12T02:39:10Z loop round ended (agent: mobile)

## 2026-07-12 09:39 · loop-runner · รอบที่ 3/10 · task: P0-MOB-05
- ทำอะไร: รัน claude headless 1 รอบ · task P0-MOB-05 → สถานะ review · ค่าใช้จ่ายรอบนี้ $4.865981500000001 (สะสม $15.9823/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T02:39:40Z loop round ended (agent: mobile)

## 2026-07-12 09:39 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 4/10: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต mobile — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $16.5466/$20 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)

## 2026-08-03 · orch-D รอบที่ 1 · task: MOB-I18N-01 (+ recon เขต mobile)
- ทำอะไร: **recon เขต mobile ครบก่อนเริ่มจอ** — อ่าน root CLAUDE.md · PLAN.md §0/§7/§8/§9/§10 · apps/mobile/CLAUDE.md · `docs/screen-map.md` (26 จอ distinct + host · B-015 ก) · foundation ทั้ง 3 ชั้น · `pototype/mobile.jsx` เต็มไฟล์ (611 บรรทัด · 5 จอ approval) · `docs/extract/i18n-full.json` ทั้ง 3 ชั้น + `phrase_patterns` · `packages/i18n/src/index.ts` · `apps/web/src/i18n/translators.ts` + pattern `*-strings.json`. แล้ว implement **MOB-I18N-01** = ชั้น i18n ของ mobile ซึ่ง**ไม่เคยมีมาก่อน**: `lib/i18n/juneflow_i18n.dart` (`t`/`tn`/`tp` semantics 1:1 กับ TS loader + `tpat` อ่าน `phrase_patterns` ตาม B-017 ก + langResolve ตาม I18N-KEYS.md §1 + `isRTL`/`dir` อ่านจาก `langs[]`) · `lib/i18n/screen_strings.dart` (sidecar loader) · `lib/i18n/i18n.dart` barrel + README · `tool/gen_i18n_asset.sh` (สำเนา verbatim + verify sha256) · `assets/i18n/i18n-full.json` (derived · sha ตรงกับ sacred `80c72577…`) · `assets/i18n/screens/_template_strings.json` · test 3 ไฟล์ 38 ตัว. **gates:** `flutter analyze` = No issues · `flutter test` = **68/68** (เดิม 16 → +38 · ในนั้น 5 ตัวโหลด asset จริง 1.27 MB ผ่าน `rootBundle`).
- ตัดสินใจอะไร: **ไม่ตัดสิน design/spec สักข้อ** — ทุกจุดที่ต้องเลือกถูกยกเป็น blocker แทน (B-240 i18n mint · B-241 D3 nav · B-242 Open Q #5 · B-243 hook). engineering ในเขต: (1) semantics ยึด `packages/i18n/src/index.ts` เป็นต้นแบบ **ไม่คิดเอง** เพื่อกัน web/mobile drift · (2) `normalizeLang` **ไม่ collapse region ทิ้ง** — I18N-KEYS.md §2 บอกว่า 19 nav key มี entry `zh-TW` ของตัวเอง ถ้า collapse `zh-TW`→`zh` ตั้งแต่ต้นจะทิ้งคำแปลที่มีอยู่จริง → แยกเป็น `fallbackChain` (`zh-TW`→`zh`→`en`→`th`) ตาม §1 langResolve เป๊ะ · (3) asset เป็น **สำเนา verbatim ไม่ prune** (ทั้งที่ตัด bn/fa/id/ja/pt ออกได้ ~) เพราะ prune = แปลงเนื้อ sacred · precedent `packages/i18n/src/i18n-full.json` ก็ sha ตรงกันเป๊ะ · (4) key ของชั้น nav/phrases **เป็นข้อความไทย** จึงเขียนใน `.dart` ไม่ได้ → ใช้ sidecar `.json` แบบเดียวกับ `apps/web/src/screens/land/land-survey-strings.json` · (5) `tpat` เมื่อไม่มี pattern ตรง → **คืน input เดิม ไม่คืนค่าว่าง** + มี `hasPatternFor()` ให้จอ assert ได้ ว่าประโยคนั้นถูกครอบแล้วจริง แทนที่จะ ship ไทยดิบเงียบ ๆ.
- เจออะไร: (1) ⚠️ **i18n = blocker ระดับ lane (B-240)** — string ไทย distinct ใน `mobile*.jsx` = 483 · มี key แค่ **103** · ไม่มี key **380** (UI-position 265) · ยืนยันด้วยมือกลุ่ม approval: chrome 55 → BORROW 23 (42%) · MISS 32 = static 24 + **templated 8** ซึ่งใส่ static key ไม่ได้ ต้องเป็น `phrase_patterns` (วันนี้มีแค่ 2 ตัว ไม่ครอบสักแบบ). manifest: `agents/orch-d-recon/mob-i18n-gap.md`. **แต่ policy มี precedent ครบแล้ว** — B-035 (ก) = mint โดย th/zh/en/ar = ค่าไทย verbatim (ไม่แปลใหม่) · B-017 (ก) = pattern อยู่ในไฟล์ · P1-PLAT-03/04 = เขต platform compile ลง SACRED-EDITS-QUEUE → Wei ratify → apply 2 สำเนา → cmp byte-identical · **เหลือให้ Wei เคาะแค่ขนาด batch + เจ้าของงาน**. (2) ⚠️ **มีสำเนา i18n-full.json เป็นชุดที่ 3 แล้ว** (`apps/mobile/assets/`) — เป็น derived ไม่ maintain มือ แต่ทุก mint ต้องรัน `tool/gen_i18n_asset.sh` + `flutter test` ต่อท้าย → ต้องเพิ่มบรรทัดนี้ในช่อง verification ของ task ที่ apply ไม่งั้น mobile จะค้างเวอร์ชันเก่าเงียบ ๆ. (3) ⚠️ **`.claude/hooks/i18n-guard.sh` ไม่ยิงใน worktree (B-243)** — hook เทียบ path กับ `CLAUDE_PROJECT_DIR` (main checkout) แล้ว `exit 0` ถ้าไฟล์อยู่นอกนั้น · ยืนยันด้วยการรัน hook ตรง ๆ: main-repo path → exit 2 · worktree path เดียวกัน → **exit 0** · แต่ §8 บังคับให้ทุก agent ทำงานใน worktree → guard ครอบเฉพาะที่ที่ไม่มีใครทำงาน · น่าจะกระทบ `zone-guard.sh`/`protect-files.sh` ด้วย (โครง path resolution เดียวกัน) — ถ้าใช่ sacred protection ก็ไม่ fail-closed ใน worktree. ผมถือกฎเอง: `lib/**.dart` ไม่มีอักษรไทยสักตัว (ไทยอยู่แค่ `*_test.dart` ซึ่งยกเว้นโดยเจตนา และ `.json` sidecar). (4) **จอ 26/26 ไม่มีภาพอ้างอิงต่อจอ** มีแต่ `gallery/g2/45-s.jpg` ระดับ host → G5 ของ mobile ต้องแคป baseline เองจาก `Juneflow Fiori.html` ก่อนสร้างทุกจอ (screen-map §6) ต่างจาก lane web. (5) `flutter test` รัน asset ผ่าน `rootBundle` ได้จริงใน `flutter test` (ไม่ต้อง device) — 5 ตัวใน `i18n_asset_test.dart` ยืนยันแล้ว.
- **ด่าน 4.5 (3-lens adversarial · parity / repo-law / dart-edge-cases) — เจอของจริง 3 อย่าง ผมแก้ครบก่อน push:**
  1. **FAIL · อักษรไทยหลุดเข้า `lib/i18n/juneflow_i18n.dart`** (3 จุด) — ผมเขียน `B-017 (ก)` ในคอมเมนต์ คือใช้ตัวอักษรไทยเป็นชื่อตัวเลือกคำตัดสินของ Wei · reviewer ชี้จุดที่คมกว่านั้น: **TASKS.md / REVIEW-QUEUE.md / B-243 ของผมเองอ้างสามที่ว่า "lib/**.dart ไม่มีอักษรไทยสักตัว" ซึ่ง ณ commit `843ab0b` ไม่จริง** และ Wei promote โดยดู REVIEW-QUEUE → คำอ้างเท็จเดินไปถึงจุดตัดสินใจ promote. แก้: ใช้ `(a)` ตาม convention ที่ `pubspec.yaml` ตั้งไว้แล้ว (`Offline level (a)/(b)`) · ยืนยันด้วยการรัน `i18n-guard.sh` ใส่เนื้อไฟล์จริงทั้ง 3 ไฟล์ → exit 0 ทุกตัว. **บทเรียน: ชื่อตัวเลือกคำตัดสิน ก/ข/ค เป็นอักษรไทย — อ้างใน `.dart` ต้อง transliterate เสมอ**
  2. **CONCERN (มี probe พิสูจน์) · `phrase_patterns` entry ที่ `re` หาย/ว่าง → `RegExp('')` match ทุก input** กลายเป็น catch-all ยึด `tpat()` ทั้งแอป และทำให้ `hasPatternFor()` รายงานว่าครอบทุกประโยค (ซึ่งเป็น guard ที่ผมออกแบบมาให้จอ fail loudly พอดี) · บล็อกนี้ Wei แก้มือในไฟล์ sacred ตาม B-017 → typo เป็น input ที่เกิดได้จริง. แก้: `tryFromJson` คืน null แล้ว drop ทิ้ง + test 4 เคส
  3. **CONCERN · ไม่มี gate จับ asset สำเนาที่ 3 ค้างเวอร์ชัน** — สองสำเนาแรกมีคน cmp byte-identical ตอน apply แต่ `apps/mobile/assets/` ไม่มีใครดู → apply mint แล้วลืม regen = แอปเสิร์ฟ key เก่าเงียบ ๆ ทุก key ใหม่ render เป็นชื่อตัวเอง. แก้: `test/i18n/i18n_asset_drift_test.dart` เทียบ **sha256 กับ sacred โดยตรง** + ตรวจว่า asset ไม่มี key เกินต้นทาง · **พิสูจน์ว่า gate ไม่ใช่ของหลอก**: เติม 1 ไบต์ลง asset → test แดงพร้อมข้อความบอกให้รัน `gen_i18n_asset.sh` · restore + regen → เขียว
  - แก้เพิ่มจาก finding ที่เหลือ (ไม่รอ verdict — ค่าแก้ถูกกว่าค่าพลาด): flag `u` ของ JS regex ไม่ถูก map → เพิ่ม `unicode:` · `$9` ที่เกิน groupCount ถูก**ลบทิ้งเงียบ** → เปลี่ยนเป็นคงโทเคนไว้ให้เห็น (ตรงกับ `String.replace` ของ JS ที่เขียน template พวกนี้มา) · `int.parse` ล้นช่วง → `tryParse` · `lang` ที่ส่งเข้า constructor ไม่ถูก normalize (setter ถูก) → normalize ทั้งคู่ · `normalizeLang` ไม่รู้จัก `zh_TW` (รูป `Locale.toString()` ของ Dart เอง) และ subtag เกิน 2 ตัว → split ด้วย `[-_]` แล้วตัด subtag ที่ 3+ · block ที่ type ผิดโยน `_TypeError` ดิบ → `_list()` ปฏิบัติเหมือนไม่มี · error ของ sidecar ไม่บอกชื่อไฟล์ → ใส่ assetPath ในข้อความ · คอมเมนต์หัวไฟล์เคยเคลม "1:1 port … cannot drift" ทั้งที่ไม่มีอะไรบังคับเชิงกล → เขียนใหม่ให้ตรงความจริง
  - ⚠️ **เจอระหว่างแก้ (เกือบพลาดเอง):** patch ด้วย python heredoc ทำให้ `\$` หลุดเข้าไฟล์ Dart 2 จุด → `'$base-${...}'` กลายเป็น string literal ไม่ interpolate · `flutter analyze` **ไม่จับ** (ถูก syntax) จับได้เพราะ test แดง → **อย่า patch Dart ด้วย heredoc ที่มี `$`** ใช้ Edit ตรง ๆ
- **หมายเหตุถึงเขตอื่น:** `lib/api/generated/**` มีอักษรไทยในคอมเมนต์อยู่แล้วหลายไฟล์ (`B-040(ก)` · `บริษัทในเครือ` ใน dms/master/company/counts/project/project_phase) — มาจาก description ใน `openapi.yaml` ที่ codegen ก๊อปมา · ไฟล์ generated (analyze ก็ exclude อยู่) แต่ **i18n-guard ไม่ exclude** → ถ้า B-243 ถูกแก้ให้ hook ยิงใน worktree จริง ไฟล์พวกนี้จะแตะไม่ได้ทันที · เจ้าของคือ contract/codegen ไม่ใช่เขต mobile จึงแจ้งไว้เฉย ๆ
- MOB-SHELL-00 (feature/mobile-shell 3ab356a · orch-B verified + merged) — the mobile app shell foundation: 5-tab MTabBar (B-241 canonical: อนุมัติ/หน้างาน/แจ้งซ่อม/Dashboard/โปรไฟล์ · me/โปรไฟล์ = honest-DISABLED, genuinely no destination in the prototype, NOT fabricated) + MobileScreenRouter (26 route ids → ScreenPlaceholder, zero fabricated screens/copy) + shared primitives (MSection/MField/MInput/MPill/MobileHeader = byte-exact prototype geometry, colors via JuneflowTokens) + Dio/AuthInterceptor (dart-define dev bearer, NO hardcoded secret). gate-4.5 PASS 7/7 + flutter analyze clean + test 84/84 (orch-B independent, not trusting the count). zero-mint (all 5 tab labels pre-existing keys · asset-drift green) · zero-Thai-in-lib · sacred untouched. Found+fixed 2 REAL app bugs while getting gates green: (1) MaterialApp forced locale th with no localizations delegate = runtime failure → +flutter_localizations Global delegates + supportedLocales from i18n.langs + ar RTL; (2) widget-test rootBundle deadlock → tester.runAsync. Forward-deps filed B-251/252/253 (mobile inbox/detail backend = orch-A · auth-entry = Wei). Unlocks the whole mobile screen wave.
- MOB-REF-01 (feature/mobile-refs c371131 · orch-B verified + merged) — captured all 26 mobile screen REFERENCE images from pototype/Juneflow Fiori.html (self-contained react+babel · served via http.server · playwright drove MobilePreview → clicked each of the 26 MOBILE_GROUPS rows → element-screenshot the iOS phone frame 390x844@2x → 780x1690 PNGs 80-211KB). Verified: 26 PNGs · captured-set == MobileScreenRouter's 26 branches EXACTLY (0 missing/extra) · none blank · spot-checked inbox (กล่องอนุมัติ + 3 KPI + PR/PO/WO cards, faithful). These are the mobile-G5 truth (mobile had no per-screen reference). ⚠ known: the inbox capture shows the prototype's own inline 4-tab bar (mobile.jsx) — the Flutter shell uses the 5-tab canonical (B-241) → mobile-G5 for the inbox/tabbed screens will mask/account for the bottom bar. No prototype/apps files touched (images only).
- notif (feature/mobile-notif 27a8081 · orch-B verified + merged) — FIRST mobile screen on MOB-SHELL-00 (validates the Flutter screen-port loop end-to-end). gate-4.5 PASS 7/7: no-fabrication CONFIRMED (notif_agg.dart derives from real wire {type,ref,read,created_at} only · displayTitle=title||ref||em-dash NEVER a sentence · icon/tone from real type · honest-omits the mock by/body/approve/relative-time — byte-identical approach to the merged web notifications port) · design-fidelity chrome matches mobile.jsx L543-609 (shell primitives, static filter pills honest) · money=NONE (GET /notifications + POST /{id}/read, mark-all client fan-out, no bulk endpoint faked) · router-registered (kBuiltRouteIds + mobileScreenBuilders) · reuse shell/Dio. flutter analyze clean + test 101/101 (orch-B independent, +17). mint B-254 (2: อ่าน/แจ้งอัปเดต). ⚠ notif.png ref = MOCK Fiori content (fabricated sentences/times) → the honest port shows real wire data → pixel-G5 N/A (would false-fail vs the mock); visual fidelity verified via gate-4.5 structural match · a Flutter-render golden baseline needs an emulator (deferred). Approval-group screen 1/3.
