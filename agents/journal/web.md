# Journal — Frontend Web (เขต: `apps/web`)

> บันทึกต่อรอบ loop ตาม PLAN.md §10 — หนึ่ง entry ต่อหนึ่งรอบ · entry ใหม่อยู่บนสุด
> รูปแบบ entry ต่อรอบ:
>
> ```
> ## [YYYY-MM-DD] · รอบที่ N · task: <task id>
> - ทำอะไร: (งานที่ลงมือทำจริงในรอบนี้ + ผล gates — เขตนี้ต้องระบุผล visual gate เสมอเมื่อมีจอ)
> - ตัดสินใจอะไร: (การตัดสินใจในเขตตัวเอง — ความขัดแย้ง design/spec ห้ามตัดสินเอง → BLOCKERS.md)
> - เจออะไร: (สิ่งที่พบ/ติดขัด/blocker ที่เปิด/สิ่งที่ agent รอบถัดไปควรรู้)
> ```

## 2026-07-15 · รอบที่ 13 · task: P1-WEB-15 (gate-4.5 fix)

- ทำอะไร: แก้ **2 finding FAIL จาก gate-4.5** บน charts primitive (WEB-15 คง `review`). **Finding #3 (§0 กฎ 2 — font/size/radius ต้องมาจาก tokens):** `apps/web/src/ui/chart.tsx` `baseChartOpts` hardcode `family:"IBM Plex Sans Thai"` ×4 (= ฟอนต์ stale จาก `pototype/styles.css` เก่า) + `size:12`/`size:10.5`/`cornerRadius:8` literal ทั้งที่ token ตรงตัวมีจริง (§0 กฎ 5 = Fiori theme). **fix:** เพิ่ม field `font`/`fsTable`/`fsTh`/`radius` เข้า `ChartTheme` + อ่านใน `chartTheme()` ครั้งเดียว/build คู่กับสี — `--font`→`family` ×4, `--fs-table`(12px)→size tooltip title/body, `--fs-th`(10.5px)→size tick x/y, `--r-md`→`cornerRadius`; helper `px(name,fallback)`=`parseFloat` (NaN→fallback) แปลง px-string→number ที่ Chart.js ต้องการ (font คงเป็น string) · `baseChartOpts(t,…)` อ้าง `t.font/t.fsTable/t.fsTh/t.radius` แทน literal · fallback mirror pattern สีเดิม (hex=prototype B-037(ก) · font/size/radius=Fiori theme value เมื่อ var ว่าง) · **แก้ header comment** ที่เคยอ้างเท็จ "the only literals are the hex fallbacks" → ระบุครบว่า color+font+size+radius มาจาก token, literal = fallback resolve-time เท่านั้น, drop stale IBM Plex ระบุ §0 กฎ 5. **Finding #1 (cross-zone lockfile churn):** diff chart.js เดิมพ่วง rewrite ~20 peer-key `zod@3.25.76`→`4.4.3` ทั้ง subtree better-auth + esbuild-kit URL (`tsx.is`→`tsx.hirok.io`) = dev-lockfile drift ที่ `pnpm install` normalize (โซน backend/platform). **fix:** `git checkout dev -- pnpm-lock.yaml` แล้ว **surgical hand-add เฉพาะ chart.js/@kurkle** 5 จุด (importer apps/web deps + `@kurkle/color@0.3.4`/`chart.js@4.5.1` resolution ใน `packages:` + snapshot ทั้งคู่) copy รูปแบบเป๊ะจากที่ pnpm produce ที่ `39d0d4b` — ไม่แตะ zod/esbuild-kit. **update 12 unit tests** (TOKENS stub +`--font`/`--fs-table`/`--fs-th`/`--r-md` distinct-from-fallback · SAMPLE_THEME + fallback assertion + `baseChartOpts` assert family/size/cornerRadius/ticks font). **ผล gates (agent):** `pnpm install --frozen-lockfile --filter @juneflow/web` ✓ (Lockfile up to date · ไม่ mutate เพิ่ม) · `tsc --noEmit` 0 ✓ · `vite build` 242 mod ✓ · `vitest run` **134/134** ✓ (chart 12/12) · `git diff dev -- pnpm-lock.yaml` = chart.js/@kurkle ล้วน (0 zod/esbuild-kit). **NO G5** (infra) · **merge = pending-orch** (คง `review`).
- ตัดสินใจอะไร (ในเขต · ไม่ใช่ spec conflict): (1) **`--r-md` resolve จริง = 4px ใต้ Fiori** (theme default · navy=8px) — finding note เขียน "(=8px)" คลาดเคลื่อน แต่ directive ชัด "source cornerRadius จาก `--r-md`" → ทำตาม directive (token ไม่ใช่ literal) · การที่ค่าเปลี่ยน prototype 8→token 4 คือ pattern เดียวกับ font (drop stale prototype value → ใช้ Fiori token ตาม §0 กฎ 5) · ไม่มี G5/consumer ตอนนี้ (dashboard B-049 defer) = ไม่มี visual gate ให้ตก · fallback cornerRadius=8 คง prototype literal ตาม B-037(ก) color-fallback pattern (fire เฉพาะ var ว่างใน node test). (2) **font fallback = Fiori `--font` stack** (ไม่ใช่ IBM Plex) — finding สั่ง drop stale IBM Plex เด็ดขาด → ไม่คงไว้แม้เป็น fallback (ต่างจาก hex color fallback ที่คง prototype literal เพราะ reviewer ไม่ได้ flag). (3) **isolate lockfile = surgical ไม่ใช่ escalate B-069** — frozen-install ผ่าน (`Lockfile up to date`) พิสูจน์ว่า pnpm พอใจ apps/web ด้วย chart.js entry ที่ hand-add โดยไม่ดัน zod churn กลับ → ไม่ต้องเปิด blocker · pre-existing zod/esbuild drift ปล่อยให้ platform (เจ้าของ pnpm-lock) regen รอบของตัวเอง.
- เจออะไร: (ก) `git status` = `MM pnpm-lock.yaml` (index=dev version จาก `checkout dev --` · worktree=+chart.js edit) = ปกติ · commit เก็บ worktree state · `git diff HEAD` = net drop zod churn + keep chart.js. (ข) frozen-install ไม่ mutate lockfile ซ้ำ (recheck diff หลัง install = chart.js ล้วน) → surgical entry ตรง spec pnpm. (ค) **ไม่แตะ sacred · ไม่มี key i18n (infra ล้วน) · ไม่มี blocker เปิด · ไม่ `git add -A` (guard iCloud `* 2.*`).** รอ orch: gate-4.5 re-review + merge (NO G5).

## 2026-07-15 · รอบที่ 12 · task: P1-WEB-15

- ทำอะไร: port **charts primitive** (UI infra · dashboard prereq B-049 · get-ahead per Wei ก) จาก `pototype/charts.jsx` L1-79 → `apps/web/src/ui/chart.tsx` + `chart.test.tsx`. อ่าน spec ครบ: charts.jsx ทั้งไฟล์ (chartTheme L3-17 · ChartCanvas L19-44 · baseChartOpts L46-78 · `Object.assign(window,…)` L79) + consumer `dashboard.jsx` `BudgetActualChart` L43+ (ยืนยัน build receives theme `t` → คืน Chart.js config bar+line composed) + primitives siblings (`ui/card.tsx`/`button.tsx` export+token style) + shell theme signal (`shell-context.tsx` L144-155 = apply **data-density** live, theme fix navy B-042). **implement:** replace `window.Chart`/`Object.assign(window)` global → ES `import { Chart, … } from "chart.js"` + ES exports · scoped `Chart.register(BarController,LineController,BarElement,LineElement,PointElement,LinearScale,CategoryScale,Tooltip,Legend,Filler)` module-load · `chartTheme()` อ่าน CSS var --text-2/--surface-3/--brand/--brand-soft/--accent/--ok/--warn/--danger/--surface/--border verbatim (fallback hex = B-037(ก)) · `baseChartOpts(t,opts)` pure themed tooltip/legend/scales + merge · `ChartCanvas({build,height=240,deps=[]})` React wrapper (canvasRef + instRef + MutationObserver repaint + effect create/destroy) · `createThemedChart()` extracted = lifecycle testable jsdom-free. **dep:** `pnpm add chart.js` → **^4.5.1** (apps/web/package.json + pnpm-lock.yaml touch = orch-authorized). **12 unit tests** (chartTheme token+fallback · baseChartOpts themed+3 merge · createThemedChart null/mount/destroy/rebuild via chart.js `vi.mock` · ChartCanvas SSR render). **ผล gates (agent):** typecheck 0 ✓ · vite build 242 mod ✓ · vitest **134/134** (+12 chart) ✓. **NO G5** — infra ไม่ใช่ screen (ไม่มี gallery ref) · **gate-4.5 + merge = pending-orch**.
- ตัดสินใจอะไร (ในเขต · ไม่ใช่ spec conflict): (1) **theme repaint = MutationObserver `["data-theme","data-density"]`** — prototype watch แค่ `data-theme` (charts.jsx:26) แต่ shell จริง drive live signal ผ่าน `data-density` (B-042 · theme fix navy) → observe ทั้งคู่เพื่อ reproduce prototype intent **และ** ให้ density/light-dark switch repaint จริง (ตรง task instruction). (2) **v3→v4 API migrate** (task mandate "pin chart.js v4" · prototype เขียนยุค v3): `grid.drawBorder:false` (ถูกลบใน v4) → sibling `border:{display:false}` · font `weight:"700"` (string · v4 FontSpec ไม่รับ) → `700` (number) — **render identical ทั้งคู่** · ไม่มี G5/consumer verify ตอนนี้ (dashboard defer) → เป็น mechanical framework-version migration ไม่ใช่ redesign. (3) **extract `createThemedChart(canvas,build)`** จาก effect body — repo vitest env = `node` (ไม่มี jsdom · ไม่ authorized เพิ่ม dep นอก chart.js) → test lifecycle (mount create/unmount destroy/deps rebuild) ผ่าน chart.js `vi.mock` + fake canvas `{}` (task: "without needing a real canvas") · mirror existing DOM-free node test style (`screens/master/*.test.ts`) · component API/behavior ไม่เปลี่ยน. (4) drop `!window.Chart` guard (charts.jsx:31) = mock mechanic (CDN-load guard · §0 กฎ 3) — ES import ทำให้ Chart มีเสมอ · เก็บ `!canvas` guard. (5) `baseChartOpts` return `ChartOptions` (chart.js type · no `any`) · overrides typed ผ่าน index (`ChartOptions["scales"]`/`plugins.tooltip`/`Partial<ChartOptions>`).
- เจออะไร: (ก) **chart.tsx = orphan** (ยังไม่มี consumer — dashboard B-049 defer) → tree-shaken ออกจาก app bundle ปกติ (chart.js ไม่ bloat จนกว่าจะ import) — proven bundles จริงผ่าน lib-mode probe (`vite build` entry=chart.tsx → 241kB มี chart.js internals `categoryPercentage`/`getDatasetMeta`/`radialLinear`) → cleanup probe config ทิ้ง output ไป /tmp. (ข) tsc union error: `o.scales?.x?.border` — `border` อยู่บน cartesian scale เท่านั้น (radialLinear ไม่มี) → assert ผ่าน `toMatchObject` เลี่ยง typed union access. (ค) **ไม่มี blocker เปิด · ไม่แตะ sacred · ไม่มี key i18n (infra ล้วน · ไม่มี user-facing string)**. รอ orch: gate-4.5 + merge (NO G5).

## 2026-07-15 · รอบที่ 11 · task: P1-WEB-14

- ทำอะไร: port จอ **`users`** (UsersPermissions) จาก `pototype/master.jsx` L906-1001 (+ UserAddForm L1003-1054 + RoleAddForm L1056-1116) ตาม recon plan. อ่าน prototype ต้นทาง + pattern siblings (master-model/org) + contract `/roles`+`/users` + seed roles + ref g2/36 ก่อนเขียน. **7 ไฟล์** `apps/web/src/screens/master/`: `role-matrix.ts` (pure ASCII logic: toRole/buildPermMatrix pad-clamp 11×5/serializePermMatrix/togglePerm/approvalLevelLabel/formatAuthLimit tri-state/countMembersByRole/formatMoney + 11 module labels+"Permission Matrix" English literal) + `role-matrix.test.ts` (18 tests) · `use-users.ts` (useRoleList/useCreateRole/useUpdateRole/useUserList/useCreateUser · generated client + unwrap + B-014 `.data` · invalidate) · `users-permissions.tsx` (Page + 320px/1fr · LEFT role list · RIGHT read-only matrix + PUT save) · `role-add-form.tsx` (interactive togglePerm matrix + POST /roles) · `user-add-form.tsx` (2-col grid + activate toggle + info box + POST /users · phone dropped) · `users-strings.json` (nav+phrase keys กัน i18n-guard) + register `router.tsx` `PORTED_SCREENS["users"]`. **ผล gates (agent):** typecheck 0 ✓ · vite build 233 modules ✓ · web unit **104/104** (+18 role-matrix) ✓ · check:routes PASS. **G5 + gate-4.5 = pending-orch** (ไม่รันเอง ตาม task — orch merge → screenshot เทียบ ref g2/36).
- ตัดสินใจอะไร (ในเขต · ไม่ใช่ spec conflict): (1) **TWO matrix แยก component** — main = read-only `<div>` cells, RoleAddForm = interactive toggle-button (ไม่ share · §0). (2) **mock→real upgrade**: prototype save-btn ยิง `ctx.notify` เฉย ๆ → wire เป็น **PUT /roles/{id}** re-save `serializePermMatrix(role.perms)` ผูกกับปุ่ม read-only เดิม (ไม่ประดิษฐ์ editable RoleEditForm — จะผิด §0). (3) **member count = GET /users + `countMembersByRole`** (C10 · mock `c` DROP) ตามที่ plan ให้ useUserList+countMembersByRole มา (backend `user_count` ก็ real แต่ตาม plan file list). (4) **auth-limit tri-state** re-synth ใน view จาก (approval_limit,approval_level): number→fmt+฿ · null&level>0→`role.limitUnlimited` var(--ok) · null&level0→em-dash. **Director seed null+level4→"ไม่จำกัด" verified** (packages/db seed ROLE_DEFS dir + roles.ts toWire null-passthrough) — สมมติฐาน plan hold. (5) **dept i18n mixed routing**: มีแค่ CONS ที่ไม่มี dict.CONS → CONS ใช้ tp phrase `CONS — ฝ่ายก่อสร้าง`, ส่วน PROC/FIN/SLS/ADM/WH ใช้ `dept.*`(t) — grep-confirmed แต่ละ label มี key จริง 1 ตัว (ไม่ประดิษฐ์ · plan บอก "tp" กว้าง ๆ แต่ 5 ตัวไม่มี phrase key → routing ตามที่ key มีจริง). (6) `role.matrixLevelLine` วางที่ RIGHT sub-line (level>0) ตาม prototype (supreme law) — ตรง key text `ระดับชั้น {level}...`. (7) BAHT `฿` = `฿` escape (ไม่มี ฿-only key · literal ฿ ติด i18n-guard U+0E00-U+0E7F · B-037 verbatim) · em-dash `—`. (8) native `<select>` แทน ds.jsx Dropdown popover (org-add-form precedent · modal ไม่มี G5 ref). (9) forms owns POST + notify ตาม plan file 2/3 (ต่างจาก model/org ที่ parent owns — plan-specified, ไม่มี user-visible diff).
- เจออะไร: (ก) **i18n-guard บล็อกไทยทุกตัวใน .ts/.tsx รวม comment** — scrub `฿`/`ไม่จำกัด`/`บันทึก`/`ดู` ออกจาก comment ทั้งหมด (0 Thai-block char เหลือ · verified) · BAHT/em-dash ใช้ escape. (ข) header comment `users.*/role.*` มี `*/` ปิด block comment ก่อนเวลา → แก้เป็น `users. / role.`. (ค) modal body render-prop args ต้อง annotate `{ctx,close}: {ctx:ShellCtx;close:()=>void}` (union body type infer ไม่ได้ · ตรง master-model precedent). (ง) prototype bold "Username" ใน addUserRoleHint = inline key text (key มีแค่ {role}/{limit} placeholder) → bold แค่ {role}/{limit} ตาม plan (Username คงปกติ · documented). (จ) RoleAddForm limit `suffix="฿"` chrome ไม่ port (ไม่มี ui primitive · label มี "(baht)" อยู่แล้ว · org-add-form precedent). **ไม่มี blocker เปิด · ไม่มี key หาย (recon ยืนยันครบ · grep-confirmed).** รอ orch: G5 g2/36 + gate-4.5 + merge.

## 2026-07-13 · รอบที่ 10 · task: P1-WEB-01

- ทำอะไร: หยิบ **P1-WEB-01** (unblock แล้ว — B-035+B-036 ตอบ, 20 `login.*` + `login.email` dict keys เข้า i18n-full.json, dep = —, ตัวเดียว `ready` ในเขต web) → `doing`. ใช้ skill `port-screen` อ่าน spec ครบรอบนี้: `NAV-ROUTES.md:133` + `pototype/extra-screens.jsx:7-48` (`ScreenLogin`) + `ForgotForm:210-222` + `shell.jsx:104-119` (login host = ScreenLogin+modal+toast render ก่อน shell) + contract `/auth/login`. **implement:** `src/screens/login/login-screen.tsx` (จอ + local modal/toast host, port 1:1) · wire `router.tsx` (route `/login` → real screen ผ่าน `PORTED_SCREENS`) · `index.html` ตั้ง `data-theme="navy"` + webfont `<link>` · `main.tsx` import `base.css`. (base.css/ui/login-submit/forgot-form + test มีจากรอบ 9 commit `fe5c748` แล้ว). **ผล gates: typecheck ✓ · build ✓ (vite 187 modules) · lint no-op ✓ · nav-parity ✓ · web unit 24/24 ✓ · G5 visual gate = PASS** (เทียบ `gallery/g4/01` — §0 structural criteria ตรงครบ, navy brand + divider x=1140 exact, residual 1.4% thr48 = glyph-edge AA + JPEG lossy เท่านั้น). **ด่าน 4.5 diff-reviewer = PASS** (sacred/zone/design-fidelity/mock-stripped/test-coverage ครบ) → commit `9d775e5`, task → `review`, เพิ่มแถว REVIEW-QUEUE.
- ตัดสินใจอะไร: **G5 แดง 2 defect รอบแรก → fix ในเขต (ไม่ใช่ spec conflict, ไม่เปิด blocker):** (1) brand panel เป็น SAP-blue ไม่ใช่ navy → `data-theme="navy"` เพราะ prototype **force navy accent app-wide** (`shell.jsx:34` "main accent set to navy per request") + gallery ทุกใบ navy + tokens ship `[data-theme="navy"]` = #0B2A4A/#133E6B ตรงเป๊ะ (fidelity fix ตาม reference ground truth, ไม่ใช่ redesign) · (2) ฟอนต์ fallback Times serif + layout เลื่อน 112px → `base.css` ไม่ถูก import ใน main.tsx (box-sizing:border-box + `body font var(--font)` หาย) → เพิ่ม import + webfont `<link>` mirror prototype head. `common.lang` ใช้เป็น stat "ภาษา" ตาม B-035 ruling ของ Wei (reference เป็นไทย → render ตรง). signup link = no-op (SignupWizard ยังไม่ port, ตรง prototype guarded). auth-failure(401) เงียบ — ไม่มี key (mock ไม่เคย fail) ห้ามประดิษฐ์ (§0 กฎ 2).
- เจออะไร: 3 fix รอบ (navy → base.css/font → confirm) ทั้งหมดในเขต apps/web ไม่แตะ sacred/นอกเขต. **broader flag (platform/tokens):** (ก) tokens default = fiori/SAP-blue แต่ prototype จริง default = navy accent — web ตั้ง `data-theme="navy"` ที่ index.html ชดเชย; ถ้า Wei ต้องการ default navy ระดับ tokens ควรพิจารณาฝั่ง platform · (ข) webfont โหลดผ่าน Google Fonts runtime (ตรง prototype) — offline/CI ไม่มีเน็ตจะ fallback; ถ้าต้อง bundle ฟอนต์ = งาน platform/infra · (ค) **QA:** harness strict threshold=0 จะ FAIL จาก reference `.jpg` lossy — calibration = คำตัดสิน Wei/BLOCKERS (บันทึกใน REVIEW-QUEUE, ห้ามผ่อน threshold เงียบ). **คิว `ready` เขต web หลังรอบนี้ = 0 ที่หยิบได้** (WEB-05 blocked · P1-WEB-01 → review · FIX-03 review) → **เตือน Wei:** เติมคิว Phase 1 web ให้ ≥5 (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]).

## 2026-07-13 · รอบที่ 9 · task: P1-WEB-01

- ทำอะไร: หยิบ **P1-WEB-01** ใหม่ (Wei ตอบ B-035 = เพิ่ม 20 `login.*` dict keys → unblock กลับเป็น `ready`, dep = —, ตัวเดียวที่หยิบได้ในเขต web) → `doing` · ใช้ skill `port-screen`. อ่าน spec ครบรอบนี้: `NAV-ROUTES.md:133` (route `login`) + `pototype/extra-screens.jsx:7-48` (`ScreenLogin`) + `ForgotForm:210-222` + primitives ต้นทาง `ds.jsx`(Icon/Btn) `pr-form.jsx`(Field) `modal.jsx`(Modal) `shell.jsx:104-119`(login host = ScreenLogin + modal + toast, render ก่อน shell) + contract `/auth/login` (`AuthLoginInput`→`AuthLoginResult{token}`). **implement (compliant, typecheck ✓ + unit 6/6 ✓ — uncommitted, resume-ready):** `src/base.css` (base type + `.num` + modal keyframes, token-derived), `src/ui/{icon,button,field,modal}.tsx` (port 1:1, สี/รัศมี = tokens), `src/screens/login/login-submit.ts`+`.test.ts` (pure auth logic: validate→POST `/auth/login` ผ่าน generated client→`setAuthToken`→outcome, 6 เคส), `src/screens/login/forgot-form.tsx` (dict keys ล้วน). **ยังไม่เขียน `login-screen.tsx` (host) + wire router/main** — ติด hook ที่ label เดียว. **ผล gates: G5 visual gate = ยังไม่ได้รัน** (จอ host ยังเขียนไม่ได้). → **escalate B-036, task → blocked.**
- ตัดสินใจอะไร: 19/20 string map เข้า **dict key** ได้ครบ: `login.*` (B-035, ไทย verbatim ทุกภาษา ตรง prototype login) + `app.name` (brand "Juneflow · …") + `common.lang` (stat "ภาษา"). mock ไม่ port (§0 กฎ 3): pre-filled seed email → เริ่ม empty (data diff, visual gate ยอมรับ) · signup link = guarded `window.openSignup` no-op (SignupWizard ยังไม่ port, ตรง prototype). geometry (px panel/field) เก็บ literal ตาม prototype เพื่อผ่าน g4/01 · สี/ฟอนต์/รัศมีปุ่ม = tokens. auth-failure(401) copy ไม่มี key (mock ไม่เคย fail) → ไม่ประดิษฐ์ (§0 กฎ 2), `console.error`+re-enable — **follow-up: ต้องมี key ถ้าจอ authenticated ต้องการ error state**. **ไม่ตัดสิน spec เอง** → เปิด B-036 สำหรับ label ที่เหลือ.
- เจออะไร (สาเหตุ block): Field label **"อีเมล"** (extra-screens.jsx:36) — B-035 จัดไว้ว่าใช้ `phrases/"อีเมล"` จึงไม่เพิ่ม dict `login.email` (มีแต่ `login.password`) · แต่ `phrases`/`nav_i18n` เป็น **Thai-as-key** ต้องใส่ literal ไทย (`tp("อีเมล")`) และ hook `i18n-guard.sh` **บล็อกไทยทุกตัวใน `apps/web/src/**/*.tsx`** → `tp()/tn()` เขียนใน web ไม่ได้เลย (ยืนยันจากรอบ 5 = **B-017** เรื่องเดียวกัน PHRASE_PATTERNS) · ไม่มี dict key ให้ "อีเมล" → email label ไม่มี key ที่ web ใช้ได้ → **B-036** เสนอ (ก·แนะนำ) Wei เพิ่ม dict `login.email`="อีเมล" (สมมาตร login.password) / (ข) allowlist ไทยใน tp/tn ที่ i18n-guard (platform/devops). **broader:** channel tn/tp ทั้งชุดใช้ใน web UI code ไม่ได้ภายใต้ i18n-guard (= B-017) — flag platform/devops. **คิว `ready` เขต web หลังรอบนี้ = 0 ที่หยิบได้** (WEB-05 blocked B-020 · P1-WEB-01 blocked B-036 · FIX-03 review) → **เตือน Wei:** ตอบ B-036 (+B-020, B-017) + promote review-queue web + เติมคิว Phase 1 web ให้ ≥5 (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]). โค้ด compliant ที่เขียนไว้ (base.css/ui/login-submit/forgot-form + test) uncommitted บน worktree รอ resume ทันทีที่ B-036 ตอบ.

## 2026-07-13 · รอบที่ 8 · task: P1-WEB-01

- ทำอะไร: หยิบ **P1-WEB-01** (port จอ Login · dep = — · ready ตัวเดียวที่หยิบได้ในเขต web · P0-FIX-03 = `review` แล้ว) → `doing`. อ่าน spec ครบตามกฎ apps/web/CLAUDE.md บรรทัด 7: `docs/extract/NAV-ROUTES.md:133` (route `login` "เข้าสู่ระบบ" · component `ScreenLogin` · ไฟล์ `extra-screens.jsx` · render ก่อน shell) + ต้นทาง `pototype/extra-screens.jsx:7-48` (`ScreenLogin`: brand panel + form email/pw + remember/forgot + ปุ่มเข้าสู่ระบบ + สมัครทดลองฟรี) + `ForgotForm` (modal ลืมรหัสผ่าน). **escalate เป็น blocker — ไม่ได้ implement โค้ดจอ** (ดูตัดสินใจ). **ผล gates: N/A** (ยังไม่แตะโค้ดจอ เพราะติด i18n conflict ก่อนเริ่ม port ตาม port-screen ขั้นที่ 5).
- ตัดสินใจอะไร: **ไม่แปลใหม่ ไม่เดา → เปิด B-035 (block P1-WEB-01).** ตรวจ `docs/extract/i18n-full.json` ครบทั้ง `dict`(59) / `phrases`(736) / `nav_i18n`(112) ด้วย exact-match — จอ Login มีข้อความไทย ~15 ชิ้น**ไม่มี key** (เช่น "เข้าสู่ระบบ", "รหัสผ่าน", "จดจำฉันไว้", "ลืมรหัสผ่าน?", "กรุณากรอกอีเมลและรหัสผ่าน", "เข้าสู่ระบบสำเร็จ", hero title/sub, "สมัครทดลองฟรี", ForgotForm ทั้งชุด) · มีแค่ "อีเมล"(`phrases`) / "ยกเลิก"(`common.cancel`) / "ระบบงานก่อสร้าง"(`dict/app.name`) / "ภาษา"(`common.lang`). สาเหตุ: `_source` ถอด i18n จาก `i18n.jsx`/`i18n-phrases.jsx`/`i18n-accounting.jsx`/`i18n-phrases3.jsx` เท่านั้น — `extra-screens.jsx` (จอ "missing production screens") ไม่ถูก extract. ตาม **PLAN §0 กฎ 2** + zone CLAUDE.md บรรทัด 13-14 (string ไม่มี key → BLOCKERS ห้ามเดา) + port-screen ขั้นที่ 5 → escalate. `i18n-full.json` เป็น sacred (web แตะไม่ได้) เสนอ Wei 3 ทาง (ก·แนะนำ Wei เติม key `auth.*` + คำแปล th/zh/en/ar / ข override + Wei ส่งคำแปล / ค ยกเว้นกฎ auth screen). ไม่มี C1–C10 ข้อใดครอบ (auth screen ไม่อยู่ในตารางคำตัดสิน).
- เจออะไร: (1) **B-035 เปิดใหม่** — i18n gap นี้กระทบทุกจอที่ port มาจาก `extra-screens.jsx` (Login/ReportsHub/SettingsCompany/NotificationsCenter/ForgotForm) ไม่ใช่แค่ Login → Wei ควรตอบเป็นชุดเพื่อปลดหลายจอ. เกี่ยวเนื่อง **B-034** (QA-03 smoke "login→shell" รอ login port) — ปลด B-035 ช่วยปลด B-034 ด้วย. (2) **คิว `ready` เขต web หลังรอบนี้ = 0 ที่หยิบได้** (WEB-05 blocked B-020 · P1-WEB-01 blocked B-035 · FIX-03 review รอ promote) — ต่ำกว่าเกณฑ์ ≥5 มาก → **เตือน Wei:** ตอบ B-035 (ปลด Login) + B-020 (ปลด WEB-05) + promote review-queue web + เติมคิว Phase 1 web (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]). (3) รอบนี้ไม่มี diff โค้ด (แก้เฉพาะ TASKS.md/BLOCKERS.md/journal) — no-progress guard: เป็นการ escalate ที่ถูกต้องตาม protocol ไม่ใช่ diff ว่างจากงานค้าง.

## 2026-07-13 · รอบที่ 7 · task: P0-FIX-03

- ทำอะไร: หยิบ **P0-FIX-03** (WEB-06 audit · เขต web · dep = — พร้อม B-028 ตอบ ก) → `doing`. เขต web มี ready 2 ตัว (P0-FIX-03, P1-WEB-01) — เลือก FIX-03 ก่อนเพราะเป็น P0 audit + spec ระบุ "ต้องตัดสินก่อนจอ authenticated แรก" (prerequisite ของ P1). อ่าน spec: B-028 ตอบ ก (web ใช้ bearer JWT ตาม contract · ไม่แตะ openapi.yaml sacred) + `apps/web/src/api-client.ts` + `packages/contracts/openapi.yaml` (`security: [bearerAuth: []]` global · `securitySchemes.bearerAuth` http/bearer/JWT) + `vite.config.ts` (proxy = TODO ยังไม่ wire) + openapi-fetch 0.17 middleware API (`.use({onRequest})`). **implement:** (1) api-client.ts เปลี่ยน cookie (`credentials:"include"`) → `apiClient.use({onRequest})` แนบ `Authorization: Bearer <jwt>` เมื่อมี token · public endpoint (`security:[]`) รันได้ไม่มี header (2) แก้ header comment เท็จที่อ้าง "Vite dev proxy ... is wired in vite.config.ts" (จริง = TODO) + ลบ comment cookie auth (3) เพิ่ม `auth-token.ts` (single localStorage bearer-JWT holder · injectable deps · mirror lang-store.ts · login P1-WEB-01 จะ setAuthToken) + `auth-token.test.ts` (G3). **ผล gates (ไม่มีจอ → ไม่มี visual gate):** typecheck ✓ · build ✓ (vite 176 modules) · **vitest 18 passed** (auth-token 7 เคส) · turbo lint web = no-op เขียว · **ด่าน 4.5 diff-reviewer = PASS** (sacred/zone/design-fidelity/auth-correctness/test-coverage). → commit `30b358b` · task → `review` · เพิ่มแถว REVIEW-QUEUE.
- ตัดสินใจอะไร: ใช้ B-028 ตอบ ก ตรงตัว (bearer ไม่แตะ sacred) — ไม่มี conflict ใหม่. เลือกทำ bearer ผ่าน openapi-fetch middleware (`.use`) แทนแก้ค่า createClient ต่อ request (สะอาด + typed) · เก็บ token ใน store แยกไฟล์ตามแพทเทิร์น lang-store (login รอบหน้าจะ set) · **ไม่เปลี่ยน default base URL `/api/v1`** (นอก scope FIX-03 + กระทบ compose reachability ของ QA-03) เพียงแก้ comment ให้ตรงจริง. ไม่ตัดสิน design/spec เอง (auth/build tooling — C1–C10 n/a).
- เจออะไร: (1) i18n-guard hook บล็อกตัวอักษรไทยในไฟล์โค้ด (comment "(ก)"/"ภาคผนวก" ครั้งแรก) → เขียน comment เป็นอังกฤษล้วน (CLAUDE.md กฎภาษา) ผ่าน. (2) web package ไม่มี lint script → turbo lint = no-op (ตรงพฤติกรรม CI เดิม) — ยืนยัน gate CI ผ่านด้วย typecheck+build. (3) **auth-token.test.ts ใช้ mirror ของ onRequest middleware** (openapi-fetch ไม่ export middleware ตรง ๆ) — diff-reviewer ตั้ง note non-blocking ว่าอนาคตอาจ import จริงเลี่ยง drift · logic store จริงถูก test ตรง. (4) **คิว `ready` เขต web หลังรอบนี้ = 1** (P1-WEB-01 login · WEB-05 ยัง blocked B-020) — ต่ำกว่าเกณฑ์ ≥5 → เตือน Wei: ตอบ B-020 ปลด WEB-05 + เติมคิว Phase 1 web (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]). รอบหน้าเขต web หยิบได้: P1-WEB-01 (port จอ Login).

## 2026-07-12 · รอบที่ 6 · task: P0-WEB-05

- ทำอะไร: หยิบ P0-WEB-05 (app shell — dep WEB-02+WEB-03 = done แล้ว, เป็น ready task เดียวที่หยิบได้ในเขต web) → `doing`. อ่านต้นทางครบรอบนี้ตามกฎ apps/web/CLAUDE.md: `pototype/chrome.jsx` (Sidebar/TopBar/ProjectSwitcher/CompanySwitcher(ref)/LanguageSwitcher(ref)/NotificationsPopover/SearchPalette/UserMenu + NAV/ROUTE_LABELS/PARENT_ID_OF_ROUTE) + `pototype/shell.jsx` (AppShell/RouteView/Placeholder/Tweaks/toast/modal host) + spec: NAV-ROUTES(ผ่าน registry P0-WEB-02), I18N-KEYS.md (3 ชั้น dict/nav/phrases), port-map.md (chrome=shell, shell=routing host, shared primitives = P0-WEB-05), contract endpoints (`/me`,`/projects`,`/project-types` มีจริง). **ไม่ได้ implement — escalate เป็น blocker** (ดูด้านล่าง). **ผล gates: N/A** (ยังไม่แตะโค้ด เพราะติด conflict ก่อนเริ่ม port).
- ตัดสินใจอะไร: **ไม่ตัดสิน design/spec เอง → เปิด B-020 (block P0-WEB-05).** พบ conflict นอกตารางคำตัดสิน C1–C10: จอ shell ต้อง render ข้อมูลจริง (user `/me` "สมชาย วัฒนกุล", projects ใน ProjectSwitcher, **badge เมนู = count จาก query จริง — C10**) ให้ตรงภาพอ้างอิง gallery ที่ populated — แต่ (1) §0 กฎ 3 ห้าม hardcode mock ข้อมูลเหล่านี้ (2) central seed **P0-BE-10 ถูก block (B-009)** (3) ไม่มี API/compose ในเขต web (**P0-DEV-01 ยัง ready**) (4) dep ของ WEB-05 ระบุแค่ WEB-02/03 ไม่รวม seed/API. ผล: **G5 (visual gate เทียบ reference populated) ทำให้ผ่านไม่ได้** โดยไม่ hardcode mock (ผิดกฎ 3) หรือไม่ตัดสินเองว่า empty-state ผ่าน (ผิดกฎ 4) → เข้าเงื่อนไข "ตัน/ขัดแย้ง spec" ของ loop protocol. เสนอ Wei 4 ทาง: (ก·แนะนำ) แยก 5a โครง chrome/shell + shared primitives + i18n/tokens/router ด้วย loading/empty (gate=CI+nav parity) / 5b full G5 หลัง seed+compose · (ข) เพิ่ม BE-10+DEV-01 เป็น dep · (ค) อนุมัติ mock ชั่วคราวใน dev · (ง) Wei กำหนดเอง. ไม่เดา ไม่เลือกเอง (PLAN.md §0 กฎ 4).
- เจออะไร: (1) **B-019 มีอยู่แล้วสำหรับ P0-WEB-05** (placeholder text "หน้านี้ยังไม่ได้สร้าง" ไม่มี i18n key — สถานะ "ไม่ block งาน") — B-020 ที่เปิดใหม่คือ conflict ระดับ block (ข้อมูล/seed/G5). (2) shared primitives ที่ shell ต้องใช้ (Icon set/Avatar/TypeBadge/Btn/Card/Modal/ConfirmDialog/Kbd/CompanySwitcher/LanguageSwitcher) อยู่นอก chrome.jsx/shell.jsx — port-map ระบุเป็นงาน P0-WEB-05 (`shared`) → ทำให้ scope จริงใหญ่กว่า "3 ชม." มาก (แจ้ง Wei ปรับ sizing/แตกงานผ่าน B-020 ทางเลือก ก). (3) tenant/platform viewMode toggle + pkgMenuAllowed/moduleOn gating ใน Sidebar ผูกกับกลไก tweak ของ prototype — ต้องแมปเข้า feature-flags/registry ที่มีแล้ว (P0-WEB-02) ตอน port จริง (5a). (4) **คิว `ready` เขต web หลังรอบนี้ = 0 ที่หยิบได้** (WEB-01/02/03/04/06 = done · WEB-05 = blocked) ต่ำกว่าเกณฑ์ ≥5 มาก → **เตือน Wei: ตอบ B-020 เพื่อปลด/แตก WEB-05** + เติมคิว Phase 1 web เมื่อ MVP นิยามพร้อม (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]).

## 2026-07-12 · รอบที่ 5 · task: P0-WEB-03

- ทำอะไร: i18n wiring (P0-WEB-03) — สร้าง `src/i18n/` 4 ไฟล์ + wire เข้า `main.tsx`. `lang-store.ts` = store framework-agnostic mirror `I18N` IIFE ของ prototype (i18n.jsx L94-116) **ตัด DOM MutationObserver ทิ้ง (กลไก mock §0 กฎ 3)** — persist `localStorage["juneflow-lang"]` (default `th`) · set `<html lang/dir>` (ar→rtl) · sync `setLang()` ของ `@juneflow/i18n` ให้ t()/tn()/tp() (ไม่ส่ง lang) สะท้อนภาษา active · **side-effect (storage/document) inject ได้ → unit-test ใน Node ล้วน ไม่ต้อง jsdom** (ไม่เพิ่ม dep นอก vitest) · `translators.ts` = `buildTranslators(lang)` bind t/tn/tp/dir/langs ต่อภาษาเดียว (pure ต่อ render) · `provider.tsx` = `<I18nProvider>` + `useI18n()`/`useLang()` ผ่าน `useSyncExternalStore` เหนือ `langStore` (= equiv prototype `useLang()` ตัด MutationObserver) · `index.ts` barrel · `main.tsx` ครอบ tree ด้วย `<I18nProvider>` + อัปเดต comment TODO→DONE. **ผล gates (G3 loader/RTL + ไม่มี string นอก i18n key · non-UI wiring → visual gate N/A จนกว่ามีจอ P0-WEB-05):** `pnpm --filter @juneflow/web test` = **11/11 pass** (lang-store 7: default th · restore persisted en · unknown→th · ar→rtl+persist juneflow-lang · zh→ltr · no-op same/unknown · subscribe/unsub; translators 4: t/tn/tp ครบ th/zh/en/ar + dir ar-only + langs 4 codes — expected ตรง i18n-full.json verbatim) · root `turbo typecheck` **10/10** · `lint` **9/9** · `test` **11 tasks** (i18n package 15/15 ยังเขียว) · `vite build` web ✓ 176 modules · nav parity **PASS 100+8+3** (WEB-02 ไม่กระทบ). **ด่าน 4.5 diff-reviewer = PASS** (reviewer re-run gates เอง: web test 11/11 · typecheck+lint 18/18 · build 176 modules · check:routes PASS · sacred 0 · zone เฉพาะ apps/web+lockfile importer · no re-translate/no token hardcode/no MutationObserver mock · B-017 deferral ถูกต้องตาม "key ไม่มี → BLOCKERS" ไม่ใช่ตัดสิน spec เอง · ไม่แตะ C1–C10) → commit `6f00628` บน `feature/web` (local-only ไม่มี remote → push/auto-merge dev รอ loop-runner) · TASKS.md P0-WEB-03 → `review` + แถว REVIEW-QUEUE
- ตัดสินใจอะไร: ไม่ตัดสิน design/spec เกินคำตัดสินที่มี. (1) **PHRASE_PATTERNS (ประโยคมีตัวเลข) → เปิด B-017 ไม่ทำในรอบนี้** — prototype i18n.jsx L617-622 มี 2 pattern (`แสดง X จาก Y รายการ`, `กรอง · N`) แต่ข้อความ (Thai src key + คำแปล) **ไม่มีใน i18n-full.json** (ชน PLAN §0 กฎ 2) **และ** hook `i18n-guard.sh` บล็อก Thai literal ใน source `.ts` เชิงกล (exit 2 — ยืนยันแล้วตอนพยายามเขียน phrase-patterns.ts) → เข้าเงื่อนไข "key ไม่มี → BLOCKERS" ของ task เอง · core wiring เสร็จ+เขียว ไม่ขึ้นกับ pattern · ยังไม่มีจอเรียกใช้ (list-footer เริ่ม P0-WEB-05+) → **B-017 non-blocking, task ไป review ตามปกติ**. (2) เลือก store แบบ inject side-effect แทน jsdom เพื่อทดสอบ G3 loader/RTL ใน Node ล้วน — ไม่เพิ่ม dep jsdom ที่จะแตะ root pnpm-lock เกินจำเป็น (เพิ่มแค่ vitest devDep ของ apps/web ตาม pattern packages/i18n). ไม่มี conflict อื่น → ไม่เปิด blocker เพิ่ม
- เจออะไร: (1) **hook `i18n-guard.sh` บล็อก Thai ทุกที่ใน `apps/web/src/**/*.ts|tsx` (รวม comment) แต่ยกเว้นไฟล์ `*.test.ts`/`*.spec.ts`** → test ใช้ Thai key ได้ (จำเป็นสำหรับ tn('งานหลัก')/tp phrase) · source ต้อง 0 Thai — จึงต้องเลี่ยง PHRASE_PATTERNS (B-017) · (2) `quick-verify.sh` hook รัน typecheck หลังทุก Edit/Write — import ก่อนใช้ + เพิ่ม test ก่อนติดตั้ง vitest = FAIL ชั่วคราว (คาดไว้) แก้ด้วยการ wire ให้ครบ + `pnpm install` · (3) ไม่มี git remote → push/auto-merge รอ loop-runner (local-only เหมือนรอบก่อน) · (4) **คิว `ready` เขต web หลังรอบนี้ = 1 ที่หยิบได้จริง (P0-WEB-05, dep WEB-02+WEB-03 — แต่ทั้งคู่ยัง `review` ไม่ `done` → จริงๆ หยิบไม่ได้ → คิวหยิบได้ = 0)** ต่ำกว่าเกณฑ์ ≥5 มาก · WEB-01/02/03/04/06 ทั้งหมดค้าง review รอ Wei promote · เตือน Wei: **promote review-queue (web 5 task) เพื่อปลด WEB-05** + เติมคิว Phase 1 web เมื่อ MVP นิยามพร้อม (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]) · ตอบ B-017 (ก/ข/ค) เพื่อปลด PHRASE_PATTERNS ก่อน port จอ list จริง

## 2026-07-12 · รอบที่ 4 · task: P0-WEB-02

- ทำอะไร: สร้าง route tree + route constants เต็มจาก `docs/extract/NAV-ROUTES.md` (P0-WEB-02). ไฟล์ใหม่ `src/routes/registry.ts` = route registry **เชิงโครงสร้างล้วน** (id/component/file/module gate/section — **ไม่มี display text**) ถอด 1:1 จาก NAV-ROUTES.md = **100 sidebar + 8 RouteView-only + 3 fin.* legacy redirect** · `parentOf()` คำนวณ parent จาก prefix ตรง PARENT_ID_OF_ROUTE (ไม่เก็บซ้ำ) · `src/router.tsx` (เขียนใหม่) สร้าง TanStack route tree จาก registry: `/`→redirect default `dashboard`, route ต่อ id (render `Placeholder` หลัง Phase-0 feature flag), fin.*→redirect route จริง · `src/feature-flags.ts` `isRouteEnabled()` default OFF ทุก route (override ผ่าน `VITE_FEATURE_ROUTES`/`VITE_FEATURE_ALL`) — ยังไม่มีจอ port จึงเป็น Placeholder ทั้งหมด · gate parity: `scripts/check-nav-parity.mjs` (dependency-free, `node --experimental-strip-types` — **ไม่เพิ่ม dep จึงไม่แตะ root pnpm-lock.yaml นอกเขต**) เทียบ registry ↔ NAV-ROUTES.md by id+component+file+parent · **ผล gates (route ตรง NAV-ROUTES 100% + CI lint/typecheck/build — โครงสร้าง non-UI, Placeholder หลัง flag → visual gate N/A จนกว่า P0-WEB-05):** `check-nav-parity` = **PASS 100+8+3 ตรงทั้งสองฝั่ง** · `pnpm turbo run typecheck lint` = **18/18 FULL TURBO** · `pnpm --filter @juneflow/web run build` = vite **170 modules ✓**. **ด่าน 4.5 diff-reviewer = PASS** (re-run parity เอง 100+8+3 + cross-check NAV-ROUTES ด้วยมือ 1:1 · sacred untouched · zone เฉพาะ apps/web · C7/C8/C10 ถูกต้อง · no redesign/re-translate) → commit `71f0b19` บน `feature/web` · TASKS.md P0-WEB-02 → `review` + แถว REVIEW-QUEUE
- ตัดสินใจอะไร: ไม่มีการตัดสิน design/spec เกินคำตัดสินที่มีอยู่. (1) **แยก label ออกจาก route constants** — registry เก็บเฉพาะโครงสร้าง (ไม่เก็บ Thai label) เพราะ (ก) i18n-guard hook + PLAN §0 ห้าม hardcode ไทยในไฟล์ UI และ (ข) menu label = nav i18n key ที่ shell (chrome.jsx) port ใน P0-WEB-05 จะ render ผ่าน t()/tn() — การยัด key ไทยตรงนี้คือ re-translate/เดา mapping จึงเลี่ยง · **C7** จึงแสดงเชิงโครงสร้าง = `boq.bom` เป็น route ชั้นหนึ่ง (แม้ ROUTE_LABELS ไม่มี) + boq.approval ผูก component ถูก; การเลือกข้อความ "อนุมัติ BOQ" (NAV side) = งาน render P0-WEB-05 (dict key `nav.boq.approval`) บันทึกใน comment · **C8** subcon.progress/contracts/subcon(alias)/subcon.accept/subcon.handover ทุกตัว `mod: "subcon"` · **C10** ไม่เก็บ badge เลข hardcode (มาจาก query จริงภายหลัง). (2) route path ใช้ id ดิบเป็น segment เดียว (`/boq.overview`) — prototype เป็น state-based (useState route) แต่ PLAN บังคับ TanStack Router; ทำ flat route ต่อ id เป็นโครงตั้งต้น ให้ P0-WEB-05 ปรับ nesting/shell. ไม่มี conflict → ไม่เปิด blocker
- เจออะไร: (1) i18n-guard hook (`.claude/hooks/i18n-guard.sh`) บล็อก **ตัวอักษรไทยทุกที่** ในไฟล์ `apps/web/src/**/*.ts|tsx` รวม comment (exit 2) — เขียน registry ครั้งแรกมี Thai ใน label+comment ถูกบล็อก 2 รอบ จนตัด Thai ออกหมด (label ย้ายไป i18n layer, comment เป็นอังกฤษตาม CLAUDE.md) · (2) ไม่มี remote (`git remote -v` ว่าง) → push/auto-merge เข้า dev รอ loop-runner เหมือนรอบก่อน (local-only) · (3) **คิว `ready` ที่หยิบได้จริงเขต web เหลือ 1 (P0-WEB-03) ต่ำกว่าเกณฑ์ ≥5** — WEB-05 ยัง block (dep WEB-02 เพิ่งเป็น review ยังไม่ done, WEB-03 ยัง ready), WEB-01/02/04/06 อยู่ review รอ Wei promote · เตือน Wei: promote review-queue + เติมคิว Phase 1 web เมื่อ MVP นิยามพร้อม (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP]) · (4) agent รอบถัดไป: P0-WEB-03 (i18n wiring) หยิบได้ — ระวัง PHRASE_PATTERNS ที่ต้อง port จาก prototype i18n code (ระบุใน packages/i18n TODO)

## 2026-07-12 · รอบที่ 3 · task: P0-WEB-06

- ทำอะไร: wire generated API client เข้า `apps/web` (P0-WEB-06). เพิ่ม `src/api-client.ts` = single `apiClient = createClient<paths>()` (openapi-fetch) โดย `paths` re-export จาก `@juneflow/contracts` (openapi-typescript gen จาก `packages/contracts/openapi.yaml`) → **type ของ request/response/param/query ทุกตัวมาจาก contract ล้วน ไม่มี model/fetch เขียนมือ** (PLAN §5 · apps/web/CLAUDE.md "API client") · `baseUrl` = `/api/v1` (servers url ของ contract, override ผ่าน `VITE_API_BASE_URL`) + `credentials:"include"` (better-auth session cookie) · `query-client.ts` เพิ่ม `unwrap` adapter แปลง `{ data, error }` ของ openapi-fetch → value/throw ที่ TanStack Query queryFn ต้องการ (normalise shape เท่านั้น — type ยังไหลจาก contract) · อัปเดต comment `main.tsx` (TODO→DONE WEB-06) · deps: `@juneflow/contracts` (workspace:*) + `openapi-fetch` ^0.17.0 (`pnpm install` +2 pkgs) · **ผล gates (CI: typecheck+build ด้วย generated types เท่านั้น — infra/wiring ไม่มี G1–G5, non-UI → visual gate N/A):** `pnpm turbo run typecheck lint` = **18/18 FULL TURBO** (contracts regenerate `paths` + web typecheck ✓) · `pnpm --filter @juneflow/web run build` = vite 168 modules ✓. **ด่าน 4.5 diff-reviewer = PASS** (reviewer รัน gates ซ้ำเอง 2/2 + build ✓ · sacred untouched · zone เฉพาะ apps/web+lockfile importer · "no hand-written models" CONFIRMED · ไม่แตะ C1–C10) → commit `70879b3` บน `feature/web` · TASKS.md P0-WEB-06 → `review` + แถว REVIEW-QUEUE
- ตัดสินใจอะไร: ไม่มีการตัดสิน design/spec. เลือก **openapi-fetch** (companion ทางการของ openapi-typescript ที่ gen types อยู่แล้ว) แทนการ hand-roll typed client — เพราะมันบริโภค `paths` type ตรงๆ จึงบังคับ "ห้ามเขียน model มือ" ได้เชิงกลไก 100% · `baseUrl` ยึด servers url ที่ contract ประกาศไว้ (ไม่เดา/ไม่ hardcode host) · `unwrap` เป็น shape-normaliser ล้วน (throw บน error) ไม่สร้าง model ใหม่ — cast `data as TData` อยู่หลัง error guard. ไม่มี conflict → ไม่เปิด blocker
- เจออะไร: (1) `@juneflow/contracts` เดิม **ไม่ได้เป็น dependency ของ apps/web** (P0-WEB-01 ใส่แค่ i18n/tokens/tanstack) — ต้องเพิ่ม `workspace:*` เอง · lockfile root +2 importer (contracts link + openapi-fetch) = แพตเทิร์นเดียวกับ P0-INT/P0-BE รอบก่อน (ยอมรับเชิงกลไก) · (2) quick-verify hook typecheck FAIL ทันทีหลังเขียน api-client.ts (module ยังไม่ resolve) → แก้ด้วย `pnpm install` แล้วเขียว — ปกติสำหรับการเพิ่ม dep · (3) คิว `ready` เขต web หลังรอบนี้: P0-WEB-02/03 ยังหยิบได้ (deps P0-WEB-01 done + BE-05 done) แต่ P0-WEB-05 ยัง block (dep 02/03 ยัง `ready` ไม่ `done`) — **คิว ready ที่หยิบได้จริงเหลือ 2 (WEB-02, WEB-03) ต่ำกว่าเกณฑ์ ≥5** → เตือน Wei: promote review-queue (WEB-01/04/06) + เติมคิว Phase 1 web เมื่อ MVP นิยามพร้อม (ห้าม agent สร้าง task ผูก MVP เอง — [TBD-MVP])

## 2026-07-12 · รอบที่ 2 · task: P0-WEB-04

- ทำอะไร: สร้าง `apps/web/docs/port-map.md` — บัญชี port ครบ **76/76** `.jsx` ที่ไม่ถูก exclude (78 ราก − `finance.jsx`/`tweaks-panel.jsx` ตาม PLAN §0 กฎ 5). โครงสร้าง: §1 จอ 46 ไฟล์ (route(s) + ภาพอ้างอิงต่อ route จาก reference-index) · §2 ไม่ใช่จอเดี่ยว 25 (shell/entry/shared/forms/viewer-embed/supporting/config/i18n-source) · §3 mobile-zone 5 (Phase 4 นอกเขต) · §4 route ต้อง cross-check (legacy redirect/badge-C10/parent-nav/gating) · §5 ความไม่ตรงกันของแหล่ง 5 จุด (D1–D5) บันทึกโปร่งใสไม่ตัดสินเอง · §6 สรุปความครบ. แหล่ง: NAV-ROUTES.md (คำตัดสิน route+ไฟล์) + INVENTORY.md §2 + reference-index.md (ภาพต่อ route). **ผล gates:** task นี้เป็น docs inventory ไม่มี G1–G5 (ไม่มีจอ/schema/contract) → gate = ครอบคลุมทุก .jsx ที่ไม่ถูก exclude: ตรวจโปรแกรม `ls pototype/*.jsx` − 2 exclude = 76 ↔ ชื่อไฟล์ในตาราง **76/76 ตรง (0 missing, 0 extra)** · §6 นับ 46+25+5=76 reconcile. visual gate ไม่ applicable (ไม่มีจอ). **ด่าน 4.5 diff-reviewer:** รอบแรก **FAIL** → แก้ → รอบสอง **PASS** → commit `7f6ac4b` บน `feature/web` · TASKS.md P0-WEB-04 → `review` + แถว REVIEW-QUEUE
- ตัดสินใจอะไร: ไม่ตัดสิน design/spec — คอลัมน์ route+ไฟล์ยึด **NAV-ROUTES.md เป็นแหล่งความจริง** (PLAN §0 ข้อ 2) เมื่อ reference-index.md (เอกสาร QA derived) attribute ต่าง → บันทึกเป็น D-note ไม่ silently เลือก. `design-canvas.jsx` (โหลด index.html แต่ไม่มี route) → ไม่ port จน Wei ยืนยันขอบเขต (D4 ไม่เดา). ไม่มี blocker (route มีคำตัดสินชัด — ไม่ใช่ conflict นอกตาราง)
- เจออะไร: (1) **ด่าน 4.5 รอบแรก FAIL** — จับได้ว่า `boq.archive`/`boq.reports` ผมผูกไป `boq-extra.jsx` ตาม reference-index (คอลัมน์ไฟล์) แทน NAV-ROUTES rows 27–28 ที่ระบุ `BOQArchive`/`BOQReports`@**boq.jsx** (RouteView@shell.jsx render จาก boq.jsx) → แก้: boq.jsx ถือครบ 5 route + boq-extra.jsx → `viewer-embed` (รายงานฝัง ไม่ใช่ route target) + เพิ่ม D5 + ปรับนับ §6 (screen 47→46, non-screen 24→25). **บทเรียน: reference-index.md คอลัมน์ 'ไฟล์' เป็น QA-derived ไม่ authoritative — route↔ไฟล์ ต้อง cross-check NAV-ROUTES เสมอ** (D1/D2 ก็มาจากเหตุเดียวกัน) · (2) คิว `ready` เขต web หลังรอบนี้: P0-WEB-02/03/06 ยัง block เพราะ dep P0-WEB-01 อยู่สถานะ `review` (ยังไม่ `done` — รอ Wei promote) · P0-WEB-04 → review · **คิว ready ที่หยิบได้จริงตอนนี้เหลือ 0** (WEB-05 dep 02/03, ทั้งคู่ block) — เตือน Wei: promote P0-WEB-01 เพื่อปลด WEB-02/03/06 มิฉะนั้นเขต web ตันจนกว่า dep ปลด

## 2026-07-12 · รอบที่ 1 · task: P0-WEB-01

- ทำอะไร: wire apps/web skeleton (เดิม scaffold มี package.json/vite/tsconfig/index.html + main.tsx render `PlaceholderShell` เฉยๆ — TanStack Router/Query เป็น dep แต่ยังไม่ต่อ) → เพิ่ม `src/router.tsx` (minimal TanStack Router: root route render `<Outlet/>` + index route render scaffold PlaceholderShell + `Register` module augmentation) + `src/query-client.ts` (single QueryClient) + rewire `src/main.tsx` mount `RouterProvider` ใน `QueryClientProvider` ใน `StrictMode` ใต้ `@juneflow/tokens/src/tokens.css` (fiori). **ผล gates (CI: lint+typecheck+build):** `npx turbo run lint typecheck build --filter=@juneflow/web` = 3 successful (typecheck ✓ · vite build 168 modules ✓ · web ไม่มี lint task → skip · tokens build upstream). visual gate ไม่ applicable รอบนี้ (skeleton ยังไม่มีจอจริง — จอเริ่ม P0-WEB-05). **ด่าน 4.5 diff-reviewer = PASS** → commit `3555f87` บน `feature/web` · TASKS.md P0-WEB-01 → `review` + เพิ่มแถว REVIEW-QUEUE
- ตัดสินใจอะไร: ไม่มีการตัดสินใจ design/spec — เก็บ router เป็น skeleton ขั้นต่ำ (root + index) ตามขอบเขต P0-WEB-01 เท่านั้น · full 44-route tree เป็นของ P0-WEB-02 · ไม่ hardcode ค่า design ใดๆ (grep hex/rgb/px = 0) · comment ทั้งหมดเป็นภาษาอังกฤษ (i18n-guard hook block Thai fragment `"อนุมัติ BOQ"` ที่ตกค้างใน comment เดิม → paraphrase เป็น `approve-BOQ label`)
- เจออะไร: (1) i18n-guard hook (`.claude/hooks/i18n-guard.sh`) บล็อกอักษรไทยแม้อยู่ใน code comment — comment ต้องอังกฤษล้วน · (2) `PlaceholderShell` เป็น scaffold-only (hardcoded English + ไม่มี token style) — diff-reviewer note #4: **ต้องไม่หลุดเข้า P0-WEB-03/05** ให้ port จริงจาก chrome.jsx/shell.jsx แทน · (3) ไม่มี git remote → push→auto-merge เป็นหน้าที่ loop-runner (เหมือน task backend/qa อื่น) · (4) รอบถัดไปในเขต web: P0-WEB-01 done แล้วจะปลด P0-WEB-02/03/06 (deps บน WEB-01) — ตอนนี้ P0-WEB-04 (port-map inventory, deps `—`) ยังหยิบได้เลย

## 2026-07-06 · bootstrap · task: —

- ทำอะไร: bootstrap ไฟล์สถานะ loop ตาม Manifest v2 กลุ่ม 3 · เขต web มี 6 task ใน `TASKS.md` (P0-WEB-01 ถึง P0-WEB-06) สถานะ `ready` — P0-WEB-04 (port-map inventory) เริ่มได้ทันทีโดยไม่รอเขตอื่น
- ตัดสินใจอะไร: ไม่มีการตัดสินใจเชิง design/spec — ความขัดแย้งถูกยกเข้า `BLOCKERS.md` (B-001, B-002)
- เจออะไร: เขตนี้ Design Fidelity เข้มสุด (PLAN.md §0 + กลุ่ม 2.2) — ทุกจอ port ตรงจาก `pototype/*.jsx` · token จาก `packages/tokens` ห้าม hardcode · ทุกข้อความ = key จาก i18n-full.json · task ส่วนใหญ่รอ P0-BE-01/04/05/12 จากเขต backend (ระบุใน dependencies แล้ว)
- 2026-07-12T02:02:15Z loop round ended (agent: web)

## 2026-07-12 09:02 · loop-runner · รอบที่ 1/10 · task: P0-WEB-01
- ทำอะไร: รัน claude headless 1 รอบ · task P0-WEB-01 → สถานะ review · ค่าใช้จ่ายรอบนี้ $2.8817955 (สะสม $2.8818/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T02:14:49Z loop round ended (agent: web)
- 2026-07-12T02:16:53Z loop round ended (agent: web)

## 2026-07-12 09:16 · loop-runner · รอบที่ 2/10 · task: P0-WEB-04
- ทำอะไร: รัน claude headless 1 รอบ · task P0-WEB-04 → สถานะ review · ค่าใช้จ่ายรอบนี้ $7.691627250000001 (สะสม $10.5734/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T02:17:27Z loop round ended (agent: web)

## 2026-07-12 09:17 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 3/10: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต web — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $11.1562/$20 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)
- 2026-07-12T02:40:48Z loop round ended (agent: web)

## 2026-07-12 09:40 · loop-runner · รอบที่ 1/10 · task: P0-WEB-06
- ทำอะไร: รัน claude headless 1 รอบ · task P0-WEB-06 → สถานะ review · ค่าใช้จ่ายรอบนี้ $3.1997295 (สะสม $3.1997/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T03:04:16Z loop round ended (agent: web)

## 2026-07-12 10:04 · loop-runner · รอบที่ 2/10 · task: P0-WEB-02
- ทำอะไร: รัน claude headless 1 รอบ · task P0-WEB-02 → สถานะ review · ค่าใช้จ่ายรอบนี้ $8.336677499999999 (สะสม $11.5364/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T03:19:05Z loop round ended (agent: web)

## 2026-07-12 10:19 · loop-runner · รอบที่ 3/10 · task: P0-WEB-03
- ทำอะไร: รัน claude headless 1 รอบ · task P0-WEB-03 → สถานะ review · ค่าใช้จ่ายรอบนี้ $6.691927249999998 (สะสม $18.2283/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T03:19:50Z loop round ended (agent: web)

## 2026-07-12 10:19 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 4/10: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต web — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $18.9609/$20 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)
- 2026-07-12T03:36:21Z loop round ended (agent: web)

## 2026-07-12 10:36 · loop-runner · รอบที่ 1/2 · task: P0-WEB-05
- ทำอะไร: รัน claude headless 1 รอบ · task P0-WEB-05 → สถานะ blocked · ค่าใช้จ่ายรอบนี้ $2.7548435000000002 (สะสม $2.7548/เพดาน $20)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-12T03:36:47Z loop round ended (agent: web)

## 2026-07-12 10:36 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 2/2: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต web — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $3.2861/$20 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)
- 2026-07-13T02:35:56Z loop round ended (agent: web)

## 2026-07-13 09:35 · loop-runner · รอบที่ 1/3 · task: P0-FIX-03
- ทำอะไร: รัน claude headless 1 รอบ · task P0-FIX-03 → สถานะ review · ค่าใช้จ่ายรอบนี้ $3.9044882500000004 (สะสม $3.9045/เพดาน $16)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-13T02:41:20Z loop round ended (agent: web)

## 2026-07-13 09:41 · loop-runner · รอบที่ 2/3 · task: P1-WEB-01
- ทำอะไร: รัน claude headless 1 รอบ · task P1-WEB-01 → สถานะ blocked · ค่าใช้จ่ายรอบนี้ $2.423447500000001 (สะสม $6.3279/เพดาน $16)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-13T02:41:56Z loop round ended (agent: web)

## 2026-07-13 09:41 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 3/3: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต web — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $6.9489/$16 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)
- 2026-07-13T05:08:59Z loop round ended (agent: web)

## 2026-07-13 12:09 · loop-runner · รอบที่ 1/3 · task: P1-WEB-01
- ทำอะไร: รัน claude headless 1 รอบ · task P1-WEB-01 → สถานะ blocked · ค่าใช้จ่ายรอบนี้ $9.158201499999999 (สะสม $9.1582/เพดาน $16)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-13T05:10:03Z loop round ended (agent: web)

## 2026-07-13 12:10 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 2/3: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต web — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $10.3608/$16 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)
- 2026-07-13T05:46:05Z loop round ended (agent: web)

## 2026-07-13 12:46 · loop-runner · รอบที่ 1/3 · task: P1-WEB-01
- ทำอะไร: รัน claude headless 1 รอบ · task P1-WEB-01 → สถานะ review · ค่าใช้จ่ายรอบนี้ $14.478051749999999 (สะสม $14.4781/เพดาน $16)
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: git progress: yes
- 2026-07-13T05:46:34Z loop round ended (agent: web)

## 2026-07-13 12:46 · loop-runner · คิวว่าง
- ทำอะไร: รอบที่ 2/3: ไม่มี task สถานะ ready ที่ dependencies ครบในเขต web — จบลูป
- ตัดสินใจอะไร: — (loop-runner เป็นกลไกอัตโนมัติ ไม่ตัดสินใจเชิง design/spec — ความขัดแย้งต้องเข้า BLOCKERS.md โดย agent ในรอบ)
- เจออะไร: งบสะสม $15.0562/$16 · เติมคิว ready ให้ครบ ≥ 5 task ต่อเขต (PLAN.md §10)

## 2026-07-13 18:15 · web execute · P0-WEB-05 app shell
- ทำอะไร: port app shell จาก chrome.jsx + shell.jsx → `apps/web/src/shell/` (18 ไฟล์) + `ui/{avatar,kbd,card,icon+full glyphs}`. Sidebar เต็ม (nav tree จาก registry+`nav-tree.json` · label ผ่าน tn/tp · 3 gate: viewMode/moduleOn/section) · TopBar (ProjectSwitcher·LanguageSwitcher·Notifications·Search) · UserMenu · Modal/Toast host · back-nav · Tweaks gear · ShellProvider (ctx แทน window.__juneflowCtx ผูก TanStack Router) · data จริง `/me`+`/projects` (generated client+Query) · badge = C10 (ไม่มี pill · ไม่มี count endpoint).
- ตัดสินใจอะไร: **แก้ B-036** — ใช้ tn/tp ใน web ได้โดยเก็บ Thai key ในไฟล์ data `.json` (i18n-guard ข้าม .json) ไม่มีไทยใน .tsx เลย (grep ยืนยัน 0). Thai string ที่ไม่มี key → ไฟล์ data + fallback (th ถูก · ตรง B-035). ความขัดแย้งนอกตารางคำตัดสิน → BLOCKERS B-039..B-042 (i18n keys·C10 count endpoint·/companies+project fields·token theme) ไม่เดา.
- เจออะไร: gates เขียวครบ — typecheck ok · vite build ok · 44 unit (24 เดิม + 20 ใหม่) · nav-parity 100% (100+8+3). smoke (playwright headless) shell mount ไม่มี error · sidebar 244px · nav 33 rows · energy section ซ่อน (realestate default) ตรง g1/01. G5 เต็ม (pixel vs gallery) ต้องมี live seeded stack (api `/me`+`/projects` + login) ซึ่ง compose api boot ยังรอ B-038 — sidebar chrome th ตรง reference จาก smoke.

## 2026-07-13 21:30 · web execute · P1-WEB-06 (P0-WEB-05 5b — shell live wiring + full G5)
- ทำอะไร: ต่อ 5a ให้ใช้ข้อมูลจริงครบ. regen generated client → (1) badges: `useBadgeCount`→GET /counts (9 keys batched · pill ซ่อนเมื่อ 0/absent · ไม่มี hardcode) (2) CompanySwitcher un-omit จาก GET /companies render ตรง company-accept.jsx:24-104 (project_count=0 ตาม B-046 แสดงตรงๆ) (3) ProjectSwitcher จาก Project ขยาย (short/color/units/phases[] sold_pct/sale_status ตาม chrome.jsx:452-601) (4) pkgMenuAllowed จาก /me.package.menus (dashboard+sub เสมอ · "*"=all) sidebar=viewMode+moduleOn+pkgOk (5) i18n consume real key — ถอด asNavKey/as PhraseKey casts (label:NavKey ที่ JSON boundary) · ตัด notifEmpty ตาม B-045 · th byte-identical (6) data-density comfortable global ที่ ShellProvider. ไฟล์: use-shell-data · company-switcher(ใหม่) · project-switcher · sidebar · topbar · notifications · chrome-i18n · nav-tree · chrome-strings.json · shell-context · tweaks-popover · badge-count + test.
- ตัดสินใจอะไร: ยึด precedent B-035/B-039 — string ใหม่ของ CompanySwitcher (3 ตัวไม่มี key) เก็บ verbatim Thai ใน chrome-strings.json → tp() → th ถูก · en/zh/ar fallback → escalate **B-047** (ไม่ block). **ไม่แก้ tests/visual/screens.manifest.json (เขต QA · นอกเขต web)** แม้ task สั่ง — เคารพ zone boundary (gate 4.5 ตรวจ) → ทำ G5 comparison เองแบบ read-only + ส่ง manifest entry ให้ QA ผ่าน B-048. G5 full-page pixel FAIL เชิงโครงสร้าง (body=Placeholder เพราะ dashboard.jsx จอแยกยังไม่ port) → **B-048** ถาม methodology gate จอ shell-only · ไม่แก้ reference · ไม่ผ่อน threshold.
- เจออะไร: gates เขียว — typecheck·vite build·**53 web unit** (เพิ่ม pkgMenuAllowed/resolveActiveCompany/packageMenus)·nav-parity 100%. G5 live compose (login สมชาย): shell render จริงจาก /me·/counts·/companies·/projects — **pkg-M gating เห็นผล (exec/labor/opex ซ่อน · accept/land/subcon แสดง)** · badge boq=1·accept=3 · switcher JF·RJP · footer สมชาย · dimension 1600x1000 ตรง ref g1/01 เป๊ะ · masked logo 12544 px (B-044) · Chrome verdict=PASS (human) · full-page diffRatio 0.829 (body un-ported). obs: GET /api/v1/notifications = 404 (backend ยังไม่ implement) — web handle graceful (empty list · ไม่ crash · ไม่มี empty-state ตาม B-045) ไม่ใช่บั๊ก web. no merge/push — stop ก่อน gate 4.5.

## 2026-07-14 · web loop · P1-WEB-07 (จอ Dashboard) — BLOCKED → B-049
- ทำอะไร: หยิบ P1-WEB-07 (route `dashboard` · ref g1/01 · default landing). อ่านครบ: `pototype/dashboard.jsx` (559 บรรทัด · read-only) + PLAN §0 + skill port-screen/visual-gate + apps/web/CLAUDE.md + port-map.md + NAV-ROUTES row + shell ที่ port แล้ว (app-shell/page/Placeholder). ตรวจ 2 แกน "ผ่านได้จริงไหม" ก่อนเขียนโค้ด: (1) data via generated client (2) ทุก string มี i18n key. ผล: **BLOCKED 2 แกนอิสระ → เปิด B-049 · ไม่ port · ไม่ commit code · ไม่ fake gate.**
- ตัดสินใจอะไร: ไม่เดา ไม่ประดิษฐ์ endpoint (openapi.yaml sacred · hook fail-closed) ไม่แปล/hardcode string ที่ไม่มี key (i18n-full.json sacred). การ port ด้วย mock data hardcode = ผิด §0 กฎ 3 (mock mechanics) · ไม่มี endpoint = ผิด task rule · ไม่มี key = ผิด §0 กฎ 2 · empty/loading-only จะ FAIL G5 vs g1/01 (จอเต็ม) อยู่ดี → escalate B-049 พร้อมรายการ endpoint ที่ขาด + Thai string ที่ขาด (exact) + prereqs. P1-WEB-07 ready→blocked.
- เจออะไร: **DATA:** dashboard = จอ aggregation — ต้องการ budget-vs-actual time series 4 ช่วง · KPI งบ/ใช้จริง/committed/คงเหลือ ต่อช่วง · cost breakdown (incl Overhead) · approval inbox รวม PR+PO+WO (17 · approver ชั้น 2) · phase built%/budgetUsed%/status · alerts&risk · health donut 71% · cashflow 7-วัน forecast · contractors งานกำลังเดิน+retention · activity feed. **ไม่มี endpoint** ใน openapi.yaml (มีแค่ /me·/counts·/projects[hierarchy unimpl · schema ไม่มี built/budgetUsed/status]·/audit-log·/gl/reports/cashflow[period GL report ≠ forecast]). **i18n:** 40/57 structural มี key (PHRASE เป็นหลัก) · 17 ขาด — default/realestate path (เห็นใน g1/01): `ผู้บริหาร`·`ช่าง/หน้างาน`·`งบ`·`Overhead`·`ล่าช้า`·`ใกล้กำหนด`·role banners 4 — type-branch (solar/civil/service): 13 ตัว (COD/WBS/Array/Performance Ratio/พลังงานสะสม ฯลฯ) — บวก B-017 dynamic number-bearing phrases (KPI subs/deltas/range labels/asof/alerts) ยังไม่ wire. **prereq:** charts.jsx wrapper (ChartCanvas/baseChartOpts) ยังไม่ port · P0-QA-08 (G5 shell/dashboard content-crop · B-048) ready ยังไม่ merge. gates: ไม่รัน (ไม่มี diff โค้ดของจอ — task blocked ก่อนเขียน). git: worktree clean · ไม่ commit · ไม่ merge.

## 2026-07-14 · web loop · P1-WEB-08 (จอ master.company) — BLOCKED → B-052
- ทำอะไร: sync `git merge dev` (HEAD 6f55a29 · B-014 envelope + company.* keys). หยิบ P1-WEB-08 (route `master.company` · ref g2/28). อ่านครบ: `pototype/master.jsx` MasterCompany L116-234 + OrgAddForm L20-114 (read-only) + PLAN §0 + skill port-screen + apps/web/CLAUDE.md + NAV-ROUTES L96 + port-map.md (master.jsx↔g2/28) + shell/ui/api-client/use-shell-data ที่ port แล้ว. ตรวจ 2 แกน "ผ่านได้จริงไหม" ก่อนเขียนโค้ด: (1) ทุก string มี i18n key (2) data via generated client. ผล: **BLOCKED 2 แกนอิสระ → เปิด B-052 · ไม่ port · ไม่ commit code · ไม่ fake gate.**
- ตัดสินใจอะไร: ไม่แปล/hardcode string ที่ไม่มี key (i18n-full.json sacred · §0 กฎ 2) · ไม่ประดิษฐ์ endpoint/restructure data (openapi.yaml sacred · §0 กฎ 3-4) · ไม่ตัด dept tree/CRUD เอง (= ตัดสินความขัดแย้ง §0 กฎ 1/4 → Wei ต้องระบุ). escalate B-052 พร้อม 30 exact missing string + ช่องว่าง contract + 3 ตัวเลือก (ตรง precedent B-050/B-051: contract patch + i18n patch แบบ B-039 → P1-BE-09 + P1-PLAT-02). P1-WEB-08 ready→blocked. ไม่รัน diff-reviewer · ไม่ merge.
- เจออะไร: **i18n:** ตรวจ 39 user-visible string → 9 มี key (`ข้อมูลกลาง`·`Company / Org`·`บริษัท`·`แก้ไข`·`ลบ`·`รหัส`·`รหัสนี้มีอยู่แล้ว`·`ยกเลิก`·`บันทึกการแก้ไข`) · **30 ขาด** (fuzzy-verify 0 hit): title `Company / Organization` · subtitle `โครงสร้างองค์กร · บริษัท · แผนก · ตำแหน่ง · เชื่อมกับ SAP B1` · ปุ่มหลัก `เพิ่มบริษัท / หน่วยงาน` · card head `โครงสร้างองค์กร` · `sync SAP เมื่อ 5 นาทีก่อน` · `แผนก` · `หน่วยย่อย` · `เพิ่มหน่วยย่อย` + OrgAddForm modal ทั้งชุด (subtitle×2·toggle×2·label×7·placeholder×5·error×4·ปุ่ม×2) + toast dynamic B-017. NB: keys ที่เพิ่งเพิ่ม (`company.info`/`company.pickTitle`/`company.taxLabel`) เป็นของ CompanySwitcher (B-047) ไม่ใช่ body ของจอนี้. **data:** g2/28 = org tree ซ้อนชั้น (3 บริษัท lvl0 + 7 แผนก lvl1/2 · code/note/parentCode/icon + collapse) + OrgAddForm CRUD จริง (add company/dept/sub-unit · edit · delete + validate + toast). schema `Company` (id/name/short/color/biz/tax_id/doc_prefix/project_count) = บริษัทในเครือ lvl0 เท่านั้น (B-041) · ไม่มี field org-tree · ไม่มี dept/org-unit row · ไม่มี POST/PATCH/DELETE → `GET /companies` render tree+CRUD ไม่ได้. gates: ไม่รัน (ไม่มี diff โค้ดของจอ — blocked ก่อนเขียน · typecheck/build/unit/G5 ไม่ applicable). G5: ไม่รัน (ไม่มีจอให้ screenshot). git: worktree clean · ไม่ commit · ไม่ merge.

## 2026-07-14 · web loop · P1-WEB-08 (จอ master.company) resume หลัง prep wave — BLOCKED (i18n residual) → B-054
- ทำอะไร: `git merge dev --no-edit` (HEAD 0d1384d+ · P1-BE-10 org-units CRUD + P1-PLAT-03 §7 org.* keys + B-014 envelope). อ่านครบก่อนเขียนโค้ด: `pototype/master.jsx` MasterCompany L116-234 + OrgAddForm L20-114 (read-only จาก main checkout) + `ds.jsx` Dropdown/Icon paths + PLAN §0 + skill port-screen + apps/web/CLAUDE.md + NAV-ROUTES L96 + port-map.md (g2/28) + ref image `tests/visual/reference/gallery/g2/28-s.jpg` + shell/ui(button/field/card/modal/icon)/api-client/query-client(unwrap)/use-shell-data(envelope→.data)/modal-host/shell-context/router/registry + contract `openapi.yaml /org-units` + `apps/api/src/routes/org-units.ts` + i18n-full.json (ทั้ง 2 สำเนา). ตรวจ 2 แกน "ผ่านได้จริงไหม": (1) data via generated client (2) ทุก string มี i18n key. ผล: **contract ปลดครบ ✓ · i18n เหลือ residual → BLOCKED → B-054 · ไม่ port · ไม่ commit code จอ · ไม่ fake gate.**
- ตัดสินใจอะไร: **แกน contract ปลดแล้ว** — P1-BE-10 ให้ `GET /org-units` (flat pre-order tree · Entity opaque `{id,parent_id,level,icon,name,code,note}` + envelope) + POST/PUT/DELETE พอ render tree ซ้อนชั้น + CRUD + collapse ครบ (ใช้ `id` เป็น key + parent ref แทน mock `code`/`parentCode` · `note`/`icon` เป็น data). **แต่ P1-PLAT-03 (§7 commit `8bccc0e`) เติม i18n ไม่ครบ B-052(A)** — verify exact-match ทั้ง 2 สำเนา: `แผนก`(card count · **เห็นใน g2/28**) + `sync SAP เมื่อ 5 นาทีก่อน`(**เห็นใน g2/28**) + `หน่วยย่อย` + toast dynamic B-017 (4) + note-composition (5) = **0 hit**. ตัด string ที่เห็น = ผิด §0 กฎ 1 (precedent B-049/B-052 "ตัด widget เอง = ผิด") · hardcode ไทย = ผิด §0 กฎ 2 + `i18n-guard.sh` block exit 2 (mechanical · สแกน `apps/web/src/*.tsx`) · ทั้งสองทาง FAIL G5 vs g2/28 + gate 4.5 → **ไม่มี compliant screen ให้ commit**. escalate **B-054** พร้อม exact missing Thai + suggested key names (th verbatim จาก prototype · en/zh/ar fallback ไทยแบบ B-039 · toast=B-017 phrase_patterns) — **ไม่ตัดสินเอง เสนอ 3 ตัวเลือกให้ Wei**. P1-WEB-08 doing→blocked. ไม่รัน diff-reviewer · ไม่ merge dev.
- เจออะไร: **i18n:** จาก 30 string ที่ B-052(A) ระบุ — §7 เติมแล้ว ~24 (page title/subtitle/breadcrumb + form ทั้งชุด field/ph/error/kind/button + kebab edit/addSub/delete + `บริษัท` phrase) ✓ · **เหลือ residual 6 กลุ่ม ไม่มี key**: (1) `แผนก` count unit [g2/28] (2) `sync SAP เมื่อ 5 นาทีก่อน` [g2/28] (3) `หน่วยย่อย` count unit [collapse] (4) toast `เพิ่มบริษัท/แผนก "{name}" ({code}) แล้ว`·`บันทึกการแก้ไข "{name}" แล้ว`·`ลบ "{name}" แล้ว` [B-017 dynamic · phrase_patterns มีแค่ 2] (5) note composition `บริษัทย่อย`·`เลขภาษี {taxId}`·`{N} คน`·`หัวหน้า: {head}`·`หน่วยงานใหม่` [client composes · route comment ยืนยัน]. NB: icon `trash` ไม่มีใน ds.jsx (grep=0 · Fiori.html=0) → prototype delete menu = blank icon (เหมือน `report`) — ไม่ใช่ blocker (ยืนยันความ faithful ไว้สำหรับรอบ port). **data:** org-units.ts คืน wire `{id,parent_id,level,icon,name,code,note}` pre-order · POST อ่าน `kind`/`name`/`code`/`parent_id`/`tax_id`/`note` (level compute server-side) · PUT partial merge · DELETE cascade subtree — พอทุกพฤติกรรม (mock single-level delete quirk = bug ตาม §6 · server cascade ถูก) · Dropdown(mode=select) ยังไม่ port ใน apps/web (ต้อง port รอบ resume). gates: ไม่รัน (ไม่มี diff โค้ดของจอ — blocked ก่อนเขียน · typecheck/build/unit N/A). G5: ไม่รัน (ไม่มีจอให้ screenshot). git: bookkeeping (BLOCKERS/TASKS/journal) commit บน feature/web · ไม่ commit code จอ · ไม่ merge dev · ไม่ diff-reviewer.

## 2026-07-14 · web loop · P1-WEB-08 (จอ master.company) — B-054 ปลดแล้ว → PORT ครบ → review
- ทำอะไร: `git merge dev --no-edit` (HEAD fde14e6 · §8/B-054 merged 11 residual dict key). port `MasterCompany`+`OrgAddForm` `pototype/master.jsx` L20-234 → `apps/web/src/screens/master/` 6 ไฟล์ (master-company.tsx · org-add-form.tsx · use-org-units.ts · org-tree.ts · org-tree.test.ts · org-strings.json) + register `router.tsx` `PORTED_SCREENS["master.company"]` + แก้ `topbar.tsx` breadcrumbs. รัน gates + **G5 live compose**. doing→review.
- ตัดสินใจอะไร:
  - **data = `GET /org-units` เดียว (fidelity-first)**: prototype ใช้ list เดียว lvl0-2 · count บริษัท=lvl0 / แผนก=lvl≥1 จาก tree เดียวกัน · **ไม่ wire `GET /companies`** (= affiliated switcher B-041 คนละ purpose · บังคับ fetch = ประดิษฐ์ data flow ที่ prototype ไม่มี · switcher ใช้ /companies ใน use-shell-data อยู่แล้ว). mutation POST/PUT/DELETE → `invalidateQueries(["org-units"])` (server = system of record แทน ORG_SEED local state · insert/splice → server pre-order · parent ผูก `id` แทน mock `code`).
  - **phrase `บริษัท`**: `i18n-guard.sh` block ไทยใน .tsx/.ts **ทุกที่** (ไม่มี exception ให้ tp()) → เก็บ key ไทยใน `org-strings.json` (guard skip .json) + boundary cast PhraseKey (ตรง pattern `shell/chrome-strings.json`). ที่เหลือ = dict key ASCII ผ่าน t() ปลอดภัย. NB: guard ใน worktree no-op เพราะ REPO_ROOT=main checkout (path ไม่ตรง) — แต่ยึดกฎ "comment/code English + string ผ่าน key" เอง (CI guard จะจับ).
  - **breadcrumbs NavKey[]→ReactNode[]**: prototype crumb = ["ข้อมูลกลาง"(dict `master.breadcrumb`) , "Company / Org"(nav)] — mix dict+nav ที่ TopBar เดิม (NavKey[]+tn() ชั้นเดียว) แสดงไม่ได้ → เปลี่ยน contract เป็น ReactNode[] pre-resolved (caller resolve t()/tn() เอง · ตรง ds.jsx Page display-string · RTL chevron คงไว้). กระทบแค่ Page/Placeholder(ไม่ส่ง breadcrumbs)+จอนี้ = จอแรกที่ใช้ breadcrumb จริง.
  - **note client-composed** (route comment ยืนยัน): compose จาก org.noteSubCompany/noteHeadPrefix/noteNewUnit/noteCountUnit + company.taxLabel → ส่งใน body. **add-sub ไม่ใส่ head → note=preset.note=undefined** (reproduce prototype quirk ตรงๆ · §0 กฎ 4). **modal header add-sub**: prototype truthiness → "แก้ไข undefined" (bug) → key ที่ `preset?.id` (real edit) ให้ add-sub แสดง add-header ตรงกับ form ที่ treat เป็น add (comment ในโค้ด · ไม่ silent-improve · ไม่ block · ไม่ใช่ missing key → ไม่เข้า BLOCKERS).
  - **icon `trash`**: ds.jsx ไม่มี glyph → render blank svg 13px (reproduce prototype `paths["trash"]||null` · §0 ห้าม invent). **Dropdown parent** = native `<select>` styled (fieldStyle) แทน ds.jsx Dropdown popover (modal ไม่มี ref g2/28 · full popover = shared primitive แยก task).
  - **toasts** = dict template interpolate `t("org.toastAdd*"/"toastEdit"/"toastDelete").replace("{name}",…).replace("{code}",…)` ใน mutation onSuccess.
- เจออะไร: **CI:** typecheck ✓ · vite build 217 modules ✓ · **63 web unit** (+10 `org-tree.test.ts`: toOrgNode/hasChildren/childCount/orgCounts/visibleRows-collapse) ✓. **G5 live**: host 5432 ชน `krs-pos-db` (project อื่น · ไม่แตะ) → `POSTGRES_PORT=5433` (internal `postgres:5432` ไม่กระทบ) · **api image ต้อง `--build`** (stale `juneflow-api:dev` เดิมไม่มี org-units route → 404 · rebuild แล้วผ่าน) · migrate-seed ผ่านหลัง retry (transient DNS/port) · **web container/vite เดิม ไม่ proxy /api** (P0-WEB-01 TODO · nginx SPA-only) → รัน `vite dev` + temp `/api`→:3000 proxy **(revert แล้ว · ไม่ commit)** · login somchai/juneflow-dev. shot 1600x1000 `/master.company` ↔ ref `gallery/g2/28-s.jpg` → **body PASS**: card "โครงสร้างองค์กร · 3 บริษัท · 7 แผนก" ตรงเป๊ะ · 10 rows nested (lvl0 brand-soft+border/lvl1 surface-2/lvl2 transparent · icon+name+code-badge+note+kebab) · breadcrumb "ข้อมูลกลาง › Company / Org" · action button · **modal OrgAddForm render ตรง prototype** (toggle+fields+placeholder+buttons). region diff (TOL48) whole 5.01% / content(x≥244,y≥56) 4.11% / org-card 4.00% = **shell chrome + seed data** (topbar back-strip อยู่ใน ref capture ไม่ใน ported shell → offset ~44px uniform ghost · C10 badge + pkg-menu(exec) live query · switchers vs back-button) · **§0 อนุญาต "ต่างได้: ตัวเลขข้อมูล seed"** · body identical. API verify: `GET /org-units` = 3 บริษัท/7 แผนก ตรง seed. git: commit บน feature/web · **ไม่ merge · ไม่ diff-reviewer** (orchestrator) · stack down หลังจบ.

## 2026-07-15 · web loop · P1-WEB-09 (จอ master.project) — B-057 ปลด → PORT ครบ → review
- ทำอะไร: `git merge dev --no-edit` (HEAD 446f179 · B-057 residual 6 dict key + fcc163a wave-release). port `MasterProject` L311-433 + `BlockAddForm` L247-309 `pototype/master.jsx` → `apps/web/src/screens/master/` (master-project.tsx · block-add-form.tsx · use-project-hierarchy.ts · project-blocks.ts · project-blocks.test.ts 14 · project-strings.json) + register `router.tsx` `PORTED_SCREENS["master.project"]`. รัน gates + **G5 live compose** (visual-gate-runner). doing→review.
- ตัดสินใจอะไร:
  - **data = real endpoints ทั้งหมด (§5 · ไม่ fake)**: `GET /projects`(envelope)+resolveActiveProject (default RJP realestate/somchai) · `GET /project-types`=hierarchy WBS labels H (data ไม่ใช่ i18n) · **`GET /projects/{id}/hierarchy`=blocks/units real counts** (P1-BE-10 impl แล้วจริง — orchestrator task-note "hierarchy ยังไม่ impl" = STALE · verify route+seed ก่อน wire · ไม่ยื่น false blocker) · `GET /models`=block `model_id`→`{code}({type})`+color join (§0 กฎ 3 FK ไม่ใช่ name-string) · `POST /projects/{id}/nodes`=add block (opaque Entity body · invalidate query).
  - **B-057 residual (6 visible string ไม่มี key)**: filed → orchestrator apply sacred i18n บน dev (446f179 · prototype-verbatim L301/323/329/341/346/357 · placeholder ตรง .replace() contract · `block.infoLine` เก็บ curly “ว่าง” U+201C/D ตรง prototype · both copies byte-identical). code อ้าง key พวกนี้ล่วงหน้าแล้ว → typecheck+G5 เขียวหลัง apply · ไม่แก้ web code เพิ่ม. **ไม่ hardcode ไทย · ไม่ตัดสินเอง · ไม่ fake gate** (แนว B-054).
  - **B-058 defer CreateProjectForm**: ปุ่ม "สร้างโครงการใหม่" (`project.createBtn` มี key) คงอยู่ (G5 layout) แต่ onClick wizard defer — CreateProjectForm L1242 อยู่นอกช่วง L311-433 (เหมือน ModelAddForm=WEB-10) **+ `POST /projects` ยังไม่ impl** ใน projects.ts → wire ไม่ได้จริง · เสนอ task แยก (backend POST /projects + createProj.* composed keys). ไม่ประดิษฐ์ endpoint/flow.
  - **unit-grid 14-col threshold algo** = verbatim จาก prototype (project-blocks.ts `unitStatus` · แยก pure logic + test 14) · phrase `เพิ่ม`/`ยูนิต`/`ก่อสร้าง` ผ่าน project-strings.json (tp · guard skip .json) · `#fff` cell text literal (B-037 · prototype hardcode) · ScopePill popover shadow literal = ตรง master-company.tsx sibling (gate-4.5 ผ่านแล้ว · ds.jsx Dropdown hardcode rgba · §0 กฎ 3 exception).
- เจออะไร: **CI:** typecheck 0 error (6 B-057 key resolve หลัง merge) ✓ · vite build 222 modules ✓ · **77 web unit** (+14 project-blocks: toBlocks/modelsById/blockTotals/unitStatus boundary/builtPct zero-guard/unitCode/typeHierarchy/phaseHead) ✓. **G5 live vs g2/32 = PASS** (visual-gate-runner · B-048/P0-QA-08 content-region): title/subtitle+WBS chain/scope pills/summary line/phase·block header/block card/unit grid 14-col/actions/breadcrumb ตรงหมด · **B-057 2 key เห็นในภาพ render ไทยจริง** (`โครงสร้าง`·`ก่อสร้างเสร็จ` ไม่ใช่ key-name) · deltas = seed-only (84/57/54 vs ref 192/95/108 · block model label · phase 1 vs 2) + pre-adjudicated shell chrome (B-047 company chip · B-044 logo · B-048 topbar ~45px offset · stale g2/32 capture). API verify: hierarchy = Block B(green/84)/C/D ตรง seed. **gate-4.5 = VERDICT PASS** (sacred/zone/fidelity line-by-line/C1-C10/test 14/B-058 defer sound · 3 note non-block: journal+B-057 status lag [ปิดใน commit นี้] + builtPct round≈toFixed). git: commit บน feature/web · **ไม่ merge เอง · orchestrator merge** · stack down หลังจบ.

## 2026-07-15 · web loop · P1-WEB-13 (จอ master.model) — B-063 keys พร้อม → PORT ครบ → review
- ทำอะไร: หยิบ **P1-WEB-13** (feature/web @ dev `b9c6d8b` มี B-063 3 key แล้ว · clean). port `MasterModel` L507-578 + `ModelAddForm` L434-505 `pototype/master.jsx` → `apps/web/src/screens/master/` (`master-model.tsx` · `model-add-form.tsx` · `model-cards.ts` · `model-cards.test.ts` 9 · `model-strings.json`) + register `router.tsx` `PORTED_SCREENS["master.model"]`. 3-col card grid read-only + add-modal. รัน gates (typecheck/build/vitest). doing→review. **ไม่ merge — orchestrator รัน G5/gate-4.5.**
- ตัดสินใจอะไร:
  - **data = real endpoints ทั้งหมด (§5 · ไม่ fake)**: `GET /models`(envelope `.data` B-014) → `toModelCard` narrow opaque Entity `{id,code,type,area,bed,bath,parking,price,currency_code,status,color,unit_count,bom_item_count}` · `POST /models` create — compose body ใน screen: **price = form-millions ×1e6 = FULL baht** + `currency_code:'THB'` · **ไม่ส่ง color/status/count** (server assign color + status='draft' — B-050) · invalidate `['models']` (reuse key เดียวกับ `useModels()` ใน use-project-hierarchy.ts · ไม่สร้าง divergent key). `useCreateModel` = thin POST+invalidate (mirror `useCreateOrgUnit`/`useCreateProjectNode`).
  - **mock mechanics dropped (§0 กฎ 3)**: BOM cell (L563) mock `248 + i*30` = **FORBIDDEN → อ่าน real `bom_item_count`** (`hasBom(c)=bom_item_count>0` · else "—") · bottom count = real `unit_count` (ไม่ใช่ `m.count` mock) · client `MODEL_COLORS` array (L449) DROP (server assign color).
  - **edit-button (L569) = render-only dead button**: prototype ไม่มี onClick → `<Btn kind="ghost" size="sm" icon="edit" />` ไม่ wire · **ไม่มี PATCH /models** (B-050 = GET+POST เท่านั้น) → ไม่ประดิษฐ์ endpoint (§0 กฎ 4). BOM-btn + add-toast = `ctx.notify` เท่านั้น.
  - **price semantics**: server เก็บ FULL baht · prototype card โชว์ millions (8.24) → `formatModelPrice(baht)=(baht/1e6).toFixed(2)` คืน **bare number string** (guard NaN/neg→"0.00") · suffix "M ฿" มาจาก `t('model.priceUnit')` ใน view (ไม่มี ฿/Thai ใน .ts) · POST คูณกลับ ×1e6.
  - **i18n key ล้วน**: dict model.* + common.cancel · **B-063 keys** `parkingLabel`/`priceUnit`/`toastAdd` (มีบน dev แล้ว) · tn navTitle "Model / แบบบ้าน" + tp phrase ใช้งาน/ร่าง/ตร.ม./รายการ/ยูนิต ผ่าน `model-strings.json` (i18n-guard skip .json) · **exact-reuse** `model.fieldBed`/`fieldBath` (form field labels) เป็นคำ card spec line + `model.parkingLabel` (B-057 precedent · flag ใน header comment) · house `<svg>` L537-539 verbatim (ds.jsx ไม่มี house glyph) · `#fff` badge bg + boxShadow + gradient alpha suffix 22/08 = literal B-037 · server color hex ตรง (borderTop/gradient/stroke/price/badge) เหมือน master-project.tsx · 0 hardcode ไทย.
- เจออะไร: **CI (agent):** typecheck 0 ✓ · vite build 227 modules ✓ · **86 web unit** (+9 model-cards: formatModelPrice 8.24/0.00/NaN/neg/inf · toModelCard narrow+missing-defaults+camelCase-fallback+numeric-string · hasBom · statusActive) ✓. **G5 + gate-4.5 = pending-orch** (ref `g2/33-s.jpg`). **เจอ+แก้:** i18n-guard hook สแกน Thai **ทั้งไฟล์รวม comment** (ไม่ยกเว้น comment · CLAUDE.md "comment=อังกฤษ") — Write รอบแรกมี Thai ตัวอย่างใน doc-comment 3 ไฟล์ (master-model/model-add-form/model-cards) → grep U+0E00-0E7F เจอ → เปลี่ยนเป็น English gloss + "M baht"/"baht symbol" ทั้งหมด → re-scan clean · gates re-run เขียว. sibling convention = Thai อยู่ใน .json/.test.ts เท่านั้น. git: commit บน feature/web · **ไม่ merge เอง · orchestrator merge**.

## 2026-07-15 · web loop · P1-WEB-10 (จอ master.ptype) — §9 i18n done + B-065 write-defer → PORT ครบ → review
- ทำอะไร: หยิบ **P1-WEB-10** (feature/web @ dev `2dbc4e9` · B-062/§9 i18n merged · B-065 filed · clean). port `MasterProjectType` L15-108 + `MODULE_LABELS` L5-12 `pototype/project-type-screen.jsx` → `apps/web/src/screens/master/` (`master-project-type.tsx` · `ptype-cards.ts` · `ptype-cards.test.ts` 9 · `ptype-strings.json` · `ptype-meta.json`) + register `router.tsx` `PORTED_SCREENS["master.ptype"]`. read-only 2-col type-card grid (4 การ์ด). รัน gates (typecheck/build/vitest). doing→review. **ไม่ merge — orchestrator รัน G5/gate-4.5.**
- ตัดสินใจอะไร:
  - **data = real endpoints (§5 · ไม่ fake · ไม่มี hook ใหม่)**: reuse `useProjectTypes()` (`GET /project-types` envelope `.data` B-014) → `toTypeCard` narrow opaque row `{id,key,name,hierarchy:string[],modules:string[]}` (server `modules` = string[] จาก seed · ไม่ใช่ `{k:1}` map ของ prototype) · reuse `useProjects()` (shell/use-shell-data) → footer `projectsByType(projects, card.key)` filter `p.type===card.key` (mock `window.useProjects` L17 + local `useState(PROJECT_TYPE_LIST())` L16 DROPPED §0 กฎ 3). ไม่สร้าง hook/endpoint ใหม่.
  - **module order = FIXED ALL_MODULES (16 · verbatim MODULE_LABELS key order)**: `enabledModules(modules)=ALL_MODULES.filter(m=>set.has(m))` — server `modules[]` วาง `pm` **กลาง** array (realestate seed: …petty, **pm**, sales_re…) แต่ g2/29 render **PM สุดท้าย** → iterate ALL_MODULES ∩ modules ให้ PM ท้ายเสมอ (ตรง prototype L46 `ALL_MODULES.filter(m=>t.modules[m])`). test ยืนยัน pm-last จาก realestate seed order.
  - **i18n split 7 DICT / 9 PHRASE (§9 zero-residual · ไม่เพิ่ม key · ไม่มี sacred round)**: ทุก chrome string resolve ครบ — planning pass verify แล้วว่า 0 missing. `moduleLabel(key)`: key∈MOD_DICT(7) → `t(ptype.mod.*)` (procure/timeline/salesRe/aftersales/lineoa/om/pm) · else → `tp(ptype-strings.json.mod[key])` (9 phrase: land/boq/subcon/inv/petty/ppa/roi/permit/warranty · Thai text = phrase key · guard skip .json). 7+9=16 partition ยืนยันใน test. navCrumb `ประเภทโครงการ (Project Type)`=nav key(tn) · title/subtitle/addBtn/secWbs/secCostTypes/modUsed/projUsage/noProjects=dict(t) · common.edit(t). **projUsage split `{n}`/`{names}`** — count render bold+num var(--text) (prototype `<b className="num">`), project names raw. **DATA render raw** (NOT i18n): row.name/row.hierarchy[]/meta nameEn·desc·costTypes[]/project names (แนวเดียว master-project render row.name raw).
  - **`ptype-meta.json` = 5 field ที่ server omit** (nameEn/desc/icon/color/costTypes) transcribe **VERBATIM** จาก `project-types.jsx` L6-51 keyed by `key` (realestate/solar/civil/service). server /project-types คืนแค่ {id,key,name,hierarchy,modules} → presentation config (สี/icon/costTypes/นามอังกฤษ/คำอธิบาย) = product-level ไม่ใช่ tenant data → เก็บ client-side (Thai อยู่ใน .json · guard skip). icon = ds.jsx glyph name (building/sun/hardhat/briefcase — มีครบ ไม่ต้อง blank-svg). defensive `FALLBACK_META` (token color · unreachable สำหรับ 4 fixed type).
  - **B-037 tokens-only exception**: per-type `color` hex จาก meta ใช้ borderTop 3px · icon-box `color-mix(14% white)` · **cost-tag inline `color-mix(13% white)`+color** (render inline ตรง ds.jsx Tag · **ไม่มี ui/Tag primitive** ในโปรเจกต์) — literal สี = verbatim prototype + ไม่มี token ตรง (precedent master-model gradient-alpha / master-project #fff · cite ใน header+comment). อื่น ๆ var(--) ล้วน (surface-2/border/text-*/ok-soft/ok/r-lg).
  - **WRITE-PATH DEFERRED (B-065)**: header add-btn (`+ เพิ่มประเภทโครงการ` `ptype.addBtn`) + per-card edit-btn (`แก้ไข` `common.edit`) **RENDER** (อยู่ใน g2/29) แต่ **NO onClick = render-only stub** — ไม่ port `ProjectTypeForm` L114-218 · ไม่ wire POST/PUT /project-types (platform-global write · backend unimplemented). mirror B-058 (master-project createBtn present/deferred) + B-050 (model edit render-only). flag defer+B-065 ใน header comment. ไม่ประดิษฐ์ endpoint/flow (§0 กฎ 4).
- เจออะไร: **CI (agent):** typecheck 0 ✓ (real exit 0 · JSON import `with {type:"json"}` sibling pattern · meta cast `Record<string,TypeMeta|string|undefined>` เลี่ยง `_source` string ชน TypeMeta · icon cast IconName at usage) · vite build 237 modules ✓ · **113 web unit (12 files)** (+9 ptype-cards: ALL_MODULES 16-order+pm-last · MOD_DICT 7/9 partition assert · toTypeCard narrow+defaults+drop-non-string-array · enabledModules realestate-seed→pm-last + filter unknown/absent + service 5-mod · projectsByType filter) ✓. **G5 + gate-4.5 = pending-orch** (ref `tests/visual/reference/gallery/g2/29-s.jpg` · 2-col grid 4 การ์ด). verify: route id `master.ptype` มีใน registry.ts:173 + NAV-ROUTES L97 (component MasterProjectType · nav crumb ตรง). i18n verify (python): 9 phrase keys ∈ phrases layer · navCrumb ∈ nav_i18n · 17 dict keys ∈ dict. git: commit บน feature/web · **ไม่ merge เอง · orchestrator merge**.

## 2026-07-15 · web loop · P1-WEB-11 (จอ master.cc) — B-068 keys พร้อม → PORT ครบ (create-wired) → review
- ทำอะไร: หยิบ **P1-WEB-11** (feature/web @ dev `afd6db4` · B-068 3 residual key [cc.thCode/cc.fldType/cc.errCodeDup] merged แล้ว · clean). port `MasterCC` L666-731 + `CCAddForm` L603-663 `pototype/master.jsx` → `apps/web/src/screens/master/` (`master-cc.tsx` · `cc-add-form.tsx` · `cc-rows.ts` · `cc-rows.test.ts` 9 · `use-cost-centers.ts` · `cc-strings.json`) + register `router.tsx` `PORTED_SCREENS["master.cc"]`. full-width table read-only + add-modal. รัน gates (typecheck/build/vitest). doing→review. **ไม่ merge — orchestrator รัน G5/gate-4.5.**
- ตัดสินใจอะไร:
  - **data = real endpoints (§5 · ไม่ fake)**: `GET /cost-centers`(envelope `.data` B-014) → `toCostCenterRow` narrow opaque Entity `{id,code,name,type,link,owner,budget,currency_code,status}` (snake+camel dual-read มิเรอร์ model-cards) · **`POST /cost-centers` CREATE-WIRED จริง** — mock `setRows((r)=>[cc,...r])` + `window.__ccExtra` (L675-676) DROPPED §0 กฎ 3 → `useCreateCostCenter` (POST + invalidate `['cost-centers']`, mirror `useCreateModel`). CCAddForm emits `CcDraft`, master-cc composes opaque body.
  - **budget = FULL baht (ไม่ ×1e6)**: `Number((draft.budget||'0').replace(/,/g,''))` — CC budget เก็บ full baht ทั้ง prototype (seed 84_400_000) + server. ×1e6 เป็น **models-only** scaling (WEB-13) — **ไม่ผูกข้ามจอ**. table cell = `formatMoney(row.budget)` (comma-group · no decimals · **no ฿ symbol** — ฿ อยู่ใน `cc.thBudget` header key เท่านั้น · mirror role-matrix.ts/ds.jsx fmt). **NO "M ฿" suffix**.
  - **project_id = โครงการ active (B-059)**: `resolveActiveProject(useProjects().data, ctx.tweaks.project)` (shell/use-shell-data · มิเรอร์ master-project). POST 400 ถ้าไม่มี project_id → **add-btn `disabled={!active}`** + `openAdd` guard `if(!active) return` (TS narrows `active` เป็น Project ใน closure). **ไม่ส่ง status** (server force 'draft') · **ไม่ส่ง currency_code** (server default THB). dup-code client check = UX pre-check (existingCodes=loaded codes · precedent model `existingCodes`); server 409 DUPLICATE_CODE authoritative.
  - **NO edit column (§0 fidelity)**: prototype table = 7 data cells ล้วน (code/name/type/link/owner/budget/status) — **ไม่มี edit-btn/edit column** ต่างจาก master-model (card grid มี dead edit-btn). ไม่ประดิษฐ์ปุ่ม. type-select = native `<select>` (org-add-form precedent · ds.jsx Dropdown popover ไม่ port · modal ไม่มี G5 ref).
  - **status badge inline (ไม่มี shared StatusBadge ใน web)**: reproduce ds.jsx `<StatusBadge size="sm">` (L93) inline — tokened bg/fg (ok-soft/ok approved · draft-soft/draft fallback ตรง `STATUS[status]||STATUS.draft`) + **dot 6px hex `#16A34A`/`#94A3B8` verbatim** (B-037(a) · STATUS map hardcode dot · ไม่มี @juneflow/tokens ตรง · **ref g2/34 โชว์ dot เขียวชัด** = G5 truth · dot คือส่วนของ StatusBadge ที่ plan สั่ง inline). label = `approved ? tp(อนุมัติแล้ว) : tp(ร่าง)`. type-pill = brand-soft/warn-soft/surface-3 (typeBadgeTone · label = raw server `type` = data ไม่ใช่ i18n). Avatar `#0F766E` verbatim (B-037(a) · Avatar default).
  - **code cell right-align — base.css gap**: prototype `styles.css:149 td.num,th.num{text-align:right}` ทำให้ code column (className="num") ชิดขวาใน ref · web `base.css .num` **ละ rule นี้ไว้** (คอมเมนต์เขียน "Right-aligned inside tables" แต่ rule ไม่มี text-align). blast-radius check: **ไม่มี `<td className="num">`/`<th className="num">`** ในจอ web ที่ merged แล้ว (grep = 0) → เพื่ออยู่ในขอบเขตไฟล์ของ plan (ไม่แตะ shared base.css) ทำ `textAlign:"right"` **inline บน code `<td>` จอนี้** (มิเรอร์ pattern budget cell ที่ prototype ใส่ inline อยู่แล้ว) — ตรง ref โดยไม่แตะไฟล์นอก plan. budget th/td = inline `textAlign:right` verbatim prototype.
  - **empty/loading state**: skeleton loading = token blocks (mirror master-company) · empty-state = empty tbody (mirror master-model empty grid · **ไม่ invent copy** — ไม่มี cc.* key สำหรับ empty message). th()/td() style helper port verbatim ds.jsx L214-220 (local ในจอ · ไม่มี shared).
  - **i18n key ล้วน**: header cc.thCode/fldName/fldType/thLink/thOwner/thBudget + common.status (t) · form cc.fldCode/phCode/errCodeRequired/fldType/optProject/optOverhead/optDept/fldName/phName/errNameRequired/fldLink/phLink/fldOwner/phOwner/fldBudget/errBudgetNumber/errCodeDup + common.cancel (t) · modal cc.modalTitle/modalSubtitle · title/subtitle/addBtn cc.title/subtitle/addBtn (t) · toast cc.toastAdd.replace('{code}') · tn navTitle `Cost Center`(nav_i18n) + tp statusDraft `ร่าง`/statusApproved `อนุมัติแล้ว` ผ่าน `cc-strings.json` (i18n-guard skip .json) · **B-068 3 key resolve แล้ว** (planning verify · grep-confirm ทุก key ∈ dict/nav_i18n/phrases) · 0 hardcode ไทย (code+comment English · link/owner default `—` em-dash = language-neutral · precedent org-add-form/role-matrix).
- เจออะไร: **CI (agent):** typecheck 0 ✓ · vite build **242 modules** ✓ · **web unit 122/122 (13 files)** (+9 cc-rows: toCostCenterRow narrow+defaults+camelCase+numeric-string · typeBadgeTone Project/Overhead/Dept/other · statusTone approved/draft/pending-fallback · formatMoney 1e6→"1,000,000"/84.4M/800k/0/round/neg/NaN/Inf) ✓. **G5 + gate-4.5 = pending-orch** (ref `tests/visual/reference/gallery/g2/34-s.jpg` · loaded table 7 rows · no edit column). verify: route id `master.cc` ∈ registry.ts:178 + NAV-ROUTES L102 (component MasterCC) · contract `/cost-centers` GET(EntityList)+POST(Entity→EntityCreated) openapi.yaml:847. git: commit บน feature/web · **ไม่ merge เอง · orchestrator merge**.

## 2026-07-16 20:45 · qa (orch-B) · LIVE-G5 QA PASS — 10 ported screens · 9 PASS / 1 FAIL
- ทำอะไร: live compose (POSTGRES_PORT=5433 · rebuild stale api) + vite dev + Playwright 1600x1000 เทียบ gallery จริง (pixel · B-048 content-region) บนจอ merged ทั้งหมด (qa worktree synced dev 8361a93) — ปิด owed live-G5 debt (ก่อนหน้า structural-G5 เท่านั้น)
- ผล: **9 PASS live-G5 debt CLEARED** = boq.list(g1/08) · master.company(g2/28) · project(g2/32) · ptype(g2/29) · cc(g2/34) · model(g2/33) · docnum(g2/35) · vendor(g2/30) · users(g2/36) — deltas = seed data + shell chrome adjudicated (B-044/047/048) · pixel-sample ยืนยัน token mapping ตรง (project tile --info/--accent · navy)
- **1 FAIL: gr.list(g1/18)** = data-wire ไม่ครบ (GET /gr subset) → B-074 (orch-A/backend zone · ไม่แก้ = QA zone)
- flags: GET /api/v1/notifications 404 (bell · backend gap) · derived-field em-dash (boq ผู้รับผิดชอบ/อัปเดต · model BOM/ยูนิต · vendor ยอดซื้อ = honest-empty backend ไม่คืน)
- git: qa worktree clean · stack down · ไม่แตะ screen code · ไม่ commit code

## 2026-07-16 21:34 · qa (orch-B) · LIVE-G5 QA ROUND 2 — 5 procurement screens · 0 regression · systemic data-wire finding
- ทำอะไร: live compose (5433) + pixel เทียบ gallery จริง บน 5 จอ merged ใหม่ (qa worktree synced dev) — boq.overview(g1/07) · boq.editor(g1/11) · po.list(g1/16) · pr.list(g1/15) · wo.list(g1/17)
- ผล: **0 PASS · 5 FAIL(data-wire) · 0 FAIL(regression)** — ports orch-A สะอาดเชิงโครงสร้างทุกจอ (layout/token/label/column-order/position ตรง ref หมด) · FAIL = honest em-dash จาก field ที่ backend ไม่คืน (แบบ gr.list/B-074)
- **SYSTEMIC insight (key): root cause = SCHEMA-level ไม่ใช่ wire-projection.** route wire คืน column จริงของ table ครบแล้ว — field ที่ ref ต้องการ = **column/line-table ที่ไม่มีใน schema** → extend GET เฉยๆ คืน null · ต้อง add column/table + seed ก่อน. per-endpoint gap:
  - GET /pr: ขาด requester/title/phase/doc_date/budget%/approval-timestamps/urgent (pr_item SEEDED ✓ · amount จริง · gap = doc-level columns)
  - GET /po: **ไม่มี po_item line-table** + ขาด deposit/paid/GR%/doc_date/installment-schedule/closed
  - GET /wo: **ไม่มี wo installment table** (งวดงาน) + ขาด scope/progress/doc_date/variation/warranty (retention wired ✓)
  - GET /gr (B-074): **ไม่มี gr_item + money column** + vendor ไม่ join + ขาด date/ordered-qty/status-label
  - GET /boq/items: ขาด `detail` column (รายละเอียด) · waterfall GR-bar inherit gr money gap
  - recommendation: **1 backend task** = migration (add columns + po_item/wo-installment/gr_item tables + boq_item.detail) + seed FLOW-A + extend GET → re-run live-G5 → 6 จอ PASS. **hand เข้า orch-A data-completeness packet** (ไม่ file blocker ซ้ำ · B-074/075 มีแล้ว)
- **pr.list DESIGN flag (แยกจาก data-wire):** ref g1/15 = 4 filter pills (โครงการ/ช่วงเวลา/ประเภท/วงเงิน) · port = 2 (โครงการ/ประเภท · pr-list.tsx L451-452 ตัด period/amount เป็น decorative). period ไม่มี wire source (data-wire) แต่ **วงเงิน/amount อยู่บน wire จริง** → droppable-but-implementable · §0 กฎ 1 (visual 100%) vs กฎ 3 (ไม่ลอก mock) → Wei adjudicate (เข้า packet)
- git: qa worktree clean · stack down · ไม่แตะ screen code

## 2026-07-16 22:25 · qa (orch-B) · LIVE-G5 QA ROUND 3 — 5 boq-flow screens · 0 regression
- ทำอะไร: live compose (5433) + pixel เทียบ gallery จริง บน 5 จอ boq-flow (qa worktree synced dev 1a5b4bf) — boq.bom(g1/10) · boq.approval(g1/12) · boq.archive(g1/13) · boq.reports(g1/14) · boq.aiqto(g1/09)
- ผล: **1 PASS · 4 FAIL(data-wire) · 0 FAIL(regression)** — boq.aiqto PASS (static demo) · ที่เหลือ faithful โครงสร้างครบ (column/token/label/stepper/tabs ตรง ref) แต่ em-dash จาก backend gap
- **cumulative round 1-3 = ~20 จอ Phase-2 · 0 regression เลย** → ports orch-A สะอาดเชิงโครงสร้างทุกจอ · verify lane ปิดรอบ wave นี้
- **gaps ใหม่เข้า FLOW-A packet:**
  - boq.bom → **MISSING ENDPOINT + line-table**: /models คืน bom_item_count scalar เท่านั้น (B-1=17) · ไม่มี endpoint คืน bom line rows → table + 4 KPI (ต้นทุน/หลัง·Mat/Sub/Lab%) em-dash. ต้อง GET /models/:id/bom-items + aggregates
  - boq.approval → **MISSING ENDPOINT (version-diff read model)**: POST approve wired แต่ read side ไม่มี (line diff ค่าเดิม/ค่าใหม่ · เพิ่ม/ลด counts · net-diff · prev-version total) · seed มีแค่ 1 pending v1/ใหม่ (ไม่มี prior version) → feature demonstrate ไม่ได้. ต้อง version-diff endpoint + seed v2+ pending
  - boq.archive → **MISSING COLUMNS + line-table**: GET /boq ขาด last-approver/approved-at/attachment-count/revise-count → em-dash · + ไม่มี revise-history (v1→vN timeline row-expand). มูลค่า=0 (boq_item sparse)
  - boq.reports → **WHOLE MISSING BACKEND FEATURE**: ไม่ call reports endpoint เลย (structural shell + honest empty per orch-A) · ต้อง reports-aggregation service (RPT-001 BOQ-vs-NonBOQ · RPT-002 revise-history · RPT-003 cost-type breakdown)
- **methodology gotcha (บันทึกไว้ live-G5 รอบหน้า):** launch vite ด้วย `VITE_API_BASE_URL=` (empty) ไม่ override client `?? "/api/v1"` → base "" → fetch miss proxy = false empty. ต้อง temp proxy `/api`→:3000 + ไม่ set env เปล่า
- git: qa worktree clean · stack down · ไม่แตะ screen code

## 2026-07-17 01:25 · qa (orch-B) · LIVE-G5 ROUND 4 (real-data) + B-084 mutation-authz matrix
- **round 4:** 5 PASS · 8 FAIL(data-wire) · 0 regression (12 FLOW-A + dashboard). **KEY: backend data-completeness สำเร็จ (API คืน field จริง verify curl) แต่ web ไม่ re-wire** → 8 จอ (gr/po/pr/wo.list · boq.list/bom/archive/reports) ยัง hardcode DASH → web re-wire task (orch-A · rows opaque Entity ไม่ต้องแก้ contract). PASS: boq.overview/editor/approval/aiqto + **dashboard real body**. B-083 = acceptable (approver=user:1 วิภา Director-role · ชื่อต่างจาก mock = honest). dashboard manifest unmask done.
- **B-084 mutation-authz matrix** (`agents/orch-b-recon/b084-mutation-authz-matrix.md`): 27 mutation → 7 gated · 12 status-only · **8 UNGATED**. CRITICAL = POST /po/:id/variation-order (po.ts:416 · no authz+no status · tier-downgrade approval-bypass weaponize ladder · survives F1) · HIGH generate-pr · models/cc ไม่ gate master.create. Wei-gated (authz model ruling) · report+exploit only.
- git: unmask merged dev · reports committed · ไม่แตะ screen code

## 2026-07-17 01:42 · qa (orch-B) · CONTRACT-LIVE expansion (Lane 1) + drift-sweep gate (Lane 2)
- Lane 1: tests/contract/live.spec.ts +14 READ endpoints schema-validated (getPo/getWo/getBoq/getPr + /projects/id/hierarchy + /models/id/bom + 7 dashboard + notifications) ALL PASS · /projects/id skip(404 declared-not-mounted)
- Lane 2: tests/contract/drift.spec.ts NEW static gate — mounted-but-undeclared=**0 (B-086 class CLEAN)** · 105 declared-but-unmounted warned(scaffolding backlog · informational) · parser-floor guard กัน vacuous-pass
- verified: static 452/84 · live 535/1 · 0 schema-drift FAIL (FLOW-A read surface contract-clean) · merged dev
- durable: contract regression gate โตตามทุก backend wave · B-086 failure-class ปิดถาวร

## 2026-07-17 11:47 · qa (orch-B) · PROCUREMENT E2E (Lane 5) + B-084 exploit-closure verify
- tests/e2e/procurement-flow.spec.ts + b084-exploit.spec.ts + _api-client.ts (gated E2E_LIVE · default 2/6-skip green)
- Test 1 money-path 4/4 PASS: PR(1.2M · ผจก.tier)→submit→pending · lower-tier(proc L2)=403 · PM(L3)=approved step2 · PO(1.2M)→approve · partial GR(received/partial/PO-open)
- **Test 2 B-084 exploit = BLOCKED (fix confirmed independent):** low-tier cut 7.36M PO = **403 ที่ mutation แรก** (require max(tier(cur),tier(new)) authority) · low-tier approve >5M = 403 · negative-floor=400 · amend-pending=409 · 0 variation recorded = exploit ปิดจริง end-to-end
- merged dev · durable: money-path regression guard + B-084 exploit-closure permanent test

## 2026-07-17 13:36 · qa (orch-B) · PROJECT-STATUS.md durable snapshot
- agents/orch-b-recon/PROJECT-STATUS.md — full project-wide status (5 audit-lane synth): board 130 tasks · 26/100 screens · 84/186 endpoints · 28 migrations · ~173 function register (BE/FE per item) · tech report (security/perf/dep/i18n/a11y) · deferred/roadmap
- interactive artifact: claude.ai/code/artifact/9cdd1315 (Wei-requested)
- read-only reference · orch-B zone · no board touch

## 2026-07-17 16:37 · qa (orch-B) · batch-8 verify references committed + 2 findings captured
- committed 12 orch-b-recon refs (4ca6ffa): B-095 fix-spec/handoff/reaudit · type-stripping-scan · live-G5 runners(finance+batch8)+readiness+verdict+harness-notes · promote-batch8-runbook · batch8-audit · b084 matrix +P2-BE-20
- gitignore *-results/ (transient diff PNGs not committed)
- finding 1: live-G5 harness gaps documented (worktree-install · storageState-auth · per-screen-viewport for g5 924x540 · lossy-jpg threshold · manifest self-check drift) → harness-notes.md
- finding 2: P2-BE-20 adds 2 ungated mutations (import + reconcile/period-lock) → b084 matrix updated (reconcile = finance-mgr gate candidate)
