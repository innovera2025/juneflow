# ORCH-D overnight brief — mobile lane — 2026-08-03 night

## Remaining inventory

- **MOB-SCREEN-INBOX (inbox · กล่องอนุมัติ) — approval entry list + summary chips** — Approval-group priority per B-240, but backend-blocked — file the /me/approvals blocker first act, skip. Unblocks the inbox→detail→approve/reject nav chain once orch-A ships it.
  - spec: pototype/mobile.jsx:24-178 (MobileApprovalInbox); apps/mobile/docs/screen-map.md §3 approval; BLOCKERS.md B-240 (inbox.* keys minted)
  - dep: BLOCKED on orch-A: GET /me/approvals + summary counts (no unified pending-MY-approval endpoint). i18n READY (batch-1).
- **MOB-SCREEN-DETAIL (detail · รายละเอียดเอกสาร) — PR detail + budget bar + approval timeline** — Source of the amount/PR-id echo that approve/reject render as route args. File blocker with inbox, skip.
  - spec: pototype/mobile.jsx:179-392 (MobileApprovalDetail); agents/orch-b-recon/mob-recon-synth.json (blockedList); BLOCKERS.md B-240 (detail.* keys minted)
  - dep: BLOCKED on orch-A: GET /pr/{id} + items + approval-chain + attachments + budget-progress (boq_api exposes only list). i18n READY (batch-1).
- **MOB-SCREEN-F2-RECEIVE (st-receive · ตรวจนับ+รับของ GR)** — Next after the PM vertical; the store GR vertical F1→F2. Needs a field/store i18n batch slice. Pull from queue tail if time remains.
  - spec: pototype/mobile-field.jsx:37-92 (MStReceive); apps/mobile/lib/api/generated/clients/boq_api.dart:186-189 (createGr)
  - dep: READY now (BoqApi.createGr exists) — offline-write, enqueue seam like E2. money=SERVER downstream (GR auto-posts AP).
- **MOB-SCREEN-B1-FIELDPROGRESS (field-progress · บันทึก Progress งวดงาน)** — mobile-screens field group; needs an i18n batch slice.
  - spec: pototype/mobile-screens.jsx:316-363 (MFieldProgress)
  - dep: READY now (period-deliver read/write path exists) — distinct from F3 daily-% which is backend-blocked.
- **MOB-SCREEN-B2-FIELDGR (field-gr · ตรวจรับงาน / GR + QC)** — Overlaps F2 receive semantics; recon which endpoint each drives before wiring.
  - spec: pototype/mobile-screens.jsx:364-428 (MFieldGR)
  - dep: READY now (createGr + QC checklist) — offline-write enqueue seam.
- **MOB-SCREEN-B3-QUICKPR (field-pr · สร้าง PR ด่วน)** — Needs BOQ-item picker recon + i18n batch slice.
  - spec: pototype/mobile-screens.jsx:429-483 (MFieldQuickPR)
  - dep: READY now (BoqApi.createPr) — offline-write enqueue seam. money=NONE at PR.
- **MOB-SCREEN-C1-LABORCHECKIN (field-checkin · GPS Check-in + งานวันนี้)** — Distinct from E2 pm-checkin; confirm the labor attendance endpoint + i18n batch.
  - spec: pototype/mobile-screens.jsx:534-583 (MFieldCheckin); agents/orch-b-recon/mob-recon-synth.json (C1 = /labor/attendance NOT /pm)
  - dep: READY now — wire to /labor/attendance (NOT /pm/workorders). Offline-write enqueue seam.
- **MOB-SCREEN-A1/A2/A3/A4 (after-sales: srv-new / srv-track / tech-jobs / tech-close)** — A4 also has a minor fidelity gap (before/after photos+materials+signature not first-class in close body) → Wei ruling. Warranty after-sales (SR-xxxx) — do NOT wire to /pm.
  - spec: pototype/mobile-screens.jsx:61/125/198/263; agents/orch-b-recon/mob-recon-synth.json (REGEN group)
  - dep: REGEN-ONLY: POST/GET /sales/service + SV-3 action machine EXIST but land_sales_api.dart is stale (Jul-14). A single Dart client regen unblocks all 4 (zero backend).
- **MOB-SCREEN-B4-FIELDSTOCK (field-stock · เบิก/คืนวัสดุ + scan)** — Confirm เบิก vs คืน op semantics at build. Offline-write enqueue seam.
  - spec: pototype/mobile-screens.jsx:484-533 (MFieldStock); migrations 0040/0041 (/inventory/*)
  - dep: REGEN new client: /inventory/items|stock|issues|transfers EXIST but there is NO inventory_api.dart — generate one. QR/barcode = device camera (mobile_scanner), not an API.
- **MOB-SCREEN-C2-HSE (field-hse · รายงานความปลอดภัย)** — The only genuinely greenfield-backend mobile screen. File the domain blocker.
  - spec: pototype/mobile-screens.jsx:584-646 (MFieldHSE)
  - dep: BLOCKED on orch-A (greenfield domain): no hse/safety/incident path in openapi and no client. Needs contract op + backend + schema + notify hook, THEN regen.
- **MOB-SCREEN-D1-EXEC (exec · Executive Dashboard)** — Hero gradient + KPI grid + custom-painter S-curve (token colors). Larger port; do after the ready waves.
  - spec: pototype/mobile-screens.jsx:647-721 (MExecDashboard); /dashboard/* incl RPT-005 EVM S-curve
  - dep: REGEN new client: /dashboard/summary|cashflow-forecast|approvals-inbox|phase-progress|budget-actual|alerts|contractors EXIST but there is NO dashboard Dart client — generate one. S-curve → /dashboard/budget-actual; approve cards reuse boq/finance approve ops.
- **Offline DEPTH wiring (SyncProcessor drain trigger / conflict / retry policy)** — Spine + enqueue + optimistic-success ship without it; only the drain is gated. Do NOT decide the level in the loop.
  - spec: apps/mobile/lib/offline/sync_processor.dart (stub); PLAN.md §11.5 Open Q#5; BLOCKERS.md B-242
  - dep: BLOCKED on Wei B-242 (ก)/(ข) depth ruling — due at the first offline-WRITE screen (E2).

## Work queue (ordered)

### #1 MOB-SHELL-00 — App shell foundation — i18n bootstrap + 5-tab MTabBar (B-241 honest-disable) + MobileScreenRouter scaffold + shared primitives + Dio/auth wiring  
- **scope:** Replace the P0 skeleton lib/main.dart with the real app shell. Load JuneflowI18n.load() at startup, set MaterialApp locale + Directionality from i18n.dir() (never hardcode). Build the shared primitives ONCE as reusable lib/widgets/: MobileHeader (mobile.jsx:7-22), MTabBar (mobile-screens.jsx:3-26 — 5 canonical tabs อนุมัติ/หน้างาน/แจ้งซ่อม/Dashboard/โปรไฟล์ per B-241; tabs with no destination render honest-DISABLED, never a fabricated screen; badge is a live query, never the mock 17), MSection/MField/MInput/MPill (mobile-screens.jsx:28/35/44/50). Scaffold lib/router MobileScreenRouter (mobile-preview.jsx:109-145 — 26 route ids; register only built screens, rest honest-disabled). Wire a single Dio + JuneflowApiClient with an auth interceptor fed a DEV bearer via --dart-define (no hardcoded secret); file the canonical mobile auth-entry blocker and proceed. Do NOT rebuild the offline queue — the P0-MOB-05 spine already exists; only expose a provider to it.
- **spec:** pototype/mobile.jsx:7-22 (MobileHeader) · pototype/mobile-screens.jsx:3-26 (MTabBar), 28/35/44/50 (MSection/MField/MInput/MPill) · pototype/mobile-preview.jsx:3-44 (MOBILE_GROUPS), 109-145 (MobileScreenRouter) · apps/mobile/docs/screen-map.md §4 (shared primitives) + §8 D3 (tab-bar) · BLOCKERS.md B-241 (5-tab MTabBar + honest-disable, RULED ก) · apps/mobile/lib/i18n/juneflow_i18n.dart + screen_strings.dart (foundation, landed) · apps/mobile/lib/api/generated/juneflow_api_client.dart · agents/orch-b-recon/mob-recon-synth.json (no-login-screen note)
- **dependsOn:** None blocking — all prereqs landed: MOB-I18N-01 runtime + B-240 batch-1 asset (dev/main), P0-MOB-05 queue spine, P0-MOB-03 Dart clients, B-241 ruling. File (do not stall on) the mobile auth-entry blocker (dev token proceeds).
- **gates:** flutter analyze (clean) + flutter test (green, keep 68/68 i18n suite) + i18n-guard (zero Thai in lib/**.dart) + asset-drift-test (green) + gate-4.5 (orch-B diff-reviewer). mobile-G5 = N/A (no single prototype screen; MTabBar/MobileHeader fidelity first validated at MOB-SCREEN-E1/NOTIF).
- **handoff:** push feature/mobile-shell + channel note C-436 to orch-B (verify+merge). Same note asks orch-A to build GET /me/approvals+summary (inbox) and GET /pr/{id}+items+approval-chain+attachments+budget (detail) so those unblock overnight.

### #2 MOB-REF-01 — Capture the 26 Fiori.html mobile reference images — prerequisite for the mobile visual gate (G5)  
- **scope:** Mobile has NO per-screen reference (screen-map §6/D2: only host g2/45-s.jpg exists). Open pototype/Juneflow Fiori.html → Mobile Approval, walk the sidebar (MOBILE_GROUPS 7 groups/26 screens), screenshot each at the 390×844 iOS viewport, one PNG per screen id. Prioritize the 5 approval refs (inbox/detail/approve/reject/notif) first so the approval ports can pass G5, then the field/pm/store/exec groups. QA-asset task only — writes NO lib/**.dart. Do not fabricate a shot for a screen you cannot render in Fiori.html; note any gap honestly.
- **spec:** apps/mobile/docs/screen-map.md §6 (visual gate Phase 4) + §3 (26-screen id list) · pototype/Juneflow Fiori.html (Mobile Approval menu — real behavior) · pototype/mobile-preview.jsx:3-44 (MOBILE_GROUPS — screen ids/labels) · tests/visual/reference-index.md (confirms g2/45-s.jpg is the only existing mobile ref) · PLAN.md §0 (visual gate) + §9 (G5)
- **dependsOn:** None (Fiori.html present). Independent of MOB-SHELL-00 — can run first/in parallel. Its output is a dependsOn for the G5 gate of every screen task below.
- **gates:** capture-integrity: exactly one PNG per screen id at 390×844, faithful to the Fiori.html screen (no crop/scale drift, no fabricated screens) + orch-B acceptance. No flutter/code gates.
- **handoff:** push feature/mobile-ref01 (raw PNGs under a mobile-scoped ref staging dir) + channel note to orch-B, who owns tests/visual/reference/ and places them in the canonical mobile ref path. Flag any screen with no faithful Fiori.html source.

### #3 MOB-SCREEN-NOTIF — notif (แจ้งเตือน) — notifications list + inline quick-approve  💰money
- **scope:** Port MobileNotifications (mobile.jsx:543-609): MobileHeader + 4 filter pills + ListView notif cards; kind==='approval' cards get inline อนุมัติ/ดู buttons. Reads DmsApi.listNotifications (filter→query param); mark-read = DmsApi.readNotification (no bulk endpoint — per-id loop or honest-disable the header อ่าน). RECON-FIRST: GET /notifications returns opaque Entity — recon the real response shape (backend handler + packages/db notification schema) before mapping title/by/body/time/kind; render only server-served fields, honest-empty the rest (no per-row mock colors, relative time only from a real created_at). Inline approve targets PR only via BoqApi.approvePr — recon each approval notif's real target doc-type first; do NOT blind-POST /pr/{id}/approve for a BOQ notif.
- **spec:** pototype/mobile.jsx:543-609 (MobileNotifications), 7-22 (MobileHeader) · agents/orch-b-recon/mob-notif-accel.md (full widget/client/i18n/honest-gap map) · apps/mobile/lib/api/generated/clients/dms_api.dart:63/70 (listNotifications/readNotification) · apps/mobile/lib/api/generated/clients/boq_api.dart:133 (approvePr) · BLOCKERS.md B-240 batch-1 (notif.markRead/filterUpdates minted)
- **dependsOn:** MOB-SHELL-00, MOB-REF-01. i18n: batch-1 covers notif.markRead/filterUpdates; RECON the residual (title ของฉัน, sub แจ้งเตือน, tabs ทั้งหมด/รออนุมัติ/ระบบ, ดู, อนุมัติ) — cross-domain reuse forbidden, so any gap → draft mob-i18n-batch-2.apply.json for orch-B. Backend recon of /notifications shape.
- **gates:** flutter analyze + flutter test + i18n-guard (zero-Thai) + asset-drift-green + gate-4.5 + mobile-G5 vs MOB-REF-01 notif ref.
- **handoff:** push feature/mobile-notif + channel note to orch-B (verify+merge). If any i18n gap found → also hand mob-i18n-batch-2.apply.json (keys-only, byte-exact) to orch-B for the sacred apply.

### #4 MOB-SCREEN-APPROVE — approve (ยืนยันอนุมัติ) — PR approve action sheet  💰money
- **scope:** Port MobileApproveSheet (mobile.jsx:393-458): full-height confirm sheet — green check disc, dynamic confirm sentence (PR id + ฿ amount arrive as route args from detail, money=SERVER — never hardcode/recompute), honest-empty 'ส่งต่อไปยัง' next-approver card (no chain endpoint exists), optional note textarea, ยกเลิก/ยืนยันอนุมัติ buttons. Single call BoqApi.approvePr(id:) — path-only, no body. HONEST GAPS (§0): the note textarea has NO wire (approve carries no comment body, unlike reject) — render for fidelity but do not fake-send; file the approve-comment-body blocker. Keep PR-scoped (no PO/WO approve endpoint).
- **spec:** pototype/mobile.jsx:393-458 (MobileApproveSheet) · agents/orch-b-recon/mob-approve-accel.md (widget/client/token/honest-gap map) · apps/mobile/lib/api/generated/clients/boq_api.dart:132-135 (approvePr, path-only) · BLOCKERS.md B-240 batch-1 (mob.approval.approve.* all minted)
- **dependsOn:** MOB-SHELL-00, MOB-REF-01, B-240 batch-1 (approve.* keys applied — no new i18n). Online-required: do NOT queue a money-mutation offline (accel §4) — reference Open Q#5/B-242 in a code comment, no drain. File approve-comment-body blocker for orch-A/Wei.
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 (money-skeptic: path-id-only, no client amount math) + mobile-G5 vs approve ref.
- **handoff:** push feature/mobile-approve + channel note to orch-B (verify+merge). No apply.json (i18n complete).

### #5 MOB-SCREEN-REJECT — reject (ปฏิเสธ + เหตุผล) — PR reject action sheet  💰money
- **scope:** Port MobileRejectSheet (mobile.jsx:460-542): warn banner + 5 radio reasons (state) + mandatory detail textarea + ยกเลิก/ส่งกลับให้แก้ไข. Call BoqApi.rejectPr(id:, body: PrIdRejectRequestBody(reason)) — reject DOES carry a {reason} body (contrast approve). money=SERVER (state transition, no client amount). PR-scoped. Dynamic PR id echo from route args.
- **spec:** pototype/mobile.jsx:460-542 (MobileRejectSheet) · agents/orch-b-recon/mob-approve-accel.md §2 (rejectPr contrast note) · apps/mobile/lib/api/generated/clients/boq_api.dart (rejectPr + PrIdRejectRequestBody) · BLOCKERS.md B-240 batch-1 (mob.approval.reject.* — 5 reasons+placeholder+submit minted)
- **dependsOn:** MOB-SHELL-00, MOB-REF-01, B-240 batch-1 (reject.* keys applied — no new i18n). Online-required like approve.
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs reject ref.
- **handoff:** push feature/mobile-reject + channel note to orch-B (verify+merge).

### #6 MOB-SCREEN-E1-PMJOBS — E1 · pm-jobs (งาน PM ของฉัน) — read list + first MTabBar consumer  
- **scope:** Port MPMJobs (mobile-pm.jsx:7-49): header + 3 client-side filter pills (วันนี้/ด่วน/ทั้งหมด over the returned list, no refetch) + tappable PM/CM job cards (tap → pm-checkin) + MTabBar active='field'. One read call PmApi.listPmWorkorders() → GetPmWorkordersResponse{data:List<Entity>}. money=NONE. RECON opaque Entity keys the server actually returns; omit fields it can't serve (dist/GPS almost certainly absent → drop the compass chip, never hardcode 0 กม.); tab counts = live list lengths not literals.
- **spec:** pototype/mobile-pm.jsx:7-49 (MPMJobs) · agents/orch-b-recon/mob-e1-accel.md (widget/client/theme/i18n-gap map) · apps/mobile/lib/api/generated/clients/pm_api.dart:82-86 (listPmWorkorders) · apps/mobile/docs/screen-map.md §3 pm-eng group
- **dependsOn:** MOB-SHELL-00, MOB-REF-01. i18n: E1 keys (งาน PM ของฉัน, วันนี้/ด่วน/ทั้งหมด, PM/แจ้งซ่อม pills, รอเริ่ม/กำลังทำ/เกินกำหนด, เริ่มงาน) are NOT in batch-1 and cross-domain reuse is forbidden → draft mob-i18n-batch-2.apply.json (slice from mob-i18n-gap.md). Backend recon of /pm/workorders fields.
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs E1 ref. (First real MTabBar G5.)
- **handoff:** push feature/mobile-e1 + mob-i18n-batch-2.apply.json → channel note to orch-B (apply sacred i18n + regen asset, then verify+merge).

### #7 MOB-SCREEN-F1-GRLIST — F1 · st-grlist (สโตร์ · รอรับของ PO) — read list, gateway to the GR flow  
- **scope:** Port MStGRList (mobile-field.jsx:6-35): pure navigation list — header + PO cards (no/vendor/items/due/urgent/warehouse) tap → st-receive + MTabBar active='field'. One read BoqApi.listPo(filter:) → GetPoResponse. money=NONE. RECON the exact pending-GR filter token from the GET /po handler before hardcoding (no generated constant → BLOCKER if unknown, don't guess). Opaque Entity — bind only keys /po actually returns; drop the ด่วน pill unless /po serves an urgency flag; delete the 3 mock POs.
- **spec:** pototype/mobile-field.jsx:6-35 (MStGRList) · agents/orch-b-recon/mob-f1-accel.md (client/filter-recon/i18n-gap map) · apps/mobile/lib/api/generated/clients/boq_api.dart:149-153 (listPo) + get_po_response.dart · apps/mobile/docs/screen-map.md §3 storefm group
- **dependsOn:** MOB-SHELL-00, MOB-REF-01. i18n: static labels รอรับของ (PO)/ตรวจนับ-รับของ/คลัง Block B have no key on recon → add to mob-i18n-batch-2.apply.json. Backend recon of /po pending-GR filter token (BLOCKER if unknown).
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs F1 ref.
- **handoff:** push feature/mobile-f1 + fold F1 keys into mob-i18n-batch-2.apply.json → channel note to orch-B.

### #8 MOB-SCREEN-D2-SALESCRM — D2 · sales-crm (Sales CRM Mobile) — pipeline read  
- **scope:** Port MSalesCRM (mobile-screens.jsx:722-780): horizontal pipeline stage chips (Lead/นัดชม/จอง/สัญญา/โอน) + lead cards (hot/warm) + 3 action buttons (โทร/LINE/นัดชม) + MTabBar active='exec'. Read the sales leads list (GetSalesLeadsResponse model exists) — RECON which client method/endpoint serves it (land_sales/sales) and the opaque Entity fields; render honest-empty for anything absent. Call/LINE/appointment actions are device intents, not API calls — wire honestly. money=NONE.
- **spec:** pototype/mobile-screens.jsx:722-780 (MSalesCRM) · agents/orch-b-recon/mob-recon-synth.json (D2 = READY, screens-misc group) · apps/mobile/lib/api/generated/models/get_sales_leads_response.dart · apps/mobile/docs/screen-map.md §3 exec group
- **dependsOn:** MOB-SHELL-00, MOB-REF-01. RECON the sales-leads client method + confirm the Dart client isn't stale (land_sales_api.dart was stale Jul-14 → if the leads read is absent, this becomes a REGEN dependency — file it, move on). i18n: sales/CRM keys → mob-i18n-batch-3.apply.json.
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs D2 ref.
- **handoff:** push feature/mobile-d2 + mob-i18n-batch-3.apply.json → channel note to orch-B. If leads read is stale-client → REGEN blocker instead, skip to next.

### #9 MOB-SCREEN-E2-CHECKIN — E2 · pm-checkin (เช็คอินจุดบริการ GPS) — the offline-write KEYSTONE  
- **scope:** Port MPMCheckin (mobile-pm.jsx:52-89): faux map card (keep decorative shell; coordinate line = real device GPS or honest-empty, never the mock 18 ม.) + info MSection (เขตบริการ/SLA/สัญญา honest-empty or from nav arg) + 2-phase CTA (เช็คอินหน้างาน GPS → success banner+time from device clock → เริ่มตรวจเช็ค → pm-checklist). Wire PmApi.checkinPmWorkorder(id, body:{gps}) through lib/offline: build SyncOperation(kind:update, endpoint:/pm/workorders/{id}/checkin) and syncQueue.enqueue() with a client uuid idempotency key, then optimistic local success. money=NONE. B-242 DEPTH RULING pending: build the SPINE + enqueue + optimistic only — do NOT implement the drain trigger / conflict policy (SyncProcessor stays a stub, gated on Open Q#5). Reference B-242 at the seam.
- **spec:** pototype/mobile-pm.jsx:52-89 (MPMCheckin) · agents/orch-b-recon/mob-e2-accel.md (keystone offline wiring + dep gaps + i18n) · apps/mobile/lib/api/generated/clients/pm_api.dart:95-99 (checkinPmWorkorder) + pm_workorders_id_checkin_request_body.dart · apps/mobile/lib/offline/sync_queue.dart + sync_operation.dart + sync_processor.dart (spine + deferred drain) · BLOCKERS.md B-242 (build-foundation-now-defer-depth)
- **dependsOn:** MOB-SHELL-00, MOB-REF-01, MOB-SCREEN-E1 (nav source), P0-MOB-05 queue spine (exists). B-242 depth = the gate for the DRAIN only, NOT for enqueue — enqueue+optimistic is level-agnostic and proceeds. Dep gaps: add uuid (load-bearing idempotency key); geolocator/connectivity are mobile-zone deps — add or honest-empty the GPS (never hardcode 13.8076,100.4519), flag in the branch note. i18n: pm.checkin* mostly in dict.pm.* but เริ่มตรวจเช็ค/สัญญา missing → mob-i18n-batch-3.apply.json.
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs E2 ref + sync_queue enqueue/idempotent-replay unit test (double-tap never double-queues).
- **handoff:** push feature/mobile-e2 + batch-3 apply.json → channel note to orch-B AND to Wei: the first offline-WRITE screen is here, so B-242 (ก)/(ข) depth ruling now gates the drain wiring.

### #10 MOB-SCREEN-E3-CHECKLIST — E3 · pm-checklist (ตรวจเช็ครายการ PM) — offline-write, PM vertical  
- **scope:** Port MPMChecklist (mobile-pm.jsx:98-147): 5 inspection rows × (before/after photo + 3-value result ปกติ/ปรับตั้ง/เปลี่ยน-ซ่อม from MPM_RESULTS) + header count ตรวจแล้ว n/5 + CTA → pm-notes. Wire PmApi checklist submit (pm_workorders_id_checklist_request_body.dart) via the same enqueue seam as E2 (photos ride POST /files). money=NONE. Same B-242 depth-deferred discipline (enqueue+optimistic, no drain).
- **spec:** pototype/mobile-pm.jsx:98-147 (MPMChecklist) + :92 (MPM_RESULTS enum) · apps/mobile/lib/api/generated/models/pm_workorders_id_checklist_request_body.dart · apps/mobile/lib/api/generated/models/get_pm_checklist_templates_response.dart · apps/mobile/lib/offline/* (enqueue seam) · apps/mobile/docs/screen-map.md §3 pm-eng
- **dependsOn:** MOB-SHELL-00, MOB-REF-01, MOB-SCREEN-E2 (queue seam proven, nav source). i18n: checklist keys → mob-i18n-batch-3.apply.json. B-242 depth still deferred (enqueue-only).
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs E3 ref + enqueue unit test.
- **handoff:** push feature/mobile-e3 + batch-3 apply.json (if residual) → channel note to orch-B.

### #11 MOB-SCREEN-E4-NOTES — E4 · pm-notes (บันทึกสาเหตุ/แก้ไข) — offline-write form, PM vertical  
- **scope:** Port MPMNotes (mobile-pm.jsx:148-181): fields สาเหตุ/การแก้ไข/ข้อเสนอแนะ/อะไหล่ที่ใช้ + honest info banner (the prototype's LINE-OA quote-create is a mock side effect — render the banner copy from i18n, do NOT wire a fabricated quote call) + CTA → pm-close. Persist via the PM update op through the enqueue seam. money=NONE. B-242 depth-deferred.
- **spec:** pototype/mobile-pm.jsx:148-181 (MPMNotes) · apps/mobile/docs/screen-map.md §3 pm-eng + §7 (LINE-OA mock note) · apps/mobile/lib/offline/* (enqueue seam) · agents/orch-d-recon/mob-i18n-gap.md (E4 keys)
- **dependsOn:** MOB-SHELL-00, MOB-REF-01, MOB-SCREEN-E3. i18n: notes keys → mob-i18n-batch-3.apply.json. Confirm the real PM-notes persist endpoint in pm_api (RECON; if absent → honest-empty draft + BLOCKER, skip).
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs E4 ref.
- **handoff:** push feature/mobile-e4 + batch-3 residual apply.json → channel note to orch-B.

### #12 MOB-SCREEN-E5-CLOSE — E5 · pm-close (ปิดงาน PM + ลายเซ็น) — completes the flagship offline PM vertical  
- **scope:** Port MPMClose (mobile-pm.jsx:182-218): 5-row job summary + tap-to-sign (signature capture replaces the mock SVG path; state signed unlocks the button) + closed state = full-screen success + return to pm-jobs. Wire the PM close op via the enqueue seam. money=NONE. Signature = a real capture widget (structure/position must match the prototype); if no signature dep exists, add one (mobile-zone) or honest-placeholder + flag. B-242 depth-deferred.
- **spec:** pototype/mobile-pm.jsx:182-218 (MPMClose) · apps/mobile/docs/screen-map.md §3 pm-eng + §7 (signature = SVG mock → real capture) · apps/mobile/lib/offline/* (enqueue seam) · agents/orch-d-recon/mob-i18n-gap.md (E5 keys)
- **dependsOn:** MOB-SHELL-00, MOB-REF-01, MOB-SCREEN-E4. i18n: close/success keys → mob-i18n-batch-3.apply.json. RECON the PM close endpoint in pm_api.
- **gates:** flutter analyze + flutter test + i18n-guard + asset-drift-green + gate-4.5 + mobile-G5 vs E5 ref.
- **handoff:** push feature/mobile-e5 + batch-3 residual apply.json → channel note to orch-B. Note: E1-E5 vertical complete = the one end-to-end offline-first flow.


## Autonomy protocol
RECON-FIRST every task: before writing any widget, in THIS round open (a) the screen's own .jsx lines cited in specSources (PLAN.md §0 rule 2 — 'claiming you read it before does not count'), (b) its orch-b-recon accelerator, (c) its screen-map.md §3 row, and (d) the real backend handler/response shape for every read (opaque Entity carries zero typed fields — bind ONLY keys the server actually serves; everything else is honest-empty, never the prototype mock literal). NEVER GUESS: any conflict outside the ruling table, any missing i18n key, any unknown filter/endpoint/field, any offline-DEPTH question → write BLOCKERS.md (grep the latest B-0 id FRESH to avoid a C-024 collision) and SKIP to the next queue item. Do not stall waiting for Wei. PER-TASK GATES = the 6-gate mobile stack (flutter analyze clean · flutter test green · i18n-guard zero-Thai-in-lib · asset-drift-test green · gate-4.5 diff-reviewer · mobile-G5 vs the MOB-REF-01 Fiori ref); offline-write screens add a sync_queue enqueue/idempotent-replay unit test. i18n discipline: mobile UI copy resolves ONLY from i18n-full.json via the keys-only sidecar (assets/i18n/screens/<screen>_strings.json) — batch-1 covers the whole approval group; for every field/pm/sales screen slice the needed keys from agents/orch-d-recon/mob-i18n-gap.md into mob-i18n-batch-N.apply.json (keys-only, byte-exact vs the .jsx, 0 existing-value change) and hand it to orch-B — NEVER edit i18n-full.json yourself (sacred, hook-blocked) and NEVER hand-translate a word. After orch-B applies a batch + regens the asset, your asset-drift test stays green; if you mint before orch-B applies, the drift gate goes red by design. OFFLINE: the P0-MOB-05 spine (SyncQueue/SyncOperation/InMemorySyncQueue/DriftSyncQueue) already exists and is level-agnostic — reuse it; enqueue + optimistic-local-success is buildable now for E2-E5, but the DRAIN trigger/conflict policy (SyncProcessor) stays a stub gated on B-242 (Open Q#5) — reference B-242 at the seam, do not implement a drain. SACRED (never touch): openapi.yaml, migrations, i18n-full.json, all CLAUDE.md, CI, docs/extract. ZONE: one worktree = apps/mobile only (+ your agents/orch-d-recon drafts + the MOB-REF-01 staging dir); never edit apps/web, tests/, or the board rows other lanes own. HANDOFF, NEVER SELF-MERGE: each finished task → push feature/mobile-<screen> + append a channel note (agents/channel.md, next C-id, CLAIM/RELEASE + ACK) to orch-B who runs verify + merges to dev; you do NOT merge to dev and NEVER to main (Wei promotes alone). If you burn through the queue, pull the next READY item from remainingInventory (F2/B1/B2/B3/C1 are client-ready).

## Cross-lane deps
orch-A (backend) — file these blockers UP FRONT (right after MOB-SHELL-00) so orch-A builds them overnight in parallel: (1) inbox needs GET /me/approvals + summary counts (no unified pending-MY-approval endpoint exists; badge 17 is a mock); (2) detail needs GET /pr/{id} + line-items + approval-chain/timeline + attachments + BOQ budget-progress, generalized across PR/PO/WO; (3) F3 field-progress needs a daily work-progress %-submit op (POST /subcon-contracts/{id}/progress) + a work-item list read; (4) C2 HSE is genuinely greenfield — needs POST /hse/reports {type,severity,description,gps,photos[]} + schema + HSE-officer notify, THEN regen; (5) optional approve-comment body on POST /pr/{id}/approve so the approve note textarea can persist; (6) confirm the canonical mobile auth entry (login screen vs reuse web token). orch-B (verify/sacred/merge) — applies every mob-i18n-batch-N.apply.json via SACRED_OVERRIDE=wei-approved:B-240 to BOTH i18n-full.json copies + regens the asset, then verifies each feature/mobile-<screen> branch (6 gates) and merges to dev; also owns tests/visual/reference/ and places the MOB-REF-01 captures. Codegen/regen (mobile-zone, reads sacred openapi, does not edit it) — A1-A4 need a land_sales_api.dart regen (stale Jul-14, missing /sales/service); B4 needs a NEW inventory Dart client generated (/inventory/* exists in migrations 0040/0041 but no inventory_api.dart); D1 needs a NEW dashboard Dart client generated (/dashboard/* exists incl RPT-005 EVM S-curve but no client). Wei — B-242 (ก)/(ข) offline depth ruling is due before the E2 DRAIN is wired (E2 enqueue proceeds without it); plus the auth-entry and approve-comment-body rulings. orch-C (web) — no shared files; zero collision.

## Work order (kickoff)
orch-D (MOBILE lane · zone apps/mobile · Flutter/Dart) — overnight autonomous run. Wei is asleep and reviews in the morning; work the queue below top-to-bottom without stalling. You do NOT merge — you push feature/mobile-<task> branches and hand each to orch-B (the verify/merge lane) via an agents/channel.md note (next id C-436, then increment).

Loop discipline (read first): root CLAUDE.md + PLAN.md §0 (pototype=law · 5 design-fidelity rules · i18n ONLY from i18n-full.json · never hand-translate · conflict→BLOCKERS never guess) + §7 (mobile screens start Phase 4) + §9 (5 gates) + §10 (feature→dev via orch-B). One worktree = apps/mobile only. Sacred files (openapi.yaml, migrations, i18n-full.json, all CLAUDE.md, CI, docs/extract) are hook-protected — never edit; a needed i18n change → draft a keys-only apply.json in agents/orch-d-recon/ and hand it to orch-B who owns the sacred apply.

Do the queue in order:
1. MOB-SHELL-00 — build the real app shell (i18n bootstrap in main.dart, 5-tab MTabBar with honest-disable per B-241, MobileScreenRouter scaffold, shared primitives MobileHeader/MSection/MField/MInput/MPill, Dio + JuneflowApiClient with a --dart-define dev bearer). Do NOT rebuild the offline queue — the P0-MOB-05 spine already exists. First act after the shell: file the backend blockers for inbox (GET /me/approvals+summary), detail (GET /pr/{id}+items+chain+attachments+budget), and the mobile auth-entry question, so orch-A can build them overnight in parallel.
2. MOB-REF-01 — capture the 26 Fiori.html mobile reference images (390×844, one per screen id, approval 5 first). This is the prerequisite for every screen's G5. Hand the PNGs to orch-B for placement in tests/visual/reference/.
3-5. Port the approval-ready trio NOTIF → APPROVE → REJECT. Their i18n is already applied (B-240 batch-1); recon each opaque-Entity response and honest-empty anything the server can't serve. approve/reject are path/reason-only money=SERVER state transitions — no client amount math, online-required (do not queue a money mutation).
6-8. Port the field/sales reads E1-PMJOBS → F1-GRLIST → D2-SALESCRM. These need new i18n — slice the keys from agents/orch-d-recon/mob-i18n-gap.md into mob-i18n-batch-2/3.apply.json (keys-only, byte-exact) and hand to orch-B; after orch-B applies + regens the asset your drift gate stays green.
9-12. Build the flagship offline PM vertical E2-CHECKIN → E3-CHECKLIST → E4-NOTES → E5-CLOSE. E2 is the offline-write keystone: reuse the existing spine, enqueue with a uuid idempotency key + optimistic local success, and STOP at the drain seam — the SyncProcessor drain/conflict policy stays deferred on B-242 (Open Q#5); reference B-242 at the seam and post a channel note that the first offline-write screen has landed so Wei can rule the (ก)/(ข) depth. Add uuid (load-bearing); geolocator/signature are mobile-zone deps — add or honest-empty, never hardcode the mock GPS/signature.

Gates for every screen: flutter analyze (clean) + flutter test (green) + i18n-guard (zero Thai in lib/**.dart) + asset-drift-test (green) + gate-4.5 (orch-B) + mobile-G5 vs the MOB-REF-01 ref; offline screens add a sync_queue enqueue/idempotent-replay unit test. MOB-SHELL-00 has no G5 (no single prototype screen). If a task blocks (missing key, unknown endpoint/filter/field, offline-depth question, any §0 conflict) → grep the latest B-0 id FRESH, write BLOCKERS.md, and SKIP to the next queue item — never guess, never stall. If you finish the queue, pull the next READY item from remaining inventory (F2/B1/B2/B3/C1 are client-ready). Hand off every finished task to orch-B; never merge to dev, never touch main.
