# ORCH-A overnight brief — backend lane — 2026-08-03 night

## Remaining inventory

- **transferSalesUnit — POST /sales/units/{id}/transfer (โอนกรรมสิทธิ์ / title-transfer)** — THE only declared money endpoint with no handler. Highest value (Program-3 climax). money=SERVER, source_doc transfer:<salesUnitId>, jv_source_doc_uq idempotency, balanced JV. Overnight outcome = a well-formed ruling blocker so Wei can rule at dawn.
  - spec: openapi.yaml:3739 (declared, operationId transferSalesUnit, NO handler — confirmed only a comment in gl-post.ts:109) · screen pototype/sales-process.jsx:496-543 'สินเชื่อ & โอนกรรมสิทธิ์ (Loan & Transfer) · บันทึกการรับเงินโอน (เชื่อม AR + GL)' → NAV sales.process · PLAN.md §7 Phase-5 sales/land = Program-3 Wave-2 · accounts already prepped gl-post.ts ACCT: advanceReceived 2040 / constructionRevenue 4020 / cogs 5010 / wip 1140 / contractAsset 1130 (B-230 comments)
  - dep: BLOCKED on Wei ruling — the exact JV recipe (clear 2040 advance accumulated from bookings+downs, recognize 4020 revenue + 2050 VAT, book loan-disbursement cash Dr 1020, Dr 5010 COGS / Cr 1140 WIP, COGS basis) is a money accounting decision NOT in PLAN.md App-C; booking/down/deal each got explicit Wei rulings (B-159/B-161) so transfer must too
- **Master-data detail + update endpoints: GET/PUT /cost-centers/{id}, POST /customers + GET/PUT /customers/{id}, PUT /doc-numbering/{id}** — Highest screen-unblock count of the buildable set. non-money. Scope docnum PUT to the non-blocked fields; if it collides with the open B-067 lock-mode enum → BLOCKERS + skip that sub-field only.
  - spec: openapi.yaml:881 /cost-centers/{id} · :793/821 /customers + /customers/{id} · :941 /doc-numbering/{id} (all declared) — handlers today only expose GET-list (cost-centers.ts:99 · customers.ts:45 GET only, no POST/PUT · doc-numbering.ts:53 GET only). Screens: master cost-center edit, customer create/edit, docnum edit (master.* NAV rows)
  - dep: READY — fully autonomous, no sacred (paths already declared, no migration; existing scoped insert/update doors)
- **sales.dashboard aggregate read endpoint (new path GET /sales/dashboard)** — moneyPath false (read rollup; money values are server-computed sums, no posting).
  - spec: B-222 (recon filed: NO aggregate endpoint exists) · pototype SalesDashboard (extra-screens.jsx) · backed data = /sales/leads (funnel) + /sales/bookings+/contracts+/downs (money) + sales_unit.status; UNBACKED = sales-vs-target (no quota schema), per-rep Top-5 (no owner_user_id) · precedent B-229 subcon.progress RULED ข thin-honest
  - dep: READY thin-honest — new openapi path = SACRED at merge (flag for Wei); build only backed rollups, em-dash unbacked KPIs. Full version (targets/quota schema + owner attribution) stays a separate Wei ruling.
- **Reports hub + report export: GET /reports/hub, POST /reports/{id}/export** — non-money. New route file registerReportsRoute wired in app.ts.
  - spec: openapi.yaml:4063 /reports/hub + :4073 /reports/{id}/export (declared, NO handler — no registerReports route file) · NAV reports.hub screen
  - dep: READY — paths declared, no sacred; export may enqueue a BullMQ job (skeleton exists) or return a synchronous file ref
- **DMS documents route + version schema: GET/POST /documents/{id}/versions + document table columns (name/size/uploader/status) + document_version table** — documents.ts route file already exists (base GET/POST). non-money.
  - spec: B-221 (recon: /documents/{id}/versions unhandled; document table lacks name/size/uploader/status; no version-history table) · pototype dms.jsx (DMSCenter) columns · openapi.yaml:3995 /documents/{id}/versions declared
  - dep: READY-with-flags — SACRED migration (new columns + version table) + it touches the B-221 schema recon; column shapes are prototype-derived (non-money). Flag migration SACRED at merge.
- **jv.no + doc-number idempotency sweep (B-168 + B-172)** — money-integrity (not a double-post — amounts stay correct). SACRED migration. Low overnight urgency.
  - spec: B-168 (gl-post.ts:144 allocJvNo max+1 unlocked read, jv.no has NO unique index → two distinct-source_doc posts share a voucher no) + B-172 (same root on ar_credit_note.no, ap_deposit.no, inventory transfer/issue doc-no) · fix = uniqueIndex(company_id,no) + isUniqueViolation→re-alloc retry (23505 handler already exists)
  - dep: Wei-DEFERRED (2026-07-27: pre-existing on main · non-money voucher-number/audit only). Cannot un-defer while Wei asleep — build the migration+retry on branch, file BLOCKERS to un-defer + SACRED-approve, hand to orch-B flagged; do NOT merge tonight.
- **Foreman progress write endpoint (mobile fm-progress % งาน)** — Phase-4 mobile-facing, lower priority. non-money.
  - spec: apps/mobile/docs/screen-map.md fm-progress (MFmProgress mobile-field.jsx:93 — sends % via mock __juneflowCtx.notify) · no /progress write path in openapi · project_node has NO progress/percent/built column (verified schema/project.ts) · read side = /dashboard/phase-progress (derived)
  - dep: BLOCKED on Wei ruling — persistence target undefined (project_node.progress column? new node_progress_log table?) + needs new openapi path + migration. File BLOCKERS + skip.
- **Warranty CLAIM flow (solar + construction) — context only** — Listed so it is not mistaken for a gap to build unprompted.
  - spec: B-212 note 'warranty add exists, ไม่มี claim' · solar warranties add built (createSolarWarranty); no claim/decision endpoint
  - dep: DEFERRED (not in current scope) — no screen actively blocked; leave for a future wave

## Work queue (ordered)

### #1 P3-BE-20 — transferSalesUnit — POST /sales/units/{id}/transfer (Program-3 Wave-2 title-transfer · MONEY · file recipe ruling first)  💰money
- **scope:** Recon the transfer/handover money event from pototype/sales-process.jsx (Loan & Transfer screen) + the advance-received(2040)/revenue(4020)/COGS(5010)/WIP(1140) accounts already prepped in gl-post.ts ACCT (B-230). The JV recipe is NOT in PLAN.md App-C, so DRAFT a proposed recipe (clear the 2040 advance accumulated from bookings+downs on this sales_unit, recognize Cr 4020 construction-revenue + Cr 2050 VAT, Dr 1020 bank for the loan-disbursement remainder, and Dr 5010 COGS / Cr 1140 WIP for cost-of-sales) and file it as a BLOCKERS ruling for Wei; set task blocked and SKIP the build. IF (and only if) Wei rules during the run: implement money=SERVER (server computes every amount from the unit + its prior booking/down JVs; client sends only ids), source_doc `transfer:<salesUnitId>`, balanced JV inside the sales_unit.status→transferred + transfer_at tx, jv_source_doc_uq idempotency guard (isUniqueViolation→409), tenant-scoped, foreign unit→404, already-transferred→409.
- **spec:** openapi.yaml:3739 transferSalesUnit (declared, no handler) · pototype/sales-process.jsx:496-543 (Loan & Transfer · เชื่อม AR + GL) · docs/extract/NAV-ROUTES.md sales.process row · PLAN.md §7 Phase-5 sales/land = Program-3 Wave-2 · apps/api/src/routes/gl-post.ts ACCT (2040/4020/5010/1140/1130 · B-230) · apps/api/src/routes/land-sales.ts createSalesBooking/createSalesDown (Dr1020/Cr2040 precedent, B-159/B-161)
- **dependsOn:** Wei ruling on the transfer JV recipe (this task files it). No code dep — accounts + jv tables + sales_unit.status column all exist.
- **gates:** IF ruled+built: db+api typecheck · full api vitest (turbo run test --filter=@juneflow/api — the ~1150-case suite stays green) · money-skeptic gate-4.5 (diff-reviewer.md, server-compute + balanced-JV + idempotent + advance-clear correctness) · LIVE E2E money-proof on real PG (finance-e2e-live.sh pattern, E2E_LIVE-gated: concurrent double-transfer must return [200,409] and post exactly ONE revenue+COGS JV; advance 2040 fully cleared, JV balances DR=CR). IF only filed: BLOCKERS entry well-formed, no branch.
- **handoff:** Overnight: BLOCKERS.md ruling entry for Wei (proposed recipe + accounts + source_doc + idempotency) + channel note to orch-B (next C-id) that P3-BE-20 is filed-and-skipped awaiting Wei. IF built: push feature/backend-sales-transfer + channel note; orch-B runs independent api suite + money-skeptic gate-4.5 + live double-post proof, then slice-merges; NEW handler only (no openapi/migration change if the JV tables suffice) — but flag any migration SACRED for Wei at merge (petty B-233 precedent).

### #2 P2-BE-59 — Master-data detail + update endpoints (cost-centers/{id}, customers POST + /{id}, doc-numbering/{id}) — non-money, fully autonomous  
- **scope:** Implement the declared-but-unhandled master CRUD verbs: GET/PUT /cost-centers/{id}; POST /customers + GET/PUT /customers/{id}; PUT /doc-numbering/{id}. Use the existing tenant-scoped select/insert/update doors (fail-closed company_id), action-via-PUT allowed for master detail (not status), AuditLog via middleware, foreign id→404, validation→400 flat error. Scope the doc-numbering PUT to the non-blocked fields (seed/reset_rule/prefix); if it collides with the open B-067 `locked` boolean→lock-mode-enum question → BLOCKERS + skip only that sub-field, ship the rest.
- **spec:** openapi.yaml:881 /cost-centers/{id} · :793/821 /customers+/{id} · :941 /doc-numbering/{id} (all declared) · apps/api/src/routes/cost-centers.ts / customers.ts / doc-numbering.ts (GET-list only today) · pototype master.* edit screens + docs/extract/NAV-ROUTES.md master rows · B-067 (docnum lock-mode enum — open)
- **dependsOn:** none — paths declared, no sacred, no migration
- **gates:** db+api typecheck · full api vitest green · new per-endpoint tests (detail 200 / foreign 404 / update round-trip / tenant-isolation cannot-read-other-company) · gate-4.5 (tenant-scope + audit). No live-money gate (non-money).
- **handoff:** push feature/backend-master-crud + channel note to orch-B; orch-B runs independent api suite + gate-4.5 then slice-merges to dev. No sacred flag (declared paths, no migration). Unblocks orch-C master edit/detail web ports.

### #3 P3-BE-21 — sales.dashboard aggregate read endpoint (thin-honest, B-222/B-229 precedent) — new SACRED openapi path  
- **scope:** Add GET /sales/dashboard returning a server-computed rollup of ONLY the data the live backend serves: funnel counts by lead stage (/sales/leads), booking+down money totals (bookings/downs JV sources), and unit-status counts (sales_unit.status). Em-dash / omit the unbacked KPIs (sales-vs-target = no quota schema; per-rep Top-5 = no owner_user_id) per the B-229 thin-honest ruling — do NOT invent target/quota/owner schema. All money values server-computed (no posting). New openapi path = SACRED at merge → flag for Wei; file a short BLOCKERS note citing B-222 (thin-honest scope, deferring the targets/quota+owner-attribution full version to a later ruling).
- **spec:** B-222 (recon: no aggregate endpoint; backed vs unbacked KPI inventory) · B-229 RULED ข thin-honest (Wei 2026-08-03, sibling precedent) · pototype extra-screens.jsx SalesDashboard + sales-crm.jsx funnel · apps/api/src/routes/land-sales.ts listLeads/listBookings/listDowns · packages/db schema sales_unit.status
- **dependsOn:** none for the thin-honest build; full version depends on a future Wei quota/owner ruling
- **gates:** db+api typecheck · full api vitest green · new test (aggregate shape · tenant-scoped totals · unbacked KPIs absent/em-dash) · gate-4.5 (server-compute of money sums, no client math). No live-money gate.
- **handoff:** push feature/backend-sales-dashboard (new handler + SACRED openapi additive path) + channel note to orch-B; orch-B gate-4.5 + obtains Wei's SACRED go for the openapi apply at merge (petty precedent), then slice-merges. Unblocks orch-C sales.dashboard port.

### #4 P2-BE-60 — Reports hub + report export (GET /reports/hub, POST /reports/{id}/export) — non-money  
- **scope:** Implement the declared-but-unhandled reports endpoints via a new registerReportsRoute wired in app.ts: GET /reports/hub returns the tenant-scoped report catalog (the reports.hub screen list); POST /reports/{id}/export triggers an export — enqueue via the existing BullMQ worker skeleton (async job id) or return a synchronous file ref, matching whatever the /exports + /files pattern already establishes. Reuse the DMS/files presigned pattern; AuditLog via middleware.
- **spec:** openapi.yaml:4063 /reports/hub + :4073 /reports/{id}/export (declared, no handler) · docs/extract/NAV-ROUTES.md reports.hub row + pototype reports screen · apps/api/src/routes/files.ts + BullMQ worker skeleton (P0-BE-13) + /exports paths
- **dependsOn:** none — paths declared, no sacred, no migration
- **gates:** db+api typecheck · full api vitest green · new tests (hub list tenant-scoped · export enqueues/returns ref · 404 unknown report) · gate-4.5. No live-money gate.
- **handoff:** push feature/backend-reports + channel note to orch-B; orch-B api suite + gate-4.5 then slice-merges. No sacred (declared paths). Unblocks orch-C reports.hub port.

### #5 P2-BE-61 — DMS documents route completion + version schema (B-221) — SACRED migration, non-money  
- **scope:** Close the B-221 DMS gaps: migration adding document columns (name/size/uploader/status) + a new document_version table; wire GET/POST /documents/{id}/versions (version-history read + add-version) on the existing documents.ts route. Column shapes are prototype-derived from dms.jsx (filename/size/uploader/status cells + upload-this-month / storage-used KPI sources). All additive/nullable. If any column shape is genuinely ambiguous (not derivable from the prototype) → BLOCKERS + ship the unambiguous subset.
- **spec:** B-221 (recon: /documents/{id}/versions unhandled · document table column gaps · no version table) · openapi.yaml:3995 /documents/{id}/versions (declared) · pototype extra-screens.jsx DMSCenter columns + KPIs · apps/api/src/routes/documents.ts (base route exists)
- **dependsOn:** none — but SACRED migration → Wei go at merge
- **gates:** db typecheck + migration check (drizzle) · api typecheck · full api vitest green · new tests (versions list/add · column round-trip · tenant-scope) · gate-4.5. No live-money gate.
- **handoff:** push feature/backend-dms-versions (handler + SACRED migration) + channel note to orch-B; orch-B gate-4.5 + obtains Wei's SACRED go for the migration apply at merge (petty precedent), then slice-merges. Unblocks orch-C dms port.

### #6 P2-BE-62 — jv.no + doc-number idempotency sweep (B-168 + B-172) — SACRED migration, Wei-DEFERRED (prepare-only)  💰money
- **scope:** Prepare (do NOT merge) the fix Wei deferred: a migration adding uniqueIndex(company_id, no) to jv + ar_credit_note + ap_deposit + inventory transfer/issue doc-number tables, and wrap each number-allocating insert in the existing isUniqueViolation→re-alloc/retry path (the 23505 handler already exists in gl-post.ts). Build it on the feature branch, then file a BLOCKERS entry asking Wei to un-defer + SACRED-approve. Because Wei is asleep and explicitly deferred this (2026-07-27), do NOT merge tonight — hand the prepared branch to orch-B flagged. This is money-integrity (voucher-number/audit), NOT a double-post (amounts already correct).
- **spec:** B-168 (allocJvNo max+1 unlocked, jv.no no unique · Wei-deferred) · B-172 (same root: ar_credit_note.no / ap_deposit.no / inventory transfer+issue doc-no · Wei-deferred, folds into B-168) · apps/api/src/routes/gl-post.ts:144 allocJvNo + isUniqueViolation:38 · packages/db schema finance.ts jv (jv_company_idx/jv_period_idx/partial jv_source_doc_uq only)
- **dependsOn:** Wei un-defer + SACRED approve (this task re-files the request). Deferred since 2026-07-27.
- **gates:** IF Wei un-defers: db typecheck + migration check · api typecheck · full api vitest green · new tests (concurrent distinct-source_doc posts → one voucher no, loser retries to next no · not a double-post) · money-skeptic gate-4.5 · live E2E on real PG (concurrent alloc race → distinct sequential nos, zero collisions). Overnight: prepared branch + BLOCKERS entry, NO merge.
- **handoff:** push feature/backend-jvno-uniq (prepared, unmerged) + BLOCKERS un-defer request + channel note to orch-B flagged SACRED-migration + Wei-deferred; orch-B holds for Wei's go, does NOT merge autonomously.

### #7 P3-BE-22 — Foreman progress write endpoint (mobile fm-progress % งาน) — file ruling, skip  
- **scope:** Recon the mobile MFmProgress persistence need (the foreman submits per-task % work; prototype only fires a mock notify). There is no /progress write path and project_node has no progress/percent/built column. The persistence target (project_node.progress column vs a node_progress_log table) + new openapi path + migration are all undefined → file a BLOCKERS ruling for Wei (proposed: node_progress_log append table + GET /dashboard/phase-progress already derives the read side) and SKIP the build. Do not guess a schema for a data model this touches broadly.
- **spec:** apps/mobile/docs/screen-map.md fm-progress (MFmProgress mobile-field.jsx:93 mock notify) · openapi.yaml:4263 /dashboard/phase-progress (read, derived — no write counterpart) · packages/db schema project.ts (no progress column) · PLAN.md §7 Phase-4 mobile
- **dependsOn:** Wei ruling on the progress persistence target (this task files it)
- **gates:** Overnight: BLOCKERS entry well-formed, no branch. IF ruled: migration + handler + tests + gate-4.5 (non-money).
- **handoff:** BLOCKERS.md ruling entry + channel note to orch-B that P3-BE-22 is filed-and-skipped awaiting Wei; unblocks orch-D mobile fm-progress once ruled.


## Autonomy protocol
RECON-FIRST, NEVER-GUESS, PER-TASK GATES, HAND TO ORCH-B — Wei is asleep and reviews in the morning. Run the queue top-to-bottom without Wei. For EACH task: (1) Read the exact spec sources listed (the screen .jsx IN THIS ROUND + the openapi path/gap + PLAN flow + gl-post.ts ACCT for money) — never build from memory (PLAN §0 rule 2). (2) Confirm the endpoint truly has no handler (grep app.get/post/put + the operationId) before building — dev is far ahead of main (34th promote 0a2224a) and many recent items already shipped on dev (AR-invoice revrec B-216/217, solar Wave-1a/1b, gl.projectpl B-227), so re-verify each is a real gap. (3) If the task is a MONEY endpoint whose JV recipe is not in PLAN.md App-C (transferSalesUnit P3-BE-20) → DRAFT a proposed recipe, write it to BLOCKERS.md for Wei, set the task blocked, and SKIP to the next queue item — NEVER post money on a guessed recipe (booking/down/deal each required an explicit Wei ruling). (4) If a task needs a NEW openapi path or migration, build it on the feature branch but it is SACRED at merge — do NOT edit sacred files past the hook autonomously; produce them on-branch and flag in the handoff that orch-B must obtain Wei's SACRED go at merge (petty B-233 precedent). (5) If a task is Wei-DEFERRED (B-168/B-172 sweep) → prepare-only on branch + re-file the un-defer request, do NOT merge tonight. (6) Run the 5-gate discipline locally (db+api typecheck → full api vitest ~1150 cases stay green → money-skeptic gate-4.5 via diff-reviewer.md → for money paths a LIVE E2E double-post proof on real PG via finance-e2e-live.sh, E2E_LIVE-gated), cap 3 red rounds then BLOCKERS+skip. (7) Do NOT merge to dev yourself — push feature/backend-<task> + post a channel.md note (next C-id) to orch-B, who owns verify+slice-merge+live-proof. The night stays productive because the ruling-blocked money items (P3-BE-20, P3-BE-22, P2-BE-62) file-and-skip early while the fully-autonomous non-money endpoint fill-ins (P2-BE-59 master CRUD, P2-BE-60 reports, and the SACRED-flagged P3-BE-21/P2-BE-61) actually ship. Journal every round to agents/journal/backend.md. If you exhaust the queue, hand off to orch-B and stop — do not invent scope beyond a real prototype/openapi/PLAN spec.

## Cross-lane deps
orch-B (verify/QA/merge/promote lane, this brief's author-adjacent): owns every dev slice-merge + independent full api suite + money-skeptic gate-4.5 + LIVE double-post E2E on real PG, and obtains Wei's SACRED go for any new openapi path / migration at merge — backend never merges itself. Wei (asleep, dawn review): owns the money-recipe ruling for transferSalesUnit (P3-BE-20), the sales.dashboard thin-vs-full scope confirmation (P3-BE-21/B-222), the DMS version schema shape (P2-BE-61/B-221 if ambiguous), the B-168/B-172 un-defer + SACRED approve (P2-BE-62), the foreman-progress persistence target (P3-BE-22), plus all SACRED applies + main promote. orch-C (web): ports the screens these unblock AFTER the endpoint lands on dev — sales.process transfer button (P3-BE-20), master detail/edit (P2-BE-59), sales.dashboard (P3-BE-21), reports.hub (P2-BE-60), dms (P2-BE-61); backend-first, web follows. orch-D (mobile): fm-progress (P3-BE-22) once ruled + the approval/GR/checkin endpoints (already built) for the 26 mobile screens; also blocked on the mobile i18n/nav/offline rounds B-240/241/242. Integrations (tax-engine/bank-file): F-AP1 WHT + F-BANK1 auto-match already wired — no new dep. Harness FYI (do not let it silently break your gates): B-243 (i18n-guard silent in worktrees) + B-244 (diff-reviewer.md Thai-comment carve-out) are open — run gate-4.5 with awareness that the worktree hooks may not fire.

## Work order (kickoff)
orch-A BACKEND overnight run — zone apps/api + packages/db + packages/contracts, one worktree. main=0a2224a (34th promote), dev is ahead + clean; verify lane clear. Read root CLAUDE.md + apps/api/CLAUDE.md + PLAN.md §0 (pototype=law, 5 fidelity rules, i18n from i18n-full.json only, conflict→BLOCKERS never guess) + §9 (5 gates) + §10 (feature→dev on CI-green+gate-4.5; orch-B owns merge) BEFORE touching code.

The backend money-endpoint backlog is nearly drained — recon confirmed the SOLE declared-but-unhandled money endpoint is POST /sales/units/{id}/transfer (Program-3 Wave-2 title-transfer), whose JV recipe needs a Wei ruling. So work the queue in order, expecting the ruling-blocked money items to file-and-skip while the non-money endpoint fill-ins ship:

1. P3-BE-20 transferSalesUnit — recon sales-process.jsx + gl-post.ts ACCT (2040/4020/5010/1140); the recipe is NOT in App-C → write a proposed-recipe BLOCKERS ruling for Wei, set blocked, SKIP. (money=SERVER + source_doc transfer:<unitId> + jv_source_doc_uq guard + live double-post proof ONLY if Wei rules mid-run.)
2. P2-BE-59 master CRUD (cost-centers/{id}, customers POST+/{id}, doc-numbering/{id}) — fully autonomous, no sacred, ship it (this is your first real merge candidate; highest screen-unblock count).
3. P3-BE-21 sales.dashboard thin-honest aggregate (B-229 precedent) — new SACRED openapi path, flag for Wei at merge; em-dash the unbacked target/owner KPIs, never invent quota schema.
4. P2-BE-60 reports/hub + reports/{id}/export — autonomous, no sacred.
5. P2-BE-61 DMS documents versions + column/version-table migration (B-221) — SACRED migration, flag for Wei.
6. P2-BE-62 jv/docnum idempotency sweep (B-168/B-172) — Wei-DEFERRED: prepare on branch + re-file un-defer, do NOT merge tonight.
7. P3-BE-22 foreman progress write — persistence target undefined → BLOCKERS ruling + skip.

Discipline every round: verify the gap is real (dev is ahead — many recent items already shipped) → build in your worktree → db+api typecheck → full api vitest (turbo run test --filter=@juneflow/api, ~1150 cases stay green) → money-skeptic gate-4.5 (diff-reviewer.md) → money paths get a LIVE E2E on real PG (finance-e2e-live.sh, E2E_LIVE-gated, [200/201,409]-style double-post proof) → cap 3 red rounds then BLOCKERS+skip. NEVER edit sacred files (openapi.yaml, merged migrations, CLAUDE.md, CI, i18n-full.json, docs/extract) past the hook autonomously — produce new openapi/migration on-branch and flag SACRED-at-merge. Do NOT merge to dev yourself: push feature/backend-<task> + post a channel.md note (next C-id) to orch-B, who verifies + slice-merges + gets Wei's SACRED go. Journal to agents/journal/backend.md. Keep going until the queue is exhausted or you hit the token cap; never stall, never guess, never invent scope beyond a real prototype/openapi/PLAN spec.
