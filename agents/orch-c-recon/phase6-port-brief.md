# Phase-6 Wave-0 — orch-C port brief (recon w10iqcalw · 8 screens)

> i18n applied via B-182 (agents/orch-c-recon/phase6-i18n-b182.apply.json · 280 keys). Consume-only, money-free reads.
> admin.overview = PORT-DEFERRED (Wei C-299 — no /admin/overview route). i18n drafted for completeness.
> line = inner ~200 mockup strings need a ruling (see BLOCKERS) — outer chrome (51 keys) is portable.

## sub.billing
- **component:** SubBilling
- **getOp:** GET /subscription/invoices (subscription.ts:81) — renders the tenant's own invoice rows (no/date/desc/amount/status). Single endpoint; no derived/other calls on this screen.
- **keys:** 12
- **uiSummary:** Read-only billing/invoice history for the current tenant. A Page (breadcrumbs 'บัญชีการใช้งาน' › 'บิล & ใบเสร็จ', title 'บิล & ใบเสร็จ · Billing', subtitle about payment history + downloading receipts/tax invoices) wrapping a single Card that holds one table. Six columns: เลขที่ / วันที่ / รายการ / จำนวน (฿, right-aligned) / สถานะ / (empty actions col). Each row shows invoice no (brand-colored, mono), date, description, formatted amount, a 'ชำระแล้ว' StatusBadge, and a ghost 'ใบเสร็จ' download button that fires a toast. No tabs, no filters, no create/edit, no modals, no empty-state (mock always has 3 rows). Only distinct interactive element is the per-row receipt download.
- **honestDivergences:**
    - actions=toast: the per-row 'ใบเสร็จ' download button calls ctx.notify(`ดาวน์โหลดใบเสร็จ ${iv.no}`) — there is NO receipt/PDF backend endpoint. Port must keep it a faithful toast (`${t('sub.billing.downloadToast')} ${iv.no}`), NOT wire a real file download.
    - mock-data → server: the entire SUB_INVOICES array (lines 31-35) is hardcoded — invoice numbers (INV-SUB-2569-012 etc.), Buddhist-era date strings ('1 ม.ค. 2569'), Thai descriptions ('Professional (รายปี) 2569', 'อัปเกรด Starter → Pro (เฉลี่ย)'), and amounts (79000/72000/18400). These must render VERBATIM from GET /subscription/invoices rows (no/date/desc/amount), never baked into the component. The 'อัปเกรด Starter → Pro' desc contains an arrow that stays server data (not an i18n key).
    - hardcoded status badge: every row renders <StatusBadge status="approved">ชำระแล้ว</StatusBadge> unconditionally (ignores iv.status, which is 'paid' for all 3 mock rows). A faithful port should map the invoice's real status field to badge tone + label; keep 'ชำระแล้ว' (sub.billing.statusPaid) for paid rows, but do not hardcode 'approved' for non-paid statuses the server may return.
    - money display: amounts pass through fmt() with the ฿ shown only in the column header. Per root money rule, the server invoice amount should carry currency_code; render amount verbatim from the server value (do not re-compute or client-format differently from the prototype's fmt).

## admin.overview **[PORT-DEFERRED]**
- **component:** <AdminOverview> — pototype/subscription-admin.jsx lines 55–110 (helper AdminKpi L50–52; joins SUB_PACKAGES from subscription.jsx L6–22)
- **getOp:** derived: there is NO /admin/overview route. All KPIs + charts DERIVE client-side from GET /admin/subscribers (admin.ts:137) — MRR = sum(row.mrr); ARR = MRR*12; 'ผู้สมัครใช้งาน' = count(status in active|trial); 'ทดลองใช้' = count(status===trial); package-share = group rows by pkg — joined to GET /admin/packages (admin.ts:117) for each package's name/color. GET /admin/invoices (admin.ts:163) is available for revenue reconciliation. Churn Rate, the KPI deltas, and the monthly-cumulative bar chart have NO endpoint = pure mock.
- **keys:** 21
- **uiSummary:** Platform-owner revenue dashboard rendered inside <Page> with breadcrumbs ['ผู้ดูแลแพลตฟอร์ม','ภาพรวมรายได้'], a title + subtitle, and a single outline 'ส่งออก' action button (download icon). Body is a 5-column KPI row — MRR (฿, +8 delta ok), ARR (ลบ., 'ประมาณการ'), 'ผู้สมัครใช้งาน' (ราย, 'จากทั้งหมด N'), 'ทดลองใช้ (Trial)' (ราย, 'รอแปลงเป็นลูกค้า'), and 'Churn Rate' (%, 'ต่ำกว่าเกณฑ์ 5%', -0.4 delta) — followed by a 1.6fr/1fr two-card grid: left 'ผู้สมัครสะสมรายเดือน' = a 6-bar monthly cumulative bar chart; right 'สัดส่วนตามแพ็กเกจ' = one Bar row per package showing package name (colored dot) and 'N ราย'. Package name/color come from SUB_PACKAGES; subscriber counts, MRR and package split are computed from the SUBSCRIBERS mock array.
- **honestDivergences:**
    - actions=toast: the 'ส่งออก' button onClick calls ctx.notify('ส่งออกรายงานรายได้ (PDF)') — a mock. No PDF-export backend exists; port must keep it firing a faithful toast, NOT a real file download.
    - owner-gating: admin.overview is the Platform-Owner console (subscription-admin.jsx). The whole screen and every derived endpoint call must be gated to the platform-owner role — a normal tenant must never reach it.
    - derive, don't hardcode: there is NO /admin/overview route. MRR (sum of subscriber mrr), 'ผู้สมัครใช้งาน' (active+trial count), 'ทดลองใช้' (trial count) and the package-share bars must be computed from GET /admin/subscribers rows, with package name/color from GET /admin/packages. The prototype's inline SUBSCRIBERS array must be replaced by the live list response.
    - hardcoded mock metric — Churn Rate: value='2.1', unit='%', sub='ต่ำกว่าเกณฑ์ 5%', delta={-0.4} are fabricated with no backend source. Render verbatim as an explicit mock/placeholder (or honest-disable); must NOT be presented as a computed live figure.
    - hardcoded mock deltas: MRR KPI delta={8} deltaTone='ok' and Churn delta={-0.4} are fabricated period-over-period arrows with no source — render verbatim as mock, do not attempt to compute them.
    - mock time-series chart: the 'ผู้สมัครสะสมรายเดือน' bar chart uses an inline MONTHS array (ม.ค.142 · ก.พ.156 · มี.ค.168 · เม.ย.175 · พ.ค.182 · มิ.ย.191) — entirely fabricated with no monthly-cumulative endpoint. Either render the mock verbatim (labels+values as-is, do not i18n the month abbreviations) or derive from subscriber 'since' dates; do not present 142→191 as real analytics.
    - FK-name-as-text: the package-share join uses SUB_PACKAGES (a frontend constant imported from subscription.jsx) matched to subscriber.pkg string codes ('pro'/'starter'/'enterprise'). In production the package name/color must come from GET /admin/packages and subscriber.pkg must be a package FK id (package_id), not a name-as-text lookup.
    - ARR is a derived estimate (mrr*12/1e6, labeled 'ประมาณการ') — keep it visibly an estimate computed from MRR, never a separately-billed number.

## sub.mine
- **component:** SubMine (pototype/subscription.jsx L41-129; also renders demo-only child <PkgDemoSwitcher/> from pototype/pkg-builder.jsx L256)
- **getOp:** derived: no /subscription/mine route exists. The active-plan card (name, tagline, yearly price, limits, modLabel, modules) DERIVES from GET /subscription/plans (subscription.ts:68) filtered to the tenant's current package (MY_SUB.pkg="pro"). Billing meta (start/renew dates, cycle) is corroborated by GET /subscription/invoices (subscription.ts:81). The usage counts (projects 7 / users 12 / storage 24 / ai 18), status, daysLeft (192), and the 'อัปเดตล่าสุด' timestamp have NO backend endpoint → render from MY_SUB mock verbatim until a usage endpoint ships.
- **keys:** 37
- **uiSummary:** sub.mine = tenant own-subscription summary. Page (breadcrumbs + title + subtitle + 2 header actions: ประวัติบิล→sub.billing, อัปเกรดแพ็กเกจ→sub.plans) over a 2-col grid (1fr / 1.4fr). LEFT: current-package Card — gradient hero (แพ็กเกจปัจจุบัน eyebrow + ใช้งาน StatusBadge + {pkg.name} + tagline + yearly price with ฿/ปี suffix), then a 4-row detail list (รอบบิล/เริ่มสัญญา/ต่ออายุถัดไป/วิธีชำระ), a warn strip 'เหลืออีก N วันก่อนต่ออายุ', and 2 mock-toast buttons (ต่ออายุ / ยกเลิก). RIGHT: usage/quota Card — header 'โควต้า & พื้นที่การใช้งาน' + mock timestamp, 4 metered rows (โครงการ/ผู้ใช้/พื้นที่จัดเก็บ/AI ถอด BOQ) each with used/cap bar + a >=75% warn line, then a modules footer (modLabel + Enterprise upgrade info strip). A demo-only <PkgDemoSwitcher/> renders at top — must be dropped. 37 i18n keys (35 screen + 2 sub.common shared).
- **honestDivergences:**
    - PkgDemoSwitcher (child rendered at SubMine top, label 'จำลองแพ็กเกจ (ดูผลที่ sidebar):') is a pure demo mechanic that mutates in-memory tenantPkg to preview sidebar package-gating. DROP it entirely from the production port (no i18n key emitted for it) per 'ห้ามลอกกลไก mock'.
    - 'ต่ออายุ' (renew) and 'ยกเลิก' (cancel) buttons call ctx.notify() with mock strings — no renew/cancel backend endpoint exists. Convert to faithful toast actions (actions=toast), keeping copy verbatim: 'ต่ออายุแพ็กเกจ Professional (รายปี)' and 'ติดต่อทีมขายเพื่อยกเลิก/ดาวน์เกรด'. Do NOT wire a real POST.
    - No /subscription/mine endpoint: usage counts (projects 7 / users 12 / storage 24 / ai 18) plus status='active', daysLeft=192, cycle='yearly' come from the MY_SUB mock object. Render these numbers VERBATIM as mock (not computed/live) — only the plan card fields (name/tagline/yearly/limits/modLabel) map to GET /subscription/plans for the tenant's active package.
    - 'อัปเดตล่าสุด วันนี้ 09:42' is a hardcoded mock timestamp. Render byte-for-byte; do NOT substitute a live/computed 'now' until a real usage-refresh endpoint supplies it.
    - MY_SUB.start='1 ม.ค. 2569' and MY_SUB.renew='31 ธ.ค. 2569' are mock Thai Buddhist-era date strings feeding the เริ่มสัญญา/ต่ออายุถัดไป rows. Render as-is (not extracted as i18n copy); replace only when a real subscription record supplies start/renew dates.
    - {pkg.name}/{pkg.tagline}/fmt(pkg.yearly)/{pkg.modLabel} are plan DATA (from SUB_PACKAGES / GET /subscription/plans), not static copy — bind to the fetched active plan, do not hardcode the 'Professional' literals. The static savings note 'ประหยัด 2 เดือน' inside priceYearlySuffix is faithful copy and stays.
    - This is a tenant screen (prefix sub), NOT admin.* — no owner-gating required; both header CTAs (ประวัติบิล / อัปเกรดแพ็กเกจ) are real client-side ctx.navigate() and are safe to keep as nav (no mutation).

## sub.plans
- **component:** SubPlans (pototype/subscription.jsx L132-184); depends on module-level SUB_PACKAGES (L6-22, plan data → server), SUB_PKG, MY_SUB.pkg (L26 mock current-plan), limitText (L38 → "ไม่จำกัด"/fmt), fmt (number format only)
- **getOp:** GET /subscription/plans — the plan cards. The port maps ONE <Card> per plan in the returned list (C1: 4 cards S/M/L/Full), replacing the hardcoded 3-entry SUB_PACKAGES constant; name/tagline/price/yearly/limits/modLabel all come from the response, not local constants. The "current package" flag (cur = p.id === MY_SUB.pkg → disabled "แพ็กเกจปัจจุบัน" button) is DERIVED: there is NO GET /subscription/mine, so match each plan id against the tenant's active plan taken from an active-marker field on /subscription/plans, or failing that derived from GET /subscription/invoices (latest paid plan). Monthly/yearly cycle toggle is pure client useState, no server call.
- **keys:** 30
- **uiSummary:** A <Page> (breadcrumbs บัญชีการใช้งาน › แพ็กเกจ & ราคา, title "แพ็กเกจ & ราคา · Plans", subtitle) whose header actions are (1) a monthly/yearly segmented toggle [รายเดือน | รายปี · ลด 2 เดือน] backed by client useState and (2) a primary "สมัครใช้งานใหม่" button. Body is a grid of plan cards (prototype: repeat(3,1fr) over the 3-entry SUB_PACKAGES; C1 port: 4 cards S/M/L/Full mapped over /subscription/plans). Each card shows a color chip + plan name + tagline; a price block ("ติดต่อทีมขาย" when price==null, else fmt(price)+"฿/"+ปี|เดือน depending on cycle); a full-width CTA — disabled "แพ็กเกจปัจจุบัน" on the active plan, "ดาวน์เกรด" for starter, "อัปเกรด" otherwise, or "ติดต่อทีมขาย" for the null-price top plan — that opens a confirm modal (title "เปลี่ยนเป็น {name}", body prefix/suffix around bold name, ยกเลิก/ยืนยัน) whose confirm fires a toast (ส่งคำขอ Enterprise · … OR เปลี่ยนเป็น {name} แล้ว); and a feature list (โครงการ / ผู้ใช้ (Seats) / พื้นที่จัดเก็บ + GB / AI ถอด BOQ + ครั้ง/ด., with "ไม่จำกัด" for -1) followed by the plan's modLabel. The popular plan gets a "ยอดนิยม" badge and a 2px brand border.
- **honestDivergences:**
    - CARD COUNT (C1): prototype hardcodes SUB_PACKAGES = 3 plans (Starter/Professional/Enterprise) and grid gridTemplateColumns:'repeat(3,1fr)'. Port MUST render 4 cards S/M/L/Full driven by GET /subscription/plans and make the column count data-driven (4), NOT hardcode 3. name/tagline/price/yearly/limits(projects,users,storage,ai)/modLabel are server fields, NOT the local constant.
    - PLAN-CHANGE IS MOCK → faithful toast: the CTA onClick opens ctx.openModal and the confirm button calls ONLY ctx.notify(...) — no PUT/POST. Phase-6 backend has no plan-change/subscribe mutation (only GET /subscription/plans + GET /subscription/invoices). Per actions=toast, keep the confirm as a faithful toast ('เปลี่ยนเป็น {name} แล้ว' / 'ส่งคำขอ Enterprise · ทีมขายติดต่อกลับใน 1 วัน'); do NOT wire a real mutation or fake a network write.
    - SIGNUP BUTTON has no backend: 'สมัครใช้งานใหม่' calls window.openSignup && window.openSignup(ctx) — a prototype global signup flow. No signup endpoint exists in Phase-6. Must be honest-disabled or open only the faithful signup modal/toast — it must not POST anything. Flag for a C-ruling.
    - CURRENT-PLAN FLAG is a hardcoded mock: cur = (p.id === MY_SUB.pkg) where MY_SUB.pkg = 'pro' (constant, L27). The 'แพ็กเกจปัจจุบัน'/disabled state must instead match the tenant's REAL active plan derived from /subscription/plans (active marker) or /subscription/invoices — compare against the server plan_id, not the local string key 'starter'/'pro'/'enterprise'.
    - MOCK PRICES/LIMITS render verbatim from server, not computed: price(2900/7900/null), yearly(29000/79000/null), limits(projects 2/10/-1, users 5/25/-1, storage 20/100/1000, ai 10/50/-1) are hardcoded in SUB_PACKAGES. Port must render these fields verbatim from the /subscription/plans response via fmt() (format only, never recompute). If the server does not yet return per-plan yearly price or the limit breakdown, those cells fall back to mock — surface any missing server field as a gap.
    - NULL-PRICE (Enterprise/Full) branch: the 'ติดต่อทีมขาย' price display, 'ติดต่อทีมขาย' CTA, 'ทีมขายจะติดต่อกลับ' modal subtitle, and 'ส่งคำขอ Enterprise · …' toast all key off price==null. Port must derive this from a null/absent price field on the server plan object, not from position or a hardcoded id.
    - MONEY GLYPH: price uses a bare hardcoded '฿' (U+0E3F) with no currency_code. Faithful to the prototype for display, but per root money rule the server amount carries currency_code — render the numeric via fmt() from the server and keep '฿/' as the static UI unit; do not invent a computed currency.

## admin.invoices
- **component:** <AdminInvoices> — pototype/subscription-admin.jsx L193-238. Child render helper: openNotifySend(ctx, org) — pototype/real-forms2.jsx L207-215 (the "ทวงถาม" dunning confirm dialog + toast). KPI cards via AdminKpi→Kpi (L50); status via StatusBadge; layout via Page/Card/Btn.
- **getOp:** GET /admin/invoices (admin.ts:163) — supplies the table rows (no, org, date, amount, status). The 3 KPI cards are derived:<over the same /admin/invoices list> — ออกบิลรอบนี้ = SUM(amount) of all rows + COUNT(rows); ชำระแล้ว = SUM(amount) where status==='paid'; ค้างชำระ = SUM(amount) + COUNT where status!=='paid'. There is NO separate summary/KPI endpoint and NO reminder/export POST route in the Phase-6 merged set.
- **keys:** 23
- **uiSummary:** Platform-owner invoice/revenue table. Header has a title/subtitle + one "ส่งออก" (export-all) outline button. Below: a 3-up KPI row (ออกบิลรอบนี้ = total billed this cycle with count; ชำระแล้ว = paid sum; ค้างชำระ = outstanding sum with count), all derived by sum/filter over the invoice list. Then a single Card wrapping a 6-column table: เลขที่ / องค์กร / วันที่ / จำนวน (฿) right-aligned / สถานะ (StatusBadge, tri-state paid→approved, pending→pending, overdue→rejected) / actions. The action cell is conditional: overdue rows show a "ทวงถาม" (dunning) soft button that opens a confirm dialog (ส่งแจ้งเตือนลูกค้า) + audit-log toast; all other rows show a ghost "ดู" button firing a download toast. No create/edit; read + two mock side-effects only.
- **honestDivergences:**
    - MOCK DATA → GET /admin/invoices: the 5 hardcoded rows (PINV-2569-0612…-0608 with orgs/dates/amounts/statuses, L194-200) must be replaced by the fetched /admin/invoices list. Do not port the literal rows.
    - FK-NAME-AS-TEXT: x.org is a free-text org name string ('บมจ. สยามพร็อพเพอร์ตี้' etc). In production the invoice row references a subscriber/tenant by *_id; render the joined org display name from /admin/invoices, not a hardcoded name.
    - KPI VALUES ARE COMPUTED, NOT STATIC: total/paid-sum/outstanding are derived via reduce/filter over the array (L202-203, L212-214). Faithful behavior = derive them over the fetched list (or server aggregate). They are NOT hardcoded mock numbers, so do not freeze them — recompute from real rows.
    - EXPORT (actions=toast): the 'ส่งออก' button only calls ctx.notify('ส่งออกใบแจ้งหนี้ทั้งหมด') (L209). No export/PDF endpoint exists → keep as a faithful toast; produce no file (or honest-disabled if toast-on-noop is disallowed).
    - ROW 'ดู'/download (actions=toast): the ghost view button only fires ctx.notify(`ดาวน์โหลด ${no}`) (L230). No invoice-download route → faithful toast only, no real download.
    - 'ทวงถาม' DUNNING (actions=toast): the overdue button opens openNotifySend — a confirm dialog then ctx.notify('ส่งแจ้งเตือน {org} แล้ว · บันทึกใน Audit Log') (real-forms2.jsx L207-214). No reminder/notification POST route in Phase-6 → reproduce the confirm+toast faithfully, but nothing is sent and NOTHING is written to the audit log despite the toast copy.
    - OWNER GATING: admin.* is platform-owner-only. /admin/invoices is an admin endpoint — gate this route to the owner/platform-admin role; ordinary tenant users must not reach it.
    - CONDITIONAL ACTION (preserve exactly): status==='overdue' → 'ทวงถาม'; every other status → 'ดู' (L230). Keep this branch verbatim, do not show both.
    - STATUS MAPPING (preserve): invoice status paid/pending/overdue maps to StatusBadge tones approved/pending/rejected (ST, L201) with Thai labels ชำระแล้ว/รอชำระ/ค้างชำระ. Drive from the row's real status field.
    - DATE FORMAT: mock dates are Buddhist-era short strings ('1 มิ.ย. 69'). Production date comes from /admin/invoices — format faithfully to the BE short form, not raw ISO.
    - LABEL DEDUP: 'ชำระแล้ว' and 'ค้างชำระ' each appear twice (KPI label + status badge). Use one key each (statusPaid/statusOverdue) in both spots — do not mint duplicate keys.

## admin.plans
- **component:** <AdminPlans> at pototype/subscription-admin.jsx:179-190. Renders <PkgAdminGrid> and opens the create/edit modal via openPkgBuilder → <PkgBuilderForm>; both PkgAdminGrid (187), openPkgBuilder (62) and PkgBuilderForm (71) are defined in pototype/pkg-builder.jsx. No AdminKpi row (unlike AdminOverview/AdminInvoices). The tenant-side helpers in pkg-builder.jsx (PkgDemoSwitcher, setTenantPkg, AiQuotaChip, openAiQuotaModal) are NOT part of admin.plans.
- **getOp:** GET /admin/packages (admin.ts:117) — plan/package cards: {size, name, price, yearly, color, popular, limits:{projects,users,storage,ai}, menus[]}. Per-card subscriber count is DERIVED from GET /admin/subscribers (admin.ts:137): count where pkg===package.id && status!=='cancelled' (mirrors PkgAdminGrid subsCount at pkg-builder.jsx:194, which reads mock window.SUBSCRIBERS). The "menus released" denominator (menus.length / total) uses the client NAV registry length (pkgAllIds, pkg-builder.jsx:21), not a server value. No POST/PUT /admin/packages route exists on merged routes (only GET /admin/packages and GET /admin/packages/:id at admin.ts:117,125), so the create/edit/save write path has no backend.
- **keys:** 41
- **uiSummary:** Owner-only platform screen. Page header = breadcrumbs (ผู้ดูแลแพลตฟอร์ม › จัดการแพ็กเกจ), title, subtitle, and a primary 'สร้างแพ็กเกจ' action that opens the package builder modal. Body = PkgAdminGrid, a 4-column card grid (currently seeded Starter/Professional/Business/Enterprise). Each card shows a size badge (S/M/L/Full), name, an optional 'ยอดนิยม' tag, price line (monthly ฿/ด. · yearly ฿/ปี, or 'ติดต่อทีมขาย' when price is null), three quota rows (โครงการ/ผู้ใช้, พื้นที่ GB, AI ถอด BOQ with ∞/ไม่จำกัด for -1), a 'เมนูที่ปล่อย n/total' progress bar with up to 5 nav-label chips + a '+N' overflow chip, a 'ผู้สมัคร: n ราย' footer, and an 'แก้ไข' ghost button. The builder modal (PkgBuilderForm) has a S/M/L/Full preset picker, name/price/projects/users/storage/AI fields, a 'ติดต่อทีมขาย' contact-price checkbox, and a live NAV-derived checkbox menu-tree with per-group indeterminate/select-all, saving with a toast.
- **honestDivergences:**
    - Owner-gating: admin.* is a platform-owner/superadmin surface. The port must gate admin.plans behind the owner role (do not render for tenant users), matching the admin.* screen family.
    - Package list source is mock: the grid reads a client-only PKG_STORE seed hardcoded in pkg-builder.jsx:41-45 (Starter 2900/29000, Professional 7900/79000 popular, Business 14900/149000, Enterprise price=null). The port MUST fetch GET /admin/packages and render its payload verbatim — do NOT hardcode these prices/limits/colors/popular flags.
    - Subscriber count is mock: subsCount reads window.SUBSCRIBERS (pkg-builder.jsx:194). Port must derive it from GET /admin/subscribers (count where pkg===package.id && status!=='cancelled'), not a static array.
    - Create/Edit/Save have NO backend: 'สร้างแพ็กเกจ'/'แก้ไข'/'บันทึกแพ็กเกจ' call PKG_STORE.save() (client store + emit, pkg-builder.jsx:50,102). Merged routes expose only GET /admin/packages and GET /admin/packages/:id (admin.ts:117,125) — no POST/PUT. Per the mock-strip rule these write actions must become honest-disabled OR a faithful toast (actions=toast); the save toast (savedToast key) must fire without persisting. Confirm with backend before wiring a write path; if none exists, escalate to BLOCKERS.md.
    - yearly is a mock derivation: PkgBuilderForm computes yearly = price*10 (pkg-builder.jsx:105). The card's ฿/ปี must render the server-provided yearly, not recompute; server should own yearly.
    - Menu-tree is built from live window.NAV via pkgNavGroups (pkg-builder.jsx:7-20): group labels (g.label) and item labels (i.label) plus '(subs)' sub-counts are NAV-registry data, NOT screen i18n keys — do not mint keys for them. The card chips call labelOf(id) to resolve a saved nav id → its nav label; persist menu IDs, never labels. The 'menus.length/total' denominator is client pkgAllIds() length, not a server field.
    - ∞ (U+221E) is rendered as a literal symbol for unlimited limits (projects/users/storage, pkg-builder.jsx:209-210); it is a non-translatable glyph constant, not extracted as copy. 'ไม่จำกัด' (AI row + checkbox) IS translatable and is keyed.
    - Do NOT port the tenant-side helpers that share pkg-builder.jsx — PkgDemoSwitcher, setTenantPkg, AiQuotaChip, openAiQuotaModal, tenantPkg (lines 232-322). They belong to sub.mine / sidebar simulation, not admin.plans.
    - PKG_STORE.remove() exists (pkg-builder.jsx:51) but NO delete button is rendered anywhere in the grid or builder — do not invent a delete affordance; port only the edit path that exists.
    - Mock numeric placeholder '7900' (price input, pkg-builder.jsx:135) is example data, not extracted as i18n; render it as-is only if a placeholder is retained.

## admin.subs
- **component:** <AdminSubscribers> — pototype/subscription-admin.jsx L113-176 (Platform-Admin subscriber table). Includes its row-click detail modal body <CompanyControl> (L241-371, mounted via openDetail L124-129) + the nested resetPw confirm modal (L254-265). Module-level SUB_ST status labels (L16) are consumed for badges + the status filter.
- **getOp:** GET /admin/subscribers (admin.ts:137) = the table rows. The "{count} ราย" badges + subtitle count are derived (rows.length / SUBSCRIBERS.length over the fetched list; client-side search/pkg/status filter). CompanyControl detail modal additionally consumes: GET /admin/users (admin.ts:150) filtered by company = the "ผู้ใช้งาน" roster tab; GET /admin/packages (admin.ts:117) = package names + quota limits (projects/users/storage/AI, "ไม่จำกัด"/"∞") shown in the control tab and quota preview. overSeat + activeUsers counts = derived over /admin/users. No /admin/overview route is involved. NOTE: every write in the modal (save/suspend/activate/block/unblock/reset-pw/invite/export) has NO merged Phase-6 backend — GETs only.
- **keys:** 67
- **uiSummary:** Platform-Admin (owner) subscriber-management screen. Page header: breadcrumbs [ผู้ดูแลแพลตฟอร์ม › จัดการผู้สมัคร], title "จัดการผู้สมัคร · Subscribers", count-driven subtitle, and an outline "ส่งออก" (CSV export) action. Body = one Card: a filter bar (org/id search input, package filter Dropdown, status filter Dropdown, right-aligned "{count} ราย" tally) over a 7-column table (องค์กร / แพ็กเกจ / โครงการ / ผู้ใช้ / MRR (฿) / ต่ออายุ / สถานะ) with a StatusBadge per row and overdue renewal text tinted red. Clicking a row opens an XL modal <CompanyControl> with a summary header (status badge, package dot, cycle รายปี/รายเดือน · MRR, ผู้ใช้ x/y · โครงการ x/y usage) and two tabs: (1) "ควบคุมแพ็กเกจ & สิทธิ์" — package select, Seats stepper (−/∞/+), quota preview grid (โครงการ/ผู้ใช้/พื้นที่/AI), an over-seat warning banner, and suspend/activate + cancel + "บันทึกการตั้งค่า" actions; (2) "ผู้ใช้งาน (N)" — a summary line, "เชิญผู้ใช้" button (disabled when over-seat), and a roster table (ชื่อ–สกุล/บทบาท/เข้าใช้ล่าสุด/สถานะ/จัดการ) with per-row reset-password (opens confirm modal) and block/unblock. All writes fire mock ctx.notify toasts only.
- **honestDivergences:**
    - OWNER-GATED: admin.subs is a Platform-Admin/owner console, NOT a tenant admin.* screen. Port must gate the route to owner/platform-admin role and rely on server authz over /admin/* — non-owner tenants must not reach it.
    - MOCK DATA → /admin/subscribers: the module-level SUBSCRIBERS array (T-1001…T-1009 with hardcoded mrr/projects/users/status/cycle) must render from GET /admin/subscribers, not literals. Row identity is a tenant/company id — key rows by *_id, not by org-name-as-text (org is display only).
    - renew is mock free-text: values like 'ทดลอง 14 วัน', 'ค้างชำระ 8 วัน', 'ยกเลิก 1 มิ.ย. 69', '31 ธ.ค. 69' are pre-baked strings. Server should supply structured renewal date/status; the red-tint logic keys off status==='overdue'. Do NOT hardcode these renewal strings as i18n.
    - Hardcoded MRR/counts must be server-driven, not literal: mrr numbers, the '{count} ราย' tallies, and subtitle count derive from the fetched list. The '—' (em dash U+2014) shown for mrr==0 must render verbatim, never '0'.
    - Export is mock (actions=toast): the 'ส่งออก' button only fires ctx.notify('ส่งออกรายชื่อผู้สมัคร (CSV)') — no export endpoint exists in the merged Phase-6 backends. Keep a faithful toast; do NOT wire a fake CSV download.
    - ALL CompanyControl writes are mock toasts with NO backend (only GETs merged): save-settings ('บันทึกการตั้งค่า'→saveToast), suspend ('ระงับการใช้งาน'), activate ('เปิดใช้งาน'), block/unblock, reset-password ('ส่งลิงก์รีเซ็ต'), invite-user ('เชิญผู้ใช้'). There is no PUT/PATCH/POST on /admin/subscribers or /admin/users. Each must become a faithful ctx.notify toast (actions=toast) or honest-disabled — never a real mutation. The seat stepper and block toggle mutate local React state only; persistence is mock.
    - Per-company roster is fabricated: COMPANY_USERS + companyUsers(id,n) generate fake names/emails/roles/last-login sized to the mock user count. Must consume GET /admin/users filtered by company. The u.role.includes('ผู้ดูแล') avatar/tag coloring is a Thai-substring heuristic on mock role text — in prod role must be a code/id, not a localized-string match (would break under i18n).
    - overSeat / activeUsers derive from mock roster length: the 'ผู้ใช้ที่ใช้งานจริง (N) เกิน…' banner and the x/y usage counters compare fabricated active-user count against a locally-edited Seats value. Must derive from real /admin/users counts + the package limit from /admin/packages.
    - Quota limits are client-side config: quota preview (projects/users/storage/AI, 'ไม่จำกัด', '∞') reads SUB_PACKAGES/SUB_PKG in-browser. Should read GET /admin/packages limits. '∞' (U+221E) and 'ไม่จำกัด' render for limit<0 — keep the glyph/text verbatim.
    - 'ยกเลิก' collides: SUB_ST.cancelled status label and the generic cancel button share th='ยกเลิก' but are distinct fields (admin.subs.statusCancelled vs admin.common.cancel) — do not merge, one drives a badge, the other an action.
    - Skip the DOM-MutationObserver translation mock entirely (if wired at file scope) — real i18n must go through key lookup from i18n-full.json, not runtime DOM text swapping.

## line
- **component:** LineOAPreview
- **getOp:** static
- **keys:** 51
- **uiSummary:** Fiori Page with a 3-column grid (300px / 1fr / 280px), fully STATIC — no server data. Header: breadcrumb ["ระบบ", "LINE OA · ลูกบ้าน"], title, subtitle, and two toast-only action buttons (QR Code outline + ตั้งค่า OA primary). LEFT column: a Card listing all 16 LINE screens from the local LINE_SCREENS const (header "Screens · {n} หน้าจอ"), each row = icon + label + group name; the selected row gets brand-soft bg + 3px brand left-border + chevR. CENTER column: an iPhone-style LINE chat mockup (390×844 green LineFrame with iOS status bar 9:41, LINE-green header "juneflow ลูกบ้าน OA · ออนไลน์ · ตอบกลับใน 5 นาที") that swaps in the selected screen's hardcoded preview component (12 in line-oa.jsx + 4 PM screens in line-pm.jsx). RIGHT column: three feature Cards — (1) OA stats card (@juneflowservice, "248 ผู้ติดตาม · 92 active", two stat tiles "1.2k / ข้อความ/เดือน" and "5 นาที / ตอบเฉลี่ย"), (2) "คุณสมบัติเด่น" 8-item bullet list, (3) a LINE-green CTA card "ตั้งค่า OA ระดับสูง" with an เข้าตั้งค่า toast button. The manifest below keys ONLY the outer real-product chrome; the inner phone mockups are demo fixture (see honestDivergences).
- **honestDivergences:**
    - getOp=static CONFIRMED: this screen consumes NO backend. It renders entirely from the local LINE_SCREENS const array + hardcoded mock preview components. None of the Phase-6 endpoints (/subscription/*, /admin/*) apply, and the sub.mine/admin.overview derivation note in the brief is N/A here.
    - actions=toast (3 buttons): QR Code (L608) and ตั้งค่า OA (L609) header buttons, plus เข้าตั้งค่า green-card button (L701), all call ctx.notify(...) mock toasts. Port must render as faithful toast() calls with the verbatim text (line.preview.qrToast / settingsToast) — do NOT wire to any real QR/LINE-Developer-Console backend (none exists). Not honest-disabled; they intentionally just toast.
    - INNER PHONE MOCKUPS ARE VERBATIM DEMO FIXTURE, NOT KEYED PRODUCT COPY: the 16 preview components (LineHome, LineBind, LiffReport, LineReport, LiffTrack, LineTrack, LinePush, LineWarranty, LineCommon, LinePayment, LinePromo, LineSales in line-oa.jsx + LinePMPlan, LinePMQuote, LinePMCert, LinePMContracts in line-pm.jsx), plus the shared LineFrame/Bubble/CardBubble/QuickReplies/InputBar chrome ('juneflow ลูกบ้าน', 'OA', '9:41', 'ออนไลน์ · ตอบกลับใน 5 นาที', 'พิมพ์ข้อความ...', 'อ่านแล้ว', frame titles แจ้งซ่อม/ติดตามสถานะ/แจ้งเตือน/ผูกบัญชีลูกบ้าน), render ~200 hardcoded Thai chat/card strings. These are illustrative screenshot content and have ZERO keys in i18n-full.json. RECOMMENDATION: keep them as inline verbatim fixtures (const arrays in the ported component) rendered byte-for-byte, NOT exploded into ~200 line.preview.* keys and NOT injected into sacred i18n-full.json. This DEVIATES from the 'every visible word = i18n key' rule, so it needs a Wei ruling / BLOCKERS.md entry to authorize the inline-fixture approach for this static-preview screen. The manifest above keys ONLY the outer real-product Page chrome.
    - GLYPH LANDMINES inside the mock fixture (byte-for-byte traps — copy raw, never ASCII-ify): (1) L489 LinePush 'ระยะเวลาตอบกลับ' row value has an INVISIBLE U+2064 (INVISIBLE PLUS) immediately before '1 ชม.' — verified in bytes; (2) baht ฿ U+0E3F in every money row (e.g. '4,850,000 ฿', '473,500 ฿', '47,350 ฿', '3,200 ฿', '96,000 ฿/ปี', '50,000 ฿'); (3) middot U+00B7 pervasive; (4) en-dash U+2013 in LinePMCert 'เวลา' = '09:14 – 10:48 น.'; (5) stars ★ U+2605 (e.g. '★5', '★ 4.8') and empty ☆ U+2606 in LiffTrack rating row; (6) em-dash U+2014 in the outer subtitle. Grep the raw prototype line before compiling each fixture value.
    - HARDCODED MOCK NUMBERS/IDS render verbatim, never computed or derived from a store: outer chrome '248 ผู้ติดตาม · 92 active', '1.2k', '5 นาที'; and all in-mock values — SR-2026-0048, BK-2026-0184, CT-2569-0084, QT-2569-0142, PMWO-2569-0312, MT-2569-018, badge counts ('3 ครั้ง','ปีนี้ 12 ครั้ง','42 วัน','อีก 12 วัน','2 รายการ'), warranty 'เหลือ 11 เดือน', payment '7/10 · 331,450 ฿'. ('Screens · {n} หน้าจอ' may stay computed since {n}=LINE_SCREENS.length from the local static list.)
    - FK-NAME-AS-TEXT is intentional mock, do NOT normalize to *_id here: person/entity names inside the fixture ('ช่างวิชัย · ประปา', 'ช่างวิชัย มีชัย', 'นภัส ใจดี', 'รุจิรา ทองสุข', 'สมพงษ์ ก.', 'คุณภัทร์รดา ดวงดี', 'คุณวรรณา ศรีจันทร์', 'Pattara_Real 🐱', unit 'B-08', contract/booking ids) are demo literals for a static screenshot — because this screen never talks to the backend, they stay as verbatim fixture text and must NOT be wired to real entity ids or lookup endpoints. (This is the opposite of a real data screen where FK-name-as-text would become *_id.)
    - NO owner-gating / NO honest-disabled needed: this is a mod:lineoa preview page (NAV-ROUTES: line → LineOAPreview), not an admin.* screen — no role/owner guard. The only interactive elements in the outer chrome are the 3 toast buttons above; there are no create/edit buttons lacking a backend to disable.
