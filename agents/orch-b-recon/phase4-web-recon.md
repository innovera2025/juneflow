# Phase-4 Web Recon — Subcon+Accept (R1/B-105) · PM (R2/B-106)

_orch-B recon-first prep · read prototype .jsx only · 2026-07-19_

**i18n draft:** R1 = 289 net-new (subcon.* + accept.*) +6 reused common · R2 = 261 net-new (pm.*) +4 reused common. All glyph-verified, collision-free, en=zh=ar=th.

## Per-screen recon (for the port lane)

### subcon.progress  ·  SubconProgress + CheckRow + SubconSummaryReport (modal) + SubconAddForm (modal)
- **contract ops:** GET /subcontractors — list (left panel): name, type, contracts count, totalValue, paid, retention, completion%, status(active|closing|pending), GET /subcontractors/{id} — detail: header stats (totalValue/paid/remaining/retention/variation net), active work-order contract (WO no, scope, start/due dates, value), GET /subcontractors/{id}/installments — payment schedule rows (no, label, pct, value, paid, retention, GR ref, date, status done|current|pending) + totals, GET /subcontractors/{id}/variations — variation orders (VR no, date, reason, add/cut value, status) + adjusted contract value, GET /subcontractors/{id}/closing — closing checklist state (all-periods, handover+inspection, retention 50% on handover, retention 50% after 12mo warranty) + remaining payable + retention, GET /subcontractors/summary — aggregate report modal: totalContracts, totalValue, totalPaid, totalRetention, avgCompletion, per-subcon rows, POST /subcontractors — create new subcontractor; ALSO registers into central Vendor Master as type 'รับเหมา' (returns generated code), POST /subcontractors (bind existing) — link an existing Vendor Master vendor (from GET /vendors?type=รับเหมา pool) as a subcontractor + open WO, GET /vendors?type=รับเหมา — vendor pool for existing-mode dropdown (window.vendorsBy('รับเหมา')): code, name, taxId, term, bank, spend, POST /subcontractors/{id}/installments/{n}/approve — approve installment → trigger payment (state transition current→done), POST /subcontractors/{id}/variations — create variation order (VR), export/print report ops (Export Excel / print report) — client-side or report endpoint, GR (goods receipt) and WO (work order) linkage referenced but owned by procurement flow
- **port notes:** MOCK MECHANICS TO STRIP: (1) SUBCONS, PROGRESS_PAYMENTS, VARIATIONS const arrays at top of file → replace with API data. (2) useState(1)/setSelId local selection → wire to selected subcontractor id. (3) window.vendorsBy('รับเหมา'), window.VENDOR_SEED mutation, and 'V-00'+random code generation in SubconAddForm.submit → replace with real vendor pool fetch + POST create. (4) ctx.openModal / ctx.notify → real modal + toast infra. (5) All fmt()/.toFixed()/.slice() computed display values are DATA.

KEYED-BUT-BORDERLINE: 'M ฿' (subcon.unitMBaht) is a MiniKpi unit suffix — could instead be static display formatting; flagged for reviewer. Column header '%' (L154) = bare percent symbol → NOT keyed (ICON_OR_NUMBER).

NOT KEYED (codes/proper nouns, exclude): grade options 'A','B','C' (single-letter codes; only 'A — พิเศษ' and 'ใหม่ · รอประเมิน' keyed); bank options KBANK/SCB/BBL/KTB/BAY/TTB (L508, proper-noun bank codes from config); taxId placeholder '0105540084234' (L479, pure-digit example → ICON_OR_NUMBER); 'รับเหมา' filter arg (L397/L421/L423, vendor-type data key, not rendered as this screen's copy — though it is shown quoted INSIDE the infoNew string which IS keyed).

REUSE MAP (identical bytes → ONE key): 'มูลค่าสัญญารวม'=subcon.statContractTotal (L55/119/300); 'Retention ค้าง'=subcon.statRetention (L57/122/310); 'คืน 12 เดือนหลังส่งงาน'=subcon.retentionReturn12mo (L57/122); 'จ่ายไป'=subcon.statPaid (L120/305); 'จ่ายแล้ว'=subcon.colPaid (L156/182/330); 'Retention'=subcon.colRetention (L157/331); 'สัญญา'=subcon.unitContract (L85/302/328); 'ผู้รับเหมา'=subcon.subcontractor (L33/326); 'ปิดสัญญา'=subcon.statusClosing (L83/256/363); 'เดิน'/'รอเริ่ม' status reused L83+L363; 'ประเภทงาน'=subcon.fieldWorkType (L444/472); common.status (L159/215/333); common.total (L190/370); common.cancel (L461/522).

THREE NEAR-DUPLICATE 'PAID' STRINGS kept SEPARATE (different bytes): 'จ่ายไปแล้ว' (L56), 'จ่ายไป' (L120/305), 'จ่ายแล้ว' (L156/182/330). Also 'งานเพิ่ม/ลด' (L123, no spaces) ≠ 'งานเพิ่ม / ลด (Variation Orders)' (L205, spaced+EN). 'มูลค่า' (L155/214) ≠ 'มูลค่า (฿)' (L329). 'คืบหน้า' (L332) ≠ 'ความคืบหน้าเฉลี่ย' (L315).

GLYPH FIDELITY (verified via codepoint dump): all inline separators = middot U+00B7 (·) not ASCII dot; toast/infoNew quotes around name/รับเหมา = ASCII double-quote 0x22 (NOT curly “ ”); dashes 'A — พิเศษ' & both info lines = em-dash U+2014; arrows in infoExisting L458 & approve toast L255 = U+2192 (→); ฿ = U+0E3F. 'ประปา-สุขาภิบาล' uses ASCII hyphen-minus.

MODALS INCLUDED IN THIS SCREEN: SubconSummaryReport (report modal, opened from 'รายงานสรุปทุกราย' button) and SubconAddForm (add modal, opened from 'เพิ่มผู้รับเหมา' button; has two modes — 'existing' bind-from-vendor-registry vs 'new' full form — toggled by tabExisting/tabNew). infoNew contains a <b> span wrapping 'ทะเบียนผู้ขายกลาง (Vendor Master)'; keep i18n value as full plain text and re-apply bold in the port.

PLACEHOLDER SKELETONS: KPI subs, list-row counts, contract meta line, closing note/info, approve button+toast, and both submit toasts are static skeletons with data tokens ({n},{count},{pct},{value},{value2},{name},{code},{desc},{start},{due},{range}) — the interpolated values are DATA; only the surrounding words are keyed.

### subcon.contracts  ·  SubcKpi (L65) · SubconContracts (L72) · openSubcContractForm (L116) · SubcContractForm modal (L123) — pototype/subcon-accept.jsx
- **contract ops:** GET list subcontractor contracts (WO/PO) with fields: no, subcon, project, scope, method (percent|distance|unit|milestone), value, retentionPct, signed, po, status, and nested periods[] (no, label, pct|qty, value, state accepted|requested|pending|rejected, gr, date, defect?, inspectCount?); plus distance/unit meta (unit, totalQty, ratePerUnit, perPeriodQty, doneQty, accSurvey), POST create subcontractor contract/WO (payload: subcon, contractValue, method, retentionPct, and for distance: unit, totalQty, perPeriodQty, ratePerUnit) — server derives period breakdown, GET company/subcontractor options for the create-modal dropdown (currently mock: บจก. รุ่งเรืองก่อสร้าง / ประปาไทย เซอร์วิส / ไฟฟ้าอินเตอร์), navigate to subcon.accept?wo={no} on row click (acceptance detail — different screen, out of scope), export contracts register (client action / GET export) — currently only fires a toast, KPI aggregates (active count, total value, periods awaiting acceptance, distance-method count) computed client-side from the list via reduce/filter — can be server-provided or derived from list response
- **port notes:** Mock mechanics to strip: SUBC_CONTRACTS records array (all row/period values = DYNAMIC_DATA, excluded — doc nos WO-2026-xxxx, PO/GR nos, company names บจก./หจก. ..., project names, money 2_150_000, dates '08 ก.พ. 69', pct/qty, defect text). SUBC_METHOD and PERIOD_STATE are LABEL/CONFIG lookup maps, NOT record arrays — I classified the 4 SUBC_METHOD `.l` labels as STATIC_UI_COPY enum labels (they are fixed UI copy for method codes percent/distance/unit/milestone, rendered in the table cell, the method-picker buttons, and the create toast). Flagging this as my one classification judgment call: they live in a const object but function as translatable enum labels, matching how status/method chips were keyed on merged master screens. PERIOD_STATE labels (ตรวจรับแล้ว/ขอตรวจรับ/ยังไม่ถึง/ตีกลับแก้ไข) and ACCEPT_CHECKLIST are NOT rendered in these three components (only in the out-of-scope SubconAccept detail page) → not keyed here.

KPI values (SubcKpi→MiniKpi): all `value` props are computed from the list (length/reduce/filter) = DYNAMIC; only label/unit/sub are keyed. SubcKpi itself has zero hardcoded strings (pure prop passthrough).

Modal: openSubcContractForm (ctx.openModal with title/subtitle keyed) + SubcContractForm body. Form local state: method picker (useState), unit dropdown, totalQty/perPeriod/rate numeric inputs (numeric defaults 1750/100/1000/5 excluded). Distance panel is conditional (method==='distance') and shows live auto-calc: nPeriods = Math.ceil(totalQty/perPeriod), per-period value = perPeriod*rate → both interpolated into subcon.distanceCalcHint. Company dropdown options are mock data → wire to real subcontractor list.

Separators/punctuation NOT keyed: the standalone ' · ' between {c.scope} and {c.project} (L101) and the empty-cell '—' (L105 else branch) are literal glue, not UI copy. '{pct}%' progress % (L104) is a symbol on a dynamic number → excluded.

Glyph watch for the i18n round: middot · = U+00B7 (title/subtitle/hint/detail); ฿ = U+0E3F (M ฿, มูลค่า (฿), มูลค่าสัญญา (฿), distanceCalcHint); em-dash — = U+2014 (modalSubtitle); arrow → = U+2192 (distanceCalcHint). distanceCalcHint matches the exact skeleton given in the task prompt example.

Reused/shared keys: subcon.subconLabel (ผู้รับเหมา) = breadcrumb crumb1 (L76) + subcon field label (L134); subcon.unitContract (สัญญา) = KPI unit L80 + L83; subcon.unitMeter (เมตร) = select option + useState default. common.export and common.cancel reused on exact byte match.

### subcon.accept  ·  SubconAccept (L5-152) + AcceptForm modal (L153-244)
- **contract ops:** GET list subcontractor work-orders/contracts for the header Dropdown (SUBC_CONTRACTS -> id + subcon name), GET one contract detail incl its periods, method, retentionPct, scope, project (SUBC_CONTRACT(woNo) -> periods[] with state requested|accepted|rejected|pending, value, qty/pct, gr, inspectCount, defect), GET project construction progress % (percent method gate: PROJECT_PROGRESS/projPct) and distance/unit survey progress (accSurvey/doneQty/totalQty), POST accept a period -> transition requested->accepted, issue GR (GR-YYYY-#### number), post AP liability = measuredVal - retention, hold retention = measuredVal*retentionPct/100, persist checklist results + measured qty + photos flags + attached docs, POST reject a period -> transition requested->rejected, record Defect note (fail-item list or default 'มีรายการไม่ผ่านการตรวจ'), POST request re-inspection -> transition rejected->requested, increment inspectCount, GET DMS documents filed per contract (SUBC_DMS_DOCS[c.no]) + POST upload/attach acceptance photos & docs into DMS bound to contract, navigation targets: subcon.handover, timeline, dms (routing, not data ops)
- **port notes:** MOCK MECHANICS TO STRIP: SUBC_CONTRACTS / SUBC_CONTRACT(woNo) / SUBC_METHOD / SUBC_DMS_DOCS / PERIOD_STATE / PROJECT_PROGRESS are external mock constants (defined elsewhere, not in this file) — replace with generated API client calls. Local `ov` state + P()/setPeriod() are in-memory period-override overlays simulating state transitions; must become real mutations. GR number `"GR-2026-0" + (200+Math.floor(Math.random()*90))` and date `"วันนี้"` are fabricated on accept — server assigns. AcceptForm attach doc name `เอกสารแนบ-${n}.pdf` + random size are mock file generation. photos = 3-slot boolean toggle array; checks = tri-state array (none|pass|fail) over ACCEPT_CHECKLIST.

ACCEPT_CHECKLIST (L154,187,189,196) is an EXTERNAL data array (not in this file) of inspection-item labels — its {label} values (L195) are treated as DYNAMIC_DATA per the data-array rule, so I proposed NO keys for individual checklist labels. If these are meant to be fixed UI copy they must be recon'd from wherever ACCEPT_CHECKLIST is defined (separate round). Flagging for the port.

DYNAMIC (correctly excluded, no keys): method badge {m.l}+{m.icon}; subtitle `${c.no} · ${c.subcon} · ${c.scope}`; Dropdown option labels `${x.no} · ${x.subcon}`; KPI values fmt(...); {c.project}; qty cell `${p.qty} ${c.unit}` / `${p.pct}%`; StatusBadge {st.l}; {p.gr}; {p.l} detail; DMS filenames {d}; {contract.unit}; {d.name}/{d.size}.

SYMBOLS INTENTIONALLY NOT KEYED (ICON_OR_NUMBER): '✓' milestone cell (L99); '—' empty-doc dash (L101); trailing ' ✓' on filled photo label (L209). These are decorative glyphs, keep as literals in port. NOTE the milestone/percent column header IS a 3-way ternary (ปริมาณ/'%'/'Milestone') at L88 — I keyed all three branches (colQty, colPct '%', colMilestone).

MODALS INCLUDED: (1) warn 'cannotAccept' modal via ctx.openModal (percent method when cumMap[p.no] > projPct) — L24-25; (2) accept modal wrapping <AcceptForm> — L28-32. AcceptForm renders three method-conditional banners (percent / distance-unit measure input / none), checklist, 3 photo slots, attach-docs list w/ empty-state, fail warning, footer payment math, reject + accept buttons (accept disabled unless allChecked && failN===0).

GLYPH WATCH for port compile/G5: middot · U+00B7 (many toasts/legends), ฿ U+0E3F, em-dash — U+2014 (L24 body, L220, L230), MINUS SIGN − U+2212 (L232 paymentBreakdown — NOT ASCII hyphen), arrow → U+2192 (L235), warn ⚠ U+26A0 (L98), check ✓ U+2713 (L174). ampersand '&' in pageTitle. English literals kept verbatim: 'Timeline','Milestone','GR','AP','Defect','DMS','GPS','Shop Drawing'. Multi-percent skeletons use two distinct tokens ({pct}+{target}) — L24 body, L173, cannotAcceptSubtitle. SubconHandover (L245+) explicitly IGNORED (separate screen subcon.handover).

### subcon.handover  ·  SubconHandover
- **contract ops:** GET one subcontract/work-order by params.wo (default SUBC_CONTRACTS[0].no) returning: no, po, subcon, project, scope, method, retentionPct, and periods[] (each with no, l, state, value, date). Screen is READ-ONLY — it filters periods where state==='accepted', computes acceptedVal=sum(value), retention=round(acceptedVal*retentionPct/100), allDone=every period accepted., Method label lookup (SUBC_METHOD[c.method].l) — from method reference/enum., Print action = client-side (ctx.notify stub 'ส่งพิมพ์เอกสารส่งมอบงาน'); no backend mutation on this screen., Navigation only: 'กลับงวดงาน' → subcon.accept?wo=c.no. No create/update/delete ops here.
- **port notes:** Scope: SubconHandover (L245-311) ONLY — everything above L245 (SubconAccept, AcceptForm) is a different screen; do NOT port it. Object.assign at L313 exposes SubconHandover.

Mock mechanics to strip: SUBC_CONTRACT((params&&params.wo)||SUBC_CONTRACTS[0].no) mock lookup → API fetch of one contract with periods; SUBC_METHOD[c.method].l map → method label from data; computed accepted/acceptedVal/retention/allDone stay client-side derivations over fetched period data. Screen is a printable Handover/Acceptance certificate — no modals, no create/edit, only 2 header actions (navigate back to subcon.accept, and print via ctx.notify).

Data vs copy pitfalls: (1) The label/value field grid (L264) pairs a STATIC label with a DYNAMIC value — only the six labels are keys; c.no/c.po/c.subcon/c.project/c.scope/methodLabel are data. (2) The accepted-period table body (L274-280) is entirely data. (3) tfoot value '−{fmt(retention)}' — the '−' (U+2212) is a numeric sign on a dynamic figure, NOT copy. (4) In conditionText the literals '12 เดือน', '50%', '50%' are hardcoded warranty-policy constants in the static copy — keep verbatim (they are NOT c.retentionPct). Only subcon.retentionDeduct in the tfoot interpolates c.retentionPct as {pct}.

Reuse: 'ผู้รับเหมา' is byte-identical as breadcrumb root (L253) and as a doc field label (L264) → collapsed to one key subcon.subcon per reuse rule. 'พิมพ์' → common.print (exact match).

Glyph fidelity confirmed via raw bytes: middot · = U+00B7 (pageTitle, conditionText); ฿ = U+0E3F (colValue); ⚠ = U+26A0 and em-dash — = U+2014 (partialWarn); sigDateLine dot groups = 12/9/9 ASCII periods. conditionLabel keeps its trailing ':'; field labels render as `{l}:` so the ':' is markup separator kept out of the label keys.

Placeholders: subcon.retentionDeduct {pct}=c.retentionPct; subcon.partialWarn {n}=accepted.length, {count}=c.periods.length. partialWarn renders only when !allDone.

Cross-screen note: SubconHandover is also reached from SubconAccept via 'ใบรับงาน' (L105) and 'เอกสารส่งมอบ' (L39) buttons which pass {wo, period}; the `period` param is unused here (this doc lists ALL accepted periods).

### accept  ·  AcceptanceCenter
- **contract ops:** GET acceptance list — aggregate pending-acceptance items across source modules (subcon periods, GR receipts, PM work-orders, customer handover); each item carries type, source doc ref, project, owner, value+currency, wait-days, due text, overdue/rejected flags, attached DMS docs, filter list by type (subcon|gr|pm|handover) + rejected flag + free-text search over doc/title/project/owner (currently client-side over ACCEPT_ITEMS -> becomes server query params), GET acceptance item detail (row + docs list) for the read-only modal, export pending-acceptance list (openExportModal), download DMS document (per-attachment) — notify/download action, navigate-out only: this screen performs NO create/approve/reject mutations; actual acceptance happens in the source module route (ACCEPT_TYPES[].route: subcon.contracts/gr.list/pm.wo/sales.loan, or subcon.accept for rejected WO follow-up)
- **port notes:** READ-ONLY aggregate inbox — no mutations on this screen. Mock mechanics to strip: (1) ACCEPT_ITEMS array (L108-119) = the data rows, ALL fields DYNAMIC (doc, title, project, owner, value, wait, due, overdue, rejected, defect, wo, docs[]). (2) ACCEPT_TYPES map (L102-107): its .l VALUES are STATIC UI copy (enum->label, 4 fixed categories rendered as tabs L179 + table badges L199 + modal subtitle L131) -> keyed as accept.typeSubcon/typeGr/typePm/typeHandover; but .color/.icon/.route are frontend config, and the `type` CODE (subcon/gr/pm/handover) comes from the API. (3) Tab filter + search `q` are client-side .filter() over the array -> server query params on port. (4) KPI counts (ACCEPT_ITEMS.length, count(t), overdue, rejected) computed from array -> server aggregates. (5) Row bg uses color-mix on --danger-soft for rejected/overdue (keep tokens). Reuse notes: 'งวดงานผู้รับเหมา'(subcon), 'ใบงาน PM'(pm), 'ส่งมอบลูกค้า'(handover) are BYTE-IDENTICAL between ACCEPT_TYPES.l and the KPI card labels -> ONE key each (typeSubcon/typePm/typeHandover). BUT GR differs: type label 'รับของเข้าคลัง (GR)' (accept.typeGr) vs KPI label 'รับของ (GR)' (accept.kpiGrLabel) -> two keys. 'กำหนด' is reused between column header (L192) and modal field label (L135) -> single accept.colDue. Non-keyed glyphs to preserve as literals in the port (NOT i18n keys, they are structural/value placeholders): '—' (U+2014) null-value fallback (L135, L207); ' ฿' (U+0E3F) currency suffix appended to dynamic fmt(value) in modal (L135); ' · ' middot separators (L131 modal subtitle, L72/L87 belong to CompanySwitcher=out of scope); '📎' emoji attachment indicator (L202). Modals: ONE detail modal (openDetail, L128-160) built via ctx.openModal — title=r.doc (dynamic), subtitle uses type label + project; plus the shared openExportModal (L167). Glyph checks done against raw lines: ⚠ = U+26A0 (L179 tab, L203 reject line, each followed by a space); em-dash U+2014 in subtitle (L166) + footer (L217); middot U+00B7 in pageTitle/kpiTotalSub/dmsHeader/openDmsBtn; ฿ U+0E3F in colValue; ASCII '...' ellipsis in searchPh. Classification I flagged for reviewer: ACCEPT_TYPES.l labels — I classified STATIC_UI_COPY (fixed enum->label UI chrome, not per-record data) even though they live in a const object; if the port instead renders labels purely from a server-provided display name, these keys become unused, but current prototype behavior supplies them client-side so they must be keyed. CompanySwitcher (L24-120) intentionally EXCLUDED (already ported).

### pm.dashboard  ·  PMKpi (L74) + PMDashboard (L95-150)
- **contract ops:** GET pm dashboard summary/metrics — jobsThisMonth (done/remaining), compliancePct + target, overdueCount, quotesPendingCount, costYtd (drives the 5 PMKpi cards), GET pm assets list filtered by status due|overdue — id, name, nextDue, status (drives the 'PM ใกล้ถึง / เกินกำหนด' panel; source pmAssets() is type-scoped by active project type), Export PM report — openPMExport(ctx, 'รายงาน PM') → generate/download PM report, navigate pm.wo (ใบงาน PM primary action) — routing only, no backend op, navigate pm.assets (row click + primary asset panel) — routing only, no backend op, NOTE: child components PMCalendar (L118) and PMUpcoming (L145) are out-of-scope and will add their own schedule/work-order fetch ops
- **port notes:** PMKpi (L74-90) is a pure presentational helper — it renders only its {label}/{value}/{unit}/{sub} props and an {icon}; it has NO literals of its own, so every KPI string above was classified at the PMDashboard call sites (L110-114).

STRIP MOCK: PM_ASSETS_BY_TYPE (L7-32), PM_STATUS (L33-36), PM_MONTHLY (L40-44) and pmAssets()/activeProjectType() — wire to real API. maxBar (L100) is computed from PM_MONTHLY but is only consumed by PMCalendar (out of scope). KPI values 46/91.5/3/1.42 and String(overdue.length) are hardcoded/computed data, NOT keys.

'%' unit (L111) is a language-neutral symbol → treated as ICON_OR_NUMBER, no key (porter keeps the literal '%').

STATUS LABELS: {st.l} (L138) resolves from PM_STATUS at L33-36 which is OUTSIDE the two in-scope components. Its label strings ('ตามแผน'/'ใกล้ถึงกำหนด'/'เกินกำหนด') are status-enum values that should live in a shared status-enum i18n surface (also reused by PMWO/contract screens), not pm.* — did NOT propose pm.* keys for them.

'Export' (L107) is a literal English Btn label. common.export is Thai 'ส่งออก' so it is NOT a byte-match REUSE — new key pm.exportBtn with the English literal 'Export' in all 4 slots (org.pageTitle convention).

GLYPH watch for the sacred round: middot U+00B7 in pm.breadcrumbPm / pm.pageTitle / pm.kpiJobsThisMonthSub / pm.assetDueLine; em-dash U+2014 in pm.subtitle; ≥ U+2265 in pm.kpiComplianceSub; en-dash U+2013 appears only in the dynamic date range (skipped). Grep the raw prototype line before compiling each value.

PLACEHOLDER contract (repo uses .replace()): pm.kpiJobsThisMonthSub two counts {n}/{count}; pm.kpiComplianceSub target {n} (defensive — porter may hardcode 90); pm.overdueCount {n}; pm.assetDueLine {no}+{date}. No modals live in these two components (openPMExport modal, PMCalendar, PMUpcoming are separate).

### pm.assets  ·  PMAssets (L155-222) + PMAssetForm (L223-257) + shared modals openAssetDetail (L259-277) & openPMExport (L279-292), all reachable from the Asset Registry screen
- **contract ops:** pm.assets.list — GET list of PM assets (fields: id, name, kind, site, cycle, last, next, status, contract) seeded/scoped by active project type; client-side search (code/name/site/kind) + kind filter today, likely server params on port, pm.assets.create — POST new PM asset from PMAssetForm (id, name, kind, site, cycle, contract); returns row prepended to list, pm.assets.get / detail — asset detail view (openAssetDetail) showing kind, cycle, last PM, next PM, contract, status, pm.wo.create — create a PM work order from an asset (openAssetDetail 'สร้างใบงาน PM' → navigates to pm.wo), navigation only: 'ดูแผน PM' → pm.schedule, export — client-side mock only (Excel/PDF/CSV via ctx.notify); no backend export endpoint implied by prototype, status labels rendered via shared PM_STATUS map (status enum → {tone,label}) — backend supplies a.status code
- **port notes:** MOCK MECHANICS TO STRIP: `pmAssets()` mock generator seeded by `activeProjectType().id` (useEffect re-seeds on project-type change); `useState(assets)` + client-side `q`/`kind` filtering; `PMAssetForm.onSave` prepends the new row to local state (`setAssets(prev => [a, ...prev])`). Replace with API list + create. Filter option list is derived from data: `[...new Set(pmAssets().map(a=>a.kind))]` — only the leading 'ทั้งหมด' (common.all) option is static; the rest are DYNAMIC kinds (skip).

STATUS LABELS (IMPORTANT): the table (L200/L210) and detail modal (L267) render `st = PM_STATUS[a.status]` → `st.l` (label) + `st.s` (tone) via <StatusBadge>. PM_STATUS is a constant defined OUTSIDE my assigned components (exported at L294) — its labels are status-chip copy but come from a shared status dict, NOT hardcoded in PMAssets markup. I classified them as DYNAMIC (status enum→label) and did NOT propose pm.assets keys for them. Flag for whoever owns the PM constants recon: those PM_STATUS labels still need i18n via the shared map, not via pm.assets.* keys.

DATA DEFAULTS (excluded, not markup copy): PMAssetForm.save fallbacks on L235 — site→'—', last→'—', next→'ตามรอบ', contract→'—', status→'ok' — are default values written into the data record (render later as a.site/a.next/a.contract cells), so they're a data/backend concern, not UI-markup copy. '—' = em-dash empty-cell placeholder; 'ตามรอบ' = default next-due text.

COMPOSED-FROM-DATA (not keyed): openAssetDetail modal title `${a.id} · ${a.name}` (L262) and subtitle `a.site` are runtime-composed from data; the ' · ' middot is just a separator, no key.

SHARED HELPERS: `openAssetDetail` and `openPMExport` are exported via window (L294) and reused across PM screens. I proposed their static strings under pm.* — a sibling PM-screen recon may propose the SAME export keys (pm.exportModalTitle/exportExcel/exportPdf/exportCsv/toastDownloading), so dedupe at merge. `openPMExport(ctx, what)`: the header Export button passes `what` = 'ทะเบียนอุปกรณ์' (pm.assetsTitle) → port should pass that same i18n key as both the export modal subtitle and the {name} token in pm.toastDownloading.

GLYPH NOTES: middot ·(U+00B7) appears with surrounding spaces on L176/L177/L248/L262/L286; em-dash —(U+2014) on L178; ASCII ellipsis '...' (not U+2026) on L185/L244. Search placeholder 'ค้นหา รหัส/ชื่อ/ที่ตั้ง...' is a DIFFERENT literal from common.search ('ค้นหา...') so it is NOT a reuse.

REUSE MAP (one key, multiple sites): pm.colKind (L187 filter hint, L194 col, L244 field, L268 detail row); pm.colName (L193 col, L241 field); pm.colSite (L194 col, L248 field); pm.colCycle (L195 col, L245 field, L268 detail); pm.colLastPm (L195 col, L268 detail); pm.colNextPm (L196 col, L268 detail); pm.colContract (L196 col, L268 detail); pm.addAssetBtn (L179 header, L253 form submit); pm.assetsTitle (L176 breadcrumb, L179 openPMExport arg). Shared primitives used: Page, Card, Btn, Icon, Dropdown, SelectOther, Field, Tag, StatusBadge, TypeBadge — no keys.

### pm.contracts  ·  PMContracts + AddProjectPicker + PMContractWizard + SelectOther + PMContractForm (pototype/pm2.jsx)
- **contract ops:** GET /pm/contracts — list maintenance contracts (grouped by project client-side), POST /pm/contracts — create contract (PMContractForm save), PUT /pm/contracts/{id} — edit contract (mode='edit'), POST /pm/contracts/{id}/renew — renew contract (preset path), GET /pm/contracts/{id} — contract detail view, GET /pm/contracts/export — Export action (openPMExport), GET /projects — project picker list (window.PROJECTS), POST /projects — create manual project (window.ProjectStore.add), GET /customers?project={id} — customer registry pool (window.customersFor), GET /project-types — project type dropdown (window.PROJECT_TYPE_LIST), POST /ap/invoices (or /pm/contracts/{id}/bill) — create AP payable from contract, then navigate ap.billing, POST /pm/plan-items — auto-generate PM calendar/support jobs when svcType=scheduled (window.PM_PLAN_ITEMS.push)
- **port notes:** MOCK MECHANICS TO STRIP: useState(PM_CONTRACTS) seed list + client-side reduce/group-by-project; window.PROJECTS, window.ProjectStore.add, window.customersFor, window.PROJECT_TYPE_LIST, window.__juneflowCtx, window.PROJECTS lookups (projOf). The auto-calendar-generation block in PMContractForm.save (L400-409, window.PM_PLAN_ITEMS.push) is pure mock — real app: backend generates PM plan items on scheduled contract create. The generated item name template '{scope} (ครั้งที่ {i}/{n})' L407 and cycle 'ตามสัญญา Support' L407 are mock data record fields, NOT UI keys.

DYNAMIC_DATA (excluded, comes from API): all c.* contract fields (no, scope, value, cycle, sla, start, end, customer, site, project, status, svcType, visits, otherCond); PMC_STATUS status labels (st.l / st.s — status config map, referenced not defined in this file); p.name/p.short/p.color/TypeBadge (project data); fmt() money; date-string defaults L380-381 ('1 ม.ค. 69' etc. — computed seeds); doc-number default L374 (prefix-2569-seq); customer pool names; project-type dropdown option names.

pmProjectInfo (L266-278) IS ENTIRELY DYNAMIC/SEED DEFAULTS — customer ('บมจ. โซลาร์ …', 'นิติบุคคล …'), site ('ไซต์โรงไฟฟ้า','ไซต์ก่อสร้าง','สำนักงานลูกค้า','อาคาร A'), equip lists ('อินเวอร์เตอร์ 16 เครื่อง','ลิฟต์โดยสาร 2 ชุด (MAXTECH)', etc.), prefix ('OM/EQ/SV/MT'). These prefill editable form fields per project type and at runtime come from customer/asset registries — do NOT key them.

GLYPH FIDELITY: curly quotes U+201C/U+201D in pm.toastProjectCreated + pm.emptyContracts; em-dash U+2014 (spaced) in pm.subtitle, pm.subtitleAddProj, pm.emptyContracts, pm.confirmBillMessage, pm.svcScheduledDesc, pm.svcOtherDesc; middot U+00B7 in breadcrumbRoot/pageTitle/tableMeta/valScheduled/badgeHasContract/wizard hints; ฿ U+0E3F in colTotalValue/unitBahtPerYear/confirmBillMessage/fieldValuePerYear; en-dash U+2013 appears only in dynamic date-range line L207 (kept as literal glyph, not a key); ellipsis is ASCII '...' (three dots, not U+2026) in all SelectOther placeholders (phSpecifyOwn/phScopeOther/phCycleOther/phSlaOther); '~' tilde literal in pm.scheduledInfo.

BARE GLYPHS (not keys): '—' em-dash fallback for empty type cell (L72); bare ' ฿' appended to fmt() money (L188, L251) — currency glyph kept as literal after dynamic value.

BORDERLINE: pm.slaInline='SLA' (L207) is a hardcoded acronym prefix inside an otherwise fully-dynamic data line ({cycle} · SLA {sla} · {start} – {end}); reported as ENGLISH_LITERAL_UI but porter may legitimately keep it inline. pm.badgeExpiring (L76 'ใกล้หมดอายุ {n}') may reuse pm.kpiExpiring label + {expiring}.

MODALS INCLUDED (all via ctx.openModal, size lg/md): (1) openForm → PMContractWizard (2-step: pick/create project → PMContractForm) for create/edit/renew; (2) openAddProject → AddProjectPicker (existing list + manual-create sub-form); (3) openProjectContracts drill-down list; (4) openContractDetail read-only view with row() helper + action buttons; (5) ctx.confirm dialog for Bill-to-AP (L255) with confirmLabel/confirmIcon. AddProjectPicker and PMContractWizard duplicate the same manual-project sub-form (name/type fields) — pm.fieldProjectName/phProjectName/errProjectName/toastProjectCreated/fieldProjectType/btnPickFromList/btnNext keys are shared across both.

REUSED KEYS across occurrences: pm.breadcrumbContracts (L30+L33), pm.addProjectBtn (L33+L112), pm.kpiUnitContract (L36+L37), pm.colProject (L48+L242), pm.svcMaLabel (L246+L389), pm.rowCustomer (L243+L425), pm.rowSite (L244+L435), pm.rowCycle (L247+L441), pm.rowStart (L249+L481), pm.rowEnd (L250+L482), pm.btnRenewContract (L257+L488), pm.btnCreateManual (L168+L346), pm.btnPickFromList (L143+L315), pm.btnNext (L144+L316), pm.fieldProjectName (L133+L306), pm.phProjectName (L134+L307), pm.errProjectName (L135+L308), pm.fieldProjectType (L138+L310), pm.toastProjectCreated (L128+L295), common.cancel (L169+L347+L487).

### pm.schedule  ·  PMSchedule (L497-538) + PMCalendar (L552-590) + PMUpcoming (L592-623)
- **contract ops:** GET PM plan/schedule items — list of upcoming maintenance plan rows (asset code, name, due date, cycle, status overdue/due/plan) sourced from PM_PLAN_ITEMS mock (L541-548), GET PM assets — pmAssets() list consumed by PMSchedule (L500), not directly rendered in these components but drives the schedule, POST create work order(s) from PM plan — genWO action (L503-523) generates PM WOs for due/overdue plan items then navigates to pm.wo; also per-item create at L614 creates one WO per asset then navigates to pm.wo, (calendar day marks/tone PM_DAY_MARKS + PM_TONE are pure client-side render derived from plan item status/day — no separate endpoint)
- **port notes:** MOCK MECHANICS TO STRIP: (1) PM_PLAN_ITEMS array (L541-548) — replace with API list of PM plan schedule items; each row = {date, asset, name, cycle, status, day}. All row-rendered values ({p.name}, {p.asset}, {p.date}, {p.cycle}, split date parts p.date.split(' ')[0]/[1]) are DYNAMIC_DATA — NO keys. (2) pmAssets() (L500) mock. (3) PM_DAY_MARKS (L549) + PM_TONE (L550) derive calendar day dot colors from status — keep as client render logic off the API status field. (4) ctx.openModal / ctx.notify / ctx.navigate('pm.wo') are the harness modal+toast+router. (5) genWO filters planItems by status!=='plan' (only due/overdue) → the '3 ใบ' count in button/toast is HARDCODED literal '3' in prototype (not derived) — transcribe verbatim, do not placeholder.

MODAL INCLUDED: genWO confirmation modal (icon 'wrench', tone var(--brand), size md) — its 4 strings captured (title/subtitle/infoLine/confirmBtn) + cancel reuses common.cancel.

CALENDAR: PMCalendar renders a fixed 30-day month grid (Array.from length:30, L566) — the day numbers {d} are pure numbers (ICON_OR_NUMBER, ignored). 7 weekday abbreviations (จ อ พ พฤ ศ ส อา, Mon→Sun) hardcoded as JSX array → classified STATIC_UI_COPY, 7 keys. 3 legend chips = keys.

FLAGGED / UNSURE: 'มิถุนายน 2569' (L552 default `title` param, renders via L558) — a Thai month + Buddhist-era year = a DATE. Real app computes the visible calendar month. Classified DYNAMIC_DATA (no key) per the dates-are-dynamic rule, but flagging because it is a hardcoded default literal, not from a data array. Port should feed the month/year header from date state, NOT an i18n key. Same components (PMCalendar/PMUpcoming) are also reused by the PM Dashboard elsewhere in the file with title props passed in — these default params only apply here on pm.schedule.

GLYPH NOTES: middot · = U+00B7 (breadcrumb L527, pageTitle L528, and dynamic row separators L513/L612 which are layout only, not keys). em-dash — = U+2014 (subtitle L529, modal infoLine L508). Placeholders: {n}=selectedDay number (L598), {name}=asset code (L614 toast).

### pm.wo  ·  PMWorkOrders (L39-110), PMWorkOrderDetail (L111-259), PhotoChip (L260-268), NoteField (L269-278), PMWOForm (L279-319)
- **contract ops:** GET pm work-orders list — status-filtered with per-status counts (all/open/inprogress/overdue/done), GET pm work-order detail by no, POST create pm work-order (PMWOForm: asset, type, tech, date → server derives contract/site/zone/sla, status=open), state-transition: check-in on-site (records check-in timestamp), PATCH checklist item results — cycle none→normal→adjust→repair — plus before/after photo|video attachments, PATCH maintenance notes (cause / fix / advice), state-transition: close work order with customer signature (status→done; triggers report-sent notification), GET checklist templates by asset kind (ลิฟต์ / ระบบดับเพลิง / default) — openChecklistManager + openChecklistPicker, GET pm assets list (pmAssets) for create-form dropdown, GET technicians list for tech dropdown, GET/export work orders (Export button → openPMExport)
- **port notes:** MOCK MECHANICS TO STRIP: (1) useState(PM_WOS) local list + setWos prepend on create; PM_WOS, PMWO_STATUS, pmAssets() are EXTERNAL mock sources (defined elsewhere, not in this file) — replace with API client. (2) Detail seeds checklist from PM_CHECKLIST template by asset-kind string-match on wo.assetName.includes('ลิฟต์'/'ดับเพลิง'); open WOs seed [] (empty). PM_CHECKLIST (L6-27) is CONFIG DATA (checklist templates), NOT UI copy — loaded from a checklist-template store at runtime and editable via openChecklistManager. (3) RESULT_OPTS (L28-33) IS a fixed UI enum of result-status chips — I keyed all 4 (pm.resultNone/Normal/Adjust/Repair); nextResult() cycles them on click (client toggle logic to reimplement). (4) PMWOForm save() fabricates no as `PMWO-2569-0${316+rand}`, derives sla via type.includes('CM'), hardcodes zone 'เขตกรุงเทพ-เหนือ' — all server-side in port; the SLA strings 'ด่วน 4 ชม.'/'ภายใน 24 ชม.' render as data (wo.sla). (5) Hardcoded times 09:14 น./10:48 น./1 ชม. 34 นาที and GPS coords are mock DATA (use {time} placeholders in the keys I flagged). (6) cause/fix are prefilled with mock strings only for inprogress WOs (L118-119) — DATA, not copy.

MODALS INCLUDED: create-WO modal (ctx.openModal → body=PMWOForm, title/subtitle keyed) and close-WO+signature modal (ctx.openModal, inline body with signature pad). Both use ctx.notify for toasts (3 toasts keyed: created/checkedIn/closed).

STATUS BADGES: st.l comes from EXTERNAL PMWO_STATUS map (st.s/st.l) — dynamic, NOT keyed here; the four TAB labels I DID key (pm.tabOpen/tabInprogress/tabOverdue/tabDone) are our own literals. Note 'กำลังดำเนินการ' is reused: tab label (L72) AND the total-time value when not closed (L245) → single key pm.tabInprogress.

GLYPH WATCH: curly quotes “ ” U+201C/U+201D in pm.emptyChecklistHint (L198) — must be byte-exact, NOT ASCII. em-dash U+2014 in pm.subtitle (L67) and pm.confirmIncomplete (L133). middot · U+00B7 in many strings (breadcrumb, title, detailSubtitle, toasts, checkedIn, formContractInfo, closedNote, readyToClose). ✓ U+2713 in pm.signHere (L138). '&' in pm.resultAdjust.

NON-KEYED GLYPHS: PhotoChip appends status indicators ' ✓' / ' +' (has-photo vs add) and empty-fallback '—' (em-dash for missing contract/date/times) — treated as indicator glyphs, not translatable copy; reproduce in markup logic. PhotoChip renders 'รูป'+label+(✓|+)+optional' (วิดีโอ)'; I keyed the words (pm.photoPrefix/before/after/videoTag) but the ✓/+ toggles stay as JSX.

EXTERNAL HELPERS called but defined elsewhere: openChecklistManager(ctx), openChecklistPicker(ctx, cb), openPMExport(ctx, title) — the title arg 'ใบงาน PM' reuses pm.breadcrumbWo.

REUSE ACROSS FILE (one key, multiple sites): pm.type (col L81 + field L236), pm.tech (col L82 + field L236 + form L304), pm.due (col L83 + field L236), pm.sla (col L83 + field L236), pm.createBtn (L68 + L313), pm.pickChecklistBtn (L189 + L199), pm.optPlanned (default L282 + option L301), common.cancel (L142 + L312). Distinct-but-similar (DO NOT merge): 'ปิดงาน' (pm.closeWoBtn L156) vs 'ปิดงาน + ลายเซ็น' (pm.closeWithSignBtn L251); 'ประเภท' (pm.type) vs 'ประเภทงาน' (pm.fieldWorkType L301); 'กำหนด' (pm.due) vs 'กำหนดวันเข้างาน' (pm.fieldScheduledDate L302); 'อุปกรณ์' (pm.fieldAsset L297) vs 'อุปกรณ์ / Site' (pm.colAssetSite L82).

## Borderline i18n decisions (surfaced to Wei — non-blocking)
- **[R1]** REVIEWER DECISION NEEDED: if the port sources period-status labels or the acceptance-criteria checklist from the API instead of hardcoding, drop these 9 keys (subcon.statusRequested + the 3 deduped status keys' PERIOD_STATE usage + subcon.checkItem*×5). Current prototype hardcodes them → keyed for fidelity. Non-blocking for apply; port can prune unused.
- **[R1]** BORDERLINE UI-vs-formatting: kept as keys (structural unit slots; "M ฿" carries the 'M' abbreviation). Bare table-header "%" (subcon.jsx L154, subcon-accept2 L88) was NOT keyed — dropped as symbol, consistent with progress-screen ruling. Reviewer may downgrade unitBaht "฿" to static formatting if desired.
- **[R2]** These are defined OUTSIDE the R2 components and render dynamically via st.l (StatusBadge). They are NOT pm.* keys — they belong in a shared status-enum i18n surface. NOTE several DUPLICATE static keys minted this round (e.g. static tab pm.tabOpen 'รอดำเนินการ' vs dynamic PMWO_STATUS.open 'รอดำเนินการ'; static pm.tabOverdue/pm.legendOverdue 'เกินกำหนด' vs PM_STATUS/PMWO_STATUS overdue 'เกินกำหนด'). Decide whether the sacred round should also mint the shared status keys or defer to a separate status-enum round; if deferred, the static UI keys above stand and the status map is wired separately.
- **[R2]** Hardcoded acronym 'SLA' prefix inside the fully-dynamic drill-down line `{cycle} · SLA {sla} · {start} – {end}`. Did NOT mint a separate key — porter should reuse pm.sla inline or keep as a language-neutral literal. Flagged as porter judgment, not a blocker to the i18n round.
- **[R2]** Hardcoded default prop that is a Buddhist-era month/year DATE. Classified DYNAMIC (no key) per dates-are-dynamic rule, but it is a literal default (not from a data array). Port MUST feed the calendar header from date state, not an i18n key. Same PMCalendar/PMUpcoming default params (pm.upcomingTitle 'แผนที่จะถึง') ARE keyed because they are real UI copy; only the month/year is a date.