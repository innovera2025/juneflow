# BLOCKERS.md — ช่องทาง escalate ความขัดแย้ง design/spec

> **ไฟล์นี้คือช่องทางเดียว (single channel) สำหรับความขัดแย้ง design/spec ทุกกรณี** — ตาม PLAN.md §0 กฎข้อ 4 และ §10
> เจอความขัดแย้งที่ไม่อยู่ในตารางคำตัดสิน (PLAN.md ภาคผนวก C) → เขียน blocker ที่นี่ แล้วข้ามไป task อื่น **ห้ามเดา ห้ามเลือกเอง**
> Sacred files (PLAN.md §10: OpenAPI · merged migrations · CLAUDE.md ทุกใบ · CI config · secrets · `docs/extract/*` · i18n-full.json) แก้ได้ผ่าน blocker ที่ Wei อนุมัติแล้วเท่านั้น
> ข้อความ UI ที่ไม่มี key ใน `i18n-full.json` = เข้า blocker (PLAN.md §0 กฎข้อ 2)

## วิธีใช้

1. id รูปแบบ `B-xxx` รันต่อเนื่อง · ระบุ task ที่ชน · ตั้งคำถามให้ชัด · เสนอตัวเลือกให้ Wei เลือก
2. ระหว่างรอคำตอบ → เปลี่ยน task ใน `TASKS.md` เป็น `blocked` แล้วหยิบ task อื่น (ยกเว้น blocker ประเภทแจ้งเพื่อทราบ/ยืนยันย้อนหลัง สถานะ "รอ Wei ตอบ — ไม่ block งาน")
3. Wei ตอบแล้ว → บันทึกลงคอลัมน์ "คำตอบ Wei" → นำไปใช้ → เปลี่ยนสถานะเป็น `ตอบแล้ว — นำไปใช้` และปลด task จาก `blocked`
4. ค่าสถานะ: `รอ Wei ตอบ` / `รอ Wei ตอบ — ไม่ block งาน` / `ตอบแล้ว — นำไปใช้` / `ปิด`

## ตาราง blockers

| id | task | คำถาม | ตัวเลือกที่เสนอ | คำตอบ Wei | สถานะ |
|---|---|---|---|---|---|
| B-001 | P0-BE-03 / P0-QA-01 (visual reference) | gallery จริงมี **106 .jpg** (manifest ระบุ 102) — ก๊อปครบทั้ง 106 เข้า `tests/visual/reference/` แล้ว ยืนยันหรือไม่ | (ก) ใช้ครบทั้ง 106 ตามจริงบนดิสก์ (ค่าที่ใช้อยู่) · (ข) Wei ระบุรายชื่อ 102 ไฟล์ที่ต้องใช้ แล้วตัดส่วนเกินออกจาก reference | — | รอ Wei ตอบ — ไม่ block งาน |
| B-002 | bootstrap กลุ่ม 2 / P0-INT-01..04 | manifest กลุ่ม 2 ให้สร้าง `packages/integrations/CLAUDE.md` แต่โครง scaffold กลุ่ม 5 มี `packages/tax-engine\|bank-file\|notifications` แยกกัน (ไม่มีโฟลเดอร์ `integrations`) — bootstrap สร้างตาม manifest ตรงตัวแล้ว จะจัดโครงสร้างอย่างไร | (ก) ย้าย 3 แพ็กเกจเข้าใต้ `packages/integrations/` · (ข) คัดลอก CLAUDE.md ไปทั้ง 3 แพ็กเกจ · (ค) คงตามเดิม (`packages/integrations/CLAUDE.md` เป็นใบกลางของเขต + 3 แพ็กเกจแยกตามกลุ่ม 5) | — | รอ Wei ตอบ — ไม่ block งาน |
| B-003 | bootstrap กลุ่ม 6 (`.claude/` harness — manifest v3) | v3 กำหนดจำนวนองค์ประกอบ `.claude/` = hooks 5 + skills 4 + subagents 3 แต่ไม่ได้ระบุรายชื่อ — orchestrator เป็นผู้เลือกองค์ประกอบชุดนี้: hooks = `protect-files.sh` · `zone-guard.sh` · `i18n-guard.sh` · `block-main-commit.sh` · `journal-append.sh` — skills = `loop-task` · `port-screen` · `visual-gate` · `run-gates` — subagents = `diff-reviewer` · `visual-gate-runner` · `spec-scout` — ยืนยันชุดนี้หรือไม่ | (ก) ยืนยันชุดนี้ (ค่าที่ใช้อยู่จริงบนดิสก์) · (ข) Wei ระบุรายการที่ต้องการแทน | — | รอ Wei ตอบ — ไม่ block งาน |
| B-004 | harness delta 11 ก.ค. 2569 (คำสั่งตรงจาก Wei) | manifest v3 กลุ่ม 6 ล็อกองค์ประกอบ `.claude/` "ห้ามเพิ่ม/ลด/เปลี่ยนชื่อโดยไม่ผ่าน Wei" และ CLAUDE.md ทุกใบเป็น sacred file — คำสั่ง delta ของ Wei (11 ก.ค. 2569) สั่ง (ก) เพิ่ม hooks 4 ตัว (`block-dangerous-bash` · `format-changed-file` · `quick-verify` · `notify`) + skills 2 ตัว (`debug-protocol` · `merge-worktree`) + subagent 1 ตัว (`log-reader`) → ชุดใหม่ = 9 hooks + 6 skills + 4 subagents (ข) แก้ CLAUDE.md 3 ใบ (root · apps/web · apps/mobile) เพิ่มบล็อก Design Fidelity/กฎอ่าน .jsx ต้นทาง (ค) ignore `pototype/` `design_handoff_juneflow/` `juneflow-extract/` ใน .gitignore — บันทึก blocker นี้เป็นหลักฐานการอนุมัติ (id นี้ใช้อ้างแทน `SACRED_OVERRIDE=wei-approved:B-004`) | (ก) ถือคำสั่ง delta เป็นการอนุมัติผ่าน Wei แล้ว — ดำเนินการทันที (ค่าที่ใช้) | อนุมัติล่วงหน้าโดยตัวคำสั่ง delta 11 ก.ค. 2569 เอง (ข้อ A1–A3 · B4 · C6–C7 · D8) | ตอบแล้ว — นำไปใช้ |
| B-006 | overnight run คืน 12 ก.ค. 2569 — อนุมัติสดจาก Wei (AskUserQuestion ก่อนเข้านอน) | ขอบเขตที่อนุมัติ: (1) orchestrator ทำ foundation commit ทั้ง repo บน branch `dev` ที่แตกจาก main — **main ไม่ถูกแตะ** Wei ตรวจแล้ว promote เอง · ยกเว้นไม่ commit: deck นักลงทุน (pptx/pdf) + `clear_layout_assets/` + `deck_assets_glam/` + ไฟล์ซ้ำ `BLOCKERS 2.md` (2) mark P0-BE-02 / P0-BE-03 เป็น `done` หลังตรวจ inventory ตรงตามเกณฑ์ gate ของ task (3) spawn loop 4 เขต (backend · integrations · qa · devops) ด้วย model Opus 4.8 ใน worktree แยกต่อเขต เพดานตาม loop-config (10 รอบ/$20 ต่อเขต) — Wei ไม่เปิด terminal เอง (4) คืนนี้ไม่มี remote/CI: orchestrator (Fable 5) ทำหน้าที่ gate แทน — merge `feature/* → dev` แบบ local ได้เฉพาะเมื่อ gates ที่ task กำหนดเขียว + diff-reviewer (ด่าน 4.5) PASS · FAIL = ไม่ merge, task กลับ rework | (ก) ตามขอบเขตข้างต้น | อนุมัติทั้ง 3 คำถาม (foundation บน dev · spawn เอง · 4 เขต) — 12 ก.ค. 2569 ก่อนนอน 8 ชม. | ตอบแล้ว — นำไปใช้ |
| B-007 | P0-INT-05 → งาน Phase 3 `TaxEngine.thailand` | 50 ทวิ: prototype จับคู่ประเภทเงินได้ด้วย `String(r.typeIdx) === types[].i` (`pototype/tax-forms.jsx` L598) โดย catalog `types[].i` มี string `"4(ก)"`/`"4(ข)"` ซึ่ง `typeIdx` แบบตัวเลขจับคู่ไม่ได้ — engine ควรกำหนด income-type identifier อย่างไร (พบระหว่าง P0-INT-05 · บันทึกใน `packages/tax-engine/docs/tax-forms-map.md` §C.4) | (ก) emit `typeIdx` เป็น string ตรง catalog 7 คีย์ `"1","2","3","4(ก)","4(ข)","5","6"` — ตรง render logic ของ prototype ที่สุด (แนะนำ) · (ข) Wei กำหนด enum ภายในของ engine + mapping layer ไป 7 คีย์นี้ | — | รอ Wei ตอบ — ไม่ block งาน |
| B-008 | overnight run — กติกา dependency ระหว่างคืน (ยืนยันย้อนหลัง) | TASKS.md กติกา 2 ต้องรอ dep เป็น `done` และกติกา 3 ให้ `done` เกิดหลัง Wei promote เท่านั้น — ระหว่าง Wei หลับ ทั้งกระดานจะหยุดหลังเขตละ 1 task (ทุก task รอ P0-BE-01) ขัดคำสั่ง "คืบหน้ามากที่สุด" · orchestrator จึงใช้กติกาชั่วคราวคืนนี้: **task ที่ merge เข้า dev แล้วหลังด่าน 4.5 PASS = `done` เพื่อปลด dependency** โดยแถวใน REVIEW-QUEUE.md คงอยู่ให้ Wei ตรวจ batch ตอนเช้าตามเดิม — Wei veto ตัวไหน = สร้าง rework task + งานที่ต่อยอดถูกตรวจซ้ำ | (ก) ยืนยันกติกานี้สำหรับ overnight run ทุกคืน · (ข) คืนต่อไปให้หยุดรอ Wei ตาม กติกา 2/3 ตามตัวอักษร | — (นำไปใช้คืนนี้อิงขอบเขต B-006 — รอยืนยันย้อนหลัง) | รอ Wei ตอบ — ไม่ block งาน |
| B-009 | P0-QA-06 (rework) + P0-BE-10 (seed) | MOCK-DATA §สรุป ระบุ Unit/SalesUnit "ไม่มี record (generate 84 ยูนิตใน sales-process.jsx ทุก reload)" แต่ PLAN.md §0 กฎข้อ 3 สั่งว่า seed ต้อง persist (generate ใหม่ทุก reload = กลไก mock ห้ามลอก) — seed จริงต้อง persist Unit กี่ record? (พบโดย diff-reviewer ระหว่างด่าน 4.5 ของ P0-QA-06) · **12 ก.ค. รอบ 15 (backend loop): P0-BE-10 หยิบแล้วบล็อก** — Unit count เป็นแกนของ gate "counts ตรง §สรุป" (§สรุป ให้ 0 · generator ให้ 84) · การ seed 84 หรือ 0 = ตัดสิน "84 vs 0" เอง ซึ่ง PLAN.md §0 กฎ 4 ห้าม (diff-reviewer FAIL P0-QA-06 ด้วยเหตุนี้) → seed ทำสมบูรณ์ไม่ได้ · **ไม่มี ready task backend อื่นที่ dep ครบ (14 รอ 13 promote)** | (ก) persist 84 ยูนิตตาม generator (code/status ตามที่ generate) · (ข) 0 record แล้วให้ backend generate runtime · (ค) Wei กำหนดเอง | — | รอ Wei ตอบ — **block P0-BE-10** (ยังไม่ block P0-QA-06 ที่ done) |
| B-010 | P0-DEV-02 (`ci.yml`) + P0-DEV-03 (`CODEOWNERS`) — ปิดงานที่ artifact เป็น sacred | ไฟล์ทั้งสองมีอยู่แล้ว + committed + ตรง gate เนื้อหา จาก foundation commit แต่อยู่ใต้ `.github/` (sacred — hook บล็อกทุกการเขียน) loop จึงปิดงานเองไม่ได้ · **orchestrator ดำเนินการคืนนี้:** DEV-03 → `done` (gate = ครอบ sacred/zone ครบ — ตรวจเนื้อหาแล้วจริงตามบรรทัดฐาน done-on-arrival ของ B-006) · **DEV-02 คง `blocked`** เพราะ gate ระบุ "pipeline รันผ่านบน branch ทดสอบ" ต้องมี remote CI ซึ่งคืนนี้ไม่มี — รอ Wei เลือก | (ก) Wei ยืนยัน DEV-03 done + mark DEV-02 done เมื่อเห็นว่า content ครบ (CI จริงรันเมื่อมี remote) · (ข) ให้ `SACRED_OVERRIDE=wei-approved:B-010` regen/verify ใต้ `.github/` แล้วเดิน gate ปกติ · (ค) DEV-02 รอจนมี remote CI แล้วรัน pipeline gate จริง | (ก) ยืนยัน DEV-03 done + DEV-02 done — content ครบ, pipeline จริงรันเมื่อมี remote (ตอบเช้า 12 ก.ค.) | ตอบแล้ว — นำไปใช้ |
| B-013 | P0-BE-08 (rework) — schema การเงิน/ขาย | `SalesUnit.contract` ใน dictionary (§อื่นๆ: `unit_id,customer_id,stage,booking,contract,down[],loan,transfer`) — คอลัมน์ `contract` เป็นเงินงวดทำสัญญา (numeric + currency_code แบบ booking/loan) หรือ reference ไปเอกสารสัญญา? (พบโดย diff-reviewer ด่าน 4.5 — agent เว้น field นี้โดยอ้าง [TBD-MVP] ซึ่งต้องห้าม จึงตีกลับ rework) | (ก) numeric เงิน + currency_code สอดคล้อง booking/down/loan (แนะนำจากบริบทเพื่อนคอลัมน์) · (ข) uuid/text อ้างเอกสารสัญญา · (ค) Wei กำหนดเอง | (ก) contract = เงินงวดทำสัญญา numeric + currency_code แบบเดียวกับ booking/down/loan (ตอบเช้า 12 ก.ค.) | ตอบแล้ว — นำไปใช้ |
| B-016 | P0-BE-11 / P0-BE-13 — better-auth vs ตาราง `user` (sacred 0000) | better-auth ต้องการ own/extend ตาราง `user` (เพิ่ม emailVerified/image คอลัมน์ camelCase) แต่ `user` ถูกสร้างใน merged migration 0000 (sacred) ตาม dictionary shape — ให้ better-auth ใช้ตารางไหน? (พบโดย diff-reviewer ด่าน 4.5 ของ BE-11 · ต้องตัดสินก่อน BE-13/DEV-01 ต่อสาย auth จริง) | (ก) ตั้ง `user.modelName` ให้ better-auth ใช้ตารางแยก `auth_user` + link company_id — ไม่แตะ sacred 0000 (แนะนำ) · (ข) map เข้าตาราง `user` เดิม + เพิ่มคอลัมน์ผ่าน migration 0005+ (เปลี่ยน shape ตาราง sacred — ต้อง Wei อนุมัติชัด) | — | รอ Wei ตอบ |
| B-018 | P0-BE-14 (feature flags) | กลไก feature flag เปิด endpoint ใหม่ `GET /feature-flags` (public, คืน {enabled:[]}) ให้ web shell ซ่อนเมนู — แต่ endpoint ไม่มีใน openapi.yaml (sacred) และ web ใช้ generated client เท่านั้น จึงบริโภคไม่ได้โดยไม่ละเมิดกฎ (พบโดยด่าน 4.5 — BE-14 ถูกตีกลับ rework) | (ก) Wei อนุมัติ contract change เพิ่ม GET /feature-flags + FeatureFlagsResponse ผ่าน SACRED_OVERRIDE · (ข) จัดเป็น infra-internal แบบ /health — web ซ่อนเมนูด้วย build-time flags ฝั่งตัวเอง (ที่ WEB-02 ทำแล้ว: VITE_FEATURE_ROUTES) · (ค) ถอด endpoint ออก คงเฉพาะ guard+env จนกว่าจะตัดสิน (rework ใช้ทางนี้ชั่วคราว — แนะนำ) | — | รอ Wei ตอบ — ไม่ block งาน |
| B-019 | P0-WEB-05 (app shell) | ข้อความ Placeholder ของ prototype "หน้านี้ยังไม่ได้สร้าง" (shell.jsx / NAV-ROUTES.md:6) **ไม่มี key ใน i18n-full.json** — จอ placeholder หลัง feature flag จะ render ข้อความนี้ไม่ได้โดยไม่ละเมิดกฎ i18n (พบโดยด่าน 4.5 ของ WEB-02) | (ก) Wei เพิ่ม key ใน i18n-full.json ผ่าน sacred override · (ข) Phase 0 จอหลัง flag OFF ไม่ render ข้อความนี้เลย (แนะนำ — เลี่ยงแตะ sacred) · (ค) Wei กำหนดเอง | — | รอ Wei ตอบ — ไม่ block งาน |
| B-005 | notify.sh — event "gate แดงครบ 3 รอบ" + env `LOOP_AGENT` | notify.sh ตรวจ event "gate แดงครบ 3 รอบ" จาก hook payload ไม่ได้ ต้องมี call-site — จุดที่ตรวจได้เชิงกลคือ `scripts/loop-runner.sh` (contract ของ prompt: `TASK_RESULT: <id> doing` หลังรันจบ = gates ยังแดงหลังแก้ครบ 3 รอบ) แต่ loop-runner.sh อยู่นอกรายการ delta จึงยังไม่แตะ · พบด้วยว่า loop-runner.sh ไม่ได้ export `LOOP_AGENT` ทำให้ hooks ที่ key ตาม env นี้ (`zone-guard` · `journal-append` · `notify` โหมด Stop) ไม่ทำงานในรัน headless เว้นแต่ Wei ตั้ง env เองตอนสั่งรัน | (ก) อนุมัติแก้ loop-runner.sh: export `LOOP_AGENT="$AGENT"` + เรียก `notify.sh` เมื่อ `ROUND_STATUS=doing` (แนะนำ) · (ข) ให้ Wei ตั้ง `LOOP_AGENT` เองตอนรัน และเรียก notify จาก skill `loop-task` · (ค) ไม่ต้องแจ้ง event นี้ | — | รอ Wei ตอบ — ไม่ block งาน |
| B-012 | P0-BE-12 (`packages/contracts` — openapi.yaml + codegen TS) | P0-BE-12 สั่งให้ backend agent "complete ALL endpoints per api-contract.md" ลงใน `packages/contracts/openapi.yaml` (ปัจจุบันเป็น scaffold 3 endpoint ที่มี marker `TODO(P0-BE-12)` ในไฟล์เอง) — ~11 กลุ่ม endpoint (Auth/Admin/Master/BOQ+AI-QTO/Subcon/PM/Finance/Land-Sales/DMS-Noti-Audit-Reports/Files/Exports/LINE) แต่ `openapi.yaml` เป็น **sacred file** ที่ hook `protect-files.sh` บล็อกการเขียน**ไม่มีเงื่อนไข** (ไม่รู้สถานะ merge). TASKS.md P0-BE-12 ระบุ "sacred **หลัง merge**" — สื่อว่า backend agent ควรเป็นผู้ author openapi.yaml ในงานนี้ แล้วจึงล็อกหลัง merge เข้า dev · เกิด catch-22: task สั่งให้แก้ไฟล์ที่ hook ห้ามแก้. ไม่แตะ sacred โดยไม่ได้รับอนุมัติ ตาม PLAN.md §0 กฎ 4 + §10 (ห้าม bypass hook) → escalate | (ก) Wei อนุมัติ `SACRED_OVERRIDE=wei-approved:B-012` ให้ backend agent complete openapi.yaml ระหว่าง P0-BE-12 (ไฟล์ล็อกจริงเมื่อ merge เข้า dev แล้ว — ตรงเจตนา "sacred หลัง merge" · ตรงแพทเทิร์น B-004 ที่ blocker id ใช้แทน override) — **แนะนำ** · (ข) Wei author openapi.yaml เอง แล้วให้ backend agent ทำเฉพาะ codegen TS + contract lint · (ค) Wei ปรับ `protect-files.sh` ให้ยอม openapi.yaml จนกว่าจะ merge ครั้งแรก (แต่ hook อยู่ใน `.claude/` = sacred เอง ต้อง Wei แก้) | (ก) อนุมัติ SACRED_OVERRIDE=wei-approved:B-012 หนึ่งรอบเฉพาะ BE-12 ให้เขียน openapi.yaml จาก api-contract.md — sacred ถาวรหลัง merge (ตอบเช้า 12 ก.ค.) | ตอบแล้ว — นำไปใช้ |
| B-011 | P0-BE-04 (`packages/tokens`) · P0-BE-05 (`packages/i18n`) · P0-BE-15 (`scripts/loop-runner`) | **zone-ownership ขัดกันเชิงเอกสาร + คิว backend ตัน**: TASKS.md หัวเขต backend เขียน "(+ แพ็กเกจ platform: contracts/**tokens/i18n** ตามกลุ่ม 5)" และจัด P0-BE-04/05 เป็น task เขต backend — แต่ **PLAN.md §8** (นิยาม zone ที่เป็นทางการ) กำหนด Backend/Platform zone = `apps/api` + `packages/db` (เจ้าของ OpenAPI) เท่านั้น **ไม่รวม** tokens/i18n · และ `scripts/loop-config.json` `agents.backend.zonePaths = [apps/api, packages/db, packages/contracts]` ก็ไม่รวม `packages/tokens`/`packages/i18n` (diff-reviewer ด่าน 4.5 บังคับ zone จากไฟล์นี้ → เขียน tokens/i18n = FAIL แน่นอน). ผลตรวจ: `packages/tokens` และ `packages/i18n` **ไม่มีเขตใดเป็นเจ้าของเลย** (web=apps/web, mobile=apps/mobile, qa=tests, integrations=tax-engine/bank-file/notifications, devops=infra/scripts/.github). ส่วน P0-BE-15 (`scripts/loop-runner.sh`+`loop-config.json`) อยู่ใน `scripts/` = เขต **devops** ไม่ใช่ backend. ผลรวม: รอบนี้เขต backend ไม่มี ready task ที่ dep ครบ **และ** อยู่ใน zonePaths จริงเลย (P0-BE-07/08/09/11/13/14 ล้วน dep chain จาก P0-BE-06 ที่ยัง `review` · P0-BE-12=blocked B-009 · P0-BE-04/05/15 นอก zone) → คิวตัน ต้อง Wei ตัดสิน (ยกเข้า journal มาแล้ว 2 รอบ ยังไม่เป็น blocker ทางการ) | (ก) เพิ่ม `packages/tokens`+`packages/i18n` เข้า `zonePaths.backend` ใน `scripts/loop-config.json` (ให้ backend agent ทำ P0-BE-04/05 ตาม TASKS.md header) — Wei แก้ไฟล์เขต devops · **แนะนำ** ตรงเจตนา TASKS.md · (ข) มอบ tokens/i18n ให้เขตอื่น (เช่น web หรือสร้างเขต platform) แล้วย้าย P0-BE-04/05 ออกจากตาราง backend · (ค) Wei ทำ P0-BE-04/05 เอง · **P0-BE-15**: ย้ายเป็น task เขต devops (`scripts/` เป็น devops) หรือยืนยันให้ devops agent ทำ — backend เขียน `scripts/` ไม่ได้ | (ก) เพิ่ม packages/tokens + packages/i18n เข้า zonePaths.backend — BE-04/05 เป็นงาน backend · scripts/ = เขต devops ต่อไป (ตอบเช้า 12 ก.ค.) | ตอบแล้ว — นำไปใช้ |
| B-015 | P0-MOB-04 (mobile screen inventory) | zone CLAUDE.md (sacred) + task ระบุ mobile*.jsx **รวม 31 จอ** แต่ enumerate source ครบทั้ง 5 ไฟล์ (window exports + top-level defs) ได้ **26 จอ distinct** เท่านั้น: mobile.jsx=5 (Inbox/Detail/ApproveSheet/RejectSheet/Notifications) · mobile-screens.jsx=12 (SrvNewReport/SrvTrack/TechJobs/TechClose/FieldProgress/FieldGR/FieldQuickPR/FieldStock/FieldCheckin/FieldHSE/ExecDashboard/SalesCRM) · mobile-pm.jsx=5 (PMJobs/Checkin/Checklist/Notes/Close) · mobile-field.jsx=4 (StGRList/StReceive/FmProgress/FmAccept) · mobile-preview.jsx=1 host (MobilePreview + router) = 26+host=27 · MOBILE_GROUPS ใน mobile-preview ก็ list 26 จอตรงกัน · ไม่มีจอ orphan ซ่อน. สมมุติฐาน "31" ที่เป็นไปได้สุด = 26 (MOBILE_GROUPS) + 5 (จอ approval ใน mobile.jsx นับซ้ำเป็น iOS-frame preview แยกตาม INVENTORY.md L70 "Mobile approval screens — แสดงใน iOS frame") = 31 (นับซ้ำ approval). gate = "ครบ 31 จอ" จึง cover ไม่ได้ถ้าไม่แต่งจอเพิ่ม (ผิด fidelity) หรือ redefine count เอง (ผิด PLAN.md §0 กฎ 4) — escalate | (ก) inventory list **26 จอ distinct** + host (นับ 26/27 เป็น complete · แก้เลข "31" ใน zone CLAUDE.md sacred เป็นงาน Wei) — **แนะนำ** ตรง source · (ข) list **31 แถว** โดยให้ 5 จอ approval ปรากฏสองบริบท (mobile.jsx standalone iOS-frame + mobile-preview MOBILE_GROUPS) = 26 distinct + 5 duplicate context · (ค) Wei ระบุนิยาม "จอ" + รายชื่อ 31 จอที่ต้องการเอง | — | รอ Wei ตอบ |
| B-017 | P0-WEB-03 (i18n wiring) → ใช้จริงเมื่อ P0-WEB-05+ port จอที่มี list-footer/filter-count | **PHRASE_PATTERNS (ประโยคมีตัวเลข) ไม่มี key ใน `i18n-full.json` + ถูก hook `i18n-guard.sh` บล็อก**: prototype `i18n.jsx` L617-622 มี 2 pattern แปล DOM สำหรับข้อความไดนามิก — `แสดง X จาก Y รายการ` → `Showing/显示/عرض` และ `กรอง · N` → `Filter/筛选/تصفية` (I18N-KEYS.md §2 PHRASE_PATTERNS). production ต้องใช้ pattern เหล่านี้เมื่อมีจอ list/filter จริง แต่: (1) ข้อความ (ทั้ง Thai source key ที่ใช้ match + คำแปล zh/ar) **ไม่มีใน `i18n-full.json`** (dynamic ใส่ static key ไม่ได้) → ชน PLAN §0 กฎ 2 "ทุก text = key จาก i18n-full.json" (2) การ port pattern ลง source `.ts` ต้องเขียน Thai literal ซึ่ง hook `i18n-guard.sh` (`.claude/` = sacred) บล็อก exit 2 เชิงกล. **core wiring ของ P0-WEB-03 (provider/hook/RTL/lang-switch ผ่าน t()/tn()/tp()) เสร็จ+เขียวแล้ว ไม่ขึ้นกับ pattern นี้ · ยังไม่มีจอใดเรียกใช้ (จอเริ่ม P0-WEB-05+) → ไม่ block** | (ก) Wei เพิ่ม 2 pattern (regex src + คำแปล zh/en/ar) เป็นบล็อก `phrase_patterns` ใน `i18n-full.json` (sacred) แล้ว web อ่าน pattern จากไฟล์ ไม่ต้อง hardcode Thai ใน source (เลี่ยง hook · แหล่งแปลเดียว) — **แนะนำ** · (ข) Wei อนุมัติ `SACRED_OVERRIDE=wei-approved:B-017` ให้พอร์ต `src/i18n/phrase-patterns.ts` แบบ verbatim จาก prototype (Thai literal ในไฟล์เดียว มี comment อ้าง i18n.jsx L617-622) + ยกเว้น hook สำหรับไฟล์นี้ · (ค) เลื่อนไปทำพร้อมจอแรกที่ใช้ list-footer จริง (P0-WEB-05+) แล้วค่อยตัดสิน ก-ข | — | รอ Wei ตอบ — ไม่ block งาน |
| B-014 | P0-BE-12 (`packages/contracts` — openapi.yaml) | `docs/handoff/api-contract.md` ระบุ pattern `GET /x?filter&page` แต่**ไม่ได้ระบุรูปแบบ envelope ของ list/pagination response** (ไม่มี total/page metadata). openapi.yaml จึงคง shape เดิมของ scaffold = **bare array ต่อ list** (representative shape เดิม ไม่เดา envelope ใหม่ — ตรง TODO เดิมในไฟล์ scaffold ที่ระบุว่า finalize envelope = contract change ต้อง Wei อนุมัติ). แจ้งเพื่อทราบ + ขอ Wei กำหนดรูปแบบก่อน FE/Mobile ผูก client จริง — **ไม่ block P0-BE-12** (contract complete + codegen เขียวด้วย bare array แล้ว) | (ก) คง bare array ต่อ list ไปก่อน (ค่าที่ใช้อยู่ — ไม่ block) · (ข) ใช้ envelope มาตรฐาน เช่น `{data[],page,page_size,total}` (ต้อง regen + FE ปรับ) · (ค) Wei กำหนดรูปแบบเอง | — | รอ Wei ตอบ — ไม่ block งาน |
