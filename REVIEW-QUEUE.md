# REVIEW-QUEUE.md — คิวงานเขียวบน dev รอ Wei promote

> ตาม PLAN.md §10 Review flow: `feature → dev (auto เมื่อ CI เขียว) → main (Wei promote คนเดียว)`
> Wei ตรวจเป็น batch: อ่านคิวนี้ + `BLOCKERS.md` → คลิกเล่นบน dev เทียบ gallery → ผ่าน = promote / ไม่ผ่าน = rework task

## วิธีใช้

**ฝั่ง agent:**

1. task ผ่าน gates ครบ (ตามคอลัมน์ gates ใน `TASKS.md`) และ auto-merge เข้า `dev` แล้ว → เพิ่มหนึ่งแถวในตารางด้านล่าง
2. เปลี่ยนสถานะ task ใน `TASKS.md` เป็น `review` + เขียน journal ประจำรอบ
3. คอลัมน์ **diff** = ลิงก์/ref ของ commit หรือ PR ที่ merge เข้า dev · คอลัมน์ **ภาพเทียบ gallery** = path screenshot จอที่สร้าง คู่กับ path ภาพอ้างอิงใน `tests/visual/reference/` (งานที่ไม่มีจอ เช่น schema/script → ระบุ "—" พร้อมหลักฐาน gate ที่ใช้แทน)

**ฝั่ง Wei:**

1. ไล่ตรวจจากแถวเก่าสุด → คลิกเล่นบน dev เทียบ gallery
2. **ผ่าน** = promote เข้า `main` → เปลี่ยนสถานะ task ใน `TASKS.md` เป็น `done` → ลบแถวออกจากคิวนี้
3. **ไม่ผ่าน** = สร้าง rework task ใน `TASKS.md` (สถานะ `ready` ระบุสิ่งที่ต้องแก้) → ลบแถวออกจากคิวนี้

## คิวรอ promote


| task id | โมดูล | diff | ภาพเทียบ gallery | วันที่ |
|---|---|---|---|---|
| P1-MOB-02 | mobile · Dart client regen §6 (/org-units + project nodes + HierarchyNode) | commit `7e80cd9` feature/mobile — P0-MOB-03 pipeline rerun (normalize 47 paths · swagger_parser 12 clients/159 req/104 models) · scoped build_runner **exact-file** --build-filter (glob form hang 7 นาที — journaled) · MasterApi.listOrgUnits/createOrgUnit/updateOrgUnit/deleteOrgUnit + createProjectNode + getProjectHierarchy typed (List<HierarchyNode>) + ProjectInput.short/units/phases · diff 14 ไฟล์ +466/-3 ทั้งหมดใน lib/api/generated · sacred untouched | — (Dart client · ไม่มีจอ) · หลักฐาน: flutter analyze No issues · flutter test 16/16 · gate-4.5 inline orch-A (generated-only additive · zone mobile ล้วน) | 2026-07-15 |
| P1-PLAT-04 | platform · i18n-full.json §9 — 91 dict keys จอ ptype/cc/docnum (SACRED_OVERRIDE=wei-approved:B-062) | orch-A direct apply บน dev — compile agent + asserted python insert · ptype.* 34 (รวม module near-miss 7 key ใหม่ verbatim ตาม B-061 + toast curly quotes) · cc.* 24 · docnum.* 33 (enum RESET/LOCK labels + template 2) · skipped-existing 24 ตรวจ exact-match แล้ว · **ทุก key verify verbatim/prefix กับ jsx ก่อนเขียน** (script assert) · dict 221→312 | — (data · ไม่มีจอ) · หลักฐาน: cmp 2 สำเนา byte-identical · json.tool ✓ · i18n 19/19 · gate-4.5 inline orch-A (append-only data · sacred edit ตรง B-062 ที่ Wei อนุมัติ) | 2026-07-15 |
| P1-BE-11 | backend · cc schema superset + docnum running→text (B-059/B-060 · ไม่แตะ openapi — Entity opaque) | feature/backend 633eacb (schema + **migration 0012 ALTER-only** · 0000-0011 byte-identical ตรวจรายไฟล์ · drizzle check clean) · 239772d (seed CC_SEED 7 แถว mock verbatim + docnum running text "0418"/"0291"/BOQ "B-02 v3") · 570d424 (GET /cost-centers full fields + POST create default draft/409 dup · /doc-numbering running string) · a311ca7 (tests) | — (schema/API · ไม่มีจอ) · หลักฐาน: **api 182/182** · typecheck/lint clean · **live PG16 disposable: migrate 0000-0012 + USING-cast บนข้อมูลจริง + seed ×2 idempotent (cc7 dn10) + API boot: POST client ส่ง status approved → 201 draft ✓ + AuditLog row ✓** · gate-4.5 inline orch-A (zone api/db ล้วน · sacred untouched) | 2026-07-15 |
| P1-WEB-08 | web · port `master.company` (MasterCompany + OrgAddForm จาก `pototype/master.jsx` L20-234) | merged dev `33b0860` (gate-4.5 inline PASS · B-056 edit-chrome fix) + glyph fix `08b55f7` (toast {name} ASCII→curly · C-022) — จอใหม่ `apps/web/src/screens/master/` (master-company.tsx · org-add-form.tsx · use-org-units.ts · org-tree.ts + test · org-strings.json) + register `router.tsx` `PORTED_SCREENS["master.company"]` + `topbar.tsx` breadcrumbs `NavKey[]→ReactNode[]` (จอแรกที่ mix dict+nav crumb · ตรง ds.jsx Page display-string) · **data generated client เท่านั้น**: `GET /org-units` (envelope `.data` → pre-order tree → nested+collapse) · POST/PUT/DELETE `/org-units` (add company lvl0/dept/sub · edit partial-merge · delete cascade · parent ผูก `id` · mutation→invalidate) · **i18n key ล้วน** org.*+common.* dict + phrase `บริษัท` ผ่าน `org-strings.json` (กัน i18n-guard) + §8 residual org.unitDept/unitSub/syncStatus + note* 4 + toast* 4 (`{name}`/`{code}` interpolate) · 0 hardcode ไทย (code+comment English) · tokens ล้วน (literal เฉพาะ radius7/shadow ที่ prototype hardcode · B-037) · `ds.jsx` ไม่มี `trash` glyph → blank (§0 ห้าม invent) · native `<select>` แทน Dropdown popover (modal ไม่มี ref · shared primitive แยก task) · **ไม่ใช้ GET /companies** (prototype ใช้ list เดียว lvl0-2 · count จาก org-units lvl0 · fidelity-first) | **G5 live compose** (`POSTGRES_PORT=5433 docker up --wait` · login `somchai@rungrueang.co.th` · vite dev + temp `/api` proxy — revert แล้ว): shot 1600x1000 `/master.company` ↔ **ref=tests/visual/reference/gallery/g2/28-s.jpg** → **screen body = PASS** (เกณฑ์ human skill visual-gate): title "Company / Organization" · subtitle · card header **"โครงสร้างองค์กร · 3 บริษัท · 7 แผนก" ตรงเป๊ะ** · sync SAP line · **10 rows nested** (lvl0 brand-soft+border · lvl1 surface-2 · lvl2 transparent · icon+name+code-badge+note+kebab) · breadcrumb **"ข้อมูลกลาง › Company / Org"** · action **"+ เพิ่มบริษัท / หน่วยงาน"** · **modal OrgAddForm render ตรง prototype** (kind toggle บริษัท/แผนก · fields+placeholder+buttons) · **region pixel diff (TOL48):** whole=5.01% · content(x≥244,y≥56)=4.11% · org-card=4.00% — **ทั้งหมด = shell chrome + seed data** (ไม่ใช่ body จอ): (1) topbar back-nav strip อยู่ใน ref capture แต่ ported shell ไม่ render → content offset ~44px uniform (ghost ใน heatmap) (2) sidebar C10 badge + pkg-menu (exec ซ่อน) = live query ≠ mock hardcode (3) switchers vs back-button — **§0 อนุญาต "ต่างได้: ตัวเลขข้อมูล seed"** · body เอง identical · API verify: `GET /org-units` = 3 บริษัท/7 แผนก ตรง seed · **CI:** typecheck ✓ · vite build (217 modules) ✓ · **63 web unit** (+10 org-tree) ✓ | 2026-07-14 |

> ✅ **batch #3 (21 งาน) promoted → `main` `43e0b70` เมื่อ 2026-07-14** — sacred rounds 4 (i18n §1/§5/§7/§8 + envelope B-014 + contract §4/§6) · routes 6 · shell 5b · Dart regen · audit อิสระ 14-agent = 0 blocker (main tree == fde14e6 เป๊ะ · diff ว่าง) · queue เคลียร์ · **batch #4 เริ่มสะสมจาก WEB-08 (master.company กำลัง port)**
> ✅ **batch #2 (46 งาน) promoted → `main` `1eb2ecb` เมื่อ 2026-07-13** — Phase 0 ครบ + P1 login · audit อิสระ 0 defects (sacred integrity · migration 0000-0008 · gates 28/29 · api 81/web 24 · seed 113/113) · queue เคลียร์
> แถวใหม่จะสะสมเมื่อมีงาน merge เข้า `dev` รอบถัดไป (WEB-05 shell · DEV-06 secret กำลังจะเดิน)
| P1-WEB-13 | web · port `master.model` (MasterModel + ModelAddForm จาก `pototype/master.jsx` L507-578 + L434-505) | **[✅ merged dev `4e79a3f` · gate-4.5 PASS · G5 g2/33 PASS · batch-4]** จอใหม่ `apps/web/src/screens/master/` (`master-model.tsx` · `model-add-form.tsx` · `model-cards.ts` +9 unit test · `use-models.ts` · `model-strings.json`) + register `router.tsx` `PORTED_SCREENS["master.model"]` · **data generated client เท่านั้น**: `GET /models`(envelope `.data` B-014) → `toModelCard` narrow opaque Entity `{id,code,type,area,bed,bath,parking,price,currency_code,status,color,unit_count,bom_item_count}` · `POST /models` = create (body `{code,type,area,bed,bath,parking,price,currency_code:'THB'}` · **price = form-millions ×1e6 = FULL baht** · **ไม่ส่ง color/status** — server assign color+status='draft' B-050 · invalidate `['models']` reuse key ร่วม `useModels()`) · **BOM cell = real `bom_item_count`** (mock `248+i*30` L563 DROPPED §0 กฎ 3) · bottom count = real `unit_count` · **edit-btn (L569) = render-only dead button** (ไม่มี PATCH /models — B-050 · ไม่ invent) · BOM-btn + add = `ctx.notify` เท่านั้น · house `<svg>` verbatim (ds.jsx ไม่มี house glyph) · **price ÷1e6 display / ×1e6 POST** (`formatModelPrice` bare number + `model.priceUnit` suffix) · **i18n**: dict model.subtitle/addTitle/addSubtitle/addInfo/priceLabel/notifyOpenBom/phCode/phType/err* + fieldCode/Type/Area/Price/Bed/Bath/Parking + common.cancel · **B-063 keys** parkingLabel/priceUnit/toastAdd · tn navTitle "Model / แบบบ้าน" + tp phrase ใช้งาน/ร่าง/ตร.ม./รายการ/ยูนิต ผ่าน `model-strings.json` (กัน i18n-guard) · exact-reuse `model.fieldBed`/`fieldBath` เป็นคำ card spec (B-057 precedent · flag ใน header) · 0 hardcode ไทย (code+comment English) · tokens ล้วน (#fff + boxShadow + alpha suffix 22/08 verbatim B-037 · server color hex ตรงตาม master-project) | **gates (agent)**: typecheck 0 ✓ · vite build 227 modules ✓ · **web unit 86/86 ✓** (+9 model-cards: formatModelPrice 8.24/0.00/NaN/neg · toModelCard narrow+defaults+camelCase · hasBom · statusActive) · **G5 + gate-4.5 = pending-orch** (orch merge → screenshot เทียบ ref) · ref `tests/visual/reference/gallery/g2/33-s.jpg` | 2026-07-15 |
| P1-WEB-14 | web · port `users` (UsersPermissions + UserAddForm + RoleAddForm จาก `pototype/master.jsx` L906-1001 + L1003-1054 + L1056-1116) | **[✅ agent gates PASS · G5 g2/36 + gate-4.5 = pending-orch · batch-4]** 7 ไฟล์ `apps/web/src/screens/master/` (`users-permissions.tsx` · `role-add-form.tsx` · `user-add-form.tsx` · `role-matrix.ts` +18 unit test · `use-users.ts` · `users-strings.json`) + register `router.tsx` `PORTED_SCREENS["users"]` · **data generated client เท่านั้น**: `GET /roles`(envelope `.data` B-014) → `toRole` narrow opaque Entity `{id,name,approval_limit:num/null,currency_code,approval_level:0-4,perms:11×5,user_count}` · `GET /users`(envelope) → member count `countMembersByRole(users,roleId)` (C10 · mock `c` L896 DROPPED §0 กฎ 3) · **PUT `/roles/{id}`** = save serialized matrix (`serializePermMatrix`) — **mock `ctx.notify` L995 → real PUT** attached to prototype's existing read-only save-btn (**ไม่ invent editable RoleEditForm** — §0 · flag ใน header) · `POST /roles` (RoleAddForm interactive `togglePerm` matrix · body `{name,approval_limit,currency_code:'THB',approval_level,perms}` · invalidate `['roles']`) · `POST /users` (UserAddForm · body `{name:first+last,email,dept:<code>,role_id,status}` · **phone kept visually · DROPPED จาก POST** — /users ไม่มี phone column · username server-derived จาก email · invalidate `['users']`) · **TWO matrix renders แยกกัน** (§0): main = read-only `<div>` cells (ไม่ clickable) · RoleAddForm = interactive toggle button — **ไม่ share component** · **auth-limit tri-state** `formatAuthLimit(limit,level)`: number→`formatMoney`+฿ · null&level>0→`role.limitUnlimited` var(--ok) green · null&level0→em-dash (language-neutral) · **Director seed null+level4 → "ไม่จำกัด" VERIFIED** (packages/db seed ROLE_DEFS dir · roles.ts toWire null-passthrough) · **i18n**: perm.view(t) col1 + สร้าง/แก้ไข/อนุมัติ/ยกเลิก(tp phrase · NOT merged into 1 translator) cols2-5 · tn breadcrumb ระบบ+ผู้ใช้&สิทธิ์ · t users.title H1 (ไม่ใช่ nav crumb) · org.noteCountUnit `คน` · 11 module labels + "Permission Matrix" = English ASCII literal (role-matrix.ts) · **dept MIXED routing**: CONS→tp phrase `CONS — ฝ่ายก่อสร้าง` (ไม่มี dict.CONS) · PROC/FIN/SLS/ADM/WH→`dept.*`(t) — แต่ละ label มี key จริง 1 ตัว (ไม่ invent · grep-confirmed) · phone label `เบอร์โทร`→tp · **B-064 keys**: `users.rolePanelCount` · `role.matrixLevelLine` (RIGHT sub-line level>0) · `role.limitUnlimited` · `users.notifyPermSaved` · `users.addUserRoleHint` (bold {role}/{limit} · prototype bold "Username" = inline key text คงปกติ) · `users.notifyUserAdded` · `role.notifyAdded` · 0 hardcode ไทย (BAHT ฿ = `฿` escape · no ฿-only key · em-dash `—` · #fff verbatim B-037 · comments English) · tokens ล้วน (brand-soft/brand active row · surface-2 header · ok/surface-3 cells) · full ds.jsx Dropdown popover ไม่ port → native `<select>` (org-add-form precedent · modal ไม่มี G5 ref) | **gates (agent)**: typecheck 0 ✓ · vite build 233 modules ✓ · **web unit 104/104 ✓** (+18 role-matrix: buildPermMatrix pad/clamp 11×5 · serialize round-trip · togglePerm immutability+out-of-range · formatAuthLimit tri-state (Director unlimited) · countMembersByRole · toRole narrow+defaults · approvalLevelLabel clamp · formatMoney) · check:routes PASS · **G5 + gate-4.5 = pending-orch** (orch merge → screenshot เทียบ ref) · ref `tests/visual/reference/gallery/g2/36-s.jpg` | 2026-07-15 |
| P1-WEB-09 | web · port `master.project` (MasterProject + BlockAddForm จาก `pototype/master.jsx` L311-420 + L247-309) | **[✅ merged dev a6d5d86 · B-057 applied (446f179) · gate-4.5 PASS · G5 g2/32 PASS · batch-4 row 2]** จอใหม่ `apps/web/src/screens/master/` (master-project.tsx · block-add-form.tsx · use-project-hierarchy.ts · project-blocks.ts + test · project-strings.json) + register `router.tsx` `PORTED_SCREENS["master.project"]` · **data generated client เท่านั้น**: `GET /projects`(envelope)+`resolveActiveProject` (active project+phases · default=RJP realestate สำหรับ somchai) · `GET /project-types`(envelope) → hierarchy labels H=โครงการ/เฟส/บล็อก·อาคาร/ยูนิต/Model (**หมายเหตุ: task premise "`GET /projects/{id}/hierarchy` ยังไม่ impl" = STALE — P1-BE-10/B-053 merged แล้ว · route impl+registered+seeded · wire จริงตาม §5 ห้าม fake**) · `GET /projects/{id}/hierarchy`(envelope) → blocks (kind=block) + real units/sold/built counts · `GET /models`(envelope) → block `model_id`→`{code}({type})`+color join (§0 กฎ 3 FK→id) · `POST /projects/{id}/nodes` = add block (BlockAddForm · server สร้าง N empty units) · unit-grid 14-col threshold algo (soldBuilt/sold/built/empty by index) verbatim master.jsx:382-389 · ScopePill = filter-pill visual (ds.jsx Dropdown popover ไม่ port · เหมือน WEB-08 native-select · options=real data · notify onChange) · **i18n**: dict project.legend*/unitViewBtn/notifyUnitGrid/createBtn + block.field*/err*/addBtn + common.import/cancel + master.breadcrumb + tp phrase เพิ่ม/ยูนิต/ก่อสร้าง (project-strings.json) · 0 hardcode ไทย (code+comment English) · tokens ล้วน (#fff verbatim B-037) · **residual 6 key = B-057** (2 เห็นใน g2/32: โครงสร้าง/ก่อสร้างเสร็จ + 4 modal/toast) · **CreateProjectForm ("สร้างโครงการใหม่" wizard) = B-058 defer** (out of L311-433 range + POST /projects ยังไม่ impl) | **gates**: typecheck = เหลือ **6 error = B-057 key เท่านั้น** (rest type-clean) · vite build (222 modules) ✓ · **web unit 77/77 ✓** (project-blocks 14 tests: toBlocks/model-join/unitStatus/blockTotals/builtPct/typeHierarchy) · **G5 = BLOCKED-ON-KEY** (2 visible string render เป็น key-name จน B-057 apply — layout+data wiring verify ผ่าน seed+test แต่ยังไม่ screenshot · gate-4.5 หลัง B-057) | 2026-07-15 |
