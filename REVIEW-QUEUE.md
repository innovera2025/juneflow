# REVIEW-QUEUE.md — คิวงานเขียวบน dev รอ Wei promote

> ตาม PLAN.md §10 Review flow: `feature → dev (auto เมื่อ CI เขียว) → main (Wei promote คนเดียว)`
> Wei ตรวจเป็น batch: อ่านคิวนี้ + `BLOCKERS.md` → คลิกเล่นบน dev เทียบ gallery → ผ่าน = promote / ไม่ผ่าน = rework task

> ✅ **41st 0-drift promote → `main` `bb9ded8` (2026-08-04 · pin a1d8912)** — **B-261 mobile money-write idempotency CONTRACT** (SACRED · Wei ruled mechanism + ratified exact diff): the offline SyncProcessor replays at-least-once; the natural-key JV guard only covers ACTION writes; a CREATE money-write (POST /gr) mints a new id per replay → double-post without this. Fix (mirrors B-167): SACRED openapi createGr +optional idempotency_key · **migration 0056** nullable gr.idempotency_key + PARTIAL unique index gr_idempotency_uq (WHERE NOT NULL) · handler catches 23505 → re-reads the ORIGINAL gr THROUGH the po/wo→project scoped chain (tenant-safe, gr has no company_id) → 201 not-dup not-409 · key absent → unchanged. orch-B money-skeptic (executor≠verifier): api 1252 (+5) · **gate-4.5 8/8** — cross-tenant idempotency-key collision resolves to 409 through the company_id INNER-JOIN chain, NEVER leaks another tenant's GR · money=SERVER (replay ignores request body, returns ORIGINAL) · **LIVE E2E real-PG: replay same key → ONE receipt same id; new key → distinct — 1 passed** (migration 0056 live-confirmed). Canonical template for attendance/progress. 0 whole-file deletions → 0-drift EMPTY (11 files · +13738 dominated by drizzle snapshot auto-gen). Follow-up B-263 (gate catch on constraint-name · non-blocking).

> ✅ **40th 0-drift promote → `main` `e55bbcf` (2026-08-04 · pin 6f95d48)** — **mobile OFFLINE-WRITE foundation** (orch-B drove execute→verify→merge · B-242 ruled level-(ก) queue-and-replay by Wei): **SyncProcessor (ก)** the deferred drain seam — FIFO by createdAt · 2xx→removed · 4xx→failed dead-letter (kept+surfaced+never-retried+non-blocking) · 5xx/network→pending+attempts+STOP (stuck head blocks behind = FIFO integrity) · no-silent-drop · re-entrancy-guarded · money=SERVER (replay SAME payload to SAME endpoint) · 12 drain tests. + **E2 PM GPS check-in** (money=NONE) the first offline-write screen: tap → real device coord via injectable GpsSource (**geolocator ^13.0.1 · B-260 Wei-approved** stack add) → enqueue POST /pm/workorders/:id/checkin → drain · 3 honest states (online-confirmed / offline-QUEUED never-fake / failed) · denied/off/no-fix → honest no-enqueue, NEVER a fabricated coordinate. **F-1 saga**: agent flagged the endpoint requires gps (400 blank) → I verified from pm.ts → Wei ruled add geolocator → wired behind an injectable seam (online-success + permission-denied both tested). Web-only target = browser Geolocation API (no manifest); native entries documented not-guessed. ZERO-MINT (10 dict stable-ids) · zero-Thai-in-lib · no sacred (pubspec+geolocator only). gate-4.5 PASS 8/8 (drain control-flow traced · termination guaranteed) · flutter clean + **test +255** independent. 0 whole-file deletions → 0-drift EMPTY (22 files +2295/-94). Forward-deps: B-261 (money-write idempotency contract · sacred) · B-262 (durable drift queue).

> ✅ **39th 0-drift promote → `main` `a950923` (2026-08-04 · pin 874b7be)** — **mobile approval FLOW end-to-end** (orch-B drove execute→verify→merge autonomously): **B-259 backend prep** (approvals-inbox InboxRow +doc `id` → mobile can navigate to detail · additive opaque-Entity no-openapi · gate-4.5 5/5 · api 1247) + **MOB-INBOX** (GET /dashboard/approvals-inbox · money=SERVER off-wire · honest-omit title/requester/urgent/project no-column · PR-tap→detail · PO/WO shown-inert) + **MOB-PR-DETAIL + seam** (GET /pr/:id + /projects · real title/need_date/vendor/requester/amount/items · item-name em-dash [WIRE GAP web-consistent] · banner status-only NO-tier [approval_step always 0 single-shot backend] · Navigator.push prId=detail.id → approve/reject become a real flow, path-id verbatim-amount). **executor≠verifier saga**: my spec had 3 contract errors, the execute-agent flagged all 3 (no guessing), I ruled each from pr.ts ground-truth + web pr.form precedent. **ZERO-MINT** (mob.approval.* namespace pre-covers both · no sacred touched). Verified: independent flutter analyze clean + **test +224** · **gate-4.5 PASS 8/8** (cross-checked live dashboard.ts/pr.ts) · money=SERVER · zero-Thai-in-lib. pixel-G5 N/A (mock refs). 0 whole-file deletions → 0-drift EMPTY (23 files +3069/-15). **Mobile approval flow COMPLETE.** Remaining mobile: PO/WO detail (future) · offline B-242.

> ✅ **38th 0-drift promote → `main` `18c7ca8` (2026-08-04 · pin fe904f3)** — **MOBILE LANE** whole bundle (executor≠verifier · gate-4.5 PASS 7/7 each · flutter analyze clean · test 170/170 independent): **MOB-SHELL-00** (5-tab MTabBar B-241 honest-disable + 26-route MobileScreenRouter + shell primitives + Dio/auth no-secret · 2 real app-bug fixes) + **MOB-REF-01** (26 mobile Fiori reference PNGs = mobile-G5 truth) + **approval group 3/3** notif(money=NONE)/approve+reject(authority=SERVER path-id · reject{reason}req · honest-empty prId) + **read group 3/3** sales-crm(GET /sales/leads)/st-grlist(GET /po+/vendors)/pm-jobs(GET /pm/workorders+/assets · deriveStatus==web wo-rows.ts) all money=NONE. Each = raw-Dio opaque-Entity repo → pure *_agg.dart → shell-primitive UI honest-empty · zero-Thai-in-lib. **i18n mint rounds 4-6** (B-254/255/256/257/258 · 26 net-new phrases · 3 copies byte-identical · mobile asset regen). pixel-G5 N/A (mock refs) → fidelity via gate-4.5. 0 whole-file deletions → 0-drift EMPTY (95 files +7160/-31). **MOBILE FOUNDATION + 8 screens COMPLETE.** Remaining mobile blocked: inbox/detail (B-251/252 orch-A) · E2-E5 offline (B-242 Wei).

> ✅ **37th 0-drift promote → `main` `9da8497` (2026-08-04 · pin 3182504)** — **gl.projectpl** (money=NONE per-project P&L · gate-4.5 CONFIRMED never-recompute) + **ap.cn-dn** (money=SERVER CN/DN register · approve-wired-not-surfaced ap.pv-precedent). i18n mint round-3 (B-249 gl.projectpl 5 [deduped 23→5, skip-if-present no clobber] + B-250 ap.cn-dn 25 · phrases 1012→1042). G5 2/2 strict-0. **WEB LANE COMPLETE** — all portable web screens ported (remaining gl.revrec/sales.dashboard/timeline/alloc/sync are Wei-deferred). 0-drift EMPTY (25 files +3218/-0).

> ✅ **36th 0-drift promote → `main` `d305a4b` (2026-08-04 · pin 4552de9)** — **opex thin-honest** (B-246=ข · Wei-ruled): renders backed budget grid + create ({dept,year,months[12]} money=SERVER THB+409-dup), honest-em-dashes every un-backed section (used/committed/%used/plan-vs-actual/multi-year/transfer) — zero fabrication. gate-4.5 PASS · vitest 1478 · G5 strict-0. **i18n mint round-2** (B-248 · 19 phrases via ข ruling · phrases 993→1012 · 3 copies byte-identical). 0-drift EMPTY (15 files +1347/-0). = 7th overnight web screen.

> ✅ **35th 0-drift promote → `main` `354254b` (2026-08-04 · pin 7d9b37f)** — OVERNIGHT autonomous web-port wave (orch-B drove execute→verify→merge itself): **6 web screens** subcon.handover · wo.form(money=SERVER) · po.form(money=SERVER) · ap.deposit(money=SERVER) · notifications · petty(money=SERVER) — executor≠verifier gate-4.5 caught 3 real design-fidelity defects → reworked. **i18n mint round-1** (B-247 Wei-ratified · 59 phrases both copies + mobile asset regen · phrases 934→993 · 0 existing-change). G5 first-capture 6/6 strict-0 (manifest 90→96). Blockers: B-245 (po.form double-PO) · B-246 (opex→Wei ruled thin-honest ข) · B-247 (mint). 0 file-deletion → 0-drift EMPTY (40 files +5434/-4).

> ✅ **34th 0-drift promote → `main` `0a2224a` (2026-08-03 · pin 4e119e9)** — this-session verify-lane wave: WEB port (admin.overview · labor.payroll money=SERVER · subcon.progress thin-honest · pr.form submit/approve/reject G5-exempt-per-gr-create) + BACKEND finance net-new (gl.revrec Dr1130/Cr4020 · ap.cn/dn Dr2010/Cr5020·Dr5100/Cr2010 · gl.projectpl P&L+opex · petty Dr5100/Cr1010 — migrations 0051-0055, all live money-proven incl 0053/0055 double-post rejection) + MOBILE i18n (B-240 batch-1 34 keys dict→3701 · MOB-I18N-01 Dart runtime flutter 68/68 · asset==sacred adba2e8b drift-gate-real) + G5 baselines + E2E_LIVE specs. orch-B: gate-4.5 each · live money-proof (b231 3/3 · b233 1/1 · labor · b227 2/2) · api 1247/1247 · web 1406/1406 · 0 file-deletion → 0-drift EMPTY. Sacred Wei-approved: openapi (petty/apcn/revrec/projectpl/opex) · i18n×2 (B-240) · migrations 0051-0055. 73 files +103785/-30.

## วิธีใช้

**ฝั่ง agent:**

1. task ผ่าน gates ครบ (ตามคอลัมน์ gates ใน `TASKS.md`) และ auto-merge เข้า `dev` แล้ว → เพิ่มหนึ่งแถวในตารางด้านล่าง
2. เปลี่ยนสถานะ task ใน `TASKS.md` เป็น `review` + เขียน journal ประจำรอบ
3. คอลัมน์ **diff** = ลิงก์/ref ของ commit หรือ PR ที่ merge เข้า dev · คอลัมน์ **ภาพเทียบ gallery** = path screenshot จอที่สร้าง คู่กับ path ภาพอ้างอิงใน `tests/visual/reference/` (งานที่ไม่มีจอ เช่น schema/script → ระบุ "—" พร้อมหลักฐาน gate ที่ใช้แทน)

**ฝั่ง Wei:**

1. ไล่ตรวจจากแถวเก่าสุด → คลิกเล่นบน dev เทียบ gallery
2. **ผ่าน** = promote เข้า `main` → เปลี่ยนสถานะ task ใน `TASKS.md` เป็น `done` → ลบแถวออกจากคิวนี้
3. **ไม่ผ่าน** = สร้าง rework task ใน `TASKS.md` (สถานะ `ready` ระบุสิ่งที่ต้องแก้) → ลบแถวออกจากคิวนี้

## คิวรอ promote

> **PROGRAM-3 Wave-2 — P3-BE-03 SV-1 sales.service SACRED QUEUED (2026-07-27 · orch-A · pin 80d2aa5)** — sacred openapi paths-append 7 ops (list/detail/create + schedule/start/fix/close · wei-approved:B-159 · 0 new component · client regen additive) + sales-service.ts (B-149 status machine · SV-2 warranty derived · money=NONE · no migration) await orch-B verify+merge. LIVE status-machine proof (full received→closed + wrong-state/terminal 409 + 404 + 401). **PROGRAM-3 SALES+LAND Wave-1 — P3-BE-02 batch-1 WRITES QUEUED (2026-07-27 · orch-A · pin b36c142)** — migration 0042 + 13 write handlers (SA-7 booking/down receipt-JV · LA-4 land-deal-buy server-deposit · Wave-A no-JV + reads · finance.create-gated money surfaces) on feature/backend await orch-B re-gate+merge. gate-4.5 round-1 FAIL (booking new-unit double-post race) → FIXED (re-key source_doc booking:<unitId>) · REAL live race proof 4-concurrent→1×201/3×409 1-unit 1-JV. money=SERVER + idempotent + fail-closed all live-proven. transferSalesUnit deferred (B-162). **WEI-RULED FIXES — P2-BE-58 B-144+B-149 QUEUED (2026-07-27 · orch-A · pin b0b0500)** — B-144 payroll POSTING_MAP realign (5030→1140) + B-149 optimistic-lock guards across 14 approve/commit handlers. gate-4.5 round-1 caught the 7 updateThroughChain guards were non-atomic (status only in resolve SELECT) → FIXED b0b0500 (guard on the FINAL UPDATE) · deterministic FOR-UPDATE-lock race proof: approve-payment loser 409 · no double-payment. Unblocks the promote. **PROGRAM-3 SALES+LAND — P3-BE-01 land-sales Wave-0 QUEUED (2026-07-27 · orch-A · pin 077823f)** — zero-ruling reads (GET /sales/leads + /land/plots · mount only · no sacred/migration/i18n) on feature/backend await orch-B verify+merge. **PROGRAM-2 OP-CORE — P2-BE-57 inventory QUEUED (2026-07-27 · orch-A · pin cb2a1b2)** — inventory 13 ops + transfer/issue on feature/backend await orch-B verify+merge. Labor lane (P2-BE-55/56) merged → dev. Prior: **QUEUE EMPTY (2026-07-21 · PHASE-3 Wave-0 + i18n Wave-A PROMOTED → main `6902dfb` · 0-drift · pin db76042)** — P2-BE-44/45 promoted → history. Next batch = orch-C gl.inbox + orch-A round-A.

| task id | โมดูล | diff | ภาพเทียบ gallery | วันที่ |
|---|---|---|---|---|
| W1-FORGOT-WIRE | web — wire the forgot-password form to `POST /auth/forgot` (it was shipping a MOCK) | → dev 2caa7fc (apps/web · 6 files · NO sacred) · **the form was a faithful 1:1 port — and that was the defect**: the prototype's submit is `ctx.notify`, a mock, and the port copied the mechanic ⇒ it closed the modal and toasted *ส่งลิงก์ตั้งรหัสใหม่ไปที่ {email} แล้ว* **without a single HTTP request** (§0 rule 1 satisfied by breaking rule 3) · `POST /auth/forgot` was mounted earlier today so the mechanic is now real with the markup untouched (reviewer diffed it against `pototype/extra-screens.jsx` itself) · **the handler's anti-enumeration design was respected**: uniform 200 for known/unknown/malformed + swallowed store failure ⇒ the UI has ONE success path and the outcome type **cannot represent 'found'**; throttle keys are requester-supplied so 429 is not an oracle either, and 429 is checked BEFORE the generic error path (a 429 also parses an error body); re-entry guarded by a **ref not state** because two clicks in one tick both read the pre-update value and would burn 2 of 5 slots · every string from an existing key, `login.forgotEmailFallback` retired (it existed only to fill the mock's toast) · orch-B VERIFIED: gate-4.5 **PASS**, revert probe both sides — restoring the mock turns **8 of 18** new tests RED and the executor stated plainly which of the other 10 stay green and why (8 unit tests hit the new module directly; 2 symmetry assertions are vacuously satisfied by a mock) · typecheck 0 · vitest **1694** / 112 files · routes 100%. | no pixel change at rest · **HONESTY, recorded not glossed:** `login.forgotSent` still asserts delivery while `deliverReset` is a no-op seam (B-282) — the slice deliberately did NOT soften the copy (that would be the form papering over a backend gap, and any replacement would be an invented string); it becomes true with no web change once delivery is real. B-300 filed: no i18n copy exists for a throttled state | 2026-08-05 |
| W1-I18N-MINT | **sacred i18n round — 34 phrases applied** (B-273 · B-280 · B-287 · B-292 · B-298) | → dev 121fc35 · applied by orch-B under Wei's STANDING pre-ratification, with every condition checked against the files BEFORE the edit: (1) Thai byte-exact from a prototype .jsx — **34 of 36 verified verbatim**, 2 HELD (see right) · (2) en=zh=ar=the Thai key, matching the 320 existing fallback entries · (3) **0 existing phrases and 0 existing dict entries changed or removed** (verified by parsing HEAD's copy and diffing) · (4) all three copies byte-identical before (6026bc15) and after (dcfb9641) · phrases **1065 → 1099**, dict unchanged 3701 · `ปกติ` skipped (already present — reconciles the pm-checklist handoff's '6 net-new' against its 7 entries) · **follow-through**: the exec sidecar test asserts the mint list against the sacred file and correctly went RED the moment these landed (its message says a slot present in the file is no longer a mint) — the 7 exec slots moved to `_zeroMintPhrases`, the stale header corrected, and revert-probed (putting one back turns it red again) · flutter 563 · web vitest 1694 · @juneflow/i18n 19/19. | **EFFECT: en/zh/ar users stop seeing raw Thai on the 10 mobile screens merged today** · ⚠️ **2 values HELD for Wei** — `ตรวจแล้ว {n}/{count}` (mobile-pm.jsx:105 writes the template itself, only the numbers tokenised) and `ซ่อม {n}` (mobile-pm.jsx:201 is MOCK row data). Six placeholder-bearing phrases already exist in the file so the pattern is precedented, but 'byte-exact' does not literally hold and the standing order says stop and ask | 2026-08-05 |
| W1-MOB-EXEC | mobile `exec` — executive dashboard (read-only, no write/queue/tap target anywhere) | → dev 0044062 (apps/mobile · NO sacred) · 3 live feeds: `GET /dashboard/summary` · `GET /boq/reports/evm` (S-curve) · `GET /dashboard/approvals-inbox` through the SAME repository the inbox screen uses · orch-B VERIFIED across **2 rounds**, 7 findings in R1, three that mattered: **F1 — the S-curve, the slice's ONLY genuinely real data block, rendered EMPTY on the seeded stack.** The rework brought up an isolated seeded DB+API and got ground truth: `evm_snapshot` has rows for exactly ONE project (RJP), all 7 share a `created_at` so the id-ASC tiebreak makes BBT primary, and the screen's real request returns `series: []`. **Dropping the scope is NOT the fix** — unscoped merges every project into a curve belonging to none and only looks right here because one project has data ⇒ scope kept, the card now NAMES its project from `summary.project_name`, empty state under that name, and every 'REAL' claim in the file headers rewritten · **F3 — the entire S-curve painter had ZERO coverage**: the reviewer made `paint()` a no-op and all 455 tests passed. Now the painter records canvas calls and pins geometry to the served series (sampled points on the polyline · arc length == Σ segments · a missing period lands at its real elapsed-months position and misses the index-spaced one by >10px) · **F5/F6** — the approvals count was a client-filtered length not the envelope `total`, and `฿` was hardcoded while `currency_code` was parsed and never read (a USD amount rendered as THB) · **R2 PASS** · revert probes both sides: no-op paint → 8 of 9 painter tests RED (the 9th 'empty series draws nothing' correctly survives — stated, not counted) · client count → 4 · hardcoded glyph → 2 · dropped scope label → 2 · **the rework reported two faults against itself**: it had introduced 7 Thai codepoints into lib comments (a property the reviewer had verified as zero) and removed them, and its i18n note said 8-of-15 zero-mint when the truth is 9-of-16 · router diff **0 removed lines** · analyze clean · flutter **563** on dev. | pixel-G5 N/A (mock refs) · B-298 mint · **B-299**: the empty-S-curve product question (4 options) | 2026-08-05 |
| W1-MOB-SERVICE | mobile after-sales SERVICE group — `srv-track` · `tech-jobs` · `srv-new` · `tech-close` | → dev 5837150 (apps/mobile · NO sacred) · one endpoint family + one state machine ⇒ one group dir and one agg (pr_action precedent); `nextServiceTransition` is the single place the machine lives so **no screen can offer a move the server would 409** · writes are online one-shot, deliberately NOT queued (a replayed create raises a second ticket — no client key on this route — and a replayed transition 409s, which the level-(ก) processor classes as permanently-failed ⇒ it would report a move that LANDED as a dead-lettered write) · dropped rather than faked, each schema-verified: unit code · timeline steps 3-5 · handover date · star rating · photo grid · preferred-slot picker · draft button · signature pad · reschedule · combined fix+close · customer-notify · orch-B VERIFIED across **2 rounds**: **R1 FAIL — the load-bearing justification for the category vocabulary was FALSE.** The slice defended its choice as 'dict + seed use the long forms'; the reviewer's set-diff showed seed stores `หน้าต่าง`/`พื้น` while dict carries `หน้าต่าง/ประตู`/`พื้น/กระเบื้อง`. `category` is **free text** ⇒ the tile's string is what lands in the DB: the port would have **forked the stored vocabulary** · **R2 PASS** — the rework ran a 3-way diff (dict/seed/prototype), found only **1 of 6** values common to all three, and instead of picking a winner **WITHHELD the write**: the six tiles still render as prototype chrome but are honest-disabled and the create sends **no `category` key at all** (column stays NULL); `newTicketBody` keeps the parameter so Wei's ruling is a one-line change in one place · it corrected the false claim in **all four** artifacts that repeated it (blocker row · apply.json · sidecar `_source` · screen-map) because **Wei rules on the row's text** · and it caught that its OWN earlier 'doc-comment sentence extension' in the shared router was in fact a re-wrap (the previous reviewer had waved it through), restoring the line byte-for-byte · analyze clean · flutter 422 on branch · router diff 0 removed lines. | pixel-G5 N/A · **B-292 now asks a DIFFERENT question than its previous revision** — the old option was framed as 'matches seed and the web register', which it does not for 2 of 6 values, so **any answer given against the old text was given on a false premise and should be re-asked** · B-293 · B-294 | 2026-08-05 |
| W1-MOB-PM-CLOSE | mobile `pm-close` — **PM chain COMPLETE 3/3** (jobs→checkin→checklist→notes→close) | → dev 9f39c8c (apps/mobile · NO sacred) · **PASS first round** · read-only BY DESIGN and honest about it: POST close would write an empty body (cause/fix/advice belong to pm-notes), signature capture needs an unratified package AND the column's encoding is undefined ⇒ **the CTA renders with no tap handler rather than firing a no-op**, and the prototype's success view is DROPPED IN FULL (it announces a completion status with no column behind it, over a certificate claim with no certificate — `lineNotifyStub` is a no-op). The merged **web** port reached the same conclusion independently. start-end/total-time/parts/recipient all em-dash — no columns exist · reviewer ran **13 revert probes (6 reproducing the executor's + 7 of its own design), every one RED** · flutter **383** (baseline 338, +45) · analyze clean · zero Thai in lib. | pixel-G5 N/A (mock refs) · **B-288 is the real decision**: ship read-only + disabled CTA (done) vs restore fidelity and state things no column backs · B-287 mint 6 phrases | 2026-08-05 |
| W1-MOB-B262-QUEUE | mobile **B-262 durable offline queue + shared processor + resume drain** | → dev 7badc71 (apps/mobile · NO sacred) · **PASS first round** · before this the app's *offline-safe* claim was **not true**: `app_services.dart` wired `InMemorySyncQueue` ⇒ queued writes died on app kill · `QueueDrainProcessor` was constructed INSIDE the pm-checkin screen ⇒ nothing drained unless that screen was mounted · and no `didChangeAppLifecycleState` existed anywhere in lib despite the policy doc promising an on-resume trigger · **the ratified level-(ก) drain POLICY is unchanged** (FIFO · 2xx remove · 4xx dead-letter and continue · 5xx defer and stop · re-entrancy guard) — only where the queue and processor LIVE has moved; the reviewer diffed the policy specifically · **respected its scope boundary and did not touch the shared router files** a concurrent slice owned · flutter 411 on dev after both mobile merges (interaction verified together, not just per-branch). | — (infra · no visual) · B-289 | 2026-08-05 |
| W1-WEB-SUBCON-CUMMAP | web `subcon-accept` cumMap guards + the `subcon` alias route | → dev 5ceed48 (apps/web · NO sacred) · **PASS first round** · the live sibling the `wo.list` sweep surfaced — on an ALREADY-MERGED screen and **worse guarded than wo.list was before its fix**: `cumMap` accumulated `pct` with **no per-element guard at all**, and the render gate read only the **FIRST** period's basis via `periodMethod()` ⇒ a mixed-basis plan passed · guards added per-element (the `cumulativeContractPct` / `hasOrdinalSeq` pattern from wo.list) · **display SEMANTICS deliberately untouched** — cumulative-vs-own-share is the open question in B-277 and moves the same seed pixels as a pending re-baseline, so the slice adds guards against printing a wrong number without deciding which right number to print · also wires the `subcon` alias that `registry.ts` declared and `router.tsx` never mapped · typecheck 0 · vitest **1676** / 110 files on dev · routes 100%. | NEW-REF only if a guard fires on seed data — stated in the slice · B-290/B-291 | 2026-08-05 |
| W1-AUTH-CHAIN | **auth production chain — invited users can finally log in** (the real launch blocker) | → dev 69696d5 (apps/api · **NO migration**: the existing unused `auth_verification` table fits · **NO openapi change**: both ops were already declared) · before this, `POST /users` wrote only the dictionary row with status invited and created **no credential**, while `/auth/forgot` + `/auth/reset` were declared but never mounted ⇒ an invited user could never sign in · now: both ops mounted, credential provisioned on invite, single-use expiring reset tokens · orch-B VERIFIED across **3 rounds**, reviewer instructed to CONSTRUCT ATTACKS not read for them: **R1 3×HIGH** — forgot lowercased nothing while the slice made the store case-canonical ⇒ an invited user whose phone autocapitalises got `200 {ok:true}` with no token/mail/log, **the exact bug the slice exists to fix, one endpoint over** · the production `DbCredentialStore` was untested (every route test injects a fake ⇒ 'single use' proven of a Map): reviewer gutted **5 real security properties at once** — token made infinitely replayable · session-deletion removed · `password: null` → a known password ⇒ **every invited account would ship with a working attacker-known credential** · constraint gate removed — and the suite stayed green · a successful reset wrote **no audit row**, with the trap that the obvious one-line fix would persist the **plaintext password and raw token into audit_log** · **R2** fixed all three (each traced+probed) leaving 4 residuals · **R3** closed them. api **1318** (baseline 1258, +60) · typecheck + lint clean · no token/password/credential reaches a response, log or column. | — (backend · no visual) · **HELD pending B-283**: the slice had added a platform-wide cross-tenant block on POST /users (an address held by tenant A could no longer be invited by B — real when a subcon's PM works for two companies) whose 409 echoed the address = a cross-tenant existence oracle that did not exist before. **Reverted; the row states it is held.** Also filed B-282 (wire the merged email transport — must ENQUEUE not await, else a timing oracle behind the uniform 200) · B-286 (undeclared 400s/429, for a contract round) | 2026-08-05 |
| W1-WO-LIST | web `wo.list` re-wire — scope · progress · the installment plan | → dev 2c201f8 (apps/web · consume-only · NO sacred) · orch-B VERIFIED across **3 rounds**, the first two both the recurring class: **R1** `cumulativeContractPct` gated on a **Σ** then asserted a **per-element** fact ⇒ an all-percent plan with one unrecorded share printed a plausible wrong threshold instead of an em-dash; and `seq` was unvalidated/non-unique ⇒ a defaulted plan made every installment claim the whole contract and every row read DP · **R2** fixed both per-element and swept 9 other sites — **but hollowed out the two tests named for the mixed-basis guard** (their fixtures put pct 0 on the non-percent row, so an earlier guard short-circuited before the one under test ever ran) ⇒ the guard was correct and completely undefended · **R3 test-only**: `git diff` over both product files R2→R3 = **0 bytes** (reviewer confirmed independently); deleting the guard now turns 2 tests RED, and the cell-scoped extractors are themselves load-bearing (re-pointing one at the wrong column goes RED rather than falsely green) · typecheck 0 · vitest **1598** · routes 100% · **unpadded**: 2 of 3 added tests are controls that do not die on their own probe, stated as such. | **NEW-REF needed** — and it should be taken **AFTER** the B-277 ruling, which moves the same seed pixels · B-284 filed: the real fix is the column contract (`unique(contract_id, seq)` + validation in POST /subcon/contracts), the web guard is a shield not a cure · sweep found a live sibling in `subcon-accept-rows` `cumMap`, **worse guarded than wo.list was** — left untouched, filed | 2026-08-05 |
| W1-MOB-PM-NOTES | mobile `pm-notes` — PM completion chain step 2 (money=NONE) | → dev 50d7f93 (apps/mobile · NO sacred · both i18n copies byte-identical · zero Thai in lib) · writes POST /pm/workorders/:id/close {cause,fix,advice} into real columns through the level-(ก) offline queue · parts-used honestly omitted (no WO column — parts live on pmQuotes.parts) · orch-B VERIFIED across **3 rounds**: **R1 ×3** — the screen documented itself as claiming nothing about a closed job while its primary button read *go to summary + close job*, and the guarding test could not see the button text · a guard no test pinned (its named test passed with the guard removed) · a failed READ disabled the whole screen **including the offline WRITE the screen exists for**, undisclosed · plus every prototype citation in the sacred handoff off by ~3 · **R2** fixed behaviour but **overstated its own result** (claimed the CTA label was the only reference deviation — false, and that claim was the framing of a ruling Wei still has to make) · **R3** corrected it · reviewer designed and ran **10 of its own revert probes before reading the executor's** · analyze clean · flutter **338** (baseline 299, +39) · kBuiltRouteIds == mobileScreenBuilders. | pixel-G5 **N/A** (mobile refs capture the prototype's mock data ⇒ an honest port false-fails) · pending Wei: B-280 mint (en/zh/ar render raw Thai until applied) · B-281 · B-285 | 2026-08-05 |
| W1-PO-LIST+BOQ-ARCHIVE | web `po.list` + `boq.archive` re-wire — two 'the API sends it, the screen em-dashes it' fixes | → dev 97058b7 (apps/web · 8 files · consume-only · NO sacred · **no blocker row needed** — nothing turned out unwireable) · po.list: `poWire` returns `doc_date`/`credit_term`/`vat`, the screen rendered docDate as DASH · boq.archive: `GET /boq` returns `approved_by_name`+`approved_at` (migration 0021), the screen documented both as absent · orch-B VERIFIED (executor≠verifier): **gate-4.5 PASS first round** — the reviewer ran its OWN class sweep and revert probe before accepting, and found no defect changing rendered output, fabricating a value, or breaching zone/sacred/i18n · typecheck 0 · vitest **1600/1600 across 106 files** (clean-dev baseline 1554 → +46, two screens + two new seam-test files) · check:routes 100% · **the only batch-3 slice to pass first time** — written against the standard set tonight (enumerate every same-shape site BEFORE editing; every new test must die when the fix is reverted). | **NEW-REF needed ×2** — both screens gain real content where dashes were; Wei-approved sacred re-baseline required before the visual gate can pass · tests/visual/ untouched | 2026-08-05 |
| W1-BOQ-BOM | web `boq.bom` re-wire — the screen's own comment denied an endpoint that exists | 8e4018e+022f463 → dev 8416528 (apps/web · 5 files + BLOCKERS.md · consume-only · NO sacred) · `getModelBom` is real (openapi + models.ts handler + generated client + 4 test assertions) yet the whole BOM line table, the cost-per-house KPI and the 3 category subtotals rendered empty on top of live data; a SECOND copy of the same stale claim lived in boq-bom-agg.ts — both corrected · orch-B VERIFIED across **2 rounds**: **round 1 FAIL** — the 7 new tests were **VACUOUS**: the reviewer reverted only the fix and re-ran → 1522 still passed, tsc still clean (they all exercised pure agg functions that were already correct, so nothing protected the wiring); plus a silent line-drop (cat outside M/S/L) went live on a monetary total printed beside an UNfiltered server item count · **round 2 PASS** — a component↔hook seam test file now exists (renderToStaticMarkup + vi.mock, matching the existing ui/chart.test.tsx harness — none invented) and the **revert probe was run independently by BOTH executor and reviewer: 6 of 11 seam tests go RED**; the executor did not pad the count, stating plainly that the other 5 are negative/em-dash assertions true in both states · money honesty: any uncategorisable served row now em-dashes EVERY cross-line total (cost-per-house · 3 category shares · each band subtotal · footer · info-formula total+grand · generate-BOQ block value) while per-line qty/price/amount still render, no category invented, server's unfiltered count kept beside the em-dash = honest incompleteness · typecheck 0 · vitest **1540** on branch → **1554 on dev** post-merge · routes 100%. **Residual folded into B-272** (not fixed here): the gate keys on `cat` only — an unreadable qty/price coerces to 0 via num(), so the row parses, dropped stays 0 and a short total still publishes (reviewer-proven live: two lines contributing 0 while the KPI published 0.07 as complete; price "2,200" → parseFloat → **2**, a wrong number not a missing one). Weaker than the original — the row shows a visible 0 and the count still matches = disclosure, not silent drop. | **G5 BLOCKED** pending Wei-approved sacred re-baseline — active ref is `app-baseline/boq-bom.png` per screens.manifest.json (NOT the gallery jpg the first report named) · tests/visual/ untouched | 2026-08-05 |
| W1-MOB-PM-CHECKLIST | mobile `pm-checklist` — PM completion chain step 1 (money=NONE) | 86744fe → dev c243ef9 (apps/mobile + agents/orch-d-recon + BLOCKERS.md · NO sacred · both i18n-full.json copies byte-identical) · reads the WO's own items[] snapshot (GET /pm/workorders), writes PUT /pm/workorders/:id/checklist through the level-(ก) offline queue — positional merge ⇒ replay-idempotent, money=NONE so B-261's key contract is not needed · before/after photo slots are genuinely backed columns (PmChecklistRow) so they are wired; photo CAPTURE stays unwired (no camera package = Wei-level stack decision) and ships inert exactly as the prototype has it · orch-B VERIFIED across **2 review rounds** (executor≠verifier): **round 1 FAIL ×3** — report claimed no mobile reference image exists (pm-checklist.png exists, 132 KB; the 'no pixel-G5' conclusion was right but the reason was fabricated) · i18n handoff carried placeholder `wei-approved:B-XXX` with no BLOCKERS row ⇒ the sacred override would match nothing and the 6 phrases would never be minted · an `onTap: null → _openChecklist` change to the ALREADY-MERGED pm_checkin screen had no test and left a comment asserting the opposite of the new truth. **round 2 FAIL ×1** — blocker-id collision (**orch-B's own allocation error**: B-270 given here and later given to the grlist slice; left as-is, `SACRED_OVERRIDE=wei-approved:B-270` would have matched a ratified POST /gr contract ruling and admitted this slice's i18n-full.json edit on the authority of a ruling that was never about i18n) → renumbered B-273/B-274 under central allocation · reviewer mutation-tested the new pm_checkin test BOTH ways (onTap→null; workOrderId dropped from the push) — fails in both ⇒ a real guard, not decoration · flutter analyze clean · **test 299** (clean-dev baseline 255, +44) · kBuiltRouteIds == mobileScreenBuilders. | pixel-G5 **N/A** — the 26 mobile refs capture the prototype's MOCK data so an honest port false-fails a pixel diff; fidelity judged via gate-4.5, which independently confirmed the layout matches `reference/mobile/pm-checklist.png` | 2026-08-05 |
| W1-PR-LIST | web `pr.list` re-wire — restore the columns/KPIs the screen documented as absent | 25912af → dev 78f2985 (apps/web · 3 files · consume-only · zero backend/contract/router/visual-reference) · orch-B VERIFIED (executor≠verifier): **gate-4.5 PASS** — reviewer confirmed no detail-only field leaked onto the list (pr.test.ts exact-key assertion pins all 17 list keys) and traced the 'ของฉัน' tab id-space end-to-end (/me → serializeUser row.id == prs.requester_id) so its count is real, not a silent 0 · typecheck 0 · vitest **1529** (+14 genuine it() blocks · baseline 1515 measured on clean dev) · check:routes 100% · fabrication none (title/phase/requester/submittedAt/avgDays/kpiOverBudget/tabRef all em-dash) · money=SERVER (amount is the server Σ; the only client arithmetic is a display roll-up identical to the KPI already merged two lines above) · zero Thai · 0 keys minted. | **NEW-REF needed** — pixels change; tests/visual/** untouched (Wei-approved re-baseline required) | 2026-08-05 |
| W1-EMAIL-ADAPTER | `packages/notifications` — implement the real email NotificationAdapter (B-269) | 2ef3457+66fa395 → dev a824ae6 (packages/notifications only · line.ts/webpush.ts deliberately still throw · NO new dependency · NO sacred) · unblocks password-reset + user invites + subscription dunning, all previously dead ends (all three adapters threw TODO(P0-INT-03) while config.ts already validated SMTP env) · orch-B VERIFIED: **gate-4.5 PASS** and the reviewer ran **7 mutation probes — all 7 KILLED** (always-present subject key · companyId leaked into envelope · data rendered into text · transport failure swallowed into fake success · synthesised messageId · deleted blank-recipient guard · deleted EMAIL_FROM assert) ⇒ the tests are not vacuous and do not survive a revert · @juneflow/notifications **33 tests / 3 files** (baseline 23) · fabrication none (messageId from the transport receipt never synthesised; subject omitted not defaulted; data/companyId dropped not invented). | — (backend · no visual) | 2026-08-05 |
| B-263-HARDEN | B-263 — gate the POST /gr idempotency 23505 catch on the constraint NAME (hardens the B-261 money-write template BEFORE it is copied to attendance/inventory/ap-deposit) | 143bd37 → dev b2d3287 (apps/api only · gl-post.ts `violatedConstraint()` + gr.ts catch + gr.test.ts · NO sacred · NO migration · NO contract) · **the prescribed one-liner was WRONG and would have silently disabled the B-261 replay in production with every test still green** — handler inserts go through drizzle, which wraps the pg DatabaseError as `DrizzleQueryError.cause`, so a top-level `err.constraint` read is always undefined; the executor proved this against a live PG 16 (pg 8 + drizzle-orm 0.45) since the repo had zero prior art, and upgraded the flat test stub to the real wrapped shape. orch-B VERIFIED (executor≠verifier): **gate-4.5 PASS** · api **1258/1258** (+6 · baseline 1252 measured on clean dev) · **LIVE E2E real PG16** compose+migrate-seed+prod-api-image: `b261-gr-idempotency` **1 passed** (replay same key → ORIGINAL receipt; new key → distinct) · sacred none · zone apps/api only. Non-block follow-up flagged by the reviewer: the constraint name is a free-floating literal in 3 places (handler/schema/test) with no mechanical link — a future index rename kills the replay silently while CI stays green. | — (backend · no visual) | 2026-08-05 |
| MOB-I18N-01 | mobile i18n runtime — t/tn/tp + tpat(phrase_patterns · B-017 ก) + langResolve + RTL + ScreenStrings sidecars | feature/mobile-i18n (apps/mobile only · asset = สำเนา verbatim sha adba2e8b… · lib/**.dart ไม่มีอักษรไทย) | — (ยังไม่มีจอ · prereq ของทุกจอ Phase 4 · analyze clean · **test 68/68** · gate-4.5 3-lens: 3 finding แก้ครบ · asset-drift gate พิสูจน์แล้วว่า fail จริงเมื่อ drift) | 2026-08-03 |
| P5-SOLAR-W1-WEB | Solar Wave-1 web — reports + audit (tail) + 3 write forms + PPA wire | e6fac23/d07dfca→dev (apps/web · reports static-launcher · audit live thin-read [Wei 2026-07-19 shape] · O&M-close/permit/warranty forms money=NONE · PPA reuse ARInvoiceForm→/ar/invoices money=SERVER B-216/217) · orch-B VERIFIED: **gate-4.5 ×2 PASS** (no dead fetch · audit 0-fabrication · PPA no-client-money · close inverted-footer verified · FK-resolver never-raw-uuid · consume-only B-214/218 0-minted byte-exact) · web tc0/vitest 1339/routes0 · **G5**: reports+audit captured (84→86) + solar ppa/permit/warranty re-baselined (button flip · roi control 0.0000%) strict-0 ×2. B-218 i18n applied (Wei sacred). OM-create honest-disabled=B-223. | reports/audit NEW-REF + 3 solar re-baseline (button-enable) · audit=B-160 date-drift follow-up | 2026-08-03 |
| P5-SOLAR-W1-BE-FIN | Solar Wave-1 backend finance-lane — AR revenue accrual JV (B-216) + idempotency guard (B-217) + warranty years (B-219) | B-216/217 c5f31c1 · B-219 1680a23 → dev (apps/api+db · migrations 0047/0048 · money=SERVER) · orch-B VERIFIED: **gate-4.5 money-skeptic ×3** (B-216 balanced JV Dr1030/Cr4010/Cr2050 server+atomic · **B-217 caught a double-post orch-A gate missed** → 3-leg guard pre-check+uniqueIndex+23505-catch race-safe · B-219 additive non-money) · api 1155 · **live 3/3** (invoice→1 JV · dup→409 single-JV · different-no→201) · two-reviewer. B-216=ค/B-217=ค/B-219=ก Wei-ruled. | — (backend · no visual) | 2026-08-03 |
| P5-SOLAR-W1a-BE | Solar Wave-1a (FLOW-EPC) backend — O&M/permit/warranty workflow writes | c1ecc2c→dev (apps/api + packages/contracts · SACRED openapi B-215 additive 4 ops · NO migration) · orch-B VERIFIED: **gate-4.5 SECURITY PASS 7/7** (money=NONE · TenantDb-door force-set+FK→404 · idempotent-close on FINAL UPDATE · additive-0-deletion · OM-no server-gen · status-server-set) · api 1151/1151 · **live E2E 4/4** node-dist (create status-server-set + tenant-via-list · close 200/409/404) · two-reviewer (orch-A self-gate CLEAN). Wei approved B-215 via chat. | — (backend · no visual) | 2026-08-02 |
| P5-W0-LAND | W0 land write-affordances — add-plot + buy-deposit un-disabled (money=SERVER) | 7fb054b→dev (apps/web · 6 files + 2 G5 re-baseline PNG) · orch-B VERIFIED: **gate-4.5 PASS** (money=SERVER both paths · buy sends NO amount → server computes deposit · toast reads server `data.deposit` not lease `amount` = the ฿0-bug fix · add-plot client-id DROPPED · area=UNIT-conversion not money · tenant-door force-scoped · confirmTransfer/viewDraft stay honest-disabled) · web tc0/vitest1216/routes0 · zero-Thai · 0-minted (add-toast dropped=B-211). **G5: land-bank + land-dd RE-BASELINED** (only delta = un-disabled button grey→active · diff proven localized · land.survey control byte-clean = no regression · both PASS 2× strict-0) | land-bank+land-dd NEW-REF (button-enable · pending Wei) | 2026-08-02 |
| P5-W1-WEB | W1 read-ports — 8 inventory/labor/land/accept display screens (read-only) | d09d81f→dev (apps/web · 32 files · 0-minted · money-free · zero sacred) · orch-B VERIFIED: **gate-4.5 PASS ×4** (all read-only · data-source isolation [stock excl transfers · transfer excl issues · issue→GET /inventory/issues] · honest em-dash on UNSEEDED inv-ledger never fabricated · money=server-precomputed formatMoney-only · accept 4 TYPED GETs no-'all' + gr=return-queue caveat · router grafted ADDITIVELY 8 imports+8 entries disjoint · NO wholesale-checkout [C-311 lesson]) · web tc0/**vitest 1320/1320** (+104 · 5 new logic modules)/routes0 · zero-Thai. Wei-ruling non-block: labor.attendance single-day scope · inv.stock alert-badge+util-bar omissions | 8 W1 G5 baselines (next row) | 2026-08-02 |
| P5-W1-G5 | W1 G5 baselines — 8 read screens pinned (manifest 76→84 = still 100% coverage) | 177286b→dev (tests/visual · 8 app-baselines + manifest +8 routes -0) · orch-B VERIFIED: each proven strict-0 ×2 (app-vs-app · workers:1 seq) · 16/16 self-checks green on the restored 84-row manifest · **inv-ledger UNSEEDED** → those baselines pin the honest empty/em-dash state (a future data-wire legitimately re-baselines) · purely additive · prototype gallery untouched | 8 NEW-REF (app-baseline · captured from merged dev W0+W1) | 2026-08-02 |
| P6-BE-MRR | B-209/B-192 server-derive MRR/ARR (closes money=SERVER law violation) | c787e28→dev (apps/api · handler-only · NO sacred) · orch-B VERIFIED: **gate-4.5 PASS** (priced off PLAN not seat-override · status denylist enum-complete skip trial/cancelled count active/expiring/overdue = Wei ก prototype-faithful T-1006-overdue-mrr=2900 · no GL B-179/188 · pkg-miss→continue NaN/crash-safe · owner-gated-only no tenant leak · Paginated envelope no contract change) · api 1139 (+3 MRR tests). Web drops deriveMrr (orch-C follow-on) | — (backend · no visual) | 2026-08-02 |
> 🏁 **31st 0-drift promote → `main` `1fbba3f` (2026-08-03 · 0-drift · pin 2be7371)** — **SOLAR WAVE-1 (FLOW-EPC) COMPLETE** (47 files · `git diff --stat 2be7371 main`=EMPTY): **Backend** — Wave-1a O&M/permit/warranty writes (openapi B-215) + **B-216/B-217 (Wei=ค)** AR invoice posts a balanced revenue JV (Dr1030/Cr4010/Cr2050 server+atomic) + idempotency guard (pre-check + migration 0047 uniqueIndex + 23505-catch) — **the money-critical invoice double-post orch-B money-skeptic CAUGHT (orch-A gate missed · two-reviewer) → Wei-ruled → guard → LIVE 3/3** + B-219 warranty years (0048) · gate-4.5 ×4 · api 1155 · live proofs. **i18n** B-213/214/218 (+173 · dict 3664 · Wei sacred · 0 existing-value change). **Web** reports + audit (thin-shape) + 3 write forms + PPA (money=SERVER reuse ARInvoiceForm) · gate-4.5 ×3 · vitest 1339 · G5 all strict-0 (reports/audit 84→86 · solar ppa/permit/warranty re-baselined · roi control 0.0000%). Deferred (Wei-gated follow-up · not promoted): B-220 settings · B-221 dms · B-222 sales.dashboard · B-223 OM-create · B-224 audit-G5 drift.
> 🏁 **30th 0-drift promote → `main` `64e7efc` (2026-08-02 · 0-drift · pin cce8334)** — **W0+W1 web wave + B-209 server MRR** (53 files · `git diff --stat cce8334 main`=EMPTY): **B-209/B-192** server-derive MRR/ARR (money=SERVER law CLOSED · web-fold=**ก** Wei — keeps deriveMrr as a display-only breakdown of the server plan-price, per-row ≠ money movement · gate-4.5 PASS · api 1139) · **W0** 2 land write-flips (add-plot + buy-deposit un-disabled · money=SERVER: buy sends no amount→server deposit · toast reads data.deposit = ฿0-bug fix · client-id dropped · **G5 land-bank+land-dd re-baselined**, land.survey control clean) · **W1** 8 read-ports (inv.items/stock/transfer/issue · labor.workers/attendance · land.pipeline · accept — read-only · 0 minted · data-source isolation · router grafted additively 8+8 disjoint [C-311 lesson] · **gate-4.5 PASS ×4** · vitest 1320/1320) · **W1 G5** 8 baselines pinned (manifest 76→84 = 100% coverage · strict-0 ×2 · inv-ledger UNSEEDED→honest-empty pinned). orch-B verified: gate-4.5 ×6 · G5 all strict-0 + 16/16 self-checks · zero sacred except B-209 handler. Open non-block (in queue): B-211 (W0 add-toast) · labor.attendance day-scope · inv.stock 2 omissions.
> 🏁 **29th 0-drift promote → `main` `a2c3b72` (2026-08-02 · 0-drift · pin 7e08399)** — **G5 100% coverage + QA hardening** (pure tests/board · ZERO app/sacred code): B-205=ก 28 NEW-REF baselines → **coverage 48/76 → 76/76 = 100% of PORTED_SCREENS** (BOQ cluster · procurement · AP money-entry · bank · master · users/login/exec — each proven: real data · determinism 0.0000% ×2 · pre-data-frame trap checked · masks only on ap.billing/ap.pv, drift API-intercept-proven + CONTROL) · B-206=ก workers:1 enforced · B-207=ก dashboard as_of masks · B-160 dashboard fix · board reconciled (7 review→done + 13 Phase-5/6 backfill + B-156/B-183 closed-verified). Full gate 76/76 PASS ×2 sequential. 28 NEW-REF Wei-accepted at promote. Open non-block: B-208 (CI vacuous) · B-209 (MRR ruled-not-implemented) · B-210 (docnum year drift).
| B-205-QA | G5 100%-coverage batch + harness hardening (Wei=ก) | 28 NEW-REF app-baselines (ap.billing/pv/aging · bank×3 · gr/pr/po/wo.list · boq×8 · master×7 · users · login · exec) → **coverage 48/76 → 76/76 = 100% of PORTED_SCREENS** · each proven (real data · determinism 0.0000% ×2 · no console err) · masks only ap.billing/ap.pv (drift proven by API-intercept + CONTROL) · B-206 workers:1 enforced · B-207 dashboard as_of 2 rects · full gate 76/76 PASS ×2 sequential | **28 NEW-REF pending Wei accept @ promote** · gallery refs g1/g2/g4/g5 untouched | 2026-08-02 |
> 🏁 **28th 0-drift promote → `main` `b6b016e` (2026-07-30 · 0-drift · pin 2ec8168)** — **PHASE-6 WAVE-1 COMPLETE** (backend 4/4 + web 3/3): W1a cross-tenant WRITE door (AIRTIGHT · live 5/5 injection/priv-esc) + 4 buttons · W1b package-CRUD (live 4/4 · money=SERVER · migration 0045) + pkg-builder form · W1c plan/seat (live 4/4 · company_id-strip no-re-home · tenant isolation · no proration · migration 0046) + 3 web writes · W1d dunning remind (live 4/4 · invoice UNCHANGED · audit→dunned tenant). Plus B-202 i18n · B-200 fixes · B-187 G5 viewMode + admin.plans re-baseline. orch-B: every sub-wave gate-4.5 SECURITY PASS + committed LIVE exploit-tests (**17/17** across the 4 backend sub-waves) · api 1136 + web 1216 · sacred additive+Wei-approved. Open non-block: B-199 · B-200 cleanup · B-204 · seats→resolver ruling · resetPw slice.
| P6-W1c-WEB | Phase-6 W1c web — 3 subscription/seat writes (owner set-package · tenant change-plan · tenant renew) | feature/web-w1c→dev (apps/web · 7 files · 0 minted keys · money-free UI · zero sacred/router) · orch-B VERIFIED: **gate-4.5 PASS** (id=SUBSCRIPTION id · money=SERVER 3 bodies carry no price/date/proration · **renew double-advance guard GENUINE** native-disabled no-remount · seat round-trip · Enterprise toast-only no-POST · targeted invalidation · **fireWithToast thunk-refactor behavior-preserving** across 3 sites+2 tests · not circular) · typecheck 0 · vitest 1216 · routes 100% · **orch-C adversarial-verify GO 0-defects**. G5: no list-level visual change (writes are in modals/CTAs) · admin.subs baseline unaffected. **2 Wei-facing copy notes (§0 keeps verbatim):** sub.plans.modalBodySuffix promises proration that B-191 defers · sub.mine.toastRenew hardcodes 'Professional (รายปี)'. | — (modal/CTA · no list change) | 2026-07-30 |
| P6-W1d-BE | Phase-6 W1d dunning remind — POST /admin/invoices/{id}/remind (B-188/189) · **closes Wave-1 backend 4/4** | b6e3aa0→dev (apps/api · handler-only · openapi PRE-DECLARED · NO sacred/migration/i18n) · orch-B VERIFIED: **gate-4.5 PASS** (money-free STRUCTURALLY — platformInvoices absent from WRITE_TABLES so an invoice is unwritable · 0 jv/GL · B-189 defer holds · ownerOnly-first 403 with records=0+notices=0 · **REAL audit attributed to the DUNNED tenant** [OTHER_COMPANY ≠ caller] · 404/400 gates prototype-faithful L230 overdue · notify seam try-wraps-notify-only so a P0-INT-03 throw degrades and the audit STILL fires) · api 1136 (+4) · typecheck 0 · **live exploit-test** next. AXIS settled: invoice-row (pototype L230) — orch-A right, orch-B's kickoff text was wrong. | — (backend · no visual) | 2026-07-30 |
| P6-W1b-WEB | Phase-6 W1b web — pkg-builder create/edit form + B-200(a) error-toasts + B-200(b) helper refactor | 1265186→dev (apps/web · 6 files · consume-only 0-minted [+B-202 keys] · money-free UI over money=SERVER backend · zero sacred/router) · orch-B VERIFIED: **gate-4.5 PASS** (money=SERVER buildPackageBody sends price_m ONLY no client-yearly · limits storage_gb/ai_per_month · PUT full-replace preserves cols · no-delete B-196 · B-200a 0-silent-catch · B-200b refactor behavior-preserving · popular Tag un-dropped · fidelity 1:1) · typecheck 0 · vitest 1215 (+21) · routes 100% · **orch-C adversarial-verify GO 0-defects**. G5: admin.plans re-baseline (popular Tag + enabled create = legit W1b-web change · NEW-REF Wei-accept) · admin.subs unchanged. **B-203 pending Wei** (Full ["*"] menu-wildcard · faithful port · non-block). | admin.plans NEW-REF pending Wei | 2026-07-30 |
| P6-W1c-BE | Phase-6 W1c plan/seat writes — owner set-package + tenant change-plan/renew + migration 0046 (B-201/195/191) | fefe9a4→dev (apps/api+db+contracts · SACRED openapi change-plan/renew additive + migration 0046 seats · wei-approved:B-201 · money-free/SERVER) · orch-B VERIFIED: **gate-4.5 SECURITY PASS** (door allowlist→subscriptions UPDATE-only · **company_id-strip LOAD-BEARING 2 layers** no-re-home · tenant change-plan/renew auto-scoped [tenant A can't reach B] · money=SERVER no-proration/no-renew-shift · renew server-date client-ignored · B-200 limits-required 400) · api 1132 (+15) · migration 0000→0046 · **committed LIVE exploit-test** phase6-w1c-plan-seat (no-re-home · non-owner 403 · own-only+no-charge · server-date) | — (backend · no visual) | 2026-07-30 |
> 🏁 **27th 0-drift promote → `main` `bc2cf9a` (2026-07-30 · 0-drift · pin e36d1a6)** — **Phase-6 Wave-1 (W1a+W1b of 4) + G5 fixes**: W1a cross-tenant WRITE door (block/unblock/suspend/resume · B-193/194 · AIRTIGHT gate-4.5+live 5/5) + 4 admin.subs web buttons · W1b package-CRUD (create/edit · no-delete B-196 · door insertOne · migration 0045 B-197 · money=SERVER · live 4/4) · B-160+B-187 G5 (date masks + per-row platform viewMode → all admin.* GREEN). orch-B **13-agent adversarial audit (GO-WITH-FIXES → landed)**: door airtight (8 vectors) · money 0-findings · sacred additive · runtime api 1117 + web 1194 + **live 9/9**. Audit documented: B-198 confirm=ข · B-199 enforce=ก DEFER · B-200 follow-ups. Remaining: W1c (plan/seat) · W1d (billing).
| P6-W1a-WEB | Phase-6 W1a web — admin.subs 4 owner actions wired (block/unblock/suspend/resume) | 557e6f9→dev (apps/web · 2 files · consume-only 0-minted · money-free · zero sacred/router) · orch-B VERIFIED: **gate-4.5 PASS** (id-semantics sub.id/u.id · companyStatus-branch B-194 · **toast-fix SOUND** mutateAsync→MutationCache fires despite ctx.confirm unmount · cross-tenant-safe backend-authoritative · disable-while-pending) · typecheck 0 · vitest 1194 · routes 100% · **orch-C caught+fixed the toast-drop bug via adversarial-verify**. G5 DEFERRED (wiring in CompanyControl detail → list baseline unaffected · admin.subs G5 blocked on B-187 viewMode). FAST-FOLLOW: jsdom component regression test (toast-fire-under-unmount · RECOMMENDED not required · money-free) | — (detail interaction · G5 blocked B-187) | 2026-07-30 |
| P6-W1b-BE | Phase-6 W1b package-CRUD (create/edit · NO delete B-196) + door insertOne + migration 0045 (B-197) | 2cb27d3→dev (apps/api+db · SACRED migration 0045 additive-nullable cols wei-approved:B-197 · POST/PUT pre-declared handler-only · money=SERVER) · orch-B VERIFIED: **gate-4.5 SECURITY PASS** (insert-primitive containment INSERT_TABLES={packages} narrower + strip id/owner-flag · money=SERVER priceY=price_m×10 client-ignored · NO delete · ownerOnly-first · migration additive-safe · global-blast-radius prototype-faithful) · api 1117 (+9) · typecheck 0 · migration 0000→0045 · **committed LIVE exploit-test** phase6-w1b-package-crud (create money=SERVER · price_y/currency client-ignored · edit persist · non-owner 403 · no-delete 404) | — (backend · no visual) | 2026-07-29 |
| P6-W1a-BE | Phase-6 W1a cross-tenant WRITE door + block/unblock/suspend/resume (B-193/194) | 600c77b→dev (apps/api+contracts · SACRED openapi additive unblock+resume B-193 · money-free · NO migration) · orch-B VERIFIED: **gate-4.5 SECURITY PASS** (door fail-closed by construction — dual is_platform_admin strip + FIXED-patch handlers + private #db + WRITE_TABLES allowlist + ownerOnly-first + single-row WHERE + target-tenant audit · enum-valid · money-free) · api 1108 (+13) · typecheck 0 · **committed LIVE exploit-test** tests/e2e/phase6-w1a-writedoor.spec.ts (cross-tenant flip+persist · non-owner 403 all · injection/priv-esc guard · idempotent) | — (backend · no visual) | 2026-07-29 |
> 🏁 **26th 0-drift promote → `main` `c5ab37e` (2026-07-28 · 0-drift · pin 18fe211)** — **Phase-6 Wave-0 COMPLETE 7/7**. Completes the wave (25th did 5/7 + backend): sub.mine + line web + GET /subscription/me (B-185=ก · tenant own-sub read + live usage projects/users/ai · storage=0 honest) + 43 line.preview i18n (B-182) + line fixture non-.tsx .json (B-184=ก · byte-exact incl U+2064 · render-rich whitelist no-XSS). **Wei-ACCEPTED 3 NEW-REF** (line.png + sub-mine.png baselines + sub-mine-daysleft-b186 mask). orch-B verified: api **1095** + web **1194** (fresh integrated) · every merge gate-4.5 PASS + G5 · /me cross-tenant safe by construction · sacred additive+Wei-approved (i18n +43 · openapi 0-removed · no migration). Next: Phase-6 Wave-1 (billing/admin writes · ruling-gated) + data-completeness (orch-A · non-block).
| P6-WEB-FINAL | Phase-6 Wave-0 FINAL — line (LINE OA preview) + sub.mine (My Subscription) → 7/7 | ff695fd dev (apps/web · line fixture-.json B-184=ก + render-rich whitelist parser · sub.mine consumes /me · 43+35 consume-only keys · money-free · zero sacred · router +4/-0 GRAFTED) · orch-B VERIFIED: **gate-4.5 PASS** (line 440/440 byte-exact + U+2064 survives + no dangerouslySetInnerHTML XSS-tested · sub.mine honest usage/404→null · router 0-removed · zero-Thai in code) · typecheck 0 · vitest 1194 · routes 100% | **2 NEW-REF** line.png + sub-mine.png (determ ≤0.004% / 0.0000%) + **1 NEW mask** sub-mine-daysleft-b186 (daysLeft live-clock strip · minimal · B-120ข class) — **Wei accept baselines + mask at promote** · gallery g2/46,38-s.jpg = §0 truth | 2026-07-28 |
| P6-BE-ME | Phase-6 GET /subscription/me (B-185=ก · sub.mine backend) | 3aa2603 dev (apps/api+db+contracts · SACRED openapi additive B-185 · money-free tenant read) · orch-B VERIFIED: **gate-4.5 PASS** (cross-tenant safe by construction — all reads TenantDb-scoped · openapi 0-removed authorized · money-free · usage honest storage=0 flagged · 401/404 flat · genuine test) · api 1095 · typecheck 0 | — (backend · no visual) | 2026-07-28 |
> 🏁 **25th 0-drift promote → `main` `1601644` (2026-07-28 · 0-drift · pin 1d502c3)** — **Solar Wave-0** (6 read GETs B-174 + 110 i18n B-175 + 5 web + 5 G5 baselines · money-free) + **Phase-6 Wave-0** (platform-owner cross-tenant door B-176/177/178 · owner=วิภา · migration 0044 · 5 owner-gated /admin/* + tenant reads B-181 + audit B-183 + 229 i18n B-182 + 5 web + 5 G5 baselines · money-free). orch-B verified: api **1091** + web **1178** (fresh integrated on pin) · every merge gate-4.5 PASS + G5 · **door AIRTIGHT** (gate-4.5 + LIVE exploit-test 3/3) · sacred additive+Wei-approved · backend untouched by web merges. **gate-4.5 caught+fixed a router stale-slice** (branch predated Solar merge → graft-not-checkout). 10 NEW-REF baselines Wei-accepted. Phase-6 remaining (next wave): sub.mine (B-185=ก) + line (B-184=ก).
| PHASE6-W0-WEB | Phase-6 Wave-0 web — 5 subscription/admin read-only screens (sub.plans/billing + admin.subs/plans/invoices) | 3aacbe1 dev (apps/web · consume-only 229 sub.*/admin.* B-182 on dev · money-free · owner-gated via registry section:platform · zero sacred) · orch-B VERIFIED: typecheck 0 · vitest 1178 · routes 100% · **gate-4.5 PASS** (11 files fidelity 100% · 0 minted · zero-Thai · money-free display-only · honest wire-gap em-dashes) — **gate-4.5 caught+I fixed a router stale-slice** (branch predated Solar merge → wholesale checkout would drop solar.* 5; regrafted additive-only 0-removed) | **5 NEW-REF app-baselines** app-baseline/{sub-plans,sub-billing,admin-subs,admin-plans,admin-invoices}.png (OWNER-auth วิภา+platform viewMode for admin · data-presence pre-verified · sequential B-186 · determ 0.0000-0.0019%) — **Wei accept at promote** · gallery g2/39,40,42,43,44 = §0 truth (untouched) | 2026-07-28 |
| SOLAR-W0-WEB | Solar Wave-0 web — 5 read-only screens (Monitoring/PPA/ROI/Permit/Warranty) | 5762650 dev (apps/web only · consume-only 110 solar.* B-175 already on dev · money-free · zero sacred) · orch-B VERIFIED: typecheck 0 · vitest 1145 · check:routes 100% · **gate-4.5 PASS** (fidelity vs solar.jsx 100% · 0 minted · zero-Thai · honest divergences a-e) | **5 NEW-REF app-baselines** tests/visual/reference/app-baseline/solar-*.png (self-check 0.00-0.0047% deterministic · seed-generic anchor per B-120) — **Wei accept as ground truth at promote** · gallery g3/01-05-s.jpg = §0 truth (untouched) | 2026-07-28 |
| P3-BE-03 | P3 Wave-2 SV-1 SACRED sales.service — 7 ops + status machine + warranty-derive (B-159) | 80d2aa5 feature/backend | — (backend · SACRED openapi paths-append 7 ops · 0 new component · client regen additive · B-149 status guards · money=NONE · SV-2 warranty derived · NO migration · 1042 api +18 · LIVE status-machine proof full received→closed + wrong-state/terminal 409 + 404 + 401) | 2026-07-27 |
| P3-BE-02 | P3 sales/land WRITE batch-1 — migration 0042 + 13 handlers (SA-7 receipt-JV · LA-4 deal · SA-6 loan · B-082 F1 gate) | b36c142 feature/backend | — (backend · NO sacred/openapi · money=SERVER + B-097 atomic + idempotent · gate-4.5 round-1 FAIL booking-race→FIXED re-key booking:<unitId> · 1024 api +46 incl 3×403 · migration 0042 live · LIVE money proof (deal-buy 7,812,000 + idem + 403 + lease 422) + REAL RACE PROOF 4-concurrent first-bookings→1×201/3×409 1-unit 1-JV · transferSalesUnit deferred B-162) | 2026-07-27 |
| P2-BE-58 | B-144 payroll POSTING_MAP realign (5030→1140) + B-149 approve-handler optimistic guards (Wei=ก) | b0b0500 feature/backend | — (backend · gl-post.ts one-map-edit + 14 approve/commit handlers guarded pre-state in UPDATE WHERE→0-row 409 · skip 9 unique-index-safe · tenant-db where?:SQL + updateThroughChain guard?:SQL on FINAL update [gate-4.5 round-1 atomicity fix b0b0500] · 983 api no-regress · DETERMINISTIC race proof subcon approve-payment: FOR-UPDATE-lock window → 409 · ap_billing unchanged · no double-payment) | 2026-07-27 |
| P3-BE-01 | land-sales Wave-0 — mount route + GET /sales/leads + /land/plots (zero-ruling reads) | 077823f feature/backend | — (backend · NO sacred/migration/i18n · opaque reads on declared ops · stage kanban axis · price_per_rai money+currency_code · 983 api +5 · live 10 leads/8 plots + 401) | 2026-07-27 |
| P2-BE-57 | inventory subsystem — 13 ops + stock_ledger + transfer/issue (B-141) | cb2a1b2 feature/backend | — (backend · sacred 13 ops + migration 0041 · standard-cost server · atomic dual-wh + issue cost-to-BOQ + neg-stock 409 · 978 api · live 19/19 · B-148 non-block) | 2026-07-27 |
| P2-BE-56 | labor write slice — create ops + OT/pay calc + payroll→JV (B-140) | 18cd95f feature/backend | — (backend · migration 0040 · money=SERVER OT calc · Dr1140/Cr1020 · 949 api · live 13/13) | 2026-07-27 |
| P2-BE-55 | labor Wave-0 — mount labor route + GET /labor/workers\|attendance\|payroll (3 reads) | 2dec493 feature/backend | — (backend · 3 opaque list reads · seeded 8 workers · 925 api · live 7/7) | 2026-07-26 |

> ✅ **batch #2 (46 งาน) → `main` `1eb2ecb` (2026-07-13)** — Phase 0 + P1 login · audit 0 defects.
> ✅ **batch #3 (21 งาน) → `main` `43e0b70` (2026-07-14)** — sacred rounds 4 (i18n §1/5/7/8 + envelope B-014 + contract §4/6) · routes/shell/Dart · 14-agent audit 0 blocker.
> ✅ **batch #4 (9 งาน) → `main` `433dda5` (2026-07-15)** — master wave web 6/7 + MOB-02 + PLAT-04 + BE-11 (migration 0012).
> ✅ **batch 5+6 (22 rows) → `main` `244912c` (2026-07-16)** — Phase-2 FLOW-A procurement backend + i18n Wave-1 (dict→1059) + migrations 0012-0017.
> ✅ **batch #7 (12 web ports + FLOW-A data-completeness) → `main` `1b7fbca` (2026-07-17)** — B-082 F1-F4 security + B-084 authz + B-085 hardening + migrations 0024/0025 (FK indexes + TOCTOU) + drizzle 0.45.2 (HIGH vuln cleared).
> ✅ **batch #8 (Wave-2 finance) → `main` `041013a` (2026-07-17 · 0-drift)** — GL/AP/bank web+api + **B-095 packaging fix** (prod boot restored · orch-B caught) + migrations 0026/0027.
> ✅ **batch #9 (MVP-B hardening) → `main` `2051e40` (2026-07-17 · 0-drift)** — bank import/reconcile (B-093) + B-094 3/3 (gl.jv locked-period · bank reverse-unique · PV SoD) + B-084 reject/reconcile/import authz gates · migrations 0028/0029 · adversarial skeptic 6/6 SOUND.
> ✅ **batch #10+11 → `main` `eb88544` (2026-07-17 · 0-drift · pin 4c64ef7)** — B-096 bank hygiene + **B-097 TenantDb transaction door** (atomic multi-write · scope-safe · **live-rollback proven**) + B-098 atomic generate-PR + orch-B audits/finance-E2E · gate-4.5 PASS · closes the B-085-class txn-door loop.
> ✅ **batch-12 + B-100 → `main` `a1421bf` (2026-07-19 · 0-drift · pin c7838ac)** — B-084 authz residual (boq /revise MD-lock + bank /match finance.create · **B-084 fully closed**) + B-099 per-user login throttle (fixes office-NAT 429) + **B-100 account-lockout DoS closed** + migration 0030 FK-index + finance-flow SoD spec-fix (**7/7 live**) + group-C Wave-1 spec · orch-B verified (full live E2E green + adversarial skeptic on authz/throttle/B-100 · 567 api) · differential-proven B-099 live · gate-4.5 PASS.
> ✅ **group-C PROGRAM → `main` `e416f17` (2026-07-19 · 0-drift · pin 985acdb)** — Dashboard/Analytics complete: Wave-1 (GET /audit-log + clock-relative seed + dashboard) + W2 (cost-type/boq-vs-nonboq/portfolio) + W3 (evm_snapshot mig 0031 + S-curve BUILD-ONCE) + W1b (activity feed) + W2b (RPT-001/003/004/005 cards + ExecDashboard) + W3b (evm/variance handlers) + B-102 (health stored-curated mig 0032) + B-103 (exec i18n +22) + SR-1/2/3 sacred · orch-B verified (sacred PERFECT · live migrate 0000-0032 · BUILD-ONCE SOUND · C10 skeptic · health mock-exact) · api 608 web 533 · non-block B-104. Wave-4 deferred.
> ✅ **Phase-4 Wave-0+1 → `main` `7741e1b` (2026-07-20 · 0-drift · pin b312f5a)** — Subcon+PM start: subcon.ts + pm.ts backend (8+8 ops · tenant-door AIRTIGHT · no migration · contract frozen) + i18n R1/R2 sacred (B-105/B-106 · +550 keys) + subcon.contracts + pm.assets web ports · orch-B verified (tenant-door skeptic AIRTIGHT + glyph-fidelity SOUND + gate-4.5 ×2 + live) · api 680 · web 560. Wave-2 gated (B-107/B-108).
> ✅ **Phase-4 Wave-2 backend → `main` `b35059d` (2026-07-20 · 0-drift · pin 90dc065)** — Subcon+PM money: approve-payment (money authority=SERVER · 4-basis · retention split → ap_billing + retention_ledger + period→paid atomic B-097 tx · %-gate advisory never-403) + pm contracts/quotes/close (per_visit autogen · LINE stub) + migrations 0033/0034 additive (B-110 name cols) + B-111 sacred openapi (listPmQuotes additive · wei-approved:B-108) · orch-B VERIFIED: money-authority AIRTIGHT static+LIVE (bogus 99999999 IGNORED→server 430k · PASS 11/11 real-PG) · tenant-door skeptic AIRTIGHT (8 HOPS→company_id) · i18n 2677 unchanged · api 726. Wave-2 web + Wave-3 (fan-in 'pm' enum · retention 50/50) next.
> ✅ **Phase-4 Wave-3 backend → `main` `16d71ac` (2026-07-20 · 0-drift · pin 1b1188d)** — acceptance-center fan-in (period/pm/house/gr · all tenant-scoped honest-empty · pm==counts.countPmOpen byte-identical) + B-112 sacred openapi (type enum +pm 1-token additive · wei-approved:B-107) + B-113 retention-release FLAGGED-not-fabricated (honest C10). orch-B VERIFIED: fan-in tenant-door AIRTIGHT (hand-audit — 4 HOPS→projects.company_id · workflow skeptic stubbed) · badge-parity + B-113 skeptic PASS · live fan-in 8/0 (period3/pm6/house4/gr4) · sacred additive · no migration · api 736. **Phase-4 backend Wave-0/1/2/3 COMPLETE.** Carry-forward: B-113 Wei ruling · web Wave-2/3 (chat-3).
> ✅ **Phase-4 META-1 enrich → `main` `3f6b841` (2026-07-20 · 0-drift · pin ecf179e)** — acceptance-center + subcon periods wires gain project_name/title(data-only doc-number)/owner/defect via scoped joins · BACKEND-ONLY (opaque Entity · no sacred openapi · no migration) · honest-empty overdue/wait/due/docs (C10) · §0 fix (title data-only not composed-Thai · งวดที่→FE B-116). orch-B VERIFIED: sacred untouched · §0 title clean (live WO-2026-0042) · tenant-door scoped (foreign→404) · live enrich 13/0 project_name populated · api 745. 13th 0-drift promote.
> ✅ **Phase-4 WEB batch → `main` `a99031d` (2026-07-20 · 0-drift · pin a32d618)** — subcon.accept + pm.dashboard/schedule(FIXED cal)/wo(functional B-117 picker) + pm.assets touch-up. CONSUME-ONLY (zero sacred · dict 1619 intact · B-106 dropped). orch-B VERIFIED: gate-4.5 ×2 PASS (B-118=ก slate ratified · orch-B caught the §0-rule-4 substitution) · zero hardcoded Thai · B-115/B-117 keys · FIXED calendar · C10 honest-empty · typecheck 0 · web 647. Visual: Wei manual dev-gallery review (automated G5 deferred → B-119 harness bring-up). 14th 0-drift promote. **🏁 PHASE-4 COMPLETE (backend Wave-0/1/2/3 + META-1 + web).**
> 🏁 **PROGRAM-1 FINANCE-TAIL → `main` `88029af` (2026-07-26 · 0-drift · pin ad929a2 · 20th)** — ap.deposit (Dr1160/Cr1010 · B-138) + 5 web (gl.statements/cashflow-DIRECT/ap.retention/master.customer/pm.contracts · gate-4.5 ×6) + i18n Wave-D+addendum (dicts 2389) + G5 fleet 28. FLOW-F 100% MVP. api 918 · web 960.
> ✅ **G5 regression gate → `main` `0e1ad79` (2026-07-20 · 0-drift · pin b38af22)** — B-119/B-120 CLOSED: visual harness bring-up (7 routes registered · 7 app-baseline refs incl subcon.accept first · settle-wait capture fix · 2 live-time masks · AA-epsilon 0.01% Wei-ruled) · round-6 fresh-stack proof 7/7 PASS + 16/16 self-checks · ZERO app-code · prototype gallery untouched (§0 design truth). Every future web merge runs the gate BEFORE merge. 16th 0-drift promote. **Phase-3 Finance opens.**
> ✅ **Phase-3 Wave-0 + i18n Wave-A → `main` `6902dfb` (2026-07-21 · 0-drift · pin db76042)** — 6 zero-ruling handlers (ar server-money+RV-409 · etax C4 · gl trial/close CE-strict · fa) + 45 tests + 273 i18n keys (dicts 1892) + charter/B-121..126. orch-B: gate-4.5 ×2 · api 790 · LIVE 15/0 (bogus-money-ignored · locked-JV-409). 17th 0-drift. Carry: /customers unserved (C-179) · B-125.
> ✅ **Phase-3 round-A + i18n Wave-B + gl.inbox → `main` `c2c1868` (2026-07-21 · 0-drift · pin 9ca4478)** — 16 sacred ops (192→208 · wei-approved:B-121..124) · migrations 0035/0036 · AR surface/CN-approve(×7/107 idempotent)/aging/tax-register/customers · /gl/post (map B-128=ก · double-derivation MATCH) + periods · FA surface ((cost−salvage)/life/12) · /tax/reports · 416 i18n keys (dicts 2308) · gl.inbox web. orch-B: gate-4.5 ×3 · api 876 · openapi additive line-verified. 18th 0-drift.
> _(records 19th–21st not logged here — stale-board debt C-117, deferred vc-audit-plans pass; main advanced 6902dfb→…→aee675f→38f6b7a)_
> ✅ **P3 Wave-1 (Sales+Land writes + SV-1 + B-165/B-167 money-race) → `main` `38f6b7a` (2026-07-27 · 0-drift · pin f09b6f0)** — migrations 0042/0043 · land-sales writes (booking/down/deal/contract · money=SERVER · B-082 F1 gate) · SV-1 sales.service 7-op status machine (wei-approved:B-159) · 8 P3 web screens + G5 baselines. **B-165→B-167 sales.down concurrent double-post CLOSED**: count+1 seq let a serialized reader escape the unique index (live b163 caught [201,409,201]) → client-provided instalment_no as the STABLE unique(sales_unit_id,seq) key (Wei=ข-expanded) + GET /sales/downs reads authoritative down_payment_txn (jsonb lost-update fix). orch-B verified: api **1043** · **b163 money-race live 3/3 @ f09b6f0** (booking+down+deal exactly-one + read-model consistent) · gate-4.5 backend(6a169b7)+web(648dfd5) PASS · B-156 riders NOT included (gr/pm identical to main · held). **22nd 0-drift.** Fast-follow (non-block): B-156(+gr.test) · B-168 allocJvNo (sacred · Wei approve) · B-169 tenant-scope.
> ✅ **P3 fast-follows (B-156 approve-guards + B-169 booking tenant-check) → `main` `0739d8b` (2026-07-27 · 0-drift · pin f6baa66)** — B-156: gr.ts return/cancel 0-row→409 + PO/WO auto-close idempotent + pm.ts decide-once 409 (Wei=ก terminal/faithful B-166) · all folded into the FINAL updateThrough UPDATE WHERE (atomic) · gr.test/pm.test exercise the 0-row branches (gate-item #8 closed). B-169: createSalesBooking in-tenant project_node check (foreign→404 fail-closed, no money). orch-B verified: api **1049** (+6 · 0 regression) · b163 booking-race live 3/3 · gate-4.5 PASS ×2 (backend batch). NON-MONEY. **23rd 0-drift.** Remaining deferred: B-168 allocJvNo dup jv.no (Wei-deferred · pre-existing · non-money) · B-160 G5 relative-time masks (orch-B QA · non-P3).
> ✅ **P3 Wave-2 (B-161 land-lease deal + B-164 buy-deal AP-subledger) → `main` `a9ed489` (2026-07-28 · 0-drift · pin c6fd568)** — B-161 lease: client first-period rent → Dr 5100 admin-expense/Cr 2010 AP + ap_billing (Wei=ง · rent acct 5100 B-173=ก · money=SERVER no-formula · deal:<plotId>:lease idempotency) + web lease form · B-164 buy AP-subledger. orch-B: gate-4.5 money-skeptic PASS (B-161 7/7 · B-164 7/7) · **b163 live 4/4** (booking+down+deal+lease exactly-one) · api land-sales 51 · web tsc/vitest. **24th 0-drift. Program-3 Sales+Land COMPLETE.**
> 🏁 **PHASE-3 FINANCE COMPLETE → `main` `6ef1f0f` (2026-07-22 · 0-drift · pin 79231b9 · 19th)** — round-B (statements honest-loss · cashflow DIRECT B-129=ก) + hardening 0037 + retention release (B-125=ก · Dr2030/Cr1020 · 0038) + 13 web ports (GL/AR/tax/FA full · B-107a ทุกจอ). api 903 · web 889 · dict 2308. FLOW-F ≈ 100% (MVP scope). Async: B-130/B-131 ratify · Wave-C 8 keys · G5 baselines 12 จอ.
> 🏁 **SOLAR TAIL (settings · dms · OM-create) → `main` `bbe738b` (2026-08-03 · 0-drift · pin 5d32a68 · 32nd)** — B-220 settings (honest-empty) · **B-221 dms** GET /documents backend (migration 0050 · FK-resolver no-uuid-leak · 13-row seed · GET-only) + DMSCenter web (category rail+table+KPI · honest-divergences) · **B-223 OM-team** backend (solar_om_ticket.team · migration 0049) + OM-create web (RF2OMForm→POST /solar/om-tickets) · B-223 i18n 3 keys (dict 3667). orch-B verify: gate-4.5 ×4 · api **1160** · web vitest **1363** · **live-PG dms 4/4 + OM-create 2/2** · G5 settings/dms first-capture + solar-monitor re-baseline (manifest 88). `git diff --stat 5d32a68 main`=EMPTY. NB: B-221 dms landed via a dev-index commit-race (5809106, byte-identical to 722948f, squashed clean). Non-block: B-222 defer · B-224 date-drift (FROZEN_NOW) · B-225 inverter-code.
> ✅ **B-224 FROZEN_NOW (G5 date-drift class) → `main` `8db8134` (2026-08-03 · 0-drift · pin 5270e84 · 33rd)** — SEED_FROZEN_NOW freezes BOTH the seed clock AND the ap/ar aging clock (business-clock.ts businessNowMs · env-gated default-unchanged · Number.isFinite fallback); seed-only was insufficient (aging on real Date.now → ap-billing 13.75% balloon). pm-dashboard/schedule/wo re-baselined to frozen 07-20 · ap-billing aligned · audit EXCLUDED (live compliance-log · Wei ruled). money=NONE (create path untouched). orch-B: gate-4.5 8/8 · api 1160 · full local G5 87/87 PASS · CI unaffected (skips captures w/o stack). NB: 0-drift check caught `-X theirs` NOT deleting audit.png (merge-base predates the file) → amended promote to `git rm` on main → re-verified EMPTY.
| P2-WEB-09b | pr.form port — PR detail (GET /pr/{id}) + submit/approve/reject state machine (B-070 tiered) | 89231fa feature/web-pr-form (graft → dev) | — (web · money=SERVER: submit/approve path-id-only · reject {reason} · VAT/net em-dashed no-client-math · gate-4.5 PASS 6/6 · zero-Thai · 24 dict+1 nav+39 phrase resolve 0-mint · web-tsc 0 · vitest 1406 · check:routes 100% · pr-form-rows 18/18 · G5-EXEMPT detail-via-internal-nav per gr-create-form precedent, design-fidelity via gate-4.5) | 2026-08-03 |
| P2-WEB-71 | subcon.handover port — subcontractor work-handover certificate (read-only) | 927e71d feature/web-subcon-handover-auto (orch-B overnight auto-port + rework) | — (web · money=NONE · gate-4.5 PASS after rework fieldMethod→colMethod · consume-only 31 keys 0-mint · zero-Thai · web-tsc 0 · vitest 1407 · check:routes 100% · G5 batch-pending-AM: goto-able first-doc-fallback) | 2026-08-04 |
| P2-WEB-73 | wo.form port — full-page create work order (WOForm from approved PR) | b7742b7 feature/web-wo-form-auto (orch-B overnight · router re-grafted additive onto dev) | — (web · money=SERVER {pr_id,vendor_id,value,retention_pct} server-owns-retention/status/docno · gate-4.5 code-PASS · PR-picker wire-forced-acceptable · i18n 0-mint value-fidelity · web-tsc0/vitest1407/routes100% · G5 batch-AM) | 2026-08-04 |
| P2-WEB-72 | po.form port — full-page create PO from approved PR | 0766e03 feature/web-po-form-auto (orch-B overnight · rework + router additive-resolve vs wo.form) | — (web · money=SERVER {pr_id,vendor_id} server-owns-lines · gate-4.5 PASS after rework back-button/bold/save-stay · consume-only 0-mint · web-tsc0/vitest1412/routes100% · G5 batch-AM · B-245 double-PO edge non-block) | 2026-08-04 |
| P2-WEB-70 | ap.deposit port — AP deposit register + create (money=SERVER) | e0eea22 feature/web-ap-deposit-auto (orch-B overnight · post-B247-mint) | — (web · money=SERVER {vendor_id,po/wo_id,amount,pct-label} balance-off-wire · gate-4.5 PASS · 19-key mint applied · tsc0/vitest1462/routes100% · G5 batch-AM) | 2026-08-04 |
| P2-WEB-77 | notifications port — NotificationsCenter (read + mark-read) | 05aff15 feature/web-notifications-auto (orch-B overnight · post-B247-mint) | — (web · money=NONE · gate-4.5 PASS no-fabrication derive-icon/tone/title · 5-key mint applied · tsc0/vitest1462 · G5 batch-AM) | 2026-08-04 |
| P2-WEB-75 | petty port — PettyCash register + claim (money=SERVER) | 02579a2 feature/web-petty-auto (orch-B overnight · post-B247-mint) | — (web · money=SERVER {category,amount,description,project_id?} ≤10k-server · gate-4.5 PASS no-fabrication 9-gaps · 35-key mint applied · tsc0/vitest1462/routes100% · G5 batch-AM) | 2026-08-04 |
| P2-WEB-76 | opex thin-honest port — company OPEX budget grid + create (B-246=ข) | 63f904c feature/web-opex-thin-auto (orch-B · post-B248-mint) | — (web · money=SERVER {dept,year,months} THB-forced+409-dup · gate-4.5 PASS thin-honest no-fabrication [used/committed/%used/plan-actual/multi-year em-dashed] · 19-key mint B-248 · tsc0/vitest1478/routes100% · G5 batch-AM) | 2026-08-04 |
| P2-WEB-78 | gl.projectpl port — per-project P&L read (money=NONE display-only) | c7dbe91 feature/web-gl-projectpl-auto (orch-B · post-B249-mint) | — (web · money=NONE gate-4.5-CONFIRMED never-recompute P&L · honest-null margins · 5-key mint B-249 deduped-from-23 · tsc0/vitest1515/routes100% · G5 batch) | 2026-08-04 |
| P2-WEB-74 | ap.cn-dn port — AP credit/debit note register + create (money=SERVER) | 53f254e feature/web-ap-cn-dn-auto (orch-B · post-B250-mint) | — (web · money=SERVER {vendor_id,ref_ap_id,amount,reason}+approve-path-id · approve-wired-not-surfaced ap.pv-precedent · 25-key mint B-250 · tsc0/vitest1515/routes100% · G5 batch) | 2026-08-04 |
| MOB-SHELL-00 | mobile app shell — 5-tab MTabBar + MobileScreenRouter + primitives + Dio/auth (foundation) | 3ab356a feature/mobile-shell (orch-B verified) | — (mobile · gate-4.5 PASS 7/7 · flutter analyze clean + test 84/84 independent · B-241 honest-disable me · zero-mint · no-secret · 26-route placeholder no-fabrication · 2 real app-bug fixes · G5 N/A shell) | 2026-08-04 |
| MOB-NOTIF | mobile notif screen — notifications list + mark-read on MOB-SHELL-00 (money=NONE) | 27a8081 feature/mobile-notif (orch-B · post-B254-mint) | — (mobile · gate-4.5 PASS 7/7 no-fabrication B-039 real-wire · flutter analyze clean + test 101/101 independent · 2-key mint B-254 · pixel-G5 N/A [notif.png=mock] design-fidelity-via-gate45) | 2026-08-04 |
| MOB-APPROVE-REJECT | mobile PR approve + reject action screens on the shell (authority=SERVER) | 8f2d4cc feature/mobile-approve-reject (orch-B · post-B255-mint) | — (mobile · gate-4.5 PASS 7/7 authority=SERVER [approve path-id · reject {reason} req · no client-decide] · no-fabrication honest-empty-prId · flutter clean + 123/123 independent · 14-key mint B-255 {no}/{amount} preserved · pixel-G5 N/A mock-ref) | 2026-08-04 |
| MOB-SALES-CRM | mobile sales CRM leads list on the shell (money=NONE read) | 2c108ad feature/mobile-sales-crm (orch-B · post-B256-mint) | — (mobile · gate-4.5 PASS 7/7 · GET /sales/leads opaque-Entity raw-Map read · pure sales_crm_agg derivation · no-fabrication honest-empty · flutter analyze clean + test independent · 1-key mint B-256 · pixel-G5 N/A mock-ref) | 2026-08-04 |
| MOB-ST-GRLIST | mobile GR-pending list (approved PO + vendors) on the shell (money=NONE read) | 685b0dc feature/mobile-st-grlist (orch-B · post-B257-mint) | — (mobile · gate-4.5 PASS 7/7 · GET /po + /vendors opaque-Entity envelope-data · pure st_grlist_agg join · no-fabrication honest-empty · flutter clean + test independent · 3-key mint B-257 · pixel-G5 N/A mock-ref) | 2026-08-04 |
| MOB-PM-JOBS | mobile PM work-order/asset jobs list on the shell (money=NONE read) | fa9a8e0 feature/mobile-pm-jobs (orch-B · post-B258-mint) | — (mobile · gate-4.5 PASS 7/7 · GET /pm/workorders + /pm/assets list-data read · pure pm_jobs_agg deriveStatus IDENTICAL to web wo-rows.ts · no-fabrication honest-empty · flutter clean + test 170/170 independent · 3-key mint B-258 · pixel-G5 N/A mock-ref) | 2026-08-04 |
| B-261 | POST /gr client idempotency key — mobile money-write CONTRACT (SACRED openapi + migration 0056) | a1d8912 dev (feature/gr-idempotency · orch-B drove · Wei-ratified) | — (api · nullable idempotency_key + partial-unique-index gr_idempotency_uq · 23505-catch→re-read ORIGINAL via tenant-scoped po/wo→project chain → 201 not-dup not-409 · mirror B-167 · money=SERVER replay-returns-original · gate-4.5 money-skeptic 8/8 cross-tenant-409-no-leak · **LIVE E2E real-PG 1 passed** replay=1-receipt · api 1252 · migration 0056 live-confirmed · template for attendance/progress · follow-up B-263) | 2026-08-04 |
| B-260 | mobile GPS checkin — geolocator (Wei-approved stack add) | 331ba81 feature/mobile-write-checkin (orch-B drove) | — (mobile · geolocator ^13.0.1 behind injectable GpsSource · real-or-null never-fabricated-coord · permission-denied honest-no-enqueue · web-only target=browser Geolocation no-manifest · native entries documented not-guessed · gate-4.5 8/8) | 2026-08-04 |
| MOB-SYNCPROC | SyncProcessor level-(ก) drain policy — the deferred offline-write seam (B-242) | 2e13fc5 dev (feature/mobile-write-checkin) | — (mobile · FIFO/2xx-remove/4xx-deadletter/5xx-stop · money=SERVER replay-same-endpoint · no-silent-drop · 12 drain tests · gate-4.5 drain-flow traced termination-guaranteed · flutter +255 independent) | 2026-08-04 |
| MOB-CHECKIN | E2 PM GPS check-in — first offline-write screen (money=NONE) | 2e13fc5 dev (feature/mobile-write-checkin) | — (mobile · enqueue→drain POST /pm/workorders/:id/checkin · 3 honest states online-confirmed/offline-QUEUED/failed · GPS honest-or-null no-enqueue-on-denied · ZERO-MINT 10 stable-ids · online-success+permission-denied tested · gate-4.5 PASS 8/8) | 2026-08-04 |
| B-259 | backend prep — approvals-inbox InboxRow carries doc `id` for mobile detail nav (additive) | 718d01d dev (orch-B direct · gate-4.5 PASS) | — (api · +id pr.id/po.id/wo.id on GET /dashboard/approvals-inbox row · opaque Entity → NO openapi change · toMatchObject test additive + id assertion · gate-4.5 5/5 CONFIRMED additive/no-sacred/tenant-scope/no-money/real-test · api 1247/1247 · unblocks mobile inbox→detail) | 2026-08-04 |
| MOB-INBOX | mobile approvals inbox — caller's pending PR/PO/WO list on the shell (money=SERVER off-wire) | ee23739 dev (feature/mobile-inbox-detail 08dcbfb · orch-B drove) | — (mobile · gate-4.5 PASS 8/8 · GET /dashboard/approvals-inbox opaque-Entity raw-Map · amount off-wire verbatim · honest-omit title/requester/urgent/project no-column · PR row→detail push · PO/WO shown-inert · zero-mint · flutter analyze clean + test +224 independent · pixel-G5 N/A mock-ref) | 2026-08-04 |
| MOB-PR-DETAIL | mobile PR detail + inbox→detail→approve/reject FLOW seam (money=SERVER) | ee23739 dev (feature/mobile-inbox-detail 08dcbfb · orch-B drove) | — (mobile · gate-4.5 PASS 8/8 · GET /pr/:id + /projects · real title/need_date/vendor/requester/amount/items · item-name em-dash [WIRE GAP web-consistent] · banner status-only no-tier [approval_step always 0 single-shot] · Navigator.push seam prId=detail.id · approve/reject path-id verbatim-amount · ZERO-MINT [mob.approval.* namespace] · flutter +224 independent · pixel-G5 N/A mock-ref) | 2026-08-04 |
| MOB-FM-ACCEPT | mobile foreman acceptance queue — pass/reject a work period (money=NONE) | 044f7f0 dev (feature/mobile-field-group f2998b4 · orch-B drove) | — (mobile · GET /acceptance-center replaces the prototype's window.ACCEPT_ITEMS mock · POST /periods/:id/inspect · **round-1 FAILED on a one-tap IRREVERSIBLE record-free reject** — rejected is TERMINAL in the backend machine and the port sent an EMPTY defect list, so one tap permanently failed a period with no record of why, on the very screen that exists to record what was wrong · round-2 PASS verified independently) | 2026-08-06 |
| MOB-FIELD-PROGRESS | mobile field progress submit — send a work period for approval (money=NONE) | 044f7f0 dev (feature/mobile-field-group f2998b4 · orch-B drove) | — (mobile · the prototype's percentage bar has NO wire source [work periods carry a status, not a percent] → em-dash, never a plausible derived number · round-1 caught the raw English status enum printing into a Thai field app · gate-4.5 PASS round-2) | 2026-08-06 |
| MOB-FIELD-PR | mobile quick-PR from site — **ONLINE ONLY, deliberately not enrolled in the sync queue** (money=NONE) | 044f7f0 dev (feature/mobile-field-group f2998b4 · orch-B drove) | — (mobile · pr.no has NO unique index, so an offline replay through the sync processor would create a DUPLICATE purchase requisition — the write is held online-only and the gap is filed as **B-295** rather than shipped as "offline-safe" · round-1 caught an id-less draft re-creating a PR on every tap, contradicting its own comment · gate-4.5 PASS round-2) | 2026-08-06 |
| B-276 | web gr.list — an over-received line masked a short line (HELD slice released) | 89d63e4 dev (feature/web-grlist-rewire 3b4538f · orch-B drove) | — (web · Σ received >= Σ ordered does NOT imply every line met its own: A {200/100} + B {0/100} tied the sum and rendered "200 / 200" + 100% green bar + ครบ badge while the detail pane printed B as "0 / 100 · ขาด 100" · isComplete now consults the module's own per-line isItemShort · bar numerator capped PER LINE before summing · CELL text deliberately unchanged [200 really was received against 200] · gate-4.5 PASS independent: web 1694→**1753** · typecheck 0 · revert probes 3 RED / 2 RED zero overlap · over-receipt test KEPT beside its twin · `ordered > 0` recorded as provably dead rather than counted as coverage · GRAFTED 3 files onto dev, BLOCKERS rows APPENDED [wholesale checkout would have dropped dev's B-276..B-299]) | 2026-08-06 |
| B-264 | api POST /gr — the B-261 idempotency replay was UNREACHABLE on an order-closing receipt (money-adjacent honesty defect) | f2710c0 dev (feature/b264-gr-replay 70e3950 · orch-B drove) | — (api · the replay lived only in the 23505 catch BELOW the anchor gate, and a FULL receipt closes the PO in the same handler → a replayed receipt for goods that WERE received answered 409, and sync_processor dead-letters every 4xx permanently · fix resolves by key+anchor through the po/wo→pr→project tenant chain BEFORE the gate [gr_idempotency_uq is GLOBAL on the key alone → a key-only lookup would leak another company's receipt] · same key on a different PO 409s, never returns someone else's doc · **23505 catch KEPT** as the concurrency backstop · NO migration [column+index verified live with \d gr] · **live-proven on real PG: FIXED→201 same id/same money, REVERTED→409, counts gr 1 · gr_item 1 · defect 1 · money 1,200,042** · reported honestly that ap_billing_rows = 0 because POST /gr writes no AP/JV at all · api 1318→**1324** · gate-4.5 PASS, verifier re-ran the live revert itself · revert probe 5 RED / 41 green with an honest account of the 3 survivors) | 2026-08-06 |
| B-307 | api POST /labor/attendance — a replayed check-in was PAYING THE WORKER TWICE (money · B-261 template, Wei pre-approved) | 12cea15 dev (feature/b307-attendance-idempotency e2eb5c3 · orch-B drove) | — (api+db · payroll sums over ROWS not distinct days ⇒ one duplicated attendance row = a full extra day of pay, and postLaborPayroll is idempotent per payroll ROW [source_doc=payroll:<id>] so the inflated amount posts as ONE clean balanced Dr1140/Cr1020 JV no guard can correlate · migration **0057** byte-for-byte the 0056 shape modulo names · **NO natural key, deliberately** — recon PROVED a split day across two cost centres is representable today as two `half` rows the payroll SUM prices correctly, and the mobile prototype renders check-in/check-out as a PAIR against one server op; unique(worker_id,day,cc_id) is no fix either since Postgres treats NULLs as distinct · zero-hop tenant [attendance carries company_id] · anchor=(worker_id,day) so a key on a different worker 409s · **23505 catch KEPT and proven reachable live — the cross-tenant 409 came FROM the catch** · gate-4.5 PASS with the verifier building its OWN PG stack + negative control: no-key 2×POST→payroll **1375** [the defect] · keyed 2×POST→**687.5** · **8 SIMULTANEOUS keyed POSTs→one row, 687.5** · cross-tenant→409 no row written, B's row untouched · api 1324→**1338** · revert probe 13 RED/32 green) | 2026-08-06 |
| B-308 | api POST /labor/payroll — ONE DAY'S WORK COULD BE BOOKED TO THE LEDGER TWICE (money · Wei ruled ก) | 9e1f003 dev (feature/b308-payroll-unique 52adb88 · orch-B drove) | — (api+db · the GL guard keys on payroll:<id> so two runs minted two ids and BOTH posted as clean balanced JVs — 1,375.00 Dr1140/Cr1020 for one day, uncorrelatable · a double-click was enough, no replay needed · migration **0058** plain CREATE UNIQUE INDEX (worker_id, period) — **PLAIN not partial because both cols are NOT NULL**, the exact test that made B-307 refuse a natural key the day before, so the two opposite decisions rest on evidence not taste · preconditions PROVEN first: seed writes 0 payroll rows so it applies clean [and correctly ABORTS on a stack that reproduced the defect — stack reset, not a DELETE in a money migration] · no UPDATE/DELETE/PUT on payroll exists so no legitimate 2nd run is representable · prototype's WEEKLY period already unrepresentable + Wei ruled monthly B-140 RG-4 · **reviewer BUILT the race deterministically** [uncommitted colliding INSERT held open in a 2nd PG session, amount 999.99, then fired the API → blocked 6s on a real index lock → **201 returning session A's row, 1 row in DB**] which proves BOTH that the catch is reachable in production AND that the B-263 constraint-name gate resolves — an undefined would have rethrown 500 · at the ledger by SQL: patched **1 JV Dr 687.50 = Cr 687.50** + 2nd post 409 · reverted **2 JVs** · revert probe 6/8 die, money test dies FIRST on the JV count · api 1338→**1346** · cost recorded as B-310, not discovered later) | 2026-08-06 |
| B-309 | api — a non-string `idempotency_key` silently switched DEDUP OFF on two money paths, one already on main | 487d5ea dev (feature/b309-key-type 84853ad · orch-B drove) | — (api · `str()` returns `""` for non-strings so a JSON NUMBER collapsed to null → the no-key path, no dedup no 400 no warning, while the client believed it sent a key · **negative control proved it live BEFORE fixing**: numeric key ×2 → 201/201, two rows, payroll **1375**, psql showing both rows stored `idempotency_key = NULL`; boolean/array/object all silent no-key creates · fix = ONE shared `readIdempotencyKey()` in procurement.ts, zero new import edges, discriminated union carrying its own message so the handlers cannot drift [drift is HOW this existed in two places] · **ABSENT untouched** — no shipped client sends a key at all, so a wrong call would have 400'd every non-mobile write · numeric STRING stays valid: gate on type not shape · reviewer built its own stack, fired **25 shapes at /labor/attendance + 12 at /gr each TWICE and row-counted in psql** [unicode · 100k-char key · duplicate JSON keys · proto-pollution], reproduced the live negative control by reverting the helper, and ran **2 mutation probes the executor did not**: over-strict→3 RED, over-broad→37 RED, proving the suite gates BOTH directions · b261/b264/b307/b308 live contracts re-run green · dev's PRE-B-309 test files green against patched src · api 1346→**1379** · zero test deletions) | 2026-08-06 |
| B-312 | api POST /inventory/issues — a replayed issue posted a SECOND JV **and took the stock twice** (money + warehouse) | c71f101 dev (feature/b312-inventory-idem · orch-B drove) | — (api+db · the handler's own comment argued no pre-check was needed because "the issue id is fresh" — which is exactly the defect under at-least-once replay · **live before the fix**: on_hand **800 instead of 900**, JV-2026-0419 + JV-2026-0420, **Σ Dr 1140 = 33,700 for one physical issue of 16,850** · the request even carried an idempotency_key and it was silently ignored · migration 0059 = the 0056/0057 shape · **ONE write protected not five** [transfers/approve already idempotent via state gate + B-149 lock; transfers writes 0 ledger 0 JV] · **CLIENT key not natural** — 4 of 5 candidate cols NULLABLE ⇒ NULL-escapable, and two identical deliveries in a day are ordinary · reviewer rebuilt the stack, reproduced both directions, and **constructed the race deterministically** [colliding INSERT held open in a 2nd PG session → API INSERT genuinely blocked → 23505 → catch fired] which also proves violatedConstraint resolves through drizzle's .cause (B-263) · **after the aborted race: 0 ledger, 0 JV, on-hand untouched = real atomicity** · revert probe 21 RED, first dead test asserts JV total AND on-hand · api 1379→**1405**) | 2026-08-06 |
| B-313 | api POST /ap/deposit — a replayed vendor deposit PAID THE MONEY TWICE (money · proven live before any change) | aae41a1 dev (feature/b313-deposit-idem · orch-B drove) | — (api+db · **phase 1 was an EXPERIMENT not a build** — the defect came from reading code, and its twin at inventory.ts was proven wrong live, so this one was reproduced first: one intended ฿250,000 → **Σ Dr 1160 = 500,000 / Σ Cr 1010 = 500,000**, two individually-balanced JVs, audit_log holding 4 indistinguishable rows with no entity id ⇒ post-hoc invisible · the backstop is ARMED but blind: a forced same-source_doc insert DID raise 23505, so it guards a re-post of the SAME deposit, but a replayed CREATE mints a fresh uuid ⇒ fresh dep: key ⇒ never fires · **CLIENT key, natural key DISPROVED LIVE** — a 5th byte-identical row still INSERTED under (company_id,vendor_id,po_id,amount) because po_id is NULL, and with a real po_id the index could not even be BUILT · migration 0060 · **the best find was not the fix: the existing "double-post" test is a FAKE INJECTION** [swaps db.transaction for a thrower, proves the catch block, never touches the index] and would have stayed green through the whole defect · reviewer used its own image tag, ran the un-patched api against the SAME database as a negative control, and **constructed the race deterministically** [colliding INSERT held open in a 2nd psql session → API blocked 8s on the real lock → 201 with session A's row → JV count unmoved = tx rolled back] · revert probe 6 RED with the JV count first · **4 mutations incl. moving the pre-check below the COA gate → exactly the B-264 placement test dies** · api 1405→**1417**) | 2026-08-06 |
| B-316 A2 | api+web land.dd — the deposit ON SCREEN and the one in the LEDGER came from two different formulas | bd3fcd8 dev (feature/b316a2-land-deposit 41c129d · orch-B drove) | — (web computed `Math.round(total*0.1)` while the server posted `round2(round2((area/1600)*price)*0.1)` to the JV, on a screen that presents the figure as purchase-agreement terms · **severity MEASURED and the blocker row corrected: all 8 seeded plots delta 0.00 in value AND string; a 69,600-case sweep caps the value divergence at 0.50 baht and the rendered string at 1 baht in ~0.25% of cases** ⇒ closer to duplication than to a wrong figure, fixed because two formulas for one money value drift the moment either is touched · **ONE helper `plotDealDeposit()` called by BOTH the read (:257) and the JV-posting write (:881)** ⇒ drift structurally impossible · no contract edit [`Entity` additionalProperties true] · **reviewer READ the G5 reference PNG** and confirmed the four strings are on the baseline and unchanged ⇒ zero pixel movement while re-baseline is blocked · write value byte-identical by textual identity of the formula ⇒ ledger provably unchanged · api 1417→**1420**, web 1758→**1760** · the agent flagged a contradiction in orch-B's own brief [DECISION GATE said stop, RULES permitted the handler-only additive field] instead of quietly picking one) | 2026-08-06 |
| B-315 | api+web POST /ap/pv — an ACCOUNTANT could approve a 3,000,000 payment by sending 500,000 (money · gates approval authority · Wei ruled ก) | 581f982 dev (feature/b315-pv-server-amount f4b5a6c · orch-B drove) | — (**exploit demonstrated live and reproduced by the reviewer on its OWN hand-reverted build sharing one DB**: unpatched stored 500,000 → tier 0 → level-0 accountant approved → JV-2026-0419 485,000 · patched stored 3,000,000 → accountant 403, FinMgr 403, MD 200 → JV-2026-0420 2,910,000 · **the client formula was also WRONG** — ap_billing.amount is VAT-INCLUSIVE [vat = amount×7/107 every seeded row; prototype label literally "รวม VAT"] so every PV overstated by **6.54%** · **rider caught: fixing only the server would have DROPPED stored WHT 27,600→25,907** because impliedWhtPct self-cancels today — both halves shipped together · amount is a SUM [645,000+96,800=741,800; triple-dup id → 700,000 not 2.1M] · reviewer tried **10 alias routes** and none reached it, and proved the retained `<=0` guard is not dead code · reviewer **corrected itself in writing** about the ledger block · api 1420→**1426**, web 1760→**1763**, live G4 9/9 · revert probe 7 api + 6 E2E red with the tier assertion on its own line · no pixel impact) | 2026-08-06 |
| B-318 + B-168 | api — six concurrent deposits shared ONE document number, and six JVs shared one jv_no (audit trail · Wei ruled ก, both in one round) | ed0f8eb dev (feature/b318-doc-numbers · orch-B drove) | — (**live before the fix**: 6 distinct requests → deposits collapsed onto one number, `JV-2026-0460` ×6 · after: 6/6 × 201, 6 distinct numbers, 0 duplicate JVs · migration **0061** FULL unique `(company_id, no)` on both tables [both cols NOT NULL ⇒ no B-307 NULL escape] · **the retry is NOT a no-op and it was proven, not argued**: Postgres aborts a transaction on a failed statement, but 15/17 JV sites allocate OUTSIDE the tx and TenantDb.transaction delegates to the root pool ⇒ each retry is top-level on a fresh connection — reviewer forced a permanent collision and showed **all 10 attempts reached the DB with `grep 25P02` over the whole PG log EMPTY** · 2 sites needed different anchors [fa.ts allocates inside the tx; gl.ts re-bases a JS batch counter mid-batch → 0435/0437/0438/0439 no gaps] · **sequential numbering survives** — DP-2026-0005 re-used not skipped when only the JV collided · exhaustion 10 attempts → **503 RETRY not 409**, verified against sync_processor.dart:227 which dead-letters 4xx and defers 5xx · api 1426→**1448** · **recon found 9 MORE allocators of the same shape → B-320**, incl. 2 that cannot take the fix because `pr`/`boq_doc` have no company_id) | 2026-08-07 |
| B-319 | api+web+tax-engine — two STATUTORY Thai rates lived only in a React screen file (Wei ruled ก) | a6b299f dev (feature/b319-statutory-rates 9036f3e · orch-B drove) | — (2% transfer fee + 3.3% SBT were float literals in `land-dd-rows.ts` ported from `pototype/land2.jsx:243-244`; zero hits anywhere in apps/api or packages ⇒ nothing would notice if the law changed · **the recon reported the RULING'S PREMISE WAS WRONG** rather than executing quietly — tax-engine owns no rate today [all methods throw TODO(P0-INT-01); rates are INPUTS], the repo's rates are consts at `ar.ts:98`/`ap.ts:101`/`land-sales.ts:100` ⇒ ก CREATES that home · **no tax law invented, and a test enforces it**: spec silent, prototype's own contract clause says fee/SBT/**stamp duty** are *"เป็นไปตามที่ตกลงในสัญญา"* [negotiable, third instrument with no field], and `land_plot` has no acquisition date so a holding-period rule is unevaluable ⇒ flat rates + a docstring saying **not a legal source** + `rates.test.ts` asserting the key set, so adding a condition turns the suite RED · **single-source-of-truth fix NOT a drift fix** — nothing writes these, neither reaches a ledger · **one value changes to the CORRECT one**: นส.3ก 442 SBT 3,373,013→**3,373,012.50** [the old whole-baht round was the prototype's DISPLAY rounding leaking into the value] · **zero rendered strings change ⇒ no pixel movement** · reviewer verified by recomputation that the screen file now has zero numeric literals and that apps/web has no tax-engine dep so it cannot be re-imported · api 1448→**1451**, web 1763→**1765** · new inconsistency recorded as B-321, third-copy i18n trap stated not closed) | 2026-08-07 |
| B-322 | SACRED — full app-baseline re-baseline · **gate 5 green for the first time** | 91dee5c dev (orch-B · `SACRED_OVERRIDE=wei-approved:B-322`) | — (99 captured · 99 promoted · **0 FAIL** · **91/99 byte-identical**, other 8 at 2-13 px vs a 160 px threshold [last round's worst was 105 px] · scope held: 48 PNGs + manifest, `gallery/` `mobile/` `shots/` untouched, `apps/`/`packages/`/`infra/` zero, manifest 99 rows with the `screens` array **BYTE-IDENTICAL** and every mask id intact · `wo-list` included because B-277 ruled CUMULATIVE ⇒ the baselined figure is the ruled one · **the round carried its own experiment and it answered the opposite of the hypothesis**: gating TWICE on the SAME stack gave run-1 91/99 and run-2 96/99 with a DIFFERENT set of screens differing ⇒ the residual is render nondeterminism [2-13 px antialiasing, rotating], **not** the `created_at` drift B-301 argued · that cleanly separates it from the 296-936 px date drift seen only across freshly-seeded stacks, which is now **B-323** · this round also carries the blocker id the 2026-08-06 attempt lacked, which was the whole of B-305's self-report) | 2026-08-07 |
| MOB-ST-RECEIVE | mobile st-receive (F2) — the storekeeper goods-receipt screen · closes B-265/266/267/268 in one slice | 0ed1215 dev (feature/mobile-st-receive-f2 5c75719 · orch-B drove) | — (mobile **19→20 of 26** · portable only because **B-264** made the replay reachable after a FULL receipt and this screen's prototype default IS a full receipt · **THE TRAP:** sending `lines[].price` = client-originated money [B-315 shape], but omitting price while sending `name` persists `price '0.00'` → the merged web `gr.list` flips `hasLineDetail()` true → `formatMoney(0)` prints a literal **`0`** in the value column, the exact fabricated zero that file's header says it exists to prevent ⇒ payload sends **neither**: `{po_id, idempotency_key, lines[{qty_ok, qty_rejected}]}` · **4 success-view claims DROPPED + a 5th**: GR number [`gr.no` read from the CLIENT body, no numbering call] · warehouse stock [grep 0 AND no warehouse column] · auto AP bill [grep 0] · procurement notified [grep 0 AND structurally unreachable — gates on `rejected>0`, screen always sends 0] · the `📷 ✓` tick [asserts an attachment; pubspec has no camera package] · **the near-miss refusal I rate highest**: `ขาด` has two byte-exact dict matches that read right in Thai TODAY and wrong once en/zh/ar land, `เกิน` has none ⇒ renders a signed number instead, with a test forbidding either key from being smuggled in later · per-line ordered qty is REAL [`pr_item.qty` is what `prOrderedQty` sums and `gr.ts:680` uses], name/unit em-dashed because `prItemWire` has neither and the fan-out is 1+1+N not 3 · route parity **20 == 20** · flutter 684→**736** · analyze clean · zero Thai in lib/ · i18n untouched) | 2026-08-07 |

| MOB-FIELD-GR | mobile field-gr (B2) — the on-site goods-receipt REVIEW screen · identity filed as B-324 (OPEN) | 3f62462 dev (feature/mobile-field-gr cc9d293 · orch-B drove) | — (mobile **20→21 of 26** · **B2 is NOT F2 ported twice** and the cheaper wrong answer was routing `field-gr` at the merged `st-receive` — what settles it: **B2 has NO input control of any kind yet renders a PARTIAL line** (ord 320 / got 280), and a create surface that cannot enter a count could never reach its own headline state ⇒ the numbers already exist · 3 confirmations each **re-derived by gate-4.5 rather than taken from the port**: `grItemWire` = `{name, ordered_qty, received_qty, unit}` is a 1:1 shape match and the only place carrying all four · the section title is byte-exactly `gr.list.receivedItems`, a **READ** key, where the create surface uses `gr.create.colOrdered/colReceived` · `POST /gr/:id/return` is defined on an EXISTING receipt while the sign CTA has no endpoint at all (`boq.ts:466-468` says so outright) · **it is the only mobile surface that can show real material NAMES and UNITS** — F2 provably cannot (B-265) · **5 surfaces DROPPED**, each verified against the schema twice: QC checklist (grep `qc\|checklist\|inspect` in gr.ts = **0**, no qc column, `defect_report` = `{id, gr_id, note}` — no per-item, no reason code ⇒ 4 ticked boxes assert an inspection nothing recorded) · photo gallery (`gr.photos` real but its ONLY writer is the create path, seed writes `[]`, pubspec has no camera/picker) · `เซ็นรับ GR` (openapi declares exactly 3 GR paths, no sign route) · the `ส่งของ · ใบส่ง` sub-line (`po` has no delivery/expected column, nothing carries a delivery-note no, `grWire.date` is the **RECEIPT** date ⇒ printing it under a *delivered on* label is a semantic fabrication) · **`คืน/ปฏิเสธ` is the one where the endpoint EXISTS and is atomic (B-156), so *no key* carried the whole argument and gate-4.5 TESTED it instead of accepting it** — byte-exact across all 3 layers (dict th-values · phrases keys · nav_i18n keys) = **zero**; `common.reject`=`ปฏิเสธ` drops the `คืน` half, the half naming what the endpoint does ⇒ misdescribes rather than under-claims; `gr.list.kpiReturns`=`คืนสินค้า` has the right words but its **en/zh/ar are still untranslated Thai** ⇒ a translator fills it as the KPI noun *Returns* and the button breaks at TRANSLATION time, not today · **⇒ the screen ships with NO action. That is a judgement, not a ruling → B-324 stays OPEN**; every answer is additive EXCEPT option ข, under which this slice is discarded · shortfall ships as a **signed quantity in the prototype's own danger tone** — `ขาด` has 2 byte-exact matches that read right in Thai today and wrong once en/zh/ar land (F2's trap), and `gr.list.shortReceived` **hardcodes the unit `เส้น` inside its value** so it would print a bar unit on a bag line; a test forbids smuggling any of them back · **2 review findings closed, both a promise the code had not kept**: `selectReceipt`'s doc asserted *received rows only* and the bare route enforced it while the **pushed-id route did not** — probed live, a RETURNED receipt was served (unreachable today, but a trap for whoever wires the GR list since the comment promises the guard) ⇒ one `_isEligible` predicate both routes call, and an ineligible pushed id resolves to **nothing** rather than falling through to a DIFFERENT document · the `unit` slot rendered `''` where the sibling em-dashes — the one field not honouring the file's own header claim · **money = NONE** — `price`/`currency_code` are on the fed wire and reach no field, a test feeds a row whose only populated values are monetary and asserts every slot stays null; **the repository has no write method at all** ⇒ the F2 price trap cannot arise · **revert probe run TWICE on purpose at the last stage**: reverting both unit sites died on site 1's assertion first, leaving site 2 unproven ⇒ re-run with only the caption reverted, also RED ⇒ each site independently covered · earlier probes 5/2/1/3/2/2 red, gate-4.5 re-ran 4 of them itself · flutter 736→**801** · analyze clean · zero Thai in lib/ · all 3 i18n copies byte-identical and untouched · route parity **21==21** as a set AND a count, with an explicit test that `field-gr` and `st-receive` are two DIFFERENT builders) | 2026-08-07 |
| B-323 | api+db+qa — seed determinism + the ordering class · **four rounds, three gate FAILs, each one earned** | 225a552 dev (feature/b323-seed-determinism d2df13b · **SQUASHED not --no-ff**, see below · orch-B drove) | — (**the pack was green only on the machine that captured it**: every schema table carries `created_at .defaultNow()`, the seed set it explicitly for exactly ONE (the org tree) ⇒ 72 seeded tables wrote a wall-clock instant and the drift GREW daily · separately `gr.list` swung 0 px ↔ 253,533 px on identical code — **and the blocker row's own explanation was wrong**: there were no equal sort keys leaving order to the DB, there were **NO sort keys**, `grep orderBy` across apps/api + packages/db = **0** across 68 list endpoints · **FIX** — the seed's TRANSACTION is wrapped, not its 74 insert sites, so the 75th insert is stamped whether or not its author knows this defect exists; row *i* gets `SEED_NOW − i·1000ms`, explicit `createdAt` still wins, anchored on `SEED_NOW` not a literal ⇒ prod/dev byte-identical to before · the stamper discovers columns from **drizzle metadata** and the dump from **`pg_attrdef`**, so the next `.defaultNow()` column cannot be silently reported as fixed (which is exactly how `evm_snapshot.captured_at` escaped round 1) · one total order applied **by what each list MEANS** — newest-first for documents/masters, entry order for a document's LINES, ascending for the 3 consumers that take `[0]` as a hero row, at-the-source for derived rows carrying no id · **ROUND 1 FAILED BECAUSE ITS EXPERIMENT COULD NOT FAIL** — the proof printed `id created_at` together so a timestamp shift alone produced DIFFERS; strip timestamps and the id sequence was **identical in the round's own reverted arm** ⇒ the ordering half was untested by the proof claiming to test it and would have passed with the fix deleted · rebuilt to compare the **id sequence alone** under 3 FORCED join plans, aborting INCONCLUSIVE below 8 joined controls; the reviewer then built its OWN negative control and got **ORDER-FLIPPED on 9** — one more than the round found · **ROUND 2 FAILED ON THE TRAP ITS OWN BRIEF PRE-REGISTERED** — `boq_item` has no `seq`; the round ordered its READS and did not stamp its WRITES ⇒ a 12-line bulk add (handler docstring: *"from BOM / Excel / AI QTO"*) ties on one `now()` and prints the priced body of the document in `gen_random_uuid()` order, **and two comments in the shipped tree asserted the stamp existed** · the round's test could not catch it because it built lines `.000Z/.001Z/.002Z` apart — the seed's stagger in miniature, where production produces a TIE · **ROUND 3 FAILED ON A COMPLETENESS CLAIM** — tie-blind comparator counts went **5 → 7 → 10**, each published as authoritative, each wrong; the sharpest survivor sat **six lines above** one the round had just fixed in the same file (`GET /labor/attendance` sorted on `day` alone — every worker on one shift shares a date), and it survived because **the seed emits zero attendance rows**: a defect the seed hides, which is what this whole blocker is about · **ROUND 4 STOPPED COUNTING** — `list-order.enforce.test.ts` walks the AST of every non-test file under `apps/api/src`, **lifts each comparator to module scope and CALLS it** with two rows differing on one key; return 0 ⇒ build fails, named by file:line ⇒ a new tie-blind sort fails by DEFAULT and nobody maintains a list · run against round 3's tree it reported **13 not 10** (9 in no prior count) · **the reviewer wrote an INDEPENDENT AST scanner and got the identical 50-site set, file:line for file:line** — the first time a number here was checked against something other than another reading — then planted 10 defects and caught 7 (misses: `rows["sort"](cmp)` · `Array.prototype.sort.call` · `.mts/.cts` — zero precedent in the repo, all now IN the blind-spot list instead of behind an "every .sort call" claim) · best probe: deleting the `ab === 0` check dropped detection **13 → 5**, so both assertions carry distinct weight · **SQUASHED DELIBERATELY, against this lane's `--no-ff` convention** — round 2 committed a **bearer token** into a tracked evidence file as a script side effect; round 4 untracked it, gitignored the shape and moved the script to `mktemp` 0600, but the blob still sat in 3 branch commits ⇒ squash keeps it out of dev history entirely (never reached origin, dev could not reach it) and the branch is deleted, so it is unreachable from every ref · a scan of **all 1807 tracked text files** found no other credential value · **10 baselines legitimately move and are NOT re-captured** — `tests/visual/reference/**` is sacred and this round has no id ⇒ **B-329** · measured not estimated, and **pixel-identical across rounds 2, 3 and 4**: pm-assets 18115 · pm-schedule 13555 · pm-wo 5292 · pm-dashboard 4401 · petty 1997 · dms 1286 · notifications 1195 · gr-list 888 · gl-inbox 800 · po-list 188 (+10 more changed only INSIDE Wei-approved masks) · the 4 `pm-*` movers were **checked not assumed**: pre-fix `/pm/assets` walked a hash-join bucket order, post-fix it returns the seed ladder · **one rendered string changes, gate-only**: `notifications` day headers วันนี้ → a literal date, because `dayBucket` compares against the BROWSER clock not `businessNowMs` — today's header is stable only by luck (seed at 23:59 + gate at 00:01 flips every row to "yesterday") · api 1451→**1510** · db 0→**19** (the package had no test script) · web **1765** unchanged · lint 12/12 · open and stated: `pr_item`/`jv_line`/`ar_invoice_line` are the same class with no reader ordering them yet — exactly the state `boq_item` was in) | 2026-08-07 |
| MOB-OPID / B-330 | mobile — an offline write survived the app being killed, then minted a SECOND idempotency key and paid twice (5 merged screens, one a MONEY path) | b1b1b6c dev (feature/mobile-opid-rehydrate 56202aa · orch-B drove) | — (**4 rounds, gate FAILED 3 of them and was right every time** · `_opId` was per-mount `State` with nothing rehydrating it ⇒ enqueue offline → app killed → reopen → drain fails → re-tap → **fresh key, two ops, two rows**, and no unique index can catch it because two distinct keys are legitimately two records; `st-receive` writes `POST /gr` ⇒ GL JV · **the queue does not know who owns an op** — one global FIFO keyed only by the caller-minted id ⇒ ownership had to be reconstructed from what the op carries; 4 screens put the record id in the PATH, `POST /gr` does not (one endpoint for every PO, anchor is `po_id` in the BODY) ⇒ matching on entityType+endpoint alone would let PO-B adopt PO-A's key, **which is not a duplicate but PO-B's receipt silently never being sent** · **ROUND 1 FAILED on the adoption WINDOW** — `_resumeQueued` fires `unawaited` from `initState` and adoption lands only AFTER `await drain()` (one HTTP round trip, unbounded — Dio has no `connectTimeout`); throughout it the CTA is live and `_opId` null, and the nested drain hits the re-entrancy guard returning an EMPTY report so **the screen showed a reassuring `รอส่ง` while the outer drain sent BOTH**. Gate reproduced it on a HEALTHY network: 2 `POST /gr`, 2 keys, 2 JVs. Fix already in the branch — `field_progress` asks the queue before minting · **ROUND 2 FAILED because the fix made `field_progress` the one screen without the companion guard** — it gained `await repo.due()` before any state flip where dev's mint path had no await at all ⇒ 2 taps, 2 ops, and online the server takes it ONCE while the app keeps a permanent dead-letter and the screen renders *ทำรายการไม่สำเร็จ* for a delivery that succeeded · **ROUND 3's fixture audit found 3 defects, 2 written in that same round** — a slow-queue fixture that delayed THEN read (tap 2 saw tap 1's enqueue ⇒ the check deduped the pair itself ⇒ **the revert probe came back GREEN**), a transport capability documented for a test that does not exist, and a unit test named *adopts the OLDEST match* that did no ordering at all (the matcher does no sorting; swapping the timestamps gives the same answer) · **and it found the row had reported a probe it never ran** — `_seeding → 1 red` was actually **0 red**, because every pm-notes fixture returned a work order with NO stored log so the seed wrote `''` into an empty controller and notified nobody. **The row now says *THIS NUMBER WAS FALSE* in those words** rather than quietly matching the number to the measurement · **ROUND 4** wrote the 4 double-tap tests the gate had prototyped — the synchronous busy flips had ZERO mechanical protection and removing each gives **2 GR + 2 JV** on st-receive while the other 844 stay green · **corrected before merge:** F2 claimed `field-progress` escapes a dead-letter in-session; it does NOT (after a 4xx `_opId` is retained ⇒ re-drain branch ⇒ never reaches the queue check ⇒ processor skips `failed` ⇒ back to `failed`) ⇒ **F2 is 3 of 5 screens, not 2**, and it is awaiting a Wei ruling so the wrong figure would have produced the wrong ruling · open + disclosed: case A (an edit typed inside the window and saved after it closes is discarded — relative to dev this trades a duplicate write for **input loss**) · B-327's flake rate measurably raised (4/16 here vs 0/7 before), root cause a fixed 50 ms wall-clock budget racing `markSynced`, deliberately NOT edited at gate time because fixing an unrelated test file mid-gate is how this branch acquired two of its defects · flutter 801→**845** · analyze clean · zero Thai in lib/ · no sacred file) | 2026-08-08 |
| MOB-FIELD-STOCK / B-339 | mobile `field-stock` (B4) — the site material-issue screen · mobile reaches **22 of 26** | 2962495 dev (feature/mobile-field-stock 319c149 · orch-B drove) | — (**the CTA's `18,000 ฿` is a LITERAL, not arithmetic** — the prototype's 3 mock rows are `{code,n,unit,stock,qty}` with **no price field anywhere in the component**, and against real seed prices the basket is 30,480; none of the 9 `/inventory` paths returns a total for a client-composed basket ⇒ reachable only by the client computing `Σ(qty × price)` = the B-316 A3 shape, **worse here because it is the number a storekeeper CONSENTS to on a button that posts a JV** ⇒ dropped · the post-write disclosure dropped too and that call was right: the 201 carries the server's `value` but the **deferred branch has no response body at all** ⇒ the figure would appear only sometimes, and **a money number that shows up intermittently is worse than none** · made STRUCTURAL not promised — `parseStockLines` never reads `price`/`value`/`currency_code` though all three are on the wire it parses · **the anchor is `from_warehouse_id` ALONE** — `POST /inventory/issues` is one path for every shelf (as `POST /gr` is for every PO) so identity must come from the body, and the two candidates are NOT interchangeable: `project_id` is re-defaulted by `_apply` on every load ⇒ including it makes a NON-default-project basket fail to match its own queued op after a remount and **mint a second key — the same double-post wearing a narrower anchor**; endpoint-only is the mirror bug and worse — **warehouse B adopting A's op means B's basket is never sent while A's outstanding write is displayed as B's** · both directions pinned by tests · because the anchor excludes the project, an adopted op may charge a project the fresh load did not default to **and the picker is about to be frozen on the strength of that op** ⇒ the screen reads `project_id` back out of the queued payload, and on a lookup miss adopts the id and **em-dashes the NAME** rather than showing a different real project's name · **the gate FAILED this on a defect the round created ITSELF, and it is the sharpest thing here:** the fix for a separate note (re-read `on_hand` after a confirmed issue) gave `_load()` a SECOND call site — but `_load()` re-resolved the subject from `widget.warehouseId`, which is **always null on the tab route** (`const FieldStockScreenHost()`, nothing ever pushes a warehouse) ⇒ 100% of production mounts follow *the register's newest* ⇒ **a warehouse created between two issues in one mount silently re-subjects the screen** (eyebrow, shelf and `_warehouseId` all flip) and the next confirm cuts stock from the WRONG shelf with a JV behind it — **undoable by nothing, since there is no return op among the 9 `/inventory` paths** · **the repository had stated as the load-bearing premise of the whole anchor decision that *NOTHING in the mount can change it* — true when written, false by the end of the same commit.** It now says the pin is **an explicit line of code rather than a property of the screen's shape**, which is a claim a future edit cannot quietly falsify · dropped, each verified against the wire: QR scan (no endpoint, no camera package) · return-material (no return op) · `WO · งวด · scope` (`material_issue` carries only `project_id` + `from_warehouse_id`) · the CTA total. Confirmation is the state change the screen can actually make — basket empties, CTA greys, balances re-read so **the shelf stops contradicting the cut** · **8 fixture defects found by asking ONE question of every fixture — what condition must this create, and can it** — incl. a refresh fixture that clamped its list so the third read returned a **physically impossible pre-issue balance**, an op fixture with an empty payload that made the dead-letter tests pass on an anchor mismatch rather than the status check they are named for (**probe-proven both ways**), and the round catching its own `?? 'w1'` default that would have anchored an op to the wrong shelf in silence · gate verified independently: every state where `_locked ∧ ¬_canSubmit` (a tap can never reach the mint) · every write to `_warehouseId` · the B-339 greps · **no id collision across 120 refs** · 5 revert probes each red ALONE · flutter 845→**933** · route parity **22 == 22** · analyze clean · zero Thai in lib/ · all 3 i18n copies untouched) | 2026-08-08 |
| B-284/B-290 verify | web — **two of ten seq/pct guards were shipped untested, and one passed for the wrong reason** | `de272ee` dev (feature/b284c-seq-guards 1c88881 · orch-B drove) | — (the brief was stale before it was written: B-284 option ค was already implemented and merged as B-290 (3a5d56f via 5ceed48), so the agent verified the merged work instead of re-implementing it, **and the verification is what was worth doing** · the “not ENTIRELY percent-basis” test set `pct: 0` on every non-percent row, so a DIFFERENT guard (`pct > 0`) returned null first — **delete the basis guard and all 31 tests stay green** · `po-wo-rows.test.ts` had got this right originally, with a comment warning about this exact trap, and the subcon port copied the intent and lost the property — same shape as B-309, a second copy of a check drifting from the first · subcon-handover had no test file at all) | 2026-08-06 |
| B-332+B-336+B-337 | api+db+contracts — **a self-service check-in door onto payroll**, as ONE migration because attendance sums into money | `8a878ad` dev (feature/b332-checkin-schema 7a54a80 · orch-B drove · gate-4.5 FAILed it twice) | — (migration 0062 additive throughout: 6 nullable columns on attendance, 1 on worker, 1 FK ON DELETE SET NULL, 2 partial unique indexes · no DEFAULT, no backfill, no USING cast, no type change on any money column · **the design corrected its own brief before writing anything**: B-307 did not use (worker, day) uniqueness — it explicitly REFUSED one, because a cost-centre-split day is legitimately two half rows with different `cc_id`, so my stated reason rested on a constraint that does not exist · the direction was still right for a stronger reason — payroll pays by summing ROWS and `day_fraction` is NOT NULL DEFAULT 1, so a check-out written as a second row is **2.0 days paid for one day worked**, and the idempotency key cannot stop it since check-in and check-out are two distinct ops with two legitimate keys) | 2026-08-08 |
| B-341 | mobile — **the offline CTA goes quiet during the on-mount drain**, across six screens at once | `244dd8b` dev (feature/b341-cta-quiet 7065939 · orch-B drove · gate-4.5 FAILed it twice, both times on the compensation rather than the change) | — (during the drain a user could re-stage a basket and confirm, the pre-mint check would adopt the PREVIOUS op, and the new basket would never be sent while the screen reported success · **the bound turned out to be a reordering, not a timeout**: `_resumeQueued` was `await drain(); then adopt` and is now `adopt; then drain`, so the quiet window contains NO network call at all — proven by enumerating every queue mutation and showing a drain can only REMOVE ops from an identity's adoptable set · **the ruling's own stated closure was false and the round said so** — B-341 claimed it also closed F1 case A; probed, reproduced, then closed properly at the second adopting site · **NO regression test ships for the back-button double-pop**: I wrote one, it passed with the fix reverted on two timings 80/80 either way, and a test that passes either way is worse than none · flutter 933→979) | 2026-08-09 |
| B-340+B-342 | api+db+contracts — **receiving goods finally adds stock**, and the lock order became a registry instead of a paragraph | `7cc23d8` dev (feature/b340-stock-money 77393c1 · orch-B drove · gate-4.5 FAILed it twice, the same mistake both times) | — (before this, stock could only ever go DOWN: `insert(stockLedgers` existed at exactly two sites, `grep -c stockLedger gr.ts` was 0, the seed wrote none — so on-hand was 0 for every item and the field-stock screen merged the same week had no working path at all · **the enumeration of lock takers was incomplete twice**: `inventory_item` has exactly three FK children and every INSERT silently takes FOR KEY SHARE in row order; round one left the new ledger writer unsorted (8/14 deadlocked), round two named “all three places” and there were **four** — `createTransfer` had been inserting `transfer_line` in client body order the whole time, which this branch's own FOR UPDATE turned into a live cycle (16/48, all 40P01, 6 of 6 rounds) · so the enumeration is no longer prose: `lock-order.enforce.test.ts` derives the children from the drizzle schema and **caught its own author within the hour** · measured both arms with separate OS processes: fixed 96/96 clean across two runs, reverted 18/48 → 500, every one a PG deadlock · api 1589→1600 · live E2E 19/19) | 2026-08-09 |
| WEB-STALE-SWEEP | web — **eight screens honest-disabled against a backend that had already shipped**, five flows, zero schema/contract/ruling | `a4affca` dev (feature/stale-deferral-sweep f6ca925 · orch-B drove · **five gate rounds, every finding real, all on the comment layer — the round's own subject**) | — (`sales-crm` was committed at 12:14:48 saying “create endpoint not filed”; the endpoint landed at 12:42:20 the same day · a disabled control reads as “unbuilt” to a human, a screenshot and any agent re-scoring the flow, so **this is what made the coverage bars misleading** · **verifying first changed the answer: only 2 of 8 were wireable** — admin.subs reset-password (which was actively lying: it toasted that an email had been sent to another person and sent nothing) and land.pipeline (a modal port, not a button) · the other six stay disabled and now say why in terms that are true, incl. land.dd where the contract declares `{checklist}` while the handler reads `dd\|dd_checklist\|ddChecklist`, so a contract-shaped PUT merges nothing and answers 200 — and **the api test masks it by sending the handler's shape** · round 1 shipped six blocker ids never filed plus a brand-new comment reading “no export endpoint exists anywhere in the contract … Verified 2026-08-10” when `openapi.yaml` declares both · **a “Verified” stamp on a false claim is worse than no stamp** · 11 rows filed B-346–B-356, incl. two ids cited 24 times across two directories that existed on no branch · web 1765→1831) | 2026-08-10 |
| B-331 | web+mobile — **the customer's signature is captured**, and the seam that could silently drop it is closed by construction | `d647a64` dev (feature/b331-signature f748bce · orch-B drove · gate-4.5 PASS round 2) | — (before this, pm-close asked for a signature the product could not take: both clients drew a pad, neither turned ink into anything the server stored · one wire contract, two clients — `{v,w,h,s}` stroke JSON where `w`/`h` are load-bearing for rescale; **pressure and timing deliberately excluded** because timing makes it a behavioural biometric, PDPA-sensitive with no consent surface here · **the finding that shaped the round**: `capture={{ ...capture.current }}` — a shallow spread shares the strokes array, so the canvas renders the signature PERFECTLY while `width` lands on the copy and the work order closes with `customer_sign` NULL under a success toast, tsc 0 / 1794 green · answered by collapsing the two references into ONE closure so a shallow copy of a binding IS the binding, proven by 6 red on re-split, not argued · **what is NOT closed is disclosed, not claimed**: handing the pad a FRESH binding stays expressible and invisible (B-358) because `apps/web` has no DOM test environment at all · **Wei ruled twice, both matching what shipped** — web adopts mobile's encode gate (B-366 discloses the prototype deviation), and pointer-cancel DISCARDS in both clients · **the fixture had two defects**: adding a real encoded third arm did NOT fix it (mutant survived 94/0) because `_pump` reuses the `State` and the screen loads once from `initState`, so a two-value loop was testing one value — `'sig-blob'` never reached the screen · 94/0→93/1 under the mutation · **bookkeeping repaired at merge, both mine**: B-357's row was not a row (its text was glued to the end of B-344's line, so an id cited 84 times across 16 files was invisible in the table), and B-365 collided with two backend commit subjects → B-366 · BLOCKERS.md GRAFTED onto dev's copy, 335 rows in → 339 out, none lost · web 1831→1878 · flutter 1043) | 2026-08-10 |
| P2-DEV-09 | devops+web+api — **the web image could not reach the API at all, and answered 200 while failing** | `7da719d` dev (feature/docker-deploy-ready 25a991b · orch-B drove · gate-4.5 FAIL→FAIL→fix) | — (`api-client.ts:23` falls back to a same-origin `/api/v1`; the baked nginx config was one printf'd `try_files … /index.html` with **no /api proxy**, so every API call was answered with index.html and **HTTP 200** — not a 404, a failure shaped like a success · measured both arms against a stand-in api that echoes the path it received: dev's image answers `/api/v1/me`, `/healthz` and a missing asset with `200 <!doctype html>`; this one returns the JSON, `ok`, and `404` · **this defect already cost a full debugging round** — that session ruled out a regression by comparing main vs dev, then let the result license “the pack is stale”, which the experiment never tested because both arms ran through the same broken proxy · **every gate finding was mine**: the `--offline` claim was false and my own log disproved it (deps copied the lockfile alone, so corepack could not see the `packageManager` pin and the install still reached the registry — proven under `--network none`), I had the nginx add_header inheritance trap **backwards** in the very file where I wrote a comment warning about it, and my fix for a duplicate Cache-Control introduced `always`, which made a **404 cacheable for a year, immutable** — a rolling deploy would leave a client permanently broken with no server-side recovery · also: three stale TODOs retired · api runs as `node` uid 1000 with an unwritable /app · HEALTHCHECK in both images · dependency-layer caching · **not done, and said in the file**: nginx master stays root because moving off port 80 would break prod ingress through a Caddyfile that is not in this repo, and no CSP because an untested one breaks the browser while every server-side check stays green) | 2026-08-10 |
| B-372 (+B-367…B-371) | api+db — **the receipt's quantity stops being the client's to choose** | `f9168e1` dev (feature/backend-unblockers 9223ed7 · orch-B drove · **gate-4.5 FAIL→FAIL→PASS**) | — (B-360 gave the server the receipt's PRICE and the orderable line set and left the QUANTITY open, so the server owned one factor of a product and the client owned the other: **`qty_ok: 99999999` on a 1,420,000 order posted 14,199,999,858 to the ledger**, read out of Postgres · Wei ruled a **percentage tolerance, not a cap**, because the prototype deliberately permits over-receipt (`mobile-field.jsx:46` clamps the lower bound only; `:76` renders `เกิน {n} {unit}` in INFO tone) while the same prototype clamps hard wherever a ceiling IS meant · **the 10% is MINE, not the spec's** — the spec states no tolerance anywhere · basis is `pr_item.qty` and can be nothing else, since `gr_item.ordered_qty` is client-supplied and falls back to the client's own `qty_ok` · **gate FAILed the first fix twice, both on the ceiling's REACH not its mechanism**: `pr.ts` accepted `qty: 0` so the `ordered <= 0` skip I had told the round to document as an honest residual was a **client-operated off switch** — proven end-to-end as an ordinary user, PO total 0.00 and the identical 14.2bn back on the ledger; and the basis was PR-grained while the accumulator was anchor-grained, so one PR raising two POs gave **22,000 received against 10,000 ordered** · both closed and measured: **no `qty=0` rows existed** so the source refusal closes it completely, the accumulator now unions both anchor chains at PR grain, and the lock moved to the PR row — **13/13 rounds one 201 fixed vs 5/13 leaking with the lock back at anchor grain, always exactly two 201s, one per PO group** · 42 concurrent ops, zero 40P01 · ledger swept: **0 JV legs over 1e9** · api 1600→1684) | 2026-08-10 |
