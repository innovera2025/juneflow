# REVIEW-QUEUE.md — คิวงานเขียวบน dev รอ Wei promote

> ตาม PLAN.md §10 Review flow: `feature → dev (auto เมื่อ CI เขียว) → main (Wei promote คนเดียว)`
> Wei ตรวจเป็น batch: อ่านคิวนี้ + `BLOCKERS.md` → คลิกเล่นบน dev เทียบ gallery → ผ่าน = promote / ไม่ผ่าน = rework task

## วิธีใช้

**ฝั่ง agent:**

1. task ผ่าน gates ครบ (ตามคอลัมน์ gates ใน `TASKS.md`) และ auto-merge เข้า `dev` แล้ว → เพิ่มหนึ่งแถวในตารางด้านล่าง
2. เปลี่ยนสถานะ task ใน `TASKS.md` เป็น `review` + เขียน journal ประจำรอบ
3. คอลัมน์ **diff** = ลิงก์/ref ของ commit หรือ PR ที่ merge เข้า dev · คอลัมน์ **ภาพเทียบ gallery** = path screenshot จอที่สร้าง คู่กับ path ภาพอ้างอิงใน `tests/visual/reference/` (งานที่ไม่มีจอ เช่น schema/script → ระบุ "—" พร้อมหลักฐาน gate ที่ใช้แทน)

**ฝั่ง Wei:**

1. ไล่ตรวจจากแถวเก่าสุด → คลิกเล่นบน dev เทียบ gallery
2. **ผ่าน** = promote เข้า `main` → เปลี่ยนสถานะ task ใน `TASKS.md` เป็น `done` → ลบแถวออกจากคิวนี้
3. **ไม่ผ่าน** = สร้าง rework task ใน `TASKS.md` (สถานะ `ready` ระบุสิ่งที่ต้องแก้) → ลบแถวออกจากคิวนี้

## คิวรอ promote

| task id | โมดูล | diff | ภาพเทียบ gallery | วันที่ |
|---|---|---|---|---|
| P0-BE-11 | backend · apps/api + packages/db — tenant-scope middleware + better-auth self-host | commit `fa36ad0` บน `feature/backend` (local-only → รอ loop-runner push→ด่าน 4.5 ที่รันแล้ว **PASS**) — implement กฎเหล็ก "company_id ทุก query": (1) `apps/api/src/db/tenant-db.ts` = **choke point เดียว** `TenantDb` — base handle เป็น private field `#db` (ไม่ expose → handler ยิง query หลุด scope ไม่ได้เชิงกายภาพ) · `select/update/delete` inject `eq(table.companyId, id)` AND กับ predicate ผู้เรียก (ไม่แทนที่) · `insert` force-set companyId หลัง spread (smuggled companyId แพ้เสมอ) · constructor throw `TenantScopeError` เมื่อไม่มี tenant (fail closed) · generic `T extends TenantTable` reject ตารางไม่มี companyId ตั้งแต่ compile-time. (2) `packages/db/src/client.ts` = `createDb()` drizzle handle (`@juneflow/db/client`) base un-scoped. (3) `apps/api/src/auth.ts` = **better-auth self-host ใน Postgres เรา** (`Pool(DATABASE_URL)` ไม่ใช่ hosted) + `bearer()` plugin (ตรง contract `Authorization: Bearer`) + `companyId` additional field (input:false ป้องกัน client ปลอม tenant) + lazy `getAuth()` (ไม่ต่อ DB ตอน import) · `resolveTenantFromAuth` อ่าน session → company_id. (4) `apps/api/src/plugins/tenant-scope.ts` = onRequest hook reject non-public request ที่ไม่มี tenant (**401 UNAUTHENTICATED** ก่อน handler รัน) + attach `request.db = TenantDb` scoped · wired ใน `index.ts` (default resolver = better-auth session). **ด่าน 4.5 diff-reviewer = PASS** (sacred ✓ — better-auth tables สร้างผ่าน better-auth CLI ไม่ใช่ drizzle migration · zone เฉพาะ apps/api + packages/db + lockfile importer · real uuid FK ไม่ใช่ name-text mock · ไม่แตะ C1–C10 · logic มี G3 test ครบ). **Note (non-blocking):** `TenantDb` รองรับ single-table เท่านั้น — join/raw SQL ยังไม่รองรับ (ไม่ใช่ scope escape เพราะ handler ไม่เคยได้ base handle) → P0-BE-13+ ที่ต้อง join ให้ขยาย wrapper ไม่ใช่เอื้อม base `db` | — (middleware/library · ไม่มีจอ) · หลักฐาน gate **G3 (unit tenant scope — ห้ามมี query หลุด scope)**: `pnpm --filter @juneflow/api test` = **14/14 pass** (tenant-db 10: fail-closed construction · select/insert/update/delete ทุกตัวพก `"company_id" = $` predicate/bind · smuggled companyId ถูก override · AND ไม่แทนที่ predicate ผู้เรียก — พิสูจน์ผ่าน drizzle `.toSQL()` ไม่ต่อ DB · tenant-scope 4: public bypass `/health` · 401 fail-closed · handler ไม่รันเมื่อไม่มี tenant · resolved tenant → `request.db` scoped) · root `turbo lint` **9/9** · `typecheck` **10/10** · `test` **10/10** · `build` **3/3** · `pnpm install --frozen-lockfile` ✓ | 2026-07-12 |
| P0-BE-04 | backend · packages/tokens — gen Flutter ThemeData | commit `50c3a33` บน `feature/backend` (local-only ไม่มี remote → รอ loop-runner push→ด่าน 4.5 ที่รันแล้ว **PASS**) — implement pipeline `gen-flutter-theme` ที่เดิมเป็น stub (`process.exit(1)`): (1) `src/flutter-theme.ts` = pure generator เดิน `themes.fiori` ทุก group (brand/shell/surface/text/status→Color · radius/table→double · font→String) แล้ว emit `abstract final class JuneflowTokens` + `ThemeData juneflowFioriTheme()` ที่อ้างเฉพาะ `JuneflowTokens.*` (ไม่มี literal ค่า design) · ทุกค่ามาจาก `src/tokens.json` ไม่ hardcode · **fiori เท่านั้น navy excluded (PLAN §0 กฎ 5)** (2) `scripts/gen-flutter-theme.ts` = CLI เขียน `gen/juneflow_theme.dart` (default in-zone, gitignored) + `--out` override ให้เขต mobile (P0-MOB-02) ชี้ `apps/mobile/lib/theme` — **commit นี้ไม่เขียนลง apps/mobile** (3) `src/flutter-theme.test.ts` 6 tests: expected อ่านจาก `tokens.json` เอง — assert ทุกสี fiori โผล่เป็น `0xFF<hex>` + **invariant กัน hardcode** (สี `0xFF……` ที่ emit ทุกตัวต้องอยู่ในเซต fiori) + navy ไม่หลุด + radius/table/font ตรง source · `src/tokens.css`/`src/tokens.json` **ไม่ถูกแตะ** (byte-identical) · wire `lint`/`typecheck`/`test`/`build` + vitest เข้า CI ครั้งแรก · **ด่าน 4.5 diff-reviewer = PASS** (sacred ✓ · zone เฉพาะ packages/tokens + lockfile importer · no hardcode/navy leak · fiori-only · logic มี test ครบ · ไม่แตะ C1–C10) | — (library/gen · ไม่มีจอ) · หลักฐาน gate **CI + output gen ตรงค่า token ต้นทาง**: `pnpm --filter @juneflow/tokens test` = **6/6 pass** · `typecheck` ✓ · `build` (gen) เขียน `gen/juneflow_theme.dart` ✓ · generated Dart ตรวจตาแล้ว = ทุกค่าตรง `tokens.json` fiori | 2026-07-12 |
| P0-BE-05 | backend · packages/i18n — loader + CI wiring | commit `8dbc6d1` บน `feature/backend` (รอ loop-runner push→ด่าน 4.5 ที่รันแล้ว **PASS**) — wire `@juneflow/i18n` เข้า CI เป็นครั้งแรก: เพิ่ม `tsconfig.json` (extends base เหมือน tax-engine) + scripts `lint`/`typecheck`/`test` + vitest `^3.2.0` devDep · เพิ่ม `src/index.test.ts` = **15 G3 tests** คลุม 3 ชั้น dict/nav/phrases ครบ th/zh/en/ar + RTL (`isRTL`/`dir` ar-only) + langResolve en-fallback (`'fr' as LangCode`) + never-invent miss path (unknown key → key) · แก้ latent NodeNext JSON import (ต้อง `with { type: "json" }`) ที่โผล่เพราะแพ็กเกจถูก type-check ครั้งแรก · **ห้ามแปลใหม่:** ทุก assertion ตรงค่าใน `i18n-full.json` (sacred, อ่านอย่างเดียว) — ar dict อ้างค่า source verbatim · **ด่าน 4.5 diff-reviewer = PASS** (sacred i18n-full.json ไม่ถูกแตะ ✓ · zone เฉพาะ packages/i18n + lockfile importer · no invented translation · no DOM-MutationObserver mock ported · ไม่แตะ C1–C10) | — (loader/library · ไม่มีจอ) · หลักฐาน gate **CI + unit loader (th/zh/en/ar+RTL)**: `pnpm --filter @juneflow/i18n test` = **15/15 pass** · typecheck ✓ · lint ✓ · root `turbo typecheck` **8/8** · `test` **7/7** (i18n เข้าคิวแล้ว) · `build` ✓ · `pnpm install --frozen-lockfile` ✓ (lockfile +vitest importer ของ i18n) | 2026-07-12 |
| P0-BE-07 | backend · packages/db — pm_quote (REWORK) | commit `9d77375` บน `feature/backend` — แก้จุดที่ด่าน 4.5 FAIL 12 ก.ค.: `PMQuote` ถูกตัดออกเงียบโดยตัดสินเอง → คืนกลับเป็นตาราง `pm_quote` ใน `schema/pm.ts` ตาม erd.html `[pmq: id, wo_id, parts[], decision]` + data-dictionary PM rel line ("ใบเสนอราคาอะไหล่ → ลูกค้าอนุมัติ (LINE)"; PLAN §6 นับ erd เป็น base) · `wo_id` real uuid FK → `pm_workorder` (cascade) · `parts` jsonb `PmQuotePartRow[]` (label/qty/price) · `decision` text (erd ไม่ enumerate → ไม่เดา enum) · `currency_code` on-row (parts money §4) · timestamptz UTC · migration `0004_chubby_siren.sql` = single additive `CREATE TABLE pm_quote` + 1 FK (ไม่แตะ 0000–0003) · อัปเดต header comment (เดิม "left out") + spec-comment ใน `index.ts` · **ด่าน 4.5 diff-reviewer = PASS** (sacred ✓ · zone เฉพาะ packages/db · erd fidelity ✓ · §4 currency/UTC/real-FK ✓ · no guessed enum · migration chain prevId ตรง 0003 · ไม่ตัดสิน conflict เอง) | — (schema-only, ไม่มีจอ) · หลักฐาน gate **G1(บางส่วน)+migration check**: `drizzle-kit check` "Everything's fine" ✓ · `pnpm run generate` = pm_quote 7 cols/1 fk ✓ · typecheck ✓ · lint ✓ | 2026-07-12 |
| P0-DEV-04 | devops · branch policy + runbook | commit บน `feature/devops` (รอ loop-runner push→ด่าน 4.5) — สร้าง `infra/runbook.md` (Manifest กลุ่ม 2.6): §0 branch policy (`feature→dev` auto-merge เมื่อ CI เขียว + diff-reviewer PASS · `main` ล็อกให้ Wei promote คนเดียว) + runbook 3 เรื่อง (1) deploy dev `docker compose -f infra/docker-compose.yml up -d --build --wait` (2) promote main = Wei คนเดียว → prod compose (3) restore DB `pg_restore ... --clean --if-exists`. เนื้อหาถอดจาก skill `run-gates` "Runbook infra" (แหล่งความจริง) + ตรวจตรงกับไฟล์จริง: service names (postgres/redis/migrate-seed/api/worker/web) · ci.yml job `auto-merge-to-dev` · prod compose ไม่มี default password. แตะเฉพาะเขต devops (`infra/`) · ไม่แตะ sacred (ci.yml/CLAUDE.md ไม่แก้) · ไม่มี secret จริง (2 hit = prose อธิบายกฎห้าม secret) | — (docs-only · ไม่มีจอ) · หลักฐาน gate: 5 gates โค้ด (schema/contract/unit/E2E/visual) ไม่ applicable กับ runbook markdown · ทดสอบ flow feature→dev จริงต้องมี remote (B-010 ตอบ ก — เหมือน P0-DEV-02) · verify ในเครื่อง: zone-scoped ✓ · sacred untouched ✓ · secret scan ✓ (placeholder/prose ล้วน) · service/ci/compose refs ตรงไฟล์จริง | 2026-07-12 |
| P0-INT-04 | integrations (tax-engine/bank-file/notifications) | commit `db01360` บน `feature/integrations` — env-driven credential config ทั้ง 3 แพ็กเกจ: `src/config.ts` (`loadTaxEngineConfig`/`loadBankFileConfig`/`loadNotificationsConfig`) driver selector mock-first (default `fake` ไม่ต้องมี cred) + per-service creds อ่านจาก env เท่านั้น + `assertEnvPresent` fail-fast ตอน driver จริง · `.env.example` ต่อแพ็กเกจ (placeholder ล้วน) · `./config` export · 14 config unit tests (default/wiring/unknown-driver/required-var/blank/port). **ด่าน 4.5 PASS** (sacred ✓ · zone: แตะเฉพาะ 3 แพ็กเกจเขตตัวเอง · ไม่มี UI/token/re-translate/mock mechanic · ไม่แตะ C1–C10 · **ไม่มี secret จริงใน repo — .env.example เป็น placeholder ล้วน** (core gate) · cred อ่านจาก env only ไม่ hardcode · logic มี test ครบ) | — (ไม่มีจอ · package config) · หลักฐาน gate: `npx turbo run typecheck test --filter=@juneflow/{tax-engine,bank-file,notifications}` = 6/6 tasks pass (config tests: tax-engine +5 · bank-file +4 · notifications +5) · secret scan `.env.example` = placeholder ล้วน (ไม่พบค่าจริง) | 2026-07-12 |
| P0-INT-03 | notifications | commit `a286d53` บน `feature/integrations` — wire real lint/typecheck/test scripts (เลิก echo placeholder, เอา build echo ออกให้ตรง tax-engine/bank-file) + `tsconfig.json` + **18 unit tests** (`src/adapters/notifications.test.ts`, `describe.each` 3 channels) สำหรับ `NotificationAdapter` interface + `Fake*`/`*NotificationAdapter` (line/email/webpush). lockfile +3 บรรทัด (vitest importer ของ notifications — แพตเทิร์นเดียวกับ P0-INT-01/02) — **ด่าน 4.5 PASS** (sacred ✓ · zone: lockfile +3 importer ยอมรับได้เชิงกลไก · ไม่มี UI/token/re-translate · payload เป็น i18n-key opaque fixture · fake ไม่ปน production (index.ts export เฉพาะ interface/types) · skeleton reject `/not implemented/` · ไม่แตะ C1–C10 · 18 tests คลุม conformance + fake output + determinism + skeleton reject ครบ 3 channels · bookkeeping สอดคล้อง) | — (ไม่มีจอ · package logic) · หลักฐาน gate: `pnpm --filter @juneflow/notifications test` = 18/18 pass (G3 fake adapter: channel id · sent + messageId keyed on recipient · error undefined · byte-identical determinism · skeleton channel id + reject) · root `typecheck` 6/6 · lint 5/5 · test 6/6 (CI ขั้นต่ำเขียว) | 2026-07-12 |
| P0-INT-02 | bank-file | commit `ee7e9b7` บน `feature/integrations` — wire real lint/typecheck/test scripts (เลิก echo placeholder) + `tsconfig.json` + **8 unit tests** (`src/kbank-direct/kbank-direct.test.ts`) สำหรับ `BankFileFormatter` interface + `FakeBankFileFormatter`/`KBankDirectFormatter`. lockfile +3 บรรทัด (vitest importer ของ bank-file — แพตเทิร์นเดียวกับ P0-INT-01) — **ด่าน 4.5 PASS** (sacred ✓ · zone: lockfile +3 importer ยอมรับได้เชิงกลไก · ไม่แตะ C1–C10 · Money มี currencyCode ตาม §4 · fake ไม่ปน production · skeleton reject `/not implemented/` · 8 tests คลุม fake output layout + empty batch + determinism ครบ · bookkeeping สอดคล้อง) | — (ไม่มีจอ · package logic) · หลักฐาน gate: `pnpm --filter @juneflow/bank-file test` = 8/8 pass (G3 fake adapter: header/detail/trailer · missing reference → empty field · filename+encoding · empty-batch zero trailer · byte-identical determinism · skeleton format-id + reject) · root `typecheck` 6/6 (FULL TURBO) · lint 5/5 (CI ขั้นต่ำเขียว) | 2026-07-12 |
| P0-INT-01 | tax-engine | commit `e2d0b35` บน `feature/integrations` — wire real lint/typecheck/test scripts (เลิก echo placeholder) + tsconfig + **17 unit tests** (`src/thailand/thailand.test.ts`) สำหรับ `TaxEngine` interface + `FakeTaxEngine`/`ThailandTaxEngine`. lockfile +3 บรรทัด (vitest importer ของ tax-engine เท่านั้น — resolved อยู่แล้วในคิว tests) — **ด่าน 4.5 PASS** (sacred ✓ · zone: lockfile +3 importer ยอมรับได้เชิงกลไก · C4 สะท้อนตรง ไม่ตีความใหม่ · mock ไม่ปน production · 17 tests คลุม logic ครบ · bookkeeping สอดคล้อง) | — (ไม่มีจอ · package logic) · หลักฐาน gate: `pnpm --filter @juneflow/tax-engine test` = 17/17 pass (G3 fake adapter: calcWht/calcVat inclusive+exclusive · e-Tax lifecycle C4 queued→sent+void · renderRdForm placeholder · skeleton rejects) · root `typecheck` 6/6 · `build` 4/4 (CI ขั้นต่ำเขียว) | 2026-07-12 |
| P0-INT-05 | tax-engine | merged to dev `394eb18` (docs: `packages/tax-engine/docs/tax-forms-map.md`) — **ด่าน 4.5 PASS** (8/8 + fidelity spot-check ~40 จุด · notes: ป้ายย่อ 3 จุดใน map ให้ยึด .jsx เป็นแหล่งจริง · promptpayId render เสมอ · B-007) | — (docs-only · หลักฐาน gate = coverage checklist ครบ 3 ฟอร์ม A/B/C + shared primitives, เทียบ `pototype/tax-forms.jsx` L1–727) | 2026-07-12 |
| P0-BE-01 | monorepo scaffold + CI tooling (platform) | merged to dev (branch `feature/backend`) — **ด่าน 4.5 PASS** (reviewer รัน gates ซ้ำเอง: lint+typecheck 11/11 · build 5/5 · note: 3 แพ็กเกจเขต integrations ยังเป็น placeholder echo — เจ้าของคือ P0-INT-01/02/03) | — (ไม่มีจอ) · หลักฐาน gate: `pnpm run lint` ✓ · `pnpm run typecheck` ✓ · `pnpm run build` ✓ ครบ 10 workspaces (dev green from day one) | 2026-07-12 |
| P0-BE-06 | packages/db — Platform/Tenant schema (Drizzle) | merged to dev (commit `d2a65cf`) — **ด่าน 4.5 PASS** (fidelity ~18 จุด vs data-dictionary/erd · reviewer migrate ลง PG16 จริง ตรวจ 7 ตาราง/8 FK ผ่าน information_schema · timestamptz/currency_code/company_id ครบ §4 · C1 enum 4 ระดับ + C5 keys ตรง · migration `0000` เป็น sacred หลัง merge นี้ — BE-07/08/09 ต้อง generate ไฟล์ใหม่เท่านั้น) | — (schema-only, ไม่มีจอ) · หลักฐาน gate G1(บางส่วน)+migration check: `drizzle-kit check` ✓ · `drizzle-kit migrate` ลง PG16@5433 ✓ · typecheck 6/6 · lint 5/5 · test 9/9 | 2026-07-12 |
| P0-QA-01 | qa · visual-gate | merged to dev @ `006fbf0` — **ด่าน 4.5 PASS** (reviewer ตรวจเอง: index ↔ ดิสก์ exact 128/128 · route 103/103 ตรง NAV-ROUTES) | — (งาน index ไม่มีจอ · หลักฐาน: `tests/visual/reference-index.md` 128 แถว = 106 .jpg + 22 .png · นับไฟล์ตรงเกณฑ์ B-001) | 2026-07-12 |
| P0-QA-06 | qa · seed fixture assertions (REWORK) | commit บน `feature/qa` (รอ loop-runner push→ด่าน 4.5) — แก้ 3 จุดที่ด่าน 4.5 FAIL 12 ก.ค.: **C1** Package 3→**4** (`pkg-builder.jsx PKG_STORE` + PACKAGE-RULES §1) · **C9** JV lines ออกจากกลุ่ม expected-0 → assert สมดุล DR=CR (JV 7 ใบ ≥14 บรรทัด) · **B-009** Unit/SalesUnit ถอดออกจาก expected-0 → `it.todo` ผูก B-009 (ไม่ล็อกค่า) | — (test spec ไม่มีจอ · หลักฐาน: `tests/seed/` · `vitest run seed` = **91 passed \| 2 todo** เขียว · expected ถอดจาก §สรุป + ยึด ภาคผนวก C เมื่อขัด · Unit persist ค้าง B-009 ไม่เดา) | 2026-07-12 |
| P0-QA-04 | qa · visual-gate harness (G5) | commit บน `feature/qa` (รอ loop-runner push→ด่าน 4.5) — harness `tests/visual/visual-gate.spec.ts` + `lib/compare.ts` (jpg-aware decode ผ่าน chromium — ไม่เพิ่ม native dep) + `lib/report.ts` (worker-safe: part files → globalTeardown consolidate) · **อ่าน `reference/` อย่างเดียว ไม่แตะ** | — (harness · ยังไม่มีจอจริง apps/web) · หลักฐาน gate = **G5 harness รันได้ + รายงาน diff อ่านได้**: `pnpm --filter @juneflow/tests test:visual` = **3 passed \| 1 skipped** (self-check: identical=PASS 0 diff · perturbed=FAIL 2500px · size-mismatch=auto-FAIL) → รายงาน `tests/visual/.results/visual-report.md` (gitignored) · capture mode skip จน `screens.manifest.json` มีจอ + `VISUAL_BASE_URL` ถึง · threshold strict=0 (ผ่อนสำหรับ jpg lossy = คำตัดสิน Wei/BLOCKERS) | 2026-07-12 |
| P0-QA-05 | qa · unit G3 | merged to dev @ `b81528c` — **ด่าน 4.5 PASS** (reviewer รันซ้ำ 48/48 เขียว · C2/C3/C5/C9 ตรง · note: transition `rejected→draft` ใน approval-matrix เป็นการต่อยอดเล็กจาก Global rule #4 — ให้ Wei เห็นตอน review) | — (test spec ไม่มีจอ · หลักฐาน: `tests/unit/` 6 ไฟล์ · `vitest run unit` = 48 tests เขียว · expected ถอดจาก spec ล้วน · posting account mapping/approval thresholds ค้าง Open Q #3/#2 ทำ `describe.todo` ไม่เดา) | 2026-07-12 |
| P0-BE-08 | backend · packages/db — sales_unit.contract (REWORK) | commit `307d234` บน `feature/backend` (รอ loop-runner push→ด่าน 4.5 ที่รันแล้ว **PASS**) — แก้จุดที่ด่าน 4.5 FAIL 12 ก.ค.: เพิ่มคอลัมน์ `sales_unit.contract` = **numeric(16,2)** money แชร์ `currency_code` แบบเดียวกับ booking/down/loan ตาม **B-013(ก)** (เงินงวดทำสัญญา ไม่ใช่ reference เอกสาร) วางระหว่าง booking↔loan ตาม field list ใน data-dictionary §อื่นๆ (`unit_id,customer_id,stage,booking,contract,down[],loan,transfer`) · คืน spec-comment ใน `schema/index.ts` ที่เคยตกหล่น field `contract` · migration `0003_milky_thunderbird.sql` = single additive `ALTER TABLE sales_unit ADD COLUMN contract` (ไม่แตะ merged 0000/0001/0002 = sacred) · ไม่อ้าง [TBD-MVP] · **ด่าน 4.5 diff-reviewer = PASS** (sacred ✓ · zone เฉพาะ packages/db · B-013 cited by number · migration chain intact prevId ตรง 0002) | — (schema-only, ไม่มีจอ) · หลักฐาน gate **G1(บางส่วน)+migration check**: `drizzle-kit check` "Everything's fine" ✓ · `pnpm run generate` = sales_unit 13 cols (+contract) ✓ · typecheck ✓ · lint ✓ | 2026-07-12 |
| P0-BE-12 | backend · packages/contracts (openapi.yaml + TS codegen) | commit บน `feature/backend` (รอ loop-runner push→ด่าน 4.5) — complete `openapi.yaml` จาก `docs/handoff/api-contract.md` ครบทุกกลุ่ม endpoint (Auth · Admin · Master · BOQ+AI-QTO · Subcon · PM · Finance · Land-Sales · DMS/Noti/Audit/Reports · Files · Exports · LINE) = **145 operations** · action-endpoint pattern สำหรับทุก state transition (submit/approve/revise/reject/deliver/inspect/close/decide/advance-stage/transfer…) ไม่มี status ผ่าน PUT ตรง · **402 QUOTA_EXCEEDED + upgrade_url** ที่ POST /projects (project quota) · POST /ai-qto/upload (AI credit) · POST /files (storage) ตาม note #4 · tenant scope = bearerAuth global (JWT carries company_id) · LINE webhook override `security: []` (x-line-signature ไม่ใช่ JWT) · **transcription ล้วน — ไม่ invent endpoint** · resource fields = opaque `Entity` ยกเว้นที่ api-contract.md ระบุ field ชัด (ไม่เดา field = ปล่อยให้ schema task) · **SACRED_OVERRIDE=wei-approved:B-012** (รอบเดียวเฉพาะ BE-12 — sacred ถาวรหลัง merge) · wired `generate`/`typecheck`/`lint` scripts + `tsconfig.json` + `src/index.ts` re-export generated types · **B-014 (ไม่ block):** list/pagination envelope ไม่ระบุใน api-contract.md → คง bare-array shape เดิม ไม่เดา envelope | — (contract/codegen · ไม่มีจอ) · หลักฐาน gate **contract lint + codegen ผ่าน**: `pnpm --filter @juneflow/contracts generate` = openapi-typescript validate+gen 145 ops (4885 บรรทัด, 0 error, 0 dup operationId) · typecheck generated ✓ · **root `turbo typecheck` 7/7 · `lint` 6/6 · `build` 2/2 · `pnpm install --frozen-lockfile` ✓ (lockfile +3 = typescript importer)** | 2026-07-12 |
