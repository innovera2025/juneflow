# Overnight Bug-Hunt Findings (orch-A Stream-4 · 2026-07-17)

> loop-until-dry adversarial hunt (45 agents · 2 rounds) · **14/34 candidates passed adversarial refute-verify** · 34 unique below
> NOTE: synth agent fizzled (returned placeholder) → recovered from journal. Can't map exactly which 14 passed, so ALL candidates listed; HIGH + clear-MEDIUM are the actionable core. Fixes NOT auto-applied overnight (respect web/triage boundary) — Wei triages.


## HIGH (6)

**[apps/api/src/routes/po.ts:416] · ⚠️TRACKED** POST /po/:id/variation-order mutates the PO's committed total (add/cut) with NO approval-authority (B-070 tier) check — it never calls callerApprovalLevel, unlike /po/:id/approve which enforces requiredApprovalLevel(amount). A VO can inflate an already-approved PO past the tiers its new value would demand, bypassing the tiered sign-off entirely.
  - scenario: A tenant user whose role has approvalLevel 2 (Procurement head) raises PO from an approved PR with total=900,000, submits, approves (level 2 suffices since <1,000,000). Then POST /po/{id}/variation-order {dir:'add', amount:9000000}. The handler (po.ts:416-477) loads the PO, validates dir/amount, inserts the VO, and sets total=9,900,000 (line 466-471) — never checking authority. Result: a 9.9M comm

**[apps/api/src/routes/users.ts:88]** POST /users has no quota gate: the 'users' (seat) quota dimension is computed by the resolver but never enforced by any endpoint, so a tenant can invite unlimited users past its paid package seat limit.
  - scenario: 'users' is a first-class QUOTA_KEY (plugins/quota.ts:20) and SubscriptionQuotaResolver.#used computes real seat usage (plugins/subscription-quota.ts:82); its own header even claims F2 closed 'seat/AI/project' abuse. But grep shows quota.check() is only ever called for 'projects' (projects.ts:205), 'ai_per_month' (ai-qto.ts:150) and 'storage_gb' (files.ts:85) — never 'users'. registerUsersRoute (us

**[apps/web/src/ui/icon.tsx:174]** Icon renders `GLYPHS[name](p, color)` with no fallback, so any `name` not in the 60-key glyph record throws `TypeError: GLYPHS[name] is not a function` (the prototype used `paths[name] || null`, which never crashed). The one screen that feeds it opaque wire data is master-company.tsx:234 (`<Icon name={r.icon as IconName} />`), where `r.icon` comes from GET /org-units.
  - scenario: GET /org-units returns any org-unit row whose `icon` is null / empty / absent, or any value outside the 60-name glyph set. org-tree.ts `toOrgNode` defaults a missing/non-string icon to `""`, and backend org-units.ts:261 stores `icon:""` as null on PUT /org-units/{id}, so `icon:null` on the wire is reachable → `GLYPHS[""]` is undefined → `GLYPHS[""](p,color)` throws during render → the entire Maste

**[apps/api/src/routes/po.ts:416] · ⚠️TRACKED** POST /po/:id/variation-order mutates po.total with no status guard and no approval-authority check; since po.total is what drives the approval-tier gate, a VO can both amend a PO in any state (should 409) and bypass tiered approval.
  - scenario: Create a PO with total 6,000,000 (needs MD/level 4). While still draft, POST /po/p0/variation-order {dir:'cut', amount:5,500,000} -> total 500,000 (handler never checks po.status, never checks caller tier). submit -> pending. approve as a level-2 หน.จัดซื้อ user (500k only needs level 2) -> 200 approved. Then POST /po/p0/variation-order {dir:'add', amount:5,500,000} -> total back to 6,000,000. Net

**[apps/web/src/screens/master/master-project.tsx:193]** MasterProject gates the entire screen on `ready = !!active && H.length >= 4`, where H is the active project type's WBS hierarchy. Any project type whose hierarchy has fewer than 4 labels leaves `ready` permanently false, so the screen renders only the loading skeleton (the `if (!ready) return <Page ...skeleton>` branch at line 241) forever — never the phase/block/unit structure.
  - scenario: User selects (via ProjectSwitcher / ctx.tweaks.project) a project of type `civil` (seed project `rdb`) or `service` (seed project `erp`). GET /project-types returns hierarchy=["โครงการ","ส่วนงาน / โซน","WBS"] (civil, len 3) or ["โครงการ","เฟส","งาน (WBS)"] (service, len 3). typeHierarchy() returns length 3, H.length>=4 is false, so master.project shows an infinite skeleton and the real content (bl

**[packages/db/src/seed/index.ts:1338]** retention_ledger rows join a WO and a contract/vendor that belong to DIFFERENT subcon firms — the wo_id and contract_id are indexed on two non-aligned schemes.
  - scenario: retentionLedgers is built with woId=det(`wo:${i}`) but vendorId=det(`vendor:${SUBC_CONTRACTS[i].firm}`) and contractId=det(`subc:${i}`). wo:i's vendor is SC-0{i+1} (index.ts:1144), while SUBC_CONTRACTS[i].firm is SC-01/SC-04/SC-03/SC-07. So ret:1 links wo:1 (vendor SC-02, contractId NULL per the F3 reconcile at 1206-1215) to vendor SC-04 / subc:1; ret:3 links wo:3 (whose real contractId is subc:1=

## MEDIUM (15)

**[apps/api/src/routes/po.ts:425] · ⚠️TRACKED** POST /po/:id/variation-order has NO po.status guard — every other PO transition (submit/approve/reject) 409s on the wrong state, but a VO can be attached to a draft, pending, or rejected PO and will mutate its stored total.
  - scenario: PO is in status 'rejected' (a dead doc) or 'draft' (uncommitted). POST /po/{id}/variation-order {dir:'add', amount:100000} returns 200, writes a variation_order row and changes pos.total on the rejected/draft PO (po.ts:466-473). Amending the financial total of a doc that was never approved (or was explicitly rejected) should 409 INVALID_STATE like every sibling transition, but the handler has no s

**[apps/api/src/routes/boq.ts:489]** POST /boq/:id/items blocks item-add only when status==='approved'. A BOQ in 'pending' (already submitted for approval) is still editable, so a doc's line contents and total can change after submission and before the MD locks it.
  - scenario: Estimator POST /boq/{id}/submit → status 'pending' (boq.ts:780). While awaiting approval, anyone POST /boq/{id}/items with new priced lines → 201 (the guard at boq.ts:489 only rejects status==='approved'), so items are inserted and docTotal changes. The MD then approves a set of items different from what was submitted for review. Guard should also block 'pending' (only draft|revise should be mutab

**[apps/api/src/routes/project-types.ts:117] · ⚠️TRACKED** Master-data create/edit/delete endpoints skip the 'master' perms gate that authz.ts defines and that only /users and /roles enforce, so any authenticated member (even a view-only role) can mutate master data.
  - scenario: authz.ts (MANAGEMENT_MODULE='master') states user/role administration lives under the 'master' perms module and its create/edit rights gate admin actions; users.ts:102 and roles.ts:217/255 enforce permAllowed(caller,'master','create'|'edit'). But the OTHER screens under the same 'master' module import no authz and perform NO perms check: POST/PUT /project-types (project-types.ts:117,168), POST /co

**[apps/api/src/routes/counts.ts:135]** The gl.inbox count scopes goods-receipts through grs.poId -> pos -> vendors with INNER JOINs, so every WO-anchored GR (po_id NULL, wo_id set) is silently excluded from the posting-inbox badge.
  - scenario: Since migration 0018 a GR may anchor on a WO instead of a PO (grs.poId was made nullable; POST /gr with wo_id creates a row with po_id=NULL). countGlInbox reads grs via db.selectThrough(grs, [{fk: grs.poId, parent: pos}, {fk: pos.vendorId, parent: vendors}]) — an inner join on grs.poId=pos.id (confirmed innerJoin in tenant-db.ts:182). A tenant that has, say, 3 unposted WO-goods-receipts and 0 PO r

**[apps/api/src/routes/po.ts:466] · ⚠️TRACKED** POST /po/:id/variation-order mutates the stored PO total with no approval-tier check and no PO-status guard, letting an approved PO be inflated past its approval tier (or driven negative) without re-approval.
  - scenario: A procurement head (approvalLevel 2, authority up to 1,000,000 THB per B-070) approves a PO with total 900,000. Any authenticated tenant user then POSTs /po/{id}/variation-order {dir:'add', amount:'500000'}: the handler does not call callerApprovalLevel and does not check po.status, so it runs newTotal = 900000 + 500000 = 1,400,000 and stores it (po.ts:466-473). The PO is now worth 1.4M — a tier t

**[apps/web/src/ui/date-picker.tsx:41]** formatThaiShort calls `Intl.DateTimeFormat.format(d)`, which throws `RangeError: Invalid time value` on an Invalid Date (unlike Date.toLocaleDateString, which returns "Invalid Date"). dashboard.tsx builds `asOfDate = new Date(summary.asOf)` (dashboard.tsx:256) and passes it to formatThaiShort in the header (dashboard.tsx:369) and to <DatePicker value={asOfDate}> whose anchor calls formatThaiShort(value) unconditionally (date-picker.tsx:243).
  - scenario: GET /dashboard/summary returns `as_of` as a non-empty but unparseable string (parseSummary keeps it verbatim via entStr). `new Date(as_of)` becomes an Invalid Date → formatThaiShort throws RangeError → the Dashboard header (and the DatePicker anchor) crash on render. Triggered by any malformed/non-ISO `as_of` from the summary endpoint.

**[apps/api/src/routes/dashboard.ts:146] · ⚠️TRACKED** Dashboard default-project resolver picks the smallest-uuid project (bbt), but all seeded budget/BOQ data lives under rjp, so the no-project dashboard summary and budget-actual return all-zero KPIs / empty categories.
  - scenario: GET /dashboard/summary and GET /dashboard/budget-actual with NO project_id resolve the tenant's primary project as first by (created_at ASC, id ASC). All 7 seeded projects share one transaction created_at (seed index.ts:940-946), so the tiebreak is the smallest deterministic uuid = det("project:bbt"), NOT rjp. But every cbs_budget / boq_doc / boq_group / boq_item / phase-with-units row is seeded O

**[apps/api/src/routes/pr.ts:558]** PR approval tier is derived from sumLines(), which only prices lines that carry a boq_item_id; expense/advance/'clear' PRs (no BOQ source) always compute amount 0, so they always require only the lowest tier (level 2) regardless of real value.
  - scenario: Create an advance/expense PR whose lines have no boq_item_id (sumLines returns amount 0 -> requiredApprovalLevel(0)=APPROVAL_LEVEL_PROC=2). submit -> pending. A level-2 หน.จัดซื้อ user calls approve -> 200 approved, even though the real advance could be, say, 3,000,000 THB which the flows.html MATRIX intends to route to MD (level 4). Every non-BOQ-priced PR category silently escapes the >500k/>2M 

**[apps/api/src/routes/boq.ts:489]** POST /boq/:id/items only rejects when doc.status === 'approved'; a 'pending' BOQ (already submitted for MD approval) stays editable, so items/total can be changed after submission and before/at approval.
  - scenario: submit a draft BOQ -> pending (total was, e.g., 1,000,000 when submitted). Before the approver acts, POST /boq/d0/items with a large new line -> 201 (guard passes because status is 'pending', not 'approved'). The MD then approves a doc whose total no longer matches what was submitted for review; the add-items path should 409 (lock) on any non-draft/non-revise status, not only on 'approved'.

**[apps/api/src/plugins/subscription-quota.ts:84]** ai_usage is never written anywhere, so SubscriptionQuotaResolver used('ai_per_month') is always 0 and the AI monthly cap can never trip for any positive limit.
  - scenario: Package ai_per_month = 50. Tenant calls POST /ai-qto/upload 10,000 times in a month. ai-qto.ts:150 checks quota, but #used sums ai_usage rows (subscription-quota.ts:84-90) and NOTHING in the codebase ever inserts an ai_usage row (grep: aiUsage only read in subscription-quota.ts and profile-data.ts). used stays 0, isWithinQuota(50,0) is always true, so 402 is never returned — AI credit is effective

**[apps/api/src/routes/po.ts:416] · ⚠️TRACKED** POST /po/:id/variation-order amends the PO's stored total with no PO status guard and no approval-level check, letting an approved PO be inflated past the tier that approved it (possible overlap with tracked B-084).
  - scenario: PO total 900,000 (below the 1M tier) is approved by a level-1 approver (หน.จัดซื้อ). Any authenticated tenant member then POSTs /po/:id/variation-order {dir:'add', amount:9000000}; the handler validates only dir/amount (po.ts:430-445), performs NO po.status check and NO callerApprovalLevel check (unlike /approve at po.ts:348-355, /submit at 313, /reject at 396), and runs updateThroughChain to set 

**[apps/api/src/routes/counts.ts:135]** gl.inbox count only walks the PO chain for goods receipts, so WO-anchored GRs are silently excluded (undercount), and returned/cancelled GRs are never filtered out (overcount).
  - scenario: countGlInbox reads GRs via selectThrough(grs, [{fk: grs.poId, parent: pos}, {fk: pos.vendorId, parent: vendors}]). Since migration 0016 grs.poId is nullable and a GR can be anchored on a WO instead (grs.woId set, poId null — see gr.ts GR_WO_HOPS). The INNER JOIN on grs.poId=pos.id drops every WO-anchored (subcon-work) receipt. Example: a tenant records a GR against an approved WO (POST /gr {wo_id,

**[apps/api/src/routes/roles.ts:288]** PUT /roles/:id is a full-replace that resets perms to {} (all rights off), approval_level to 0, and approval_limit to null whenever those fields are omitted — unlike the partial-merge PUTs in vendors.ts/org-units.ts.
  - scenario: parseRoleBody (roles.ts:146) always returns perms: permsFromInput(body.perms) → {} when body.perms is absent, approvalLevel: level ?? 0, and approvalLimit: null when approval_limit is absent; the update at line 288 writes all of them. Concrete case: an admin renames the Director role via PUT /roles/{dir} with body {name:"MD"} (no perms/approval_level). The role's entire 11x5 permission matrix is s

**[packages/db/src/seed/index.ts:1289] · ⚠️TRACKED** jv.source_doc is seeded as human text ("GR auto", "Petty", "REM"...) but the schema documents it as a polymorphic "table:uuid" ref and counts.ts parses exactly that, so the GL posting-inbox 'posted' detection never matches → badge over-counts.
  - scenario: Seed inserts jvs with sourceDoc: jv.source where jv.source ∈ {REM, Manual, GR auto, Allocate, FA auto, Petty}. counts.ts countGlInbox (routes/counts.ts:80,143-148) builds the `posted` set via SOURCE_DOC_REF = /^(pv|rv|gr|payroll):([0-9a-fA-F-]{36})$/. None of the seeded source_doc strings match, so `posted` is always empty. JV-2026-0416 ("GR auto") is meant to represent gr:0 (GR-2026-0148) as alre

**[packages/db/src/seed/index.ts:1125] · ⚠️TRACKED** pr_item lines are seeded ONLY for pr:0 (all 4 rows hardcode prId: det("pr:0")), so GET /pr derives amount=0 for the other 9 PRs even though the mock pr-list shows a real มูลค่า for each.
  - scenario: The prItems insert (Array.from({length:4}) at index.ts:1123-1127) sets prId: det("pr:0") on every line. pr.ts GET /pr (routes/pr.ts:340, sumLines) derives each PR's amount = Σ pr_item.qty × boq_item.price. Only pr:0 has lines, so PR-2026-0417..0409 (pr:1..9) return amount:0 / currency THB in the list. The PR list screen shows a populated amount for exactly one of ten rows and 0 (em-dash) for the r

## LOW (13)

**[apps/api/src/routes/po.ts:441] · ⚠️TRACKED** variation-order 'cut' validates amount only as >= 0 (po.ts:441), never against the current total, so a cut larger than the PO total drives pos.total negative.
  - scenario: Approved PO with total=100,000. POST /po/{id}/variation-order {dir:'cut', amount:500000}. amount passes the >=0 check; newTotal = 100000 - 500000 = -400000 (po.ts:467) is stored and returned as the PO's committed amount, corrupting downstream tier logic and reporting.

**[apps/api/src/routes/pr.ts:593]** reject (pr/po/wo) and return/cancel (gr) have no approval-authority check — any authenticated tenant user can reject a pending high-value doc, asymmetric with the tier-gated approve path. Spec-dependent, but the ability of a null/level-1 role to kill a doc only MD could approve is a plausible missing guard.
  - scenario: Pending PR with amount=3,000,000 (approve requires MD, approvalLevel 4, per requiredApprovalLevel). A tenant user with approvalLevel 1 POST /pr/{id}/reject {reason:'x'} → 200, PR moved to 'rejected' with no authority gate (pr.ts:593-630). Same pattern in po.ts:375 (reject), wo.ts:384 (reject), gr.ts:464 (return) and gr.ts:500 (cancel) — none call callerApprovalLevel.

**[apps/api/src/routes/../plugins/subscription-quota.ts:64]** SubscriptionQuotaResolver's fail-closed claim only holds when a tenant has ZERO subscription rows; a cancelled/expired/past_due subscription still grants that package's limits via the `?? subs[0]` fallback.
  - scenario: #limit does `subs.find(active||trial) ?? subs[0]` (subscription-quota.ts:64). The header/comment says a tenant with no resolvable active subscription gets no allowance (deny). But a tenant whose only subscription row has status 'cancelled' (or 'expired'/'past_due') hits the `?? subs[0]` branch and receives that stale package's limits instead of being denied — so a churned/suspended tenant keeps cr

**[apps/api/src/routes/dashboard.ts:777]** dashboard/contractors computes contract progress from work_period status === 'passed' only, whereas wo.ts treats a period as done when status is 'passed' OR 'paid', so a fully-paid subcontract under-reports progress on the dashboard versus the WO detail screen.
  - scenario: A subcon contract has all installments in status 'paid' (which is 'more done' than 'passed'). GET /wo/{id} reports progress via isPeriodDone (passed||paid) = 100%. GET /dashboard/contractors sums only status==='passed' amounts (dashboard.ts:776-782, passedByContract), so progress_pct = 0% for the same contract. Two endpoints give contradictory progress for identical data; the dashboard widget unde

**[apps/api/src/routes/dashboard.ts:708]** The cashflow-forecast 'next 7 days' window is end = start + 7 days with an inclusive test (d >= start && d <= end), covering 8 calendar days (today through today+7 inclusive).
  - scenario: With today = 2026-07-17 (UTC midnight), start=2026-07-17 and end=2026-07-24; inWindow accepts a due date of exactly 2026-07-24, so a payable/receivable falling on day 7 is counted despite the widget being labeled 'next 7 days' (dashboard.ts:679/707-709). net_total therefore includes one extra day's cashflow. Should be an exclusive upper bound (d < end) or end = start + 6 days.

**[apps/web/src/screens/dashboard/dashboard.tsx:368]** `activePhase.name.split(" · ")[0]` accesses `.name` on a wire-sourced project phase with no null guard. The sibling helper project-blocks.ts `phaseHead` guards the same field with `(name ?? "")`, but the dashboard does not — an inconsistency indicating the dashboard path is the unguarded one.
  - scenario: GET /projects returns a project whose active/first phase has a null or missing `name` (ProjectPhase.name is marked required in the contract, so this is a server contract violation rather than normal data). `activePhase.name` is null → `.split(...)` throws `TypeError: Cannot read properties of null (reading 'split')` → the Dashboard header subtitle crashes on render.

**[packages/db/src/seed/index.ts:1389]** service_ticket.unit_id is seeded to a kind='block' node (block:B) but the schema documents it as the sold unit (a kind='unit' node), so the FK resolves to the wrong node kind.
  - scenario: serviceTickets are seeded with unitId: det("block:B") for all 7 rows. schema service_ticket.unit_id references project_node and is documented as "the sold unit" (extensions.ts:242-248), i.e. a kind='unit' node. block:B is a kind='block' node, so the FK resolves but points to the wrong node kind. Any consumer that later joins service_ticket -> project_node to show the unit code/label (expecting a B

**[packages/db/src/schema/finance.ts:355]** bank_statement stores signed money amounts inside its lines jsonb but the table has no currency_code column, violating the repo 'every money column carries currency_code' rule.
  - scenario: bank_statement stores signed money amounts inside the lines jsonb (seed index.ts:1306-1312: {v: -894205.61, ...}) but the table has NO currency_code column, unlike every other money-bearing table and unlike the sibling jsonb-money pattern in pm_quote/sales_unit which explicitly ride a row currency_code column. A future reconcile/bank handler reading lines[].v has no currency to attach. No consumer

**[packages/db/src/seed/index.ts:1336]** retention_ledger rows pair woId=wo:i with vendorId/contractId from SUBC_CONTRACTS[i], but the WO->firm and contract->firm orderings differ, so a row links a WO to a different subcon vendor than its own vendor_id/contract_id.
  - scenario: retentionLedgers row i is seeded with woId=det(`wo:${i}`) but vendorId=det(`vendor:${SUBC_CONTRACTS[i].firm}`) and contractId=det(`subc:${i}`). The WO->firm mapping (wo:i -> SC-0{i+1}) and the SUBC_CONTRACTS firm order (SC-01, SC-04, SC-03, SC-07) differ, so e.g. retention row 1 links wo:1 (whose real vendor is SC-02) to vendorId=SC-04 / contractId=subc:1 (SC-04's WO-2026-0051). The retention ledg

**[apps/api/src/plugins/subscription-quota.ts:65]** The quota resolver falls back to subs[0] when no active/trial subscription exists, so a cancelled/expired subscription still grants its package's full quota (fails open toward the tenant).
  - scenario: Tenant's only subscription row has status='cancelled' (or 'expired'). #limit does subs.find(s=>s.status==='active'||s.status==='trial') ?? subs[0], so it selects the cancelled row and returns pkg.limits[key] instead of the fail-closed {limit:0,used:1}. A non-paying/deactivated tenant keeps creating projects and (nominally) AI/seats at its old package allowance rather than being denied — the intend

**[apps/web/src/screens/master/master-company.tsx:234]** MasterCompany renders `<Icon name={r.icon as IconName}>` where r.icon is opaque server data (toOrgNode reads e.icon as an arbitrary string). The Icon component does `{GLYPHS[name](p, color)}` (ui/icon.tsx:174) with no guard, so any icon string not in the GLYPHS set makes `GLYPHS[name]` undefined and calling it throws TypeError, crashing the whole org-tree render.
  - scenario: A GET /org-units row carries an `icon` value outside the ~70-name glyph set (org_units.icon is free-text; org-units.ts:218-220 lets a POST/PUT body's `icon` string persist verbatim, `str(pick(body,'icon')) || iconForLevel(level)`). Rendering that node calls GLYPHS[<bad>](...) → 'GLYPHS[name] is not a function'. Latent with current data: the seed and OrgAddForm (which never sends `icon`) only ever 

**[apps/api/src/routes/dashboard.ts:735]** cashflow-forecast receivables leg counts only ar_invoice.amount (ex-VAT) while the payables leg counts ap_billing.amount + vat, so net_total is biased toward outflow.
  - scenario: In cashflowForecast the payables push amount: -round2(num(b.amount) + num(b.vat)) (line 722) but the receivables push amount: round2(num(inv.amount)) (line 735), omitting inv.vat. ar_invoice.amount and vat are separate NOT NULL columns (finance.ts) exactly like ap_billing, so the customer's cash-in is amount+vat. Example: an AR invoice due inside the 7-day window with amount=1,000,000 vat=70,000 c

**[apps/api/src/routes/companies.ts:48]** GET /companies returns only the 3 affiliated GROUP_COMPANIES (all with project_count 0) and omits the tenant's own company, which is the row that actually owns all 7 projects.
  - scenario: Seed gives the 9 tenant companies group_parent_id=NULL and the 3 เครือ companies (JF/JE/JC) group_parent_id=CO1 (index.ts:851-857). companies.ts computes head = own.groupParentId ?? own.id = CO1, then members = companies WHERE group_parent_id = CO1 → {JF,JE,JC} only; company:T-1001 (บจก. รุ่งเรืองก่อสร้าง) is excluded because its own group_parent_id is NULL (not CO1). All 7 seeded projects have co
