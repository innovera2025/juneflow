{
  "name": "@juneflow/tests",
  "version": "0.0.0",
  "private": true,
  "description": "QA zone harness: contract (G2) / e2e (G4) / visual (G5) gates per PLAN.md section 9. Scripts are guarded to pass green while no tests exist yet (P0-QA-02/03/04).",
  "scripts": {
    "test": "pnpm run test:unit && pnpm run test:seed && pnpm run test:contract && pnpm run test:e2e && pnpm run test:visual",
    "test:unit": "vitest run unit",
    "test:seed": "vitest run seed",
    "test:contract": "vitest run contract",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "test:visual": "playwright test --config visual/playwright.visual.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@types/pg": "^8.11.10",
    "js-yaml": "^4.1.0",
    "pg": "^8.13.1",
    "vitest": "^3.2.0"
  }
}
