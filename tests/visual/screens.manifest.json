{
  "note": "Real-capture list for the visual gate (P0-QA-04). Each entry: {screen, route, ref, masks?} where ref is relative to tests/visual/reference/ and masks is an OPT-IN list of Wei-approved mask keys from lib/masks.ts MASK_REGISTRY (P0-QA-07) — shell-bearing screens list \"sidebar-logo-b044\" (B-044: reference logo lockup is an older locale render); screens without the sidebar lockup (e.g. login, P1-WEB-01) must omit masks. Populate a row when the matching apps/web screen exists and is reachable at VISUAL_BASE_URL. route→ref mapping is authoritative in tests/visual/reference-index.md (P0-QA-01). Empty until apps/web screens land (P0-WEB-05+); the harness self-verifies its engine meanwhile.",
  "screens": []
}
